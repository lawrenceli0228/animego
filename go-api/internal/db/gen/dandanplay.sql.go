// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: dandanplay.sql

package dbgen

import (
	"context"
)

const getAnimeByBgmID = `-- name: GetAnimeByBgmID :one
SELECT
    anilist_id,
    title_romaji,
    title_english,
    title_native,
    title_chinese,
    cover_image_url,
    cover_image_color,
    poster_accent,
    episodes,
    status,
    season,
    season_year,
    format,
    average_score,
    bangumi_score,
    bangumi_votes,
    bgm_id,
    source,
    duration
FROM anime_cache
WHERE bgm_id = $1
LIMIT 1
`

type GetAnimeByBgmIDRow struct {
	AnilistID       int32    `json:"anilistId"`
	TitleRomaji     *string  `json:"titleRomaji"`
	TitleEnglish    *string  `json:"titleEnglish"`
	TitleNative     *string  `json:"titleNative"`
	TitleChinese    *string  `json:"titleChinese"`
	CoverImageUrl   *string  `json:"coverImageUrl"`
	CoverImageColor *string  `json:"coverImageColor"`
	PosterAccent    *string  `json:"posterAccent"`
	Episodes        *int32   `json:"episodes"`
	Status          *string  `json:"status"`
	Season          *string  `json:"season"`
	SeasonYear      *int32   `json:"seasonYear"`
	Format          *string  `json:"format"`
	AverageScore    *float64 `json:"averageScore"`
	BangumiScore    *float64 `json:"bangumiScore"`
	BangumiVotes    *int32   `json:"bangumiVotes"`
	BgmID           *int32   `json:"bgmId"`
	Source          *string  `json:"source"`
	Duration        *int32   `json:"duration"`
}

// findSiteAnime last-resort lookup:  Bangumi search yields a bgmId, this
// query resolves it to a local anime_cache row so the /match handler
// can return a populated siteAnime envelope.  Same projection as
// SearchAnimeCacheForDandanplay for consistent downstream mapping.
//
// Returns pgx.ErrNoRows when no row matches — handler treats that as a
// null siteAnime (Express returned `null` in the same branch).
func (q *Queries) GetAnimeByBgmID(ctx context.Context, bgmID *int32) (GetAnimeByBgmIDRow, error) {
	row := q.db.QueryRow(ctx, getAnimeByBgmID, bgmID)
	var i GetAnimeByBgmIDRow
	err := row.Scan(
		&i.AnilistID,
		&i.TitleRomaji,
		&i.TitleEnglish,
		&i.TitleNative,
		&i.TitleChinese,
		&i.CoverImageUrl,
		&i.CoverImageColor,
		&i.PosterAccent,
		&i.Episodes,
		&i.Status,
		&i.Season,
		&i.SeasonYear,
		&i.Format,
		&i.AverageScore,
		&i.BangumiScore,
		&i.BangumiVotes,
		&i.BgmID,
		&i.Source,
		&i.Duration,
	)
	return i, err
}

const searchAnimeCacheForDandanplay = `-- name: SearchAnimeCacheForDandanplay :many

SELECT
    anilist_id,
    title_romaji,
    title_english,
    title_native,
    title_chinese,
    cover_image_url,
    cover_image_color,
    poster_accent,
    episodes,
    status,
    season,
    season_year,
    format,
    average_score,
    bangumi_score,
    bangumi_votes,
    bgm_id,
    source,
    duration
FROM anime_cache
WHERE
    title_chinese ILIKE $1
    OR title_native ILIKE $1
    OR title_romaji ILIKE $1
    OR title_english ILIKE $1
LIMIT 10
`

type SearchAnimeCacheForDandanplayRow struct {
	AnilistID       int32    `json:"anilistId"`
	TitleRomaji     *string  `json:"titleRomaji"`
	TitleEnglish    *string  `json:"titleEnglish"`
	TitleNative     *string  `json:"titleNative"`
	TitleChinese    *string  `json:"titleChinese"`
	CoverImageUrl   *string  `json:"coverImageUrl"`
	CoverImageColor *string  `json:"coverImageColor"`
	PosterAccent    *string  `json:"posterAccent"`
	Episodes        *int32   `json:"episodes"`
	Status          *string  `json:"status"`
	Season          *string  `json:"season"`
	SeasonYear      *int32   `json:"seasonYear"`
	Format          *string  `json:"format"`
	AverageScore    *float64 `json:"averageScore"`
	BangumiScore    *float64 `json:"bangumiScore"`
	BangumiVotes    *int32   `json:"bangumiVotes"`
	BgmID           *int32   `json:"bgmId"`
	Source          *string  `json:"source"`
	Duration        *int32   `json:"duration"`
}

// Queries used by /api/dandanplay/* orchestration (P2.6).
//
// One query — SearchAnimeCacheForDandanplay — replaces Express's
// buildKeywordRegex + AnimeCache.find($or 4 titles).  We use ILIKE
// with tokenised pattern building (handler tokenises the keyword,
// joins with '%' wildcards, passes the single pattern via $1).  The
// existing trgm GIN indexes on title_chinese / title_native /
// title_romaji / title_english make this cheap.
//
// The handler is responsible for keyword token extraction (same regex
// as Express's `[\p{L}\p{N}]+` group) and pattern composition.  This
// keeps the SQL stable across keyword shapes and avoids generating
// dynamic SQL per request.
// Returns up to 10 anime_cache rows whose title columns ILIKE the
// caller-built keyword pattern.  Field selection mirrors the projection
// Express exposed via .lean() — every column the 3-phase /match path
// + /search route needs.
func (q *Queries) SearchAnimeCacheForDandanplay(ctx context.Context, titleChinese *string) ([]SearchAnimeCacheForDandanplayRow, error) {
	rows, err := q.db.Query(ctx, searchAnimeCacheForDandanplay, titleChinese)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SearchAnimeCacheForDandanplayRow{}
	for rows.Next() {
		var i SearchAnimeCacheForDandanplayRow
		if err := rows.Scan(
			&i.AnilistID,
			&i.TitleRomaji,
			&i.TitleEnglish,
			&i.TitleNative,
			&i.TitleChinese,
			&i.CoverImageUrl,
			&i.CoverImageColor,
			&i.PosterAccent,
			&i.Episodes,
			&i.Status,
			&i.Season,
			&i.SeasonYear,
			&i.Format,
			&i.AverageScore,
			&i.BangumiScore,
			&i.BangumiVotes,
			&i.BgmID,
			&i.Source,
			&i.Duration,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
