# AnimeGo 开发日志

---

## [3.30.0] - 2026-09-06

### 把「只被 CI 跑过一次」的代码拿去做变异测试，六个变异全活了下来

昨天 #156/#157/#158 三个 PR 的测试都只有 CI 的一次通过。本地 Docker 起来之后把它们全部做了一遍变异，共 21 个变异：

| 目标 | 变异数 | 结果 |
|---|---|---|
| `windowTitles`（纯函数） | 6 | 全部被杀 |
| `#157` 无标题行过滤 | 2 | 全部被杀 |
| `admin ReEnrichIDs` | 7 | 全部被杀 |
| **`sweepOne` 的路由 / 兜底 / 盖戳** | **6** | **全部存活** |

★★ **纯函数覆盖得最好，而「这一行走哪条路」一条都没覆盖。** `windowTitles` 有自己的表驱动测试；它周围的三个决定——这一行该问哪个上游、兜底到底进不进得去、什么都没产出的行有没有被盖戳——一个断言都没有。CI 全绿对这三件事什么都没说。

存活的六个：`!mapSpeaks` 取反、删掉「ddp 条目指向别的番」那条臂、空列表时不兜底直接结束、rejected 不盖戳、empty 不盖戳、越界时退回拒绝而不是开窗。

### 这些测试只能写在一个地方

`sweepOne` 未导出，外部测试包够不到；worker 故意持有具体的 `*dbgen.Queries` 和 pool（写入是和 CLI 共用的四语句事务），内存 double 顶不了数据库那一半；而放在 `internal/queue` 下加 build tag 的话**永远不会被执行**——CI 的集成步骤只扫 `./test/integration/...`，正是那个步骤自己注释里记着的「整个包两个月没编译过」的腐烂方式。

剩下的形状只有一种：**驱动导出的 `Work()` 跑真实的一遍扫描**，只假造两个上游客户端，结果从数据库里读回来。六条测试各自钉一个决定，逐条验证能杀掉对应的变异——A7 被两条杀（它同时反转了两个方向的含义）。

### ★ 我自己写错了一个变异，它「存活」但什么也没说明

给 `noBinding` 那段写的第一个变异是把
```
if x { a; continue }
```
拆成
```
if x { a }
if x { continue }
```
——**语义完全等价**。它当然「存活」，而这个存活不是覆盖率缺口，是我的变异写错了。改成「`noBinding++` 整行删掉」之后立刻被杀。

★ **变异测试的报告里，「存活」有两种含义**：断言不够，或者变异根本没改变行为。只数存活个数会把后者读成前者。

### 另外

`episode_titles_releasing.go` 一处注释把测量日期写成了 2026-09-25（未来），改回 2026-09-05。

---

## [3.29.0] - 2026-09-06

### ★★ 那个「大概是 flake」的 500，是一条真的并发缺陷

上一轮合 PR#158 时看错了 check 名字——两个 Playwright workflow 只有一个是 PR 本地栈——在 sandbox 红着的情况下合了，当时把它记成「套件不稳」。回去把服务日志翻出来，同一毫秒里三个 `POST /api/subscriptions` 重叠，一个 201，一个 500：

```
13:55:45.175  POST /api/subscriptions  201
13:55:45.291  WARN  upsert subscription failed  cause="no rows in result set"
13:55:45.292  POST /api/subscriptions  500
```

★ **数有几个绿的，不算验 CI。** 逐个念出 check 的名字和它的结论才算。

### 机理：`ON CONFLICT DO NOTHING` 的结果，同一条语句读不回来

`InsertSubscriptionIfAbsent` 原本是 `DO NOTHING` 加一条 `UNION ALL` 读回臂。撞键时 PostgreSQL 会让这条语句**等**对方事务的 speculative token，但语句的快照是**开始等之前**取的：对方提交后，插入臂没有行，而读回臂同样看不见那条刚提交的行——它比快照新。零行，`:one` 把零行变成 `pgx.ErrNoRows`，处理器只能报 500。

先在裸 SQL 里把它复现出来再动代码：同一条查询，**顺序执行返回 1 行，重叠执行返回 0 行**。重叠是唯一的自变量。

改法是让冲突臂拿行锁而不是读快照——`DO UPDATE SET status = subscriptions.status`，把存着的值写回它自己。这是个构造上的 no-op：status / current_episode / score / last_watched_at / created_at / updated_at 全部保持原样，§4 decision 3「status 只由人改」照旧成立；它存在只是为了给 `RETURNING` 一行可还。整条 `UNION ALL` 因此消失，SQL 比原来短。

### 让一个竞态可测：等到 Postgres 说它真的堵住了

回归测试把这个竞态**摁住不动**：一个事务插入后挂起不提交，处理器在自己的 goroutine 里发同一个请求撞上去，主 goroutine 轮询 `pg_stat_activity` 直到出现 `wait_event_type = 'Lock'` 且语句文本里带这条查询的名字（sqlc 会把 `-- name:` 头一起发出去，所以改了查询体也还认得），确认真堵住了才提交。

★ **提交早了就什么也没测到**——那只是两次顺序插入，而顺序执行本来就是好的。

修前跑：500，错误串和线上一字不差。修后：201，且返回的是对方那行的 `dropped` 状态。

变异四条：冲突臂改 `EXCLUDED.status` → 状态断言挂；冲突臂改成产不出行 → 主断言挂。★ 还有一条**变异开猛了反而问不出话**：给冲突臂加 `WHERE false` 会让顺序路径也返回零行，于是「去掉等待守卫是否还能抓到」这个问题它答不了——最后是拿裸 SQL 的顺序/重叠对照回答的。

### 同一个写法在仓库里有三处

按症状搜只会找到一处，按**写法**搜（`DO NOTHING` + `UNION ALL` 读回 + `:one`）找到三处：

| 查询 | 有人调吗 | 撞上时的用户可见结果 |
|---|---|---|
| `InsertSubscriptionIfAbsent` | 有 | 500 |
| `CreatePendingReport` | 有 | **404「举报对象不存在」** |
| `InsertNotificationDedupe` | 无 | — |

举报那条更阴：处理器本来就把 `ErrNoRows` 当成「目标没了」映射成 404，于是并发举报同一目标的人被告知对象不存在，而他自己的举报正躺在审核队列里。

★ 它**不能**用同样的 SQL 改法：`reports` 上是两条**部分**唯一索引（评论一条、用户一条），而 `DO UPDATE` 只能推断其中一条。所以改在处理器：空结果重试一次。重试跑在新快照上，撞键的能读回那条 pending 举报；目标真的不存在则仍然空，仍然 404。两种含义就此分开，各有一条不需要容器的单测；变异（去掉重试、重试两次）都被调用次数断言抓住。

第三条没有任何调用方，只在注释里被提到过一次。**不改动没人调用的查询**，但把这颗雷写进它的注释：谁要接它，先看 subscriptions 那条的长版说明。

### ★★ CI 自己的诊断说反了，而我写的第二版判据同样是错的

同一次 main 上的 sandbox 失败是另一回事——`/seasonal/spring/2026` 预热 404，是已知的 Turbopack「`[lang]` 下嵌套动态段整段不编译」。守卫打印的判据说「缺 `generate-params` + 编译时间接近零 = dev-server 缺陷；有 `generate-params` + 几百 ms = 真回归」，这次日志两条都不成立：`/anime/0` 无 `generate-params` 而路由完全正常，`/seasonal` 第一次是 650ms 并不接近零。

于是我改成「看这条路由有没有属于它的 `○ Compiling <段>` 行」——**然后本 PR 自己的失败把这一版也证伪了**：那次运行整份 dev 日志一条 `○ Compiling` 都没有，而 `/` 和 `/welcome` 都正常编译并返回 200。本地起 dev 复核，同样一条都不打印。再补一刀：**真产品 404 的重复请求也会掉到 ~100ms**（`/anime/0` 第 2/3/4 次分别 137/153/101ms），所以「重试稳定在几十毫秒」也分不开两者。

★★ **迄今试过的每一条「看日志形状」的判据都有反例。** 守卫文案因此改成不再断言任何签名，只给出一分钟内能出结论的动作：**切到这个 commit，本地 `npx next dev`，打同一条路由**。季度页会吞掉 API 失败，不用起后端。本地能出 200 就是 (1)，本地也 404 就是 (2) 且你已经拿到了复现。

本 PR 就是这么判的：本地 `/seasonal/spring/2026` 返回 200（850ms，首次编译），`/anime/0` 返回 404，与线上表现一致——沙箱那次红的不是这次改动。

### 判断

服务端的幂等创建现在真的幂等了，包括重叠调用。客户端为什么会对同一部番同时发出两次创建没有一起改——契约修好之后那是个浪费，不是缺陷。

---

## [3.28.0] - 2026-09-05

### ★★ 一集配错标题有三条互不重叠的路，我一开始以为只有一条

从「每十行分集标题就有一行落在自己那一季的集数之外」查起（**21,001 / 203,019 行，涉及 960 部**），本以为是集号归一化那个 `offset := floor(min Sort) - 1` 的锅——它和 PR#108 从库网格里拔掉的 `shift = lowest - 1` 是同一个假设。

★ **形状相似不等于成因相同。** 那个减法总把最小集映射成 1，**不可能让序列冲出上界**；冲出上界的是 bgm 的一个 subject 覆盖范围大于一个 AniList 条目。缺的一直是上界校验，不是那个减法。认出一个熟悉的反模式之后，仍然要单独证明它和眼前的现象有因果关系。

### 路 1：上界是窗口，不是过滤器

按有没有 PREQUEL 边把 960 部拆开，两种处理方式的代价完全不对称：

| 分组 | 数量 | 含义 |
|---|---|---|
| 无 PREQUEL | 693 | 第一季，前缀就是本季，溢出的是尾巴 |
| 有 PREQUEL | 267 | 续季，**槽位 1..N 现在装的是上一季的标题** |

一刀切拒绝会为了修 267 部而毁掉 693 部本来正确的标题。所以 `normalizeEpisodeTitles` 改成三分支：shifted 合身则原样保留；溢出且 offset 已知则取 `[offset+1, offset+total]` 这个窗口；溢出且 offset 未知则一行不写。★ `known:false` 不是 `offset:0`，在没人确认过的原点上取前缀，产出的是一个**渲染得像正确答案的猜测**。

`total <= 0` 直接放行不设界——`ListEpisodesBgmCandidates` 的 `WHERE ac.episodes IS NULL` 保证了 episodes_bgm 那个调用方永远走这条，它的工作恰恰是从这份列表推导集数。

★ **变异测试里「上界差一」第一轮活了下来**，因为唯一相关的那条测试传了已知 offset，走窗口分支返回同样结果。补一条「恰好填满 + offset 未知必须写入」才抓住。一条测试覆盖到某行代码，不等于它能区分那行代码的变体。

### 路 2：一个 subject 描述一个作品，而模糊匹配那条门没有这条守卫

聚合数字有个盲区：这一整轮的数全部派生自同一个谓词 `episode > episodes`，于是**「编号没超界但对应错了」的情况一条都不会出现在任何一张表里**。它是在举具体例子时才暴露的——JoJo 石之海 Part 2 的详情页显示的是 Part 1 的分集标题，而它 `max_ep 12 ≤ episodes 26`，上界一次都不会触发。

查全之后：**541 个 subject 被两个以上 anilist_id 共用**，牵涉 1,161 行、22,019 条分集标题。★★ **其中 `bgm_match_source='id_map'` 的是 0 个** —— `BindBgmIdsFromIdMap` 从写下来就有「已被占用就不绑」的 `NOT EXISTS` 守卫，`UpdateBangumiV1`（模糊匹配那条路）没有。这个不对称就是全部碰撞的来源。

`UpdateBangumiV1` 现在先查 `CountAnimeBoundToBgmID`，占用则 **park 成 `needs-review`** 而不是跳过——留在 version 0 的行会经孤儿扫描回来、每一轮再撞一次。SQL 里另加 `NOT EXISTS` 兜住预检关不掉的竞态，所以零行写入的日志从 `stale_skip` 改成 `stale_or_taken`：它不再只有一个原因。读失败返回错误而不是继续：**一次读不到，不是可以绑定的许可**。

数据侧清理后：共用 subject **541 → 77**，共用条目 **1,161 → 163**，越界行 **21,001 → 12,208**。

### ★★ 同一个病犯了两次：换了数据源，没换「相信单一源」这个习惯

**第一次**，把 `bgm_id_map` 的 schema 注释「Authoritative」当成了证明。拿 dandanplay 复核后，264 个可裁决 subject 里 **id_map 错了 26 个（约 10%）**，主导模式是「剧场版的 subject 被指到 TV 系列」。救我的不是判断力，是两条出于别的理由加的保守过滤——剧场版通常 1 集 1 行，`rows > episodes` 为假，正好被排除。**是巧合，不是设计。**

**第二次**，转头把 dandanplay 当成了新的权威。★★ `bangumi/bgmtv/{id}` 是**反向**查询，而 dandanplay 自己有多个条目共用同一个 `bangumiUrl`，反查只返回其中任意一个。它对 subject 18692 说是 anilist 501，bgm.tv 的地面真相是 `ドラえもん / 2005-04-15` = 8687。我照着 park 了 Doraemon (2005)，又照着把一个本来判对的条目回滚成了错的。**正向（给定 AniList id 找 bangumiUrl）可靠，反向不可靠。**

最终靠 bgm.tv 自己的 `name` / `date` / `eps` / `platform` 四个独立信号交叉才收敛：对全部 549 个已 park 条目做权威审计，**492 判对、10 可能判错、9 打平**，按硬规则纠正了 11 条。

★ 一处不可逆损失：有一个条目是临时用单条命令备份的，**只备了 `anime_cache` 没备标题行**，它被 park 时删掉的 100 行恢复不了。批量流程每次都是两张表一起备的，加急操作漏了。

### 路 4：RELEASING 补写扫描空转了很久，而数据一直在上游

三次相隔六小时的扫描报的是同一组数：

```
candidates 123  accepted 0  rejected 77  empty 46
candidates 123  accepted 0  rejected 77  empty 46
```

对当时到期的 132 行按扫描自己的判定顺序分类：

| 归宿 | 数量 |
|---|---|
| dandanplay **有**该 subject 但不发布 AniList 交叉链 | 68 |
| dandanplay 没有该 subject | 35 |
| 列表比本季宽 | 2 |
| 本该写入成功 | 27 |

dandanplay 的交叉链是这个扫描唯一能自己做的身份检查，所以缺链就终止。而**缺链是常态不是边角**：103 / 132。bgm.tv 对其中约一半有带名字的正片（抽 18 个，9 个有），而 `bangumi_v2` 路径本来就在用它。

三处修改：**(1)** ddp 无法背书时改问 bgm.tv，身份闸门复用 episodes_bgm 那边的 `evaluateBinding`，`undecided` 仍然不算通过——换的是上游，不是证据门槛。**(2)** `rejected` 和 `empty` 现在也盖 `episode_titles_at`：两者原来都不打戳，而候选查询 `NULLS FIRST`，于是这些行永远排在队首、能服务的行排在它们后面。★★ 这是 migration 0015 和 0023 各自记录过的同一个 stall 的**第三扇门**：「我们什么都没决定」必须和「我们从没看过」可区分。**(3)** 过宽的列表改取窗口而不是整体拒绝，用的是和 `normalizeEpisodeTitles` 同一个 `[offset+1, offset+total]`，且都经由 `episodeBound` 取数——两个写入方不可能对「一季从哪开始」有分歧。

★ 测量本身也翻过一次车：第一版统计脚本 grep 的 `episode_type` 在 `/v0/episodes` 的响应里根本不存在，于是 18 个样本全返回 0——**看起来像「bgm.tv 也没数据」这个干净结论**。用已知有 13 集的 subject 校准才发现。今天第三次出现同一形状（`bool_or` 全 NULL 返回 NULL、标签配对校验器给内容稀烂的文件放行、grep 错字段）：**校验器回答的是它自己的问题，不是我的问题。**

### 仍然不能说「都绑对了」

已绑定 12,997 个里，**4,664 个没有任何来源记录**（占 36%），任何审计都够不到；`id_map` 6,422、`fuzzy_high` 1,910。未绑定 5,048 个（2,473 从未尝试、2,542 明确拒绝）。仍有 77 个 subject 被共用，**其中 21 个来自 id_map 自身的重复**——新守卫堵的是模糊匹配那条门，堵不住这一类。

⚠️ 兜底与盖戳**没有测试**：那个 worker 持有具体的 `*dbgen.Queries`（写入是共享事务），需要集成套件的用例，本轮未补。

---

## [3.27.0] - 2026-09-05

### ★★ 一轮 4,000 次的预算，2,730 次会花在昨天已经回答过的行上

今天这轮分集标题回填第一次启动，200 行只写出 **1** 行，花掉 196 次请求。

`--resume` 的名字比它做的事宽：`episode_titles_at` **只在写标题的同一个事务里打戳**，所以一行「上游没有」或「没有任何来源为这个绑定背书」的行从来不打戳，于是永远留在候选列表的头部，每一轮都被重新问一遍。这个默认本身是对的——重问一个空 subject 只花一次请求，而漏掉一行没写过的会静默丢掉它——但**它的代价不是常数**。

实测：5,436 个候选里 **2,730 个排在 09-04 那轮停下的位置之前**，而它们昨天已经全被判成 `NO_TITLES / UNDECIDED / DDP_REBIND / SCOPE_MISMATCH`。这些判决不会隔夜改变，那 200 行 1 次写入就是证据。而这份预算和线上弹幕共用同一个功能组——白烧的不只是自己的一轮。

★ **断点是反推出来的，不是猜的。** 把 09-04 那轮看到的候选集重建出来（今天的候选 ∪ 那轮打过戳的行 = 6,910 行），它最后一次写入落在 `anilist_id 141932`，排在第 **4,132** 位——正好是它 4,000 次请求预算耗尽的位置。

`--start-after-id` 因此是**预算工具不是正确性工具**：被它跳过的行仍然是候选、仍然没打戳，不带这个 flag 的一轮照样会走到它们。边界用 `<=` 而不是 `<`，因为传进去的 N 是上一轮**够到过**的最后一个 id，那一行已经被问过了。真正的修法是把「问过」和「写过」分开记，那需要一列和一个 migration。

### 分集标题：2,709 行从没被问过的行，2,628 次请求跑完

在预算内**完整收尾**——`SKIPPED` 0（整条尾巴一次跑干净），没有中止，`QUOTA_SPENT` 和 `FETCH_FAIL` 各 0。

| 分类 | 数量 | 占比 |
|---|---|---|
| **WRITTEN** | **1,296** | 47.8% |
| NO_TITLES | 969 | 35.8% |
| UNDECIDED | 265 | 9.8% |
| DDP_REBIND | 159 | 5.9% |
| SCOPE_MISMATCH | 20 | 0.7% |
| QUOTA_SPENT / FETCH_FAIL / MAP_CONFLICT / SKIPPED | 0 | — |

写入率 **47.8%**，对照重走老路的 **0.5%**——同一份预算，同一个上游，差别只在问的是谁。

★★ **「titles written: 15,441」不等于新增了 15,441 条标题。** 那个计数器数的是**被 upsert 碰到的行**：`UpsertEpisodeTitleSourced` 的 `DO UPDATE` 分支没有 WHERE，所以一次冲突不管 CASE 最后换没换值都记一行。库里能核对到的变化是：分集标题 218,319 → **219,359**（+1,040 条新行）、有中文名的 90,294 → **92,621**（+2,327）、有任何标题的番剧 12,082 → **12,150**（+68）、完成打戳的 8,092 → **9,390**（+1,296 部）。其余一万多次是在已有行上更新或复述同一个值——我用的两个聚合数（行数、非空中文名数）分不出这两者。⚠️ 同一个口径问题对 3.26.0 那条「新写入 17,646 条」同样成立。

★ `SCOPE_MISMATCH` 20 次仍是守卫在干活：抽 6 个样本分别是条目 13 集 / 上游列表到 49 集、12→26、23→47、12→28、1→3、24→72。

★★ **「从没被问过」的存量到此清零。** 剩下的 4,138 个候选**全部至少被问过一次**：约 2,489 个 `NO_TITLES`（dandanplay 索引里没有这个 subject）、约 1,021 个 `UNDECIDED`、约 575 个 `DDP_REBIND`、约 42 个 `SCOPE_MISMATCH`。所以「明天再跑一轮」这个动作**已经没有对象了**——再跑一轮是花 4,138 次请求把昨天的答案再听一遍。这三类各自是绑定质量问题、不是回填进度问题。

---

## [3.26.0] - 2026-09-04

### ★★ 同一个字段既报「我拒绝」也报「我没有」，而我只认了前一半

上一版把 dandanplay 的 `errorCode` 接了出来，判据是「非零即拒绝」。部署十分钟后线上暴露了它的另一半：

```
HTTP/1.1 200 OK
{"bangumi":null,"errorCode":7,"success":false,"errorMessage":"无法找到指定的资源"}
```

**code 7 是「我没有这条」—— 那是一个答案，不是拒绝。** 而对一个大部分条目 dandanplay 根本没收录的目录来说，它是**常态**。于是 `/api/dandanplay/episodes/{id}` 对这类番返回 500，后台 RELEASING sweep 的日志被 `episode_titles fetch failed` 灌满，十分钟 34 条，每一条都是正常答案。同一窗口内配额错误**零次** —— 上一版想抓的东西一次没触发，触发的全是误伤。

讽刺的是上一版的 commit message 里专门写了「用 `!Success` 会把这个修复变成同一批调用的全面中断」并避开了它，然后一头撞进它的兄弟坑。**两个坑的形状一模一样：把一个承载多种含义的字段当成二值信号。**

改对的判据不是「成功还是失败」，而是**这个码在描述「那个资源」还是在描述「我们能不能问」**：

| 码 | 是什么 | 结果 |
|---|---|---|
| 429 配额 | 拒绝 | `ErrQuotaExceeded` |
| 7 未找到 | 答案 | `(nil, nil)`，静默，和以前一样 |
| 其余 | 未编目 | `(nil, nil)` + 一条带码和原文的 WARN |

★ **未编目改成失败开放，是有意反转上一版。** 一个从没见过的码，是「关于资源的另一个事实」的可能性远大于「一种新的拒绝方式」，而这个客户端在读者前面：猜错方向猜成「拒绝」的代价是线上 500，猜成「答案」的代价只是一行日志。**而日志正是原始缺陷真正缺的东西** —— 它返回 nil 且毫无信号。

★★ **两条测试断言的是日志而不是返回值**，因为两个分支都返回 `(nil, nil)`，行为分不出来。这不是事后聪明：第一版测试在把 code 7 的编目条目删掉之后**照样通过**，是变异测试抓出来的 —— 一条声称在测常量、实际在测默认分支的测试。

★ 用户面零影响。按状态码字段核对，部署后 dandanplay 路由上唯一那条 500 的 UA 是 `curl/7.71.1`，也就是我自己的验收探针。**救回来的不是测试而是「部署后主动逐个端点打一遍」** —— 冒烟脚本只打 `/`、`/api/health` 和一个详情页，这条路径它一个都不碰。

### 一个测试为了够到九行纯函数，把 `next/image` 拖进了共享模块缓存

`next-app (bun test)` 在**一个前端文件都没改**的分支上连挂两次，而 main 在逐字节相同的 next-app 树上是绿的。

`continueWatchingTotal.test.ts` 测的是 `resolveWatchingTotal`，两个数字的九行算术。它 import 了 `ContinueWatching` 组件，而组件拉 `FadeImage`、`FadeImage` 拉 `next/image`，后者的 `deployment-id` 模块**在 import 时**执行：

```js
if (typeof window !== 'undefined') {
    deploymentId = document.documentElement.dataset.dplId;
}
```

bun:test 全套共用一个进程和一份模块缓存，而这个套件里有三个文件会在运行时往全局塞 `window`（`pendingSubscribe`、`SubscriptionSetProvider`、`api.buildHeaders`）。于是这个 import 会不会炸，取决于**「别的文件第一次拉进 Next 的那一刻，恰好是谁在跑」** —— 套件里没有任何东西控制这个顺序。这就是它专挑不相干的 diff 发作的原因。

★ 我第一次判成随机 flake 让重跑，重跑照样挂 —— **「连挂两次」本身就是「这不是随机」的证据**，我却又找了一遍理由。修法是把纯函数挪到已经存在的、不含 Next 的 `continueWatchingState.ts`。

验证是前后对照而不是等 CI 变绿：在 `window` 存在、`document` 不存在的进程里，旧的 import 形状 0 pass / 1 fail 并复现同一条 `ReferenceError`，新的 5 pass / 0 fail。

### 分集标题又跑了一轮：4,000 次请求换 17,646 条标题

在 `--limit 4000` 的预算边界上正常收尾 —— **没有中止、零 FETCH_FAIL、零 QUOTA_SPENT**。

新写入 **17,646 条**分集标题（其中中文 **10,636** 条），新覆盖 **1,384 部**番剧（6,708 → 8,092），撤回 0。剩余候选 6,804 → 5,420，等下一个额度日。

| 分类 | 数量 | 占比 |
|---|---|---|
| SKIPPED（预算外） | 2,706 | 39.8% |
| NO_TITLES | 1,520 | 22.3% |
| **WRITTEN** | **1,384** | 20.3% |
| UNDECIDED | 756 | 11.1% |
| DDP_REBIND | 416 | 6.1% |
| SCOPE_MISMATCH | 22 | 0.3% |
| FETCH_FAIL / QUOTA_SPENT / MAP_CONFLICT | 0 | — |

★ **`SCOPE_MISMATCH` 22 次是守卫在工作，不是故障。** 这 22 部的 dandanplay 分集列表长度超过条目自身的两倍，写进去会把整个系列的集号错位盖到一季上。

★ **`FETCH_FAIL` 为 0 是上面那个 code 7 修复的验收。** 用修复前的二进制跑，这 1,520 个 `NO_TITLES` 会有相当一部分变成 `FETCH_FAIL`，报告直接失真 —— 而失真的方向恰好是「看起来上游坏了」，最容易让人停下一轮本来该跑完的批。

★ `UNDECIDED` 756 + `DDP_REBIND` 416 = 1,172 行拿到了上游数据却**故意没写**：前者是没有任何独立来源为这个绑定背书，后者是 dandanplay 说这个 subject 属于另一个 anilist 条目。只报告不动手。

---

## [3.25.0] - 2026-09-04

### ★★ 上游用 HTTP 200 告诉我们它拒绝了，而我们把拒绝读成了「没有数据」

dandanplay 把应用层错误放在 200 的响应体里：

```
HTTP/1.1 200 OK
{"success":false,"errorCode":429,"errorMessage":"已达到接口调用配额上限"}
```

payload 字段直接缺席。而客户端只对 `resp.StatusCode` 分支，全仓从头到尾**没有任何地方解析过 `success` / `errorCode` / `errorMessage`**。于是 200 落进解码分支，`bangumi` 解成 nil，`fetchEpisodes` 返回 `(nil, nil)` —— 这个包里每一个调用方都把它读作「上游没有这个 id 的数据」。

配额耗尽因此和「条目本来就是空的」完全同形，而三个使用方各自从中得出了一个不同的错误结论：

- **播放器**告诉用户「这部番没有弹幕」，而那部番是有弹幕的；`/match` 还会把这个判决缓存 10 分钟
- **分集标题回填**把该行记为「没有标题」—— 那正是它的空结果熔断器用来累积的信号，于是「配额耗尽的一轮」和「跑过一批没标题的番」产生的证据一模一样
- **交叉链接探针**记下「dandanplay 没有为这个条目发布 AniList 链接」，那恰好是唯一会让一行不可绑的结论，然后把它当成测量结果报了出来

第三个就是这次发现它的方式，而且是**能量化的**：2026-09-04 一次探针花掉 103 次详情调用，dandanplay 后台的月度计数只动了 1。每一次被拒绝的调用都被记成了一条发现。

同一时刻分组验证过：`/api/v2/search/anime` 答 `success:true`，而 `/api/v2/bangumi/{id}` 和 `/api/v2/bangumi/bgmtv/{id}` 都答 200 + `errorCode 429` —— 因为**额度是按功能组算的**，只有番剧详情组被跑光。所以这件事既不能从别的调用是否健康推断，也不能从它自己的 HTTP 状态码推断。

修法是给四个信封结构共享一个 `apiStatus`，每次解码后跑 `checkStatus`，返回 `ErrQuotaExceeded` 或 `ErrUpstreamRejected`。★ 判据用的是 `errorCode != 0` 而**不是** `!Success`：有些端点在正常响应里根本不带 `success` 字段，而 Go 里缺席的 bool 就是 false —— 用 `!Success` 会把这个「修静默失败」变成「同一批调用全线中断」。有一条测试专门盯着这个反向风险。

拒绝**不在进程内重试**（日额度要等上游自己的日界，在一次作业的退避窗口里重试只是白花共享预算），也**不进缓存**（缓存一次拒绝会让它活得比拒绝本身还久）。

### 修好客户端反而会让回填变笨，所以两件事必须一起做

配额拒绝以前是 `(nil, nil)` → 记成 `NO_TITLES` → 熔断器累积 → 最终中止。理由错了，结果对了。

客户端修好之后拒绝变成 error → 落进 `FETCH_FAIL` —— 而 `nextEmptyStreak` **对任何非 NO_TITLES 的分类都清零**。熔断器从此再也不会触发，一轮跑批会对剩下每一行都问一遍、被拒一遍，每行 800ms，把同一个事实学上几千次。

所以配额耗尽现在是它自己的分类，第一次出现就中止。它不是 `FETCH_FAIL` 的一种口味，因为它是唯一一个**在讲这一轮而不是讲这一行**的结论：剩下每一行都会得到同样的答案。停下来不损失任何东西——`episode_titles_at` 只在写标题的同一个事务里打戳，没被读到的行不打戳，下次 `--resume` 仍是候选。

判断函数被抽出来是为了能被测试够到，而测试**驱动的是真客户端打真的那个 200 响应体**，不是手搓的 sentinel。sentinel 出门时被包了两层（`checkStatus` 一层、`do()` 一层），一个只对裸 sentinel 生效的匹配器能编译、能通过基于同样假设写的测试，然后把生产上每一次拒绝都判成 `FETCH_FAIL`。

### 探针只测得到目录的头部，而这张表的头部已经被证明不具代表性

交叉链接探针存在的意义是产出一个数——map 沉默的行里 dandanplay 能绑上多少——而这个数会被乘以约 4,400 去做决策。在此之前唯一能控制它成本的是 `--limit`，那会让它在一个按 `anilist_id` 排序的查询里中途停下。于是每一次跑得起的探测都只看了目录的头部。

这个偏差**在同一张表、同一个排序、同一批人口（未绑定行）上是量过的**：id-map 那一轮在头部测到 34.3% 有 map 佐证，而中段各带是 69–78%。差了大约一倍。`anilist_id` 大致按时间排，而被测的东西（dandanplay 的收录、以及它发不发布 AniList 链接）完全有理由随作品年代变化。摘要里其实早就印着一句「把这个外推当成一个区间」的免责声明——这一版把那句话换成了一个真数。

`-probe-sample N` 在整个有序集合上等距取 N 行。用等距而不是随机：这个排序没有周期性可以让步长产生混叠，不用多查一次库，而且可复现——同样的 N 对同样的目录取到同样的行，两次跑批可以对比而不只是平均。

★ 索引用的是比例式 `i*len/want` 而不是固定步长，而**这是测试抓出来的**：4463/300 截断成 14，300 步 × 14 停在 4186，无论样本多大，最后 277 行永远取不到。那是同一个头尾偏差换到另一头，而且样本越小它越大。

### dandanplay 能补的 bgm_id 有多少：6.8%，而天花板是 24.2%

600 行全目录等距样本（每 7 行取 1），仪器已用正向对照验证过：

| 分类 | 数量 | 占比 |
|---|---|---|
| BINDABLE | 41 | 6.8% |
| SUBJECT_TAKEN | 3 | 0.5% |
| NO_BGM_LINK | 36 | 6.0% |
| ANILIST_MISMATCH | 37 | 6.2% |
| NO_ANILIST_LINK | 25 | 4.2% |
| **NO_SEARCH_HIT** | **455** | **75.8%** |
| FETCH_FAIL | 3 | 0.5% |

绑定率 6.8%（95% CI 4.8%–8.9%），全量 4,465 行外推约 **305 行可绑**、约 5,983 次请求。

**决定做 sweep。** 成本落在两个不同的额度组上：约 4,465 次搜索走搜索组（该组常年闲置），约 1,518 次详情走番剧详情组（一天配额的 30%）。一天跑完、约 305 个新绑定、一次性。机会捕获对这批几乎必然为零——四分之三的行 dandanplay 索引里根本没有，用户也不会去播这些冷门条目。

★ 命中位置直方图是 `#1 → 76, #2 → 4, #3 → 0`：候选上限从 3 降到 2 零代价。

★★ **75.8% 的 NO_SEARCH_HIT 和匹配算法无关**，它是这条路的天花板。dandanplay 索引里没有这些条目，再好的匹配器也够不到四分之三——「用 dandanplay 补 bgm_id」的上界是 24.2%，不是 100%。

---

## [3.24.0] - 2026-09-03

### 两个接口对同一个 subject 给出不同答案，而流水线两边各用一个

V1 绑定 bgm_id 走的是**老搜索接口** `/search/subject/{关键词}`，V2 读取 subject 走的是 **v0 接口** `/v0/subjects/{id}`。这两个接口对匿名调用者的可见范围并不一样：老接口照常返回 R18 条目，v0 接口把它们藏起来、答 404。我们两边都不带 Authorization 头（客户端就没设，部署里也没有 Bangumi token），于是每一部 R18 作品都能在 V1 绑定成功，然后在 V2 永远 404。

不是推断，是同一台机器同一分钟里打出来的：老搜索接口按关键词返回 subject 63029，`/v0/subjects/63029` 同时答 404。随机抽 59 个卡住的 bgm_id，59 个全 404；抽 3 个已经完成过 V2 的绑定，3 个全 200。

顺带纠正一条记在 TODOS 里的旧结论。之前抽样 12 个也得到 12/12 的 404，据此判断这批是「`fuzzy_high` 绑到了不存在的 subject id」，理由是 404 响应体写着 resource has been removed。那句话是 Bangumi 的固定文案，原文是 *can't be found in the database **or** has been removed*，它并不声称条目被删了。拿当时被点名的那个 id 去老搜索接口查，`207567 僧侶と交わる色欲の夜に…` 好端端地在结果里——另外两个抽样 id 也是。**绑定是对的，只是我们读不到**。这个区别决定了修法方向完全相反：按旧结论应该解绑，按事实必须保留。

### ★★「永久跳过」跳过的是写入，不是任务

V2 判定 404 为永久失败、`return nil` 不重试，这个判断是对的——匿名请求重试一万次也不会变成登录请求。错的是它**什么都没写**。

行留在 `bangumi_version = 1`，而 1 是系统里没有任何生产者会去捞的值：orphan scan 取 0，heal-CN 取 2，V1 的几个生产者也全取 0。于是这些行同时具备两个性质——**任何 sweep 都够不到，后台却一直把它们算作待办**。后台那个「Re-enrich v1」按钮做的事，正是把已经证明跑不完的 V2 作业再入队一遍：请求花掉了，行一动不动，计数一点不降，按钮永远点不完。

到定位时有 811 行躺在这里，753 行是 OVA，最早的从 2026-06-04 起就没动过。判据不用猜：这 811 行的 `bangumi_score` **全是 NULL**，而这个字段只有 V2 会写，所以那个 NULL 就是「V2 一次都没完成过」的证据。

现在 V2 在这个 404 上先写终态再跳过。同一个按钮因此从「重放失败」变成「收敛」：它入队的每一行，要么完成 V2，要么永久离开 version 1。

### 终态为什么是 version 3 加一个时间戳，而不是一个 flag

只改 `bangumi_version` 就能让行离开流水线——写 3 即可，这个 schema 里 3 早就不是「第三阶段跑过了」而是「这行能走的路走到头了」，`PromoteAnimeToV3` 给的正是 V3 永远碰不到的行。但 v3 也住着一万六千多行健康数据，所以单靠它，行不是离开了流水线而是**离开了视野**，「我们手里有多少个读不到的绑定」当场变成无法回答的问题。而这个问题是有决策挂在后面的——它就是要不要去弄一个 Bangumi token 的全部理由。

不加列的替代写法 `bangumi_version >= 3 AND bgm_id IS NOT NULL AND bangumi_score IS NULL` 今天碰巧只匹配 37 行，但它从构造上就是错的：一个存在、可读、只是没有评分的 subject 会留下一模一样的 NULL，把「读不到」和「没东西可读」混成一个。

`admin_flag` 也装不下，理由 migration 0023 面对同样的选择时已经写过一遍：它的 CHECK 只认 `needs-review` 和 `manually-corrected`，是一个后台已经在读的两值人工流程标记，往里加机器状态等于把两个无关的状态机塞进一列，并且破坏所有现有读取方的穷尽假设。

所以是一个独立的列，而且是时间戳不是枚举——只有一种结果要记，一个成员的枚举是把事实伪装成词表。NULL 表示「从没被观察为不可读」，这是每一行从没被问过时的诚实默认值；有值表示「在那一刻，`/v0/subjects/{bgm_id}` 说找不到」，这是这次观察能支撑的最强说法。上游的答案可能变，时间戳顺便说明我们这份副本有多旧，也让将来的复查天然有游标。

`UpdateBangumiV2` 在每次成功读到 subject 时清掉这个戳——**没有回到 NULL 的路径的标记，早晚会变成谎话**。

### GREATEST 不是保险，是刚发生过的事

写终态那条语句用 `GREATEST(bangumi_version, 3)` 而不是直接赋 3，因为它有两个入口：V1 链过来的 version-1 行（3 是升级），和 id-map sweep 链过来的老 version-3 行（本来就是终态，不能动）。

直接赋值今天也能跑对，但同一个文件里的邻居 `UpdateBangumiV2` 就是直接赋 2 的——而这一版之前，id-map sweep 绑定的行链式跑完 V2，正是被那句话**从 3 拽回了 2**，一次 750 行。棘轮不是防御性编程，是照着刚踩过的坑写的。

同一条语句还钉住了绑定（`AND bgm_id = 我们探测的那个 id`），和 `UpdateDescriptionCn`、`UpsertEpisodeTitleSourced` 一个道理：这次 404 是关于**一个** subject 的判决，若在取数据和写入之间有人改了绑定，这个判决就会落到一个根本没被探测过的 subject 头上。绑定移动了则影响 0 行，worker 把这个 0 记进日志而不是当成写成功。

`bgm_id` 一律保留。绑定没错，只是匿名读不到；清掉它等于丢掉一份有 token 就立刻有价值的工作，还会把这个 subject 当成无主的重新交回给 id-map sweep。

### migration 故意不回填

那 811 行完全可以在 migration 里一条 UPDATE 全标掉，而这样做是错的：**判断一个 subject 读不读得到，唯一诚实的办法是去问，而 migration 问不了**。59 个样本给出的上限是大约 5%——小，但不是零。从样本推广到全体，会把本该能富化的行标成不可读。

所以它们走修好之后的 worker 排干，每行一次真实请求。这正是「Re-enrich v1」按钮一直在做的事，只是从这一版起它终于是解药而不是重演。

### 后台多了一行不能点的数字

`subjectUnreadable` 计数挂在 v3 那个图例点上，和已有的「其中 N 无中文」并排。

这条 bar 的前提是「上面每个数字都能被复现、也都能被处理」，而这个数字故意破坏了后半句——除了换一个带认证的客户端，没有任何操作能改变它。渲染它仍然是两害相权：这些行终态停在 v3，没有自己的一行就和完整富化过的行长得一模一样，而「我们有多少个读不到的绑定」会在唯一展示所有富化数字的地方消失。

有一条测试专门断言这个数字**不出现在任何 `<button>` 里**。给它配个按钮，就是把刚修掉的缺陷原样造一遍：一个把上游已经拒绝过的作业重新入队的控件。

---

## [3.23.0] - 2026-09-03

### 番剧详情页开始有每集的标题

此前分集标题只有 Bangumi 一个来源，覆盖到一半就停了。这一版把 dandanplay 接成第二个来源，并把两个来源的冲突规则写死在数据库里：每个字段单独记来源，优先级 `manual > ddp > bangumi`，低优先级的写入不会覆盖高优先级的值。

覆盖率 53.8% → 67.6%，中文标题 28.6% → 31.3%。有分集标题的番剧从 11,205 部到 11,315 部。

### ★★ 一个空列可以「占着」一个字段，让它永远填不上

来源列是这一版加的，而旧的写入语句不写来源。两者同时在线的那四个小时里，`episodes_bgm` 那个 sweep 跑了 60 次，留下 2,010 行**值是空的、来源却写着 `ddp`** 的记录。

这不是无害的脏数据。写入语句是拿**来源列**比优先级的，所以一个空列挂着 `ddp` 标签，读起来就是「这个字段已经被 ddp 占了」，于是每一个优先级不高于 ddp 的自动写入者都会被它挡回去——1,966 个集位因此变成永久填不上，而页面上它们只是空白。

修法分两半：写入路径统一收口到带来源的那条语句（旧的直接删掉，留着就还会有人用），存量用 migration 0030 双向修复——有标签没有值的，撤销标签；有值没有标签的，标成 `bangumi`（能产生这个形状的只有三个写入者，全都读 Bangumi，所以这是事实不是猜测）。

生产上执行后孤儿行 2,010 → 0。

### `btrim()` 不去全角空格

集成测试写到一半发现的：`btrim(text)` 不带第二个参数时**只去 ASCII 空格**。制表符、换行、以及 **U+3000 全角空格**全部原样通过 `NULLIF`，落成一行渲染出来是空白、却又按上面那条规则占住了字段的记录。

现在显式给了字符集 `E' \t\n\r　 ﻿'`。

### ★★ 熔断器一次都没能触发

上一版给回填加了熔断：连续 200 行拿不到任何标题就中止，避免上游停摆后继续空烧请求。它不工作。

实测：dandanplay 在约 4,700 次请求处停止应答，跑批又空走了 **350 行**没有中止。

原因是连空计数只看行的分类。**8,747 个候选里有 718 行（8.2%）与另一行共用同一个 bgm subject**——因为目录里有 541 个 subject 被多行持有。客户端对分集响应有 24 小时进程内缓存，第二个共用者直接从内存拿到真数据，判成「拿到了但拒绝采用」，旧规则把这当成上游还活着，于是**平均每 12 行就把计数清零一次**。

可那个缓存响应根本没碰过上游。

新规则一句话：**只有真正走到网络的行才能移动这个计数**，两个方向都是。没问过上游的行保持中立——它既不指控也不背书。同一个区分顺带修掉另一个错：缓存命中此前被算进了「本次请求数」，虚高了运行自报的开销。

为什么原来的测试没抓到：判定式 `shouldAbortOnEmptyStreak` 从上线第一天就有单测而且一直是对的；出错的是**喂给它的那个数**，它当时躺在循环里，一个测试都没有。

### ★★ 回填把线上弹幕功能打瘫了一天

dandanplay 的额度是**按功能组、按天**算的。而 `/api/v2/bangumi/*` 归在「番剧详情」组——那也正是 `/match` 取分集列表用的组，两个 Phase 都是。

所以一次不设上限的回填不是自己跑到停就完了：**它会把线上弹幕功能一起带下线到当天结束**。

这是实测的，不是推演。跑批把该组跑到 5,000/5,000 之后，线上对 `bgmId=291171` 返回「Anime not found on dandanplay」——而那个 subject 正是同一轮跑批两小时前刚成功读过分集标题的。

`--limit` 的默认值从 0（无上限）改成 4,500，留一成给线上。月额度是日额度的 24 倍且远未用尽，所以唯一稀缺的资源是「天」,、长回填的正确形态是跨天分批。

### 一部四集的 ONA 挂着 3,528 个分集标题

`CountMain` 从写下来那天就带着这个信号，注释里说阈值应该等分布出来再定,、别拍脑袋。分布出来了。

按**比值**（写入的最高集号 ÷ 目录集数）分档：

```
比值        番剧   越界行   绑定被 id-map 确认   平均目录集数
<=1.10      122      183          89%              29.1
1.10-1.50    95      423          75%              17.1
1.50-2.00   198    1,829          63%              10.9
>2.00       475   17,880          41%               4.2
```

按绝对溢出量分不开的两拨东西，比值一下就分开了。≤1.10 一档 89% 由独立映射确认、平均 29 集,、那是目录少算一两集——**标题是对的**，按绝对量切会把它们一起误伤。>2.00 一档平均目录集数 4.2，占全部损坏的 88%。

最后一列排除了显而易见的修法：那 475 部里有 **195 部的绑定被 id-map 确认**。所以这不是绑错，解绑是错的——Bangumi 合法地用一个 subject 覆盖 AniList 拆成正片和短片的内容，绑定是对的,、只是集列表比它指向的那一行宽。

守卫因此**整条拒绝而不是截断**：属于整个系列的列表，它的第 1 集是系列的第 1 集，头和尾一样不可信。目录集数未知的 2.9% 放行——没有分母就没有判据。

### 那些 Bangumi id 早就在库里，只是没人去用

826 部番剧没有绑定 bgm 条目，所以拿不到中文名、评分、简介、分集标题。而答案早就躺在 `bgm_id_map` 里——V1 富化流程第一步就查这张表而且无条件信任它，但那条语句带着 `bangumi_version = 0`，只处理「从没富化过」的行。这些行停在 version 3，是更早年代批量设的状态。

答案有了，也没有任何代码路径会回头去用。跟分集标题那次是同一个棘轮：每部番只走一次流水线，之后数据源变好也够不着了。

新的 sweep 就是这个缺失的入口。信任依据是免费量出来的：已绑定且这张表也有记录的行里，**3,769 行是更早的流水线独立绑的，这张表 100% 一致，零分歧**。

守卫有三道，而且都是必需的——`anime_cache.bgm_id` **没有唯一索引**（已经有 541 个 subject 被多行持有，加不上），所以 SQL 是唯一防线：
- 同批次内两行争一个 subject 的，两个都拒（数据修改语句看不见自己的写入，没有这条它们会双双通过检查然后双双绑上）
- subject 已被别的行占用的，拒
- 写入时再核对一次 `bgm_id IS NULL`

一次绑完 791 行，剩的 35 条正是守卫该拒的 33 + 2。全程重复 subject 数稳在 541，一个没多。

### 791 条绑定逐条核对：1 条错绑，而且只有年份和集数抓得到

绑完之后逐条对着 Bangumi 上的条目核了一遍——日文原名、开播年份、集数，三项都来自 AniList 那条独立的数据链，所以是真的第二意见而不是自证。

874/940 通过，零抓取失败。查出 1 条错绑：

```
              我们（AniList 111137）      绑到的 subject（bgm 320824）
标题          WAVE!!～サーフィンやっぺ!!～   WAVE!!～サーフィンやっぺ!!～
格式          MOVIE                        TV
年份          2020                         2021
集数          3                            12
```

标题**逐字相同**，相似度 1.00。**光靠标题相似度永远查不出这条**——是 2020 年的剧场版三部曲被绑到 2021 年的 12 集 TV 版上，TV 版的中文名、评分、简介都挂在了剧场版页面上。抓到它的是年份和集数这两个佐证信号。

### ★★ 没有任何办法告诉 sweep「这条别碰」

解绑那条错绑的时候才发现：清掉 `bgm_id` 正是让这一行成为候选的唯一条件，所以**这次纠正等于把它直接递给了下一轮 sweep**。

而且绕不过去——后台的编辑接口写不了 null 的 bgm_id（`null` 会被当成「没有字段要更新」），删 `bgm_id_map` 那行也没用，那张表每次启动都会被清空后从内置文件重灌。

修法是让候选集排除 `admin_flag = 'manually-corrected'`。这两个 flag 值在这里方向相反,、而这正是它们分成两个值的意义：**`needs-review` 是一个问题，map 有资格回答；`manually-corrected` 是已经给出的答案。**

顺带纠正了一处注释：写入语句里保留 `manually-corrected` 的那个分支说保护在那儿——不在，而且新位置更强。保留标记却把行重新绑回去，等于保留了一次刚被自己撤销的纠正的审计记录。

### 首页：每日更新提到了正在热议下面

原顺序是 正在热议 → 大家都在追 → 我的在追 → 本周更新，现在本周更新排第二。放送日历是这一页上唯一每天都在变的板块，它上面的两个列表都是常青内容。
---

## [3.22.0] - 2026-08-29

### 番剧详情页：每部番有了自己的颜色 —— 但只借色相角，不借颜色

`anime_cache.poster_accent` 是从封面图取样出来的原色。此前它被直接交给 `color:` 用（`.hero-relation-chip:hover`），也就是说 hover 后那行字好不好认，**取决于这部番的封面碰巧是什么颜色**。

实测 6 个真实封面色对纯黑的对比度：

```
#7caf62 绿    8.17:1  ✓
#d4a017 金    8.84:1  ✓
#e45d35 橙    5.92:1  ✓
#8b5cf6 紫    4.96:1  ✓
#2e6fbf 深蓝  4.14:1  ✗ 低于 WCAG 4.5:1
#c2185b 玫红  3.58:1  ✗ 低于 WCAG 4.5:1
```

跨度 2.5 倍，六个里两个不合格。根因不是取样算法 —— 生产库 16,547 部有色相的番里，**72.1% 偏暖**（黄 32.1% + 橙 27.5% + 红 12.5%）。暖色在黑底上亮，冷色暗，而"直接拿画面颜色当 UI 颜色"这件事本身**没有下界**。

所以新的做法是：只从封面拿 **OKLCH 色相角**，明度和彩度由设计系统固定。每部番仍然是自己的颜色，但落在一条保证可读的带子里。同样 6 部重新推导后是 7.45–7.81:1，跨度 1.05 倍。

### ★★ 写在 `:root` 里的派生色是死的 —— 六部番全渲染成同一个紫

第一版是这样写的：

```css
:root { --poster-hue: 292.7; --poster-tone: oklch(76% .085 var(--poster-hue)); }
```

看起来完全正确。它不工作。

**自定义属性的 `var()` 替换发生在声明它的元素上，不是使用它的元素上。** `--poster-tone` 在 `:root` 上就已经用 `:root` 的色相算完了，继承下去的是一个算死的紫色 —— 子元素再怎么改 `--poster-hue` 都没用。

这个失效没有任何征兆：样式表是对的，markup 是对的，CSS 变量确实在 HTML 里，代码审查看不出来。是在真实浏览器里逐个量像素时才发现六部不同颜色的番吐出同一个 `#b2a7e3`。

修法是 `globals.css` 的 `.poster-scope`：把三个 tone **重新声明在写 `--poster-hue` 的同一个元素上**。`HeroAccent` 同时输出 class 和行内 hue，两者是一个机制，拆开任何一半都退回上面那个静默失效 —— 所以这条写进了 SSR 测试。

推论已写进 DESIGN.md：**永远不要在比 `--poster-hue` 更靠上的选择器上组装 `--poster-tone*`。**

### 76% 这个明度是穷举证明的

不是挑的，也不是从几个样本归纳的。全部 360 个色相跑一遍，`oklch(76% 0.085 h)` 在最严的 `--bg-elevated` 上最低 6.22:1，零例外；能过 4.5:1 的最小明度是 68%，所以留了 8 个百分点余量。

这条证明在 `lib/oklch.test.ts` 里是**可执行的测试** —— 改 L 或 C，它会直接点名哪些色相破了。同一个文件还反向锁住 `--poster-tone-low`：断言它必须**低于** 3:1，因为一旦有人把它调亮到能当文字用，它就会被当文字用，而它不是按那个标准管的。

### 这套数学此前没有任何测试

`sampleCoverAccent.ts` 的头注释解释过为什么把 OKLab 矩阵内联在里面：「legacy 那边的 parity tests 已经钉住了这套数学」。

那个 `client/` 目录早就不在仓库里了。所以那些测试从它离开那天起就没再跑过，而这套数学在 next-app 侧的唯一入口需要 canvas，本身无法单测。

抽成 `lib/oklch.ts` 之后补了 21 个测试（含 OKLab 往返、色相归一化、灰度拒绝），并做了变异验证：降明度、抬灰度阈值、破坏一个矩阵系数、去掉 0–360 归一化、解析失败返回 0° 而不是 null —— 5 个变异全部被抓到。`sampleCoverAccent.ts` 同时从 350 行减到 214 行。

### 顺带修掉两处「不可点击的东西涂着操作蓝」

DESIGN.md 写了两遍：`--accent` 只给主操作，「可点击 = `--accent`，这条没有例外」。而格式徽章（"TV"）和四个区块标题都是 **`<span>` / `<h2>`**，点不了，却都涂着 iOS Blue。它们现在走 poster 色阶 —— 徽章不再假装自己是按钮，hero 也在一个克制的位置上有了这部番的身份色。

### 区块顺序重排，并补上「作品信息」表

顺序原本是「简介 → 关联作品 → 角色 → 制作 → 集数 → 推荐」，现在是「简介 → 作品信息 → 集数 → 角色 → 制作 → 关联 → 推荐」。

**集数从第 6 位提到第 3 位**：那是回访者唯一来找的东西。**关联作品从第 2 位降到第 6 位**：它是导航离开本页的入口，放在第二位等于在人看到任何内容之前就把人送走。

新增的「作品信息」是一张 `<dl>` 表（季度/开播/状态/集数/单集时长/类型/原作/制作）。它的存在让 hero 那句 facts 从 11 项砍到 6 项 —— 类型、季度、制作这些不适合塞进一句话：没有标签，读者得自己推断「TV」是类型、「MADHOUSE」是制作；有了标签，那句话就不再是一句话。

没有值的行照样渲染，画一个破折号。字段缺失本身就是信息，而一张每部番形状都不一样的表，比一张有洞的表更难跨页扫读。

### 集数列表：方格网格 → 行式列表

原来是 `minmax(88px, 1fr)` 的数字方格。**88px 只装得下大约四个汉字**，所以每一集的标题都渲染成省略号 —— 区分第 14 集和第 15 集的唯一信息，一直是看不见的。而集标题正是回访者来这个区块的原因。

现在每行是「圆圈 · 补零集号 · 标题 · 日文原题 · 评论数」，四列自适应，1440px 下四列、手机上一列，不需要断点。

- 勾选标记从 ✓ 字形换成固定 16px 的圆圈。旧的 ✓ 比给它预留的盒子高 1px，所以格子翻成已看的那一刻整个网格会跳
- 「最远看到」从底部横条换成行首的左脊
- 讨论入口从钉在右上角的 24×20 贴纸变成行尾的按钮 —— 贴纸读起来像徽章，不像控件
- 区块标题原本是 13px 大写字母加 2px 字距，而页面上其他标题都是 17px 常规大小写，导致集数区块看起来和它上下的区块不是同一类东西。现已统一

### ★ 字体：三个网页字体加起来，一个中日韩字形都没有

Sora、DM Sans、JetBrains Mono 全都以 `subsets: ["latin"]` 加载。所以站上**每一个中文字**都是由回落链渲染的，而那一环之前是 `system-ui` —— macOS 上是 PingFang，Windows 上是 Segoe UI（中日韩覆盖是个按安装情况而定的问题），Linux 上什么都可能。

新增 `--font-cjk` 显式列出 PingFang SC / Hiragino Sans GB / Source Han Sans SC / Noto Sans CJK SC / Microsoft YaHei。在 Mac 上这不是视觉改动（system-ui 本来就解析到这儿）；它是在 system-ui 解析不到的平台上，阻止页面回落到衬线体或一个字形度量对不上的字体。

拉丁字体仍排在最前：它们没有中日韩字形可以抢，浏览器会逐字回落 —— 这就是为什么一条声明能同时服务 “MADHOUSE” 和「葬送的芙莉莲」。

另加 `--font-jp`（Hiragino Sans / Yu Gothic / Noto Sans JP），用于日文原名和集数原题。汉字在日文和简体中文里字形不同（直 骨 令 等），日文标题用中文字体排出来能读，但对认得出区别的人是明显别扭的。

### ★ 又一次双词典漂移

把集数提示里的「点一下方格」改成「点一下某一集」（方格已经不存在了），只改了 `zh.ts`，页面纹丝不动。EpisodesGrid 是客户端组件，走 `t()` 读的是 `zh-spa.js`。

`spaDictCoverage` 是绿的 —— 它检查 key 存不存在，不检查值是否一致。两边的 key 都在，只是值不同。

### Hero 重做成整幅背景图 —— 这是换设计，不是调样式

原来的 hero 是「固定高度的横幅条 + 下方内容 + 封面跨在接缝上」。现在是「整幅背景图 + 内容压在图上、底部对齐」。

改动前后（1440px）：

```
              改前     改后    demo
banner       400px    556px   556px
操作行        y799     y535    y510      ← 从简介之后提到 facts 正下方
简介          y586     y657    （hero 外独立区块）
```

四个互相咬合的几何值变成两个：横幅高度和负 pull 存在的意义就是制造「内容压在图上」的重叠，而 `align-items: flex-end` 白送这个效果。

- 遮罩从「40% 起、到底 0.95 黑」改成「66% 起、到底纯页面色」。原来那条把最暗的一段压在画面中部 —— 也就是角色的脸上 —— 去保护根本不在那儿的文字
- `object-position: center 38%`：这个盒子比 1900×400 高得多，裁剪主要发生在竖直方向，而动画主视觉的脸都在上三分之一
- 简介移出 hero，成为自己的区块，右侧带 AniList / Bangumi 评分卡（带分带色和进度条）。正文是全页最长的一段文字，压在图片上既难读，又逼着遮罩必须暗到能盖住画面

### ★ 改了三条 e2e 断言，逐条说明理由

放宽守卫是守卫烂掉的标准路径，所以三条都写清了「它原本保护什么，现在还保护不保护」。

1. **`banner height === 400` → `=== 556`**。它保护的是「手机端的改动不许波及桌面」。这次是**有意**改桌面，数值随之更新。顺带改成四舍五入再比较：38.6vw 落在 555.828125，`toBe()` 一个浮点会把断言钉死在当前视口的亚像素上。

2. **「封面必须跨在横幅下沿」→「封面必须完全在图内」**。这条**方向反了**，因为它保护的东西不存在了：原注释说那个重叠是「hero 的整个视觉理念」，而那是针对「横幅条 + 负 margin 拉上来」那套设计说的。现在图是整个 hero 的背景，没有接缝可跨；封面越过下沿反而意味着内容溢出了 hero —— 那才是这里值得抓的缺陷。

3. **`synopsis y < 422` → `y < 844 - 300`**。把界限从坐标改成**「有多少正文可读」**，因为那才是要守的属性，而满足它的坐标取决于布局。简介移出 hero 花掉约 90px：最差的一部（20 字标题配七个分类）从 416 退到 490，常见的在 407。300px 是九到十行中文。原来那个缺陷（y=799，只剩 45px 可见）依然被这条挡下。

移动端 hero 下限设 340px 而不是「尽可能小」：内容底部对齐，下限等于内容高度就意味着一点画面都露不出来，标题会直接贴在导航栏下沿 —— 实测过 280px，就是那个样子。

### 补两个词典 key

`detail.scores` / `detail.votes`（评分面板的 aria-label 和「人评分」）。三个语言的 `.ts` 都加了；这两个只在服务端组件里用，不走 `t()`，所以不涉及 `-spa.js`。

### Hero：七颗药丸 + 一整行分类药丸，收成一句话

hero 此前把三条并列的信息带堆在标题下面：一行七颗徽章药丸（分数、BGM 分数、格式、状态、集数、季度、Bangumi 链接）、一行制作信息、再一行分类药丸。它们现在是一句用 `·` 分隔的话。

药丸是错的容器。这些每一项都是**读一次的事实** —— 一个分数、一个格式、一个集数 —— 没有一个是控件；而一个填充的圆角矩形正是本站表示「这个能按」的语汇。十四颗堆三层还吃掉简介上方约 90px 的竖直空间。

标题同时从 `clamp(22px, 4vw, 36px)` 提到 `clamp(28px, 4.6vw, 58px)`。这是 hero 里唯一的尺度对比，而它之所以立得住，正是因为下面那行不再和它抢注意力。

实测 @390px，简介的起始位置：

```
                          改前    改后
葬送的芙莉莲               491  →  333
Re:Zero 第四季（最差）     ~625  →  416
排球少年（字段少）          374  →  315
```

随机 16 部样本：最小 318、中位 337、最大 367，**没有一部越过半屏线**。

### 分数保留了分带色，但不再有药丸

`scoreTextStyle()` —— 只出前景色。e2e 断言相应改成「背景要么透明，要么必须同带」。

这条断言原本钉的是**一致性**，不是药丸的存在：线上那个缺陷是 87 分渲染成 `rgb(48,209,88)` 绿字配 `rgba(255,159,10,0.12)` 琥珀底，前景和背景各说各的。没有背景时，能和前景不一致的颜色数量是零。

放宽断言正是守卫烂掉的常见方式，所以做了变异验证：把 `scoreTextStyle` 改成永远返回高分带（30 分也涂绿）→ 红；把不同带的背景加回去（重现线上原 bug）→ 红。两个变异都先确认写进了文件才跑。

### ★ 两个只有实测才会发现的静默失效

**媒体查询整段无效。** 移动端的字号压缩写在了 `.title` / `.facts` 的定义**之前**，同特异性下后面的规则赢，所以整个 `@media (max-width: 600px)` 块被覆盖。它的症状是**没有症状** —— 加之前和加之后，手机上量出来的数字一字不差。移到文件末尾才生效。

**换行后每行开头挂着一个孤立的分隔点。** 分隔符用 `::before` 画在后一项上，而伪元素是独立的 flex item，桌面单行时完全正确，手机上换到四行就每行行首多一个 `·`。改成画在前一项的 `::after` 上，它就待在前一项的盒子里，跟它一起换行。

### 顺带

- 删掉 17 个不再有引用的类，`page.module.css` 428 → 308 行
- `.factsLink` 的焦点环用 DESIGN.md 规定的那一个

### 详情页下半部分：从 42 处内联样式搬进 CSS

hero 以下四个区块（关系、角色、制作人员、推荐）此前是 42 个内联 style 对象、约 200 条声明。内联样式**写不了 hover、写不了焦点环、写不了媒体查询**，而且它赢过所有样式表规则 —— 所以那四个区块此前既没有交互态，也没有任何断点，而且任何人针对它们写的 CSS 都是静默失效的。

- **角色 & 声优**：头像 58×76 → 64×86，列宽下限 400px；两半镜像排列，两张脸在外侧、名字在中间相对 —— 两个左对齐的相同块读起来像四列表格，镜像后才读得出「配对」
- **制作人员**：头像 36 → 44px，并且真的显示出来了
- **关系**：从带边框的横条卡改成海报网格
- **推荐**：保留横向滚动条（那是代码注释里明确选择过的），但滚动条不再被 `scrollbar-width: none` 藏起来 —— 一个不声明自己能滚的滚动容器
- 全部去框：只留发丝线和 hover 抬升，不再是一屏的矩形

顺带修了一个真 bug：推荐卡的分数原本硬编码 `#30d158`，**55 分和 90 分涂着同样的"好"绿**。现在走 `scoreScrimStyle()`，实测 ★8.5/★8.1 绿、★7.4 橙。

还修了一个变量遮蔽：`StaffSectionView` 的 map 参数叫 `s`，把同名的 CSS module 导入整个遮蔽掉了，所以那段代码里没有任何 class 是够得着的。

### 集数网格

- 「已看」从一层 12% 绿底变成三个叠加信号：渐变面、绿描边、绿数字
- 讨论入口从钉在右上角的 24×20 贴纸变成整条底栏，并把评论数当作真正的信息显示（4.4 倍点击面积，但仍未达 DESIGN.md 的 44px 下限 —— 达标要么每格再高 20px，要么上分页，这里明说而不是半修）
- 「最远看到」从底部 2px 横条改成 3px 左脊（`inset box-shadow`，跟着圆角走，少一个 DOM 节点）
- 移动端 ≤600px 每行 4 格而非 3 格

**修了一个焦点指示器被裁掉的缺陷**：toggle 上的 `outline` + `outline-offset` 配了一个 `overflow: hidden` 的外壳 —— overflow 会裁剪后代的 outline，所以键盘用户几乎看不到焦点在哪。而且原来的焦点样式藏在组件注入的 `<style>` 字符串里，`focusRing.test.ts` **扫不到它**。现在两条规则都在 module 文件里，走 DESIGN.md 规定的那一个环。

**还修了一处布局跳动**：勾号的 `minHeight: 14` 比字形行盒少 1px，所以登录探测落地、格子翻成「已看」时整个网格会跳。实测 71/72 → 78/78。

### 有一处颜色被实测推翻了

集数格最初也用 poster 色阶染「展开」态。在真实色相下渲染后发现：色相 ~135° 时展开的格子**看起来像"已看"**，~25° 时**看起来像"报错"**。这个网格里每一格都在用颜色编码状态，而一个可以落在任何角度的色相没法同时当状态用。展开态退回 `var(--accent)`，`--poster-tone` 在这个组件里只剩一处 —— 区块标题，那是标识不是状态。

### 验证

- 7 部真实番剧端到端实测，色相 18.1° / 53.4° / 84.2° / 135.8° / 204.0° / 236.0° / 350.2°，每一部的 `--poster-hue` 都精确等于其封面实际色相，徽章对比度 7.45–7.81:1，7/7 达标
- 生产构建产物里 `oklch(76% 0.085 var(--poster-hue))` 原样保留，`.poster-scope` 未被哈希
- ISR 闸门 PASS（`dynamicParams` / prerender-manifest 均未变），hero 几何一寸未动（e2e 钉死桌面 banner 400px、cover 210×300）
- 单测 1828 通过 0 失败

---

## [3.21.0] - 2026-08-29

### 新增 — 热议栏里那张推广卡，从来没有人数过

首页「正在热议」把三种东西铺进同一个网格：本地库引导条、一张置顶的 `/welcome` 推广卡，以及 N 张讨论卡。**只有讨论卡带 `onNavigate`。** 于是这个栏目能回答「有多少人点开了讨论」，却回答不了「有多少人点开了那张坐在同一个网格里、长着同一个卡片形状、就在讨论卡上面一格的推广卡」。

后一个数字对「这个位置还适合放什么」这件事更有用，而它一直没被记录。

### ★★ 补一行埋点是错的 —— 因为现成的分母不是这张卡的分母

顺手的做法是：给卡片加上 `onNavigate`，除以我们已经在记的曝光数。**那样算出来的率是高的。**

`hot_discussions_impression` 由 `trackHotDiscussionsImpressionOnce` 发出，而它在 `itemCount < 1` 时提前返回。`/welcome` 卡在那个条件**之外** —— 有没有讨论可显示，它都渲染。所以每一次空列表渲染，卡片都在屏上、都能点，而曝光**一条都不记**。拿这个数当分母，等于把恰好这些渲染排除在外，把点击率按「列表多久空一次」的比例抬高。

而这个率，正是这次埋点存在的全部理由。

所以新增的是成对的两个事件：`welcome_card_impression` 在挂载时发出、不带任何数量门，与 `welcome_card_open` 自成一对。

顺带得到一个以前没有任何东西记录的数：**两个曝光数之差，就是热议列表多久空一次。**

### ★★ 迁移动两条 CHECK，不是一条

0020 把 `community_engagement_target_chk` 写成了一个**封闭的两分支 OR**，每一支都把自己的 `event_type` 写死在里面：

```sql
(event_type = 'hot_discussions_impression' AND anilist_id = 0 AND episode = 0)
OR
(event_type = 'discussion_open'            AND anilist_id > 0 AND episode > 0)
```

一个只加进 `community_engagement_event_type_chk` 的新事件，会通过那条检查，然后**被这一条拒掉** —— 它哪一支都不匹配。故障形态是：请求过了 Go 白名单，在插入时炸成约束违反，也就是一个 500，而且是在客户端已经部署、已经在发事件之后才出现。

0028 把分支改成**按 target 形状分组**而不是按事件分组：一支给不指向任何番剧的事件，一支给唯一指向番剧的那个。**它仍然是封闭的** —— 打错的 `event_type` 依旧哪一支都不匹配、依旧被拒 —— 但以后再加一个无目标事件，是 IN 列表里的一行，不是第三条 OR 分支。

### ★ 测试夹具里那两个率，故意设成不同的数

`0.25` 和 `0.5`。如果 handler 把分母交叉了（拿 welcome 的点击去除以热议的曝光），算出来是 `0.5` —— 一个看起来完全合理的数字。两个率若相等，这个变异就是活的。

### 验证 — 四个变异，每一个都先证明打进去了才看红

| 变异 | 结果 |
|---|---|
| 给 welcome 曝光装回 `itemCount` 门 | 前端 5 条红 |
| 把 welcome 的去重键并回热议的键 | 去重独立性那条红 |
| 迁移里只放宽 `event_type_chk` | 500，`violates check constraint "community_engagement_target_chk"` |
| handler 里交叉分母 | 拿到 0.5，期望 0.25 |

第三个变异复现出的，正是上一节描述的那个故障 —— 不是推理出来的，是打在真 Postgres 上跑出来的。

### ★ 回滚里那句 DELETE 是承重的

`ADD CONSTRAINT ... CHECK` 会校验存量行。回滚时若还留着新事件类型的行，收窄回去的 `event_type_chk` 会被它们违反，**整个回滚中止** —— 落在既不是 0027 也不是 0028 的形状上。所以行先删。

拿真 Postgres 实测过：带着新事件数据回滚会正确清掉它们、保留旧数据、把两条约束还原成 0020 的原文；把那句 DELETE 去掉，则如预期以 `is violated by some row` 中止。

这是**丢数据**，不是清理：回滚会扔掉 0028 生效期间收集的全部推广卡计数，而且没有 per-user 行可以重建。文件里写明了这一点。

### 后台

多一张卡：推广卡点击率，提示行给出它自己的分子分母。**不要跨这两对取比值** —— 代码注释、SQL 注释、Go 结构体注释和后台组件注释里各写了一遍，因为交叉之后算出来的是个合理数字，不是个报错。

---

## [3.20.2] - 2026-08-29

### 修复 — 沙箱把故障打印出来，然后原样放行

沙箱 e2e 套件间歇性变红有几个月了，一直记在案上叫「查不出原因的 flake」。**原因不是查不出来 —— 预热步骤在每一次失败的运行里都把证据打出来了。**

在 dev server 下，**每一条带有嵌套在 `[lang]` 底下的动态段的路由，会整轮都解析不出来** —— `[lang]/anime/[id]`、`[lang]/reset-password/[token]`、`[lang]/u/[username]`、`[lang]/seasonal/[season]/[year]`。`[lang]` 是唯一动态段的路由照常服务，`[lang]` 之外的动态路由（`/sitemaps/anime/sitemap/[id].xml`）也照常。照失败那次的 next-dev.log 数：三段路径 **0 成功 / 19 次**。

两个 diff 完全不重叠的 PR（#127、#128）第一次尝试**挂在同样的九条 spec、同样的行号上**，而且都在没改任何代码的重跑里变绿。就是这个配对排除掉了两边的 diff。

### ★★ 同一个 404，一个是产品的答案，一个是路由器的答案

两个签名把故障态和健康态分开：

- 每一个 200 都带一个 `generate-params` 相位；每一个 404 都没有。
- 同一条 `/anime/0`，两种耗时：

```
健康：404，942ms（next.js 870ms）—— 段编译了，页面在 anilistId <= 0 上主动 404
故障：404， 42ms（next.js   5ms）—— 从没编译过，404 来自路由器
```

**状态码相同，来源不同。只读状态码分不出这两者 —— 这正是它活了这么久的原因。**

### ★★ 一整套否定断言，对全面停摆是结构性失明的

预热步骤用一条规则判十个裸路径：**只要不是 5xx 就算编译过了**。`warmed /reset-password/warmup -> 404` 这一行**印在日志里、在两次失败的运行里都印了** —— 然后被跳过，因为 404 不是 5xx。那个页面不调任何 API、也没有 `notFound()` 分支，健康的栈里根本不可能 404。翻 14 次历史运行，这一行出现过 3 次，**这 3 次尝试全部失败**。

spec 也抓不到。`not-found-status.spec.ts` 断言的是「不存在的 id 要答 404」，所以一次**每条动态路由都 404** 的运行完美满足它 —— 它的十条断言**因为错误的理由保持了绿色**。整个目录都在 404 的时候，这个文件里唯一变红的那条，是配对的那个**肯定**用例。

### 变更 — 每条预热路由自己声明该答什么

不匹配就在日志还读得动的地方把 job 弄挂。有两件旧规则表达不了的事因此成立：

- `/library` 和 `/admin` 断言 307 —— 此前没有任何东西检查它们，跳转要是退化成 200，预热会说「编译过了」然后走人。
- `/anime/0` 断言 404，但**被明确记成「它本身不具诊断性」**，因为它在健康态下也答 404。

这一类路由的**肯定探针**放进 `globalSetup`，紧接在 `ensureAnimeDetail` 种下 id 21 之后。这个位置两个方向都承重：更早是真的没有那行，而 `loadDetail` 是带 `revalidate: 60` 取的，所以行存在之后 Next 还会把那个 404 端出来整整一分钟。

它的失败信息比检查本身还长，是故意的：`globalSetup` 抛异常发生在浏览器登录之前，`.auth/user.json` 于是从没写出来，后面每一条 spec 都报「storage state 文件不存在」。**那条更响、重复、而且是错的报错，才是读者会去追的那条**，所以这条信息先排除种子、再排除鉴权、点名真正的条件，最后直说：别跟着日志里的下一条错误走。

**重试和断言是一件事，不是两件。** 只在 5xx 上重试、断言另做，会让一次「再问一次就好了」的临时 404 直接挂掉；合成一件事的代价是有界的 —— 在第一条真正答错的路由上烧掉 30 秒然后退出，仍然是一分钟左右就挂，而不是让套件跑满三分钟、再报九条看不懂的 spec。

### 测试 — 变异验证

打的是**从 workflow 文件里抠出来的那段循环原文**，不是照着重打一遍的副本，由一个回放真实录到的状态码的 stub server 驱动：

| 状态 | 本次守卫 | main 上的守卫 |
|---|---|---|
| 健康 | exit 0 | exit 0 |
| `/reset-password/warmup` = 404 | exit 1 | exit 0 ← 就是这个 bug |
| `/library` = 200（跳转坏了） | exit 1 | exit 0 ← 一样漏 |

### ★ 第二条变异的价值不在那个红绿

它逮住的是我第一版把「嵌套动态段」那句提示**硬编码进了失败行** —— 于是一次 `/library` 上的跳转退化，会被告知「去读一读打包器的问题」。提示现在只在状态码确实是 404 时才出现。

### 更正 — 把这个 flake 归因给 AniList 限流是错的

TODOS.md 新增三条，其中一条是更正：已有的那条 AniList 限流笔记声称自己**多半就是这个 flake 的成因**，它不是 —— 症状是 404 不是 500；受害路由里有从不调用 AniList 的（`/reset-password/[token]` 就是）；那次失败的 `/anime/*` 请求 42ms 就返回了、其中 5ms 在 next.js 里，什么都没到过第三方。**那个缺陷本身是真的、可独立复现，错的只是归因。**

同一句错误的因果判断也写在 `not-found-status.spec.ts` 的注释里，一并删掉，换成这个文件最需要读者知道的那句：在那个状态下，这里每一条否定断言都会通过，而且是因为错误的理由。

另外，`TODOS.md` 里躺着一行裸的 diff3 冲突标记 `||||||| 6b31a3e` —— 3.19.1 那次带进来的，在一个公开仓库跟踪的文件里，渲染成两条条目之间的字面垃圾。一行，删掉。

---

## [3.20.1] - 2026-08-29

### 修复 — 打一个字搜一次：一个五字标题走了四趟服务端往返

`/search` 的输入框防抖 400ms，然后靠**往 Server Component 推一个新 URL**来出结果。打线上实测，一个字一个字地打 `进击的巨人`：一个五字标题**四趟完整的服务端往返**，而且路上有三件事是错的。

1. **从来没有过 pending 态。** `router.push` 跑在 React transition 里，所以旧结果就那么原样杵着，直到新 payload 落地。那段等待读起来像卡死，不像「正在做事」。
2. **字会被吃掉。** 页面在每次响应上都拿服务端的 `q` 重新给输入框播种（`useEffect(() => setQ(initialQ), [initialQ])`），所以打字快过一次往返时，**更早那次按键的回包会把输入框倒回去**。探针打的是 `进击的巨人`，URL 最后停在 `进击的人` —— 那个 `巨` 是被**覆盖**掉的，不是被丢掉的。
3. **没算上输入法。** Chrome 对一次组合输入的**每个字母**都会触发 `input`（也就是 React 的 `onChange`），所以同一个标题顺带还搜了 `j`、`jin`、`jinj`、`jinji`。这些按定义在目录里什么都匹配不到，于是每一个都穿过本地查询、出到 AniList —— 正是这个项目被限流的那个上游。线上实测：`/api/anime/search?q=jinji` 返回 total 0。

### 变更 — 查询搬进浏览器

`SearchExperience` 自己 fetch `/api/anime/search`，就地替换结果，用 `history.replaceState` 保住 URL 可分享，中止被顶替掉的请求，并把本次会话内答过的查询留着，让退格不花钱。组合输入期间**不排任何调度**，直到那个字被提交；`onBlur` 是泄压阀 —— 万一 `compositionend` 没来，也不至于把这个页面剩下的生命里的搜索静默关掉。

防抖定在 **1000ms**。驱动一个真的拼音输入法打 `进击的巨人`，三种速度下每个标题的请求数：

| 每字之间的停顿 | 400ms 时 | 1000ms 时 |
|---|---|---|
| 150ms（偏快） | 1 | 1 |
| 400ms（正常） | 1 | 1 |
| 800ms（偏慢） | 4 | 1 |

回车、搜索按钮、类型标签都跳过这段等待。

### ★ 分页和它是同一个 bug，不是另一个

防抖要问的是「控件是不是已经和屏幕上的东西不一样了」，而输入框**只可能表达第 1 页**。拿整个 query 去比，就让每一次身处第 2 页都看起来像一次「待生效的、要回第 1 页」的改动 —— 点了下一页的读者，**一秒钟之后被静默送回第 1 页**。现在比的是 terms；分页链接仍然是真的 `<a>`，中键还能开新标签。

### ★★ 水合之前打的字，进了 DOM 没进 state

写这条 spec 时顺手写的探针**六次里有三次报告「功能坏了」**，而功能是好的。

React 会给它水合过的节点挂上 `__reactFiber$…` 开头的键，**在这些键出现之前，一次按键落进 DOM 就再也到不了 state**。`next dev` 是按需编译的，所以冷路由上这个窗口有好几秒宽。这条 spec 因此有一个 `hydrated()` 前置：等到 `input[name="q"]` 上出现 `__react` 前缀的键才开始打字。它不是伪装成 sleep 的偏执 —— **任何会打字的 e2e 都要先等它**，否则失败的样子和「功能真坏了」一模一样。

同一件事也是这次给表单补上 `method=get`、locale 感知的 `action` 和 `name="q"` 的理由：这条路由是流式的，所以存在一个「输入框已经在屏幕上、React 还没水合它」的窗口，在那个窗口里按回车，现在是一次普通导航，而不是什么都不发生。

Server Component 仍然渲染第一批结果，所以一条分享出去的链接、一个爬虫，拿到的和以前一样。

### 测试

`e2e/specs/sandbox/search-typing.spec.ts` 断言的是**请求条数和输入框里剩下什么，不是结果**。这是故意的：它因此不需要种任何行，挂的时候也是因为它名字里那件事，而不是因为某个 fixture 漂了。

- 输入法那条用 CDP 的 `Input.imeSetComposition` 驱动一次真的拼音组合，每个字母之后停 500ms（比修复前的 400ms 防抖长）。它带一条**反空洞断言**：组合提交之后必须搜到一次 —— 没有这条，一个**什么都不搜**的实现也会通过。
- `replaceState` 那条断言 `history.length` 不增长，否则「后退」会变成打字过程的慢放重播，而不是搜索的出口。
- 分页那组是 `serial` 的：整个套件 `fullyParallel`，不锁的话每个领到用例的 worker 都会各自 `beforeAll` 种一遍，而第一个 `afterAll` 会把行从别人脚下删掉 —— **那个现象读起来和这条 spec 要抓的产品 bug 一模一样。**

---

## [3.20.0] - 2026-08-29

### 新增 — 弹幕速度三档，全在慢的那一端，默认最慢

插件自带五档（穿屏 10 / 7.5 / 5 / 2.5 / 1 秒），只给其中三档起了名字，把 5 放在正中，而播放器一直把那个 5 写死。一集弹幕密的时候，5 秒不够把一行读完它就走了。

换成三档，全部落在慢的那一端，默认最慢：

```
极慢 10s   缓慢 8s   适中 6.5s
▲ 默认
```

每一档都比原来写死的 5 慢，而且每一档都有名字 —— 插件把 7.5 和 2.5 标成 `hide: true`，所以旧控件是**三个名字压在五个位置上**，中间两格无名。

### ★★ 天花板是 10，而且不是我们的

`artplayer-plugin-danmuku` 在 `config()` 里夹：

```js
this.option.speed = clamp(this.option.speed, 1, 10);
```

所以一个「看起来更慢」的档位不会更慢地渲染，它会**静默变成 10** —— 实测 `config({speed: 15})` 和 `config({speed: 12})` 读回来都是 10。

最难受的是它从界面上完全看不出来：滑条靠 `option.speed` 与档位值**精确相等**去找手柄，于是一个超范围的档位会塌到持有 10 的那一格上，**手柄停在错误的标签下面**。原本想要的「极慢 15s / 缓慢 10s」因此做不出来 —— 那两档会变成同一个值。`speedLadderIsRenderable()` 和它的测试把这条钉死，因为下一个想要「更慢」的人，第一反应就是填一个更大的数字（我就是）。

### ★ 存量值回落到默认，不是往梯度上夹

这个选择和字幕大小、倍速一样会持久化。但控件在**插件自己的配置面板**里，不是 artplayer 的设置菜单，我们没有任何 onChange 可以挂 —— 唯一的信号是 `artplayerPluginDanmuku:config`，而它**对插件的每一次配置变更都触发**，包括每次切集我们递过去的那份弹幕列表。用一个 ref 把它收敛成「每次真变更写一次」，`writeDanmakuSpeed` 拒绝任何不在梯度上的值。

读的一侧，不在梯度上的存量值**回落到默认**，而不是往梯度上夹。这次改动之前播过番的人，要么什么都没存、要么存着旧的 5，两者都没有对应的滑条位置 —— `findIndex` 返回 -1，手柄无处可停。

### ★ 验证必须驱动真控件

hover 齿轮，点轨道上中间那一格。未存任何值时打开是 `极慢/10s`，点一下变 `缓慢/8s` 并写入 `"8"`，重开播放器回到 `缓慢`。

**直接调 `config()` 会改速度但标签不刷新** —— 标签是滑条自己的 `onChange` 重绘的。我第一次就是这么测的，读起来和产品 bug 一模一样。

### 已知未闭合

插件的配置面板是硬编码中文（按类型屏蔽 / 滚动 / 顶部 …），所以档位名用中文才与周围一致，而 `/en` 下那个面板本来就是中文。

---

## [3.19.2] - 2026-08-29

### 修复 — Chrome 把画面调灰了，而 JavaScript 看不见这件事

上报是「网站看起来比我本地播放器发白」。不是播放器的渲染 bug，也不是文件的问题：Chrome 合成 `<video>` —— 而且只对 `<video>` —— 会过一次传递函数转换，**把中间调抬起来，两端钉死**。

拿 16 级灰阶楔子对着 ffmpeg 的解码测，用的是一个**完全不带色彩标记**的文件（无 `colr` atom、VUI unspecified —— Baha / ANi 的 WEB-DL 就是这个形状）：

```
源      ffmpeg   Chrome        源      ffmpeg   Chrome
  0        0        0          136      136      147
 17       17       16          170      170      179
 51       51       58          204      203      210
102      102      114          255      255      255
```

平均绝对误差 7 级，峰值 +12。两端锁死、中间被压平 —— 这正是「发灰」读起来的样子。mpv、IINA、VLC 不做这个转换，所以同一个文件在它们那里更有对比。

### ★★ 它藏得这么好，是因为从 JS 里怎么看都是对的

解码本身没问题 —— 用 `canvas.drawImage(video)` 把帧读回来，和 ffmpeg 差在 2 级以内。**只有合成后的输出被移位了**，所以每一种从 JavaScript 检查像素的办法都会告诉你画面很好。

只能靠**截屏**抓，而且要在同一张截图里放一块同名义色值的 CSS 色块当对照 —— 色块出来是精确值，这才证明位移是视频专有的，而不是截图路径本身的。

### ★★ 表是实测的，不是推导的

解析的 BT.709 → sRGB 模型预测暗部会被大幅抬起，**而 Chrome 并没有这么做** —— 模型说 17 → 32，Chrome 把它留在 16。所以拿一个 `feFunc type="gamma"` 的指数去拟合中间调，必然把底端压过头：1.136 是最好的幂拟合，它仍然把 17 压成了 10。

提交的表是**实测曲线的逆**。放回 Chrome 里跑，楔子的平均绝对误差回到 0.31 级、暗部那一级完好；在上报的那个文件上，整帧落在 ffmpeg 的 1 级以内：

```
ffmpeg 参考   122.8 / 108.9 / 85.4   饱和 37.5%
修复前        133.7 / 118.4 / 92.6   饱和 36.8%
修复后        124.4 / 109.2 /  84.5  饱和 37.9%
```

### 播放代价测不出来

1920×1080、4000 条弹幕、ASS 字幕层开着，交替跑 6 次 20 秒：**每一次丢帧都是 0**，开不开滤镜都一样。迟到帧的计数随运行顺序单调上升而与滤镜无关（有一次不开滤镜的跑分比开滤镜的还差），所以那个漂移是机器负载，不是这次的修正。

### ★ 两处一碰就坏的细节

- **全屏那条规则现在要同时写两个滤镜。** `filter` 是**单个属性**，只留 `opacity(0.999)` 会让修正**只在全屏下**消失。`opacity()` 保留而不是当成冗余删掉：它是阻止硬件 overlay 提升的东西，也就是当年修字幕闪烁（#24）的东西，而「只留 `url()` 滤镜是否也挡得住」没有重新验证过。
- **`color-interpolation-filters="sRGB"`。** SVG 的默认值是 `linearRGB`，那会把这张表用在完全错误的色彩空间里。

### 范围：只给 `<video>`

jassub 的字幕 canvas 和弹幕层是我们自己在 sRGB 里画的，从来没走过 Chrome 的视频路径 —— 给它们也上修正，等于把这次消掉的误差重新引进去。

Chrome-only 是**决定**，不是特性检测：Safari 和 Firefox 对视频传递函数的处理方式不同，而我们没有测过它们。

### 测试

`videoTransferFilter.test.ts` 钉住三件事：id 在两个文件之间一致、全屏规则里两个滤镜都在、表的形状。一个目标不存在的 `filter: url(#id)` 会**不报任何错**地把视频原样渲染出来，所以任意一侧改名都会是无声的。

---

## [3.19.1] - 2026-08-29

### 修复 — 三条声称「结束会话」的路径，每条都留下了一份还能用的凭证

登出、密码重置、管理员改密码，三条路径都声称结束一个会话。**每条都以不同的机制留下了一份还能用的凭证，而仓库里没有任何一条测试看得见其中任何一条。**

**登出跑在 `RequireAuth` 后面。** 一个闲置超过 access TTL（默认 15 分钟）的标签页，会在中间件那里拿到 401，handler 根本不执行 —— 没有 Clear-Cookie，没有数据库写入 —— 而浏览器手上那个 refresh cookie 还在，`proxy.ts` 会在**下一次导航**就把它花掉。用户看到的是一个登出了的界面，然后被静默地重新登了回去。这不是竞态：闲置超过那个时长，每次都发生。现在登出改成**从 refresh cookie 认证**，并且在任何东西可能失败之前就无条件清 cookie。

### ★★ `fetch` 对 429 是 resolve，不是 reject

上面那个修复自己带着一个洞，是评审里发现的。

路由前面的限流是**从中间件**答 429 的 —— 在 handler 写出任何 Clear-Cookie 头之前。而 Navbar 只 `await` 了那个 fetch，没有查 `res.ok`：**`fetch` 在 429 上是 resolve 的**，所以 catch 分支一次都没进过，`setUser(null)` 照常执行。同一个 bug，换了个触发条件，而且在多人共用同一个出口地址时是日常可达的。

现在登出不再和刷新共用同一份限流预算；客户端也拒绝在服务端没有确认清掉 cookie 的情况下，把导航栏画成登出态。那句写着「剩下唯一的缺口是请求根本没送到」的注释，**错的方式和它替换掉的那条注释一模一样：它断言了一个路由层并不提供的保证。**

### 轮换没有 compare-and-swap

Refresh 是读行 → 签两个 JWT → **无条件**写回。一次落在这个窗口里的登出，会被那次在途的轮换覆盖掉，然后把一个还活着的 refresh token 交回去：**用户按了登出，然后还登着。**

`RotateRefreshToken` 现在是 `:one ... RETURNING username, role` 加 `AND refresh_token = @expected`，顺带把顺利路径从 SELECT → 签名 → UPDATE 收成一条语句。

### ★★ 签名有效 ≠ 这个 token 还活着

登出自己那次写是**不带范围的**。它认证的是一个**签名**，而签名在整个 refresh TTL 里都有效 —— 包括服务端早就撤销掉它所签的那个 token 之后。于是一份**已死的凭证**（一个旧浏览器 profile、一份过期备份）可以被反复重放到 `/logout`，把它的前主人挡在会话之外。

`ClearRefreshTokenIfMatches` 把这次写限定在调用方**还能证明自己持有**的那个会话上。`previous_refresh_token` 也在匹配范围里，这样一个刚刚轮换过的标签页仍然登得掉自己。

### ★ 改密码的语句只清了三列里的一列 —— 而它的后果是 500，不是漏了个 token

`refresh_token`、`previous_refresh_token`、`refresh_rotated_at` 是**一份状态**：Refresh handler 在轮换后 30 秒内会回落到那个宽限位。只把第一列置空，行就落在了读取侧没预期过的形状上，宽限分支会解引用一个 nil 的 `*refresh_token` —— 任何一次改密码之后的 30 秒内，经 Recoverer 变成 500。

**这一条要说准，因为直觉的猜法是错的**：它从来没有发出过 token，那次 panic 发生在 response writer 之前。**是可用性问题，不是凭证问题。** 三条语句现在都清三列，读取侧的 nil 守卫留着。

### 顺带修的三件先于本分支存在的问题

同一轮评审找出来的：

- `proxy.ts` 在它自己那次 SSR refresh 上**没有把真实客户端 IP 转发下去**，于是全站每一次服务端 refresh 都塌进同一个桶里。桶抽干的时候，用户被渲染成未登录。`lib/api.ts:buildHeaders` 早就这么做了，它的注释描述的正是这个失败，只是调用点一直没跟上。
- 管理员改密码端点**按字节数**校验，而注册**按 rune 数**（go-playground `min=6`），所以 `密码密码` 被公开表单拒绝、却被管理员路径接受。一个只有管理员能用的端点成了全站设置密码最弱的地方，方向是反的。
- `UpdateBangumiV1` 无条件写，所以一个陈旧的重复 river job 可以覆盖掉已经被 v2/v3 富化过、或人工纠正过的绑定。现在是 `:execrows` 且带 `bangumi_version = 0` 守卫，被拒绝的写打 `stale_skip`，而不是接着链 V2。

### 已知未闭合 —— 「改密码会让所有已有会话失效」这句话，代码做不到

go-api 和 next-app 里都有注释这么写。**它们做不到**，而这是查过的、不是推的：access token 是无状态 JWT，签发之后服务端对它不再有发言权，所以诚实的上限就是它自己的有效期，注释现在照实写。

没有任何面向用户的文案许过这个诺 —— 被这句注释误导的，是**正在处置一次入侵、一边照着管理员路径操作的运维**。主动撤销这条不在本次范围内，记在 TODOS.md 里。

### 测试 —— 这一类缺陷对现有测试是结构性不可见的

handler 的 fake 只记录参数，**看不见一条 SQL 字符串到底写了什么**，所以这次的每一处改动对它们都是结构上不可见的。

- `test/integration/user_session_columns_test.go` 打一个真 Postgres 读实际的列，包括一次**故意把两个参数写反**的 CAS 调用 —— 两个参数都是 `*string`，所以调反了照样编译、照样通过每一个 fake。
- `internal/db/session_columns_test.go` 扫 SQL 源码，让规则覆盖到**还没写出来的语句**。它拒绝猜：一个碰了会话列的 CTE 或 `UPDATE ... FROM` 会挂，而不是被跳过；任何一次它无法证明写进去的是活 token 的 `refresh_token` 写入，都必须显式声明。它自己的**解析器自检**逮住了第一版是瞎的 —— `SET` 是**行首**，而它找的是带前导空格的 `" SET "`。
- `internal/admin/set_password_test.go` 覆盖了一个此前一条测试都没有的端点，而 `fakeUserDB` 的接线一直都在。

上面每一条规则和断言都做了变异验证：把源码里的改动反过来，确认测试确实变红。SQL 扫描的四个盲点就是这么找出来并补上的。

---

## [3.19.0] - 2026-08-29

### 新增 — 输入简体，也能搜到只有繁体标题的那 1,262 部

目录里有 1,262 行只有繁体标题、没有简体标题，所以打 `进击的巨人` 找不到《進擊的巨人》。成因在上游而且很具体：简体标题来自 Bangumi 富化，而这批行的 `bgm_id` 覆盖率只有 **9.4%**，全目录是 **70.8%** —— 根本没有 subject 可以读出一个简体名来。它们也不冷门：315 部是 TV，里面有进击的巨人最终季、排球少年、BLEACH 千年血战篇、药屋少女、冰海战记。

现在搜索把关键词过一遍随包的 OpenCC `s2twp` 表，把结果作为**一条额外谓词**去匹配 `title_hant`。**不落库，也不改任何显示** —— 这正是要这么做的全部理由：把转换出来的标题回填进 `title_chinese`，等于把机器输出直接推上每一个 SEO 面，而迁移 0022 建 `title_hant_seo` 就是为了拦这件事；`title_chinese` 没有这道闸，因为它**就是**页面 title 和 JSON-LD 读的那个字段。

### ★ 一条谓词，不是五条

折出来的 pattern 是繁体文本，繁体文本只可能命中繁体列。把它复制到另外四列上，是四次**结构上不可能返回行**的索引扫描。而当关键词没有可折的东西时，调用方传的就是它本来那个 pattern，于是这条谓词**塌成一条重复项，而不是塌成一个两边都得记着的分支**。

### ★★ 它到底管多少用 —— 实测，不是假设

在 5,160 行**同时**有简体标题和一个**权威**繁体标题（`title_hant_source <> 'opencc'`）的数据上，把简体标题折过去，看它是不是存着的繁体标题的子串（也就是这条谓词做的事）：

| | 命中率 |
|---|---|
| 折叠后 | 40.7% |
| 不折叠 | 7.4% |

大约五倍半的触达，**不是「解决了」**。

剩下的 59% 不是转换问题，任何一张转换表都修不了：台湾和大陆很多时候就是用不同的名字发行。2,237 条 miss 和存着的标题**长度都不一样**，也就是干脆是另一个译名 —— 海賊王 对 航海王、三眼小子 对 三眼神童、鋼鐵奇兵 对 金屬對決。够到那些要一张别名表。TODOS.md 收的是这条**带着这组测量数字**，不是一句猜测。

### ★ 折叠器是构造参数，不是 setter

setter 会被忘掉，而忘掉它就等于把目录静默地退回上面那个状态 —— 没有报错，没有日志行。

降级路径也实跑过：把 `HANT_DATA_DIR` 指到一个不存在的地方启 go-api，它 2 秒内起来并健康，打一行 `folding disabled`，照常提供搜索，而 e2e 带着自己的诊断信息变红。这既是这条特性的变异测试，也是「数据集缺失不是一次故障」的证明。

### ★ `s2twp` 不是纯字形转换 —— 这个坑记进了 e2e

`wp` 后缀是**台湾用词替换**，所以 `折叠` 出来是 `摺疊`，不是 `折疊`。拿这种词搭的测试探针，会在特性完全正常的时候挂掉 —— 花了两轮才看出来。

### 验证

- 打生产表，只读：`进击的巨人` 从 12 行变 18 行，**新增的六行全部是只有繁体标题的行**
- 本地整栈走 HTTP 端到端，探针种成**只有折叠能够到**的样子 —— 它的简体列和查询没有任何共同字
- 顺带：`hant.NewConverterFromDir`，让一个只需要转换文本的调用方不必加载 609KB 它用不上的 resolver 数据集，文件名也留在那个记着它为什么存在的包里

---

## [3.18.3] - 2026-08-28

### 修复 — /zh-Hant/search 的 18 张卡片里有 12 张写着简体

线上实测：`/zh-Hant/search` 的 18 张结果卡里 12 张显示简体标题。首页、季度页、日历、详情页全是对的 —— 分别数了 54、20、14、22 张卡，一张没错。

繁体这条链路没有任何东西是坏的。`lib/formatters.ts` 里的 zh-Hant 梯子从迁移 0022 起第一顺位就读 `titleHant`，go-api 也从那时起就在返回这个字段：`GetAnimeByAnilistIDs` 选了它，AniList 路径和本地目录路径都走这同一条查询。

差别在于**其它页面是把整行原样透传给 `<AnimeCard>` 的**，所以字段到不到，跟哪个 TypeScript 类型提没提它无关 —— `pickTitle` 在运行时按字符串键读。只有 `/search` 是逐字段手搭卡片对象，而 `titleHant` 不在那张单子上。少一个可选属性不是类型错误，每个文件单独看都很合理，而简体标题对繁体读者也读得懂 —— 没有任何一环会把它顶出来。

### ★ 它是被 #123 揭出来的，不是被 #123 弄坏的

在 #123 之前，繁体查询根本查不出东西，也就没有卡片可以渲染错。**一个修复看起来揭出了一个新 bug 的时候，值得先问它是不是本来就在那儿。**

### ★ 梯子要靠探测拿到，不能靠解析

`cardDataTitles.test.ts` 不去解析 `TITLE_LADDER` 那个私有常量，而是**拿探针问 `pickTitle` 自己**：抄常量的测试会和坏实现一起点头，跟和好实现点头一样痛快。拿到梯子之后它断言两件事 —— `AnimeCardData` 声明了任何一条梯子会读的字段，以及每一处手搭的卡片字面量都把它们赋上了。两半都做了变异验证：删接口字段、删构造点字段，各自产出一条指名道姓的失败。

### 测试

`e2e/specs/sandbox/search.spec.ts` 是沙箱套件里**第一条搜索 spec** —— 这也正是同一个页面上能同时住着两个缺陷的原因。它断言渲染出来的文字，那是单测够不到的那一半：数据一直是对的，是页面在送往卡片的路上把字段扔了。同样做了变异验证。

e2e 种一个简繁标题**互不相同**的探针（`ensureAnimeCached` 为此长出 `titleHant` 字段），每个 locale 都断言两个方向 —— 期望的字形在、另一个字形不在。只断言「在」，一张两种都渲染的卡会通过；只断言「不在」，一张空卡也会通过。`/en` 也覆盖了：它的梯子**故意停在 romaji**，不往下掉到中文，而证明这一点的正是一个没有英文标题的探针。

本地验证：1756 条单测通过、tsc 干净、lint ratchet 通过，受这次改动影响的五条 spec（search、not-found-status、robots-posture、anime-detail、locale-routing）85/85。`library-*` 那几条在这个 worktree 里挂，但**把改动 stash 掉之后挂得一模一样**，所以那是这个环境的空库和全新浏览器 profile，不是这里的事。

---

## [3.18.2] - 2026-08-28

### 修复 — 中文搜不到，不是因为缺简繁折叠，是因为压根不查本地库

`/api/anime/search` 是个纯 AniList 代理：把关键词转发给 AniList 的 GraphQL，把返回的东西 upsert 进库，再把那些行读回来取富化列。AniList 索引 romaji、English、日文和 synonyms。**它不索引中文标题。**

于是一个用中文搜的读者拿到的，是 AniList 碰巧收了哪个 synonym —— 这就是为什么失败看起来是随机的、而不是系统性的。改动前的生产实测：

```
進擊的巨人   0 条    anime_cache 的 title_hant 里有它 (16498)
鬼滅之刃     0 条    anime_cache 的 title_hant 里有它 (101922)
葬送的芙莉莲 0 条    anime_cache 的 title_chinese 里有它 (154587)
葬送的芙莉蓮 1 条    繁体形式搜得到，简体搜不到
进击的巨人   1 条    同一个查询的日文写法搜出 20 条
```

`title_chinese` 和 `title_hant` 这两列在库里都填得很满。**而没有一条是从搜索够得到的。**

现在搜索先查本地目录，只在本地对这个关键词一无所有时才落到 AniList —— 那正是自愈的那一种情形：AniList 回答，已有的 upsert 把行归档，下一次搜同一个词就是本地的了。带 genre 过滤仍然直接走 AniList，因为本地查询没有 genre 谓词，拿它回答等于静默忽略掉过滤条件。

顺带的收益是大部分搜索流量离开了 AniList：一串未缓存的查询本来可能打光配额，然后以「AniList rate limited」的形式呈给读者。

### ★ 「本地没有」和「数据库不舒服」要有相反的响应

`runLocal` 在目录里没有这个关键词时返回 `(nil, nil)` 而**不是**错误 —— 那是 fall through 的信号。查询真的失败时返回错误，但也不致命：AniList 那条路本身就是一个完整答案，本地失败只让这一个请求丢掉中文匹配，不丢掉请求本身。

而「翻过头的页」仍然返回一个页面而不是 nil：总数一旦非零，本地目录就拥有这个答案，一个 3 页结果的第 9 页是空的，那才是正确的分页 —— 在那里 fall through 会把 AniList 的第 9 页（完全另一堆东西）递给读者。

### 新增 — 迁移 0027：`title_hant` 的 `gin_trgm_ops` 索引

0002 给另外四个标题列都建了，0022 加 `title_hant` 时没建，而且 0022 自己就点名了这个缺口：

> "a reader who types 進擊的巨人 will not match the title_chinese trigram index […] that is a search change, not a locale change"

这次就是那个 search change。

用普通 `CREATE INDEX` 而不是 `CONCURRENTLY`：在一个会回滚的事务里对着生产表计时，建索引耗时 **270ms**（3.2MB 索引）—— 那就是 `anime_cache` 的写入要等的时间。而 `CONCURRENTLY` 不能跑在事务块里，golang-migrate 恰恰是把多语句文件当一个隐式事务交给 PostgreSQL 的。在这里伸手去拿它，等于拿 270ms 的锁去换一个可能直接把部署搞挂的迁移。0002 那四个兄弟索引也是普通的，同一个理由。

trigram 是逐字符的，所以对 CJK 有效：两个字的查询照样产出补齐的 trigram。一个字的查询可能不产出，会退化成对整张表的顺序扫描 —— 这是可接受的，也是没有为它单设守卫的原因。

### ★ 排序：AniList 那条路从来就没排过序

排序是精确匹配 → 前缀 → 子串 → `average_score`。`GetAnimeByAnilistIDs` 按评分排，所以搜「进击的巨人」第一条答的是「第三季 Part.2」。本地这条路在那次重读之后用 `reorderByIDs` 把自己决定的顺序恢复回来。

### ★ 通配符必须在进 ILIKE 之前转义

不转义的话，**搜「100%」会匹配表里的每一行**，搜「_」匹配每一个单字标题。转义顺序有讲究：**先转反斜杠** —— 放在后面会把这个函数自己刚加进去的反斜杠再转一遍。

### ★ 单测用的是 fake，证明不了 SQL

所以验证走到单测之外：

- 查询对着生产表只读跑过：上面那三条死查询各回 5 行，精确匹配在第一位
- `EXPLAIN ANALYZE` 确认干活的就是新索引（五个索引的 BitmapOr，`title_hant` 贡献 18 行，另外四个各贡献 0 行），2.3ms
- 本地全栈端到端组装：繁体和简体都解析得出来；搜「%」返回的是标题里真含一个百分号的那一行，而不是本地栈里的每一行
- 两个变异都咬得住：去掉 `reorderByIDs` 挂排序测试，去掉转义挂 5 条
- 一个故意写坏的 0027 会让集成套件失败 —— 说明迁移闸门是真的在应用它

### ★★ 顺带修掉的 e2e flake，根因是一个真实的产品缺陷

#122 加的那个 spec 在紧接着的下一个 PR 上就被 CI 抓到了：`expected a redirect, got 404`，外加一条 `/en answers 404` 重试才过。本地三次里挂两次。两个独立原因，都是我自己的。

**第一个是 fixture。** 一个别名用户在 `beforeAll` 里种下、`afterAll` 里删掉，生命周期横跨整个文件 —— 而重试会重跑 hook，serial 组的 `afterAll` 可能和后一次尝试的 `beforeAll` 交错。与其钉死到底是哪种交错造成的，现在每个测试自己创建、自己删除，用测试名和 worker 命名。单个测试之外的任何东西都观察不到那一行，于是这一类失败是**消失了**，不是被收窄了。

配套加了一道前置检查，因为 **fixture 缺失和跳转坏掉回的都是 404，没有这道检查它们就是同一种红。** 它先断言 go-api 解析得出这个 handle，并在失败信息里把话说清楚。（这道检查自己的第一版发的是 `%40`，对着一个完好无损的 fixture 挂了 —— Go 路由匹配的是字面 `@`，这正是 `canonicalHandle.ts` 的 `encodePathSegment` 要把它还回去的原因。恰好就是这道检查存在的理由，所以现在理由写进了代码。）

**第二个原因更有意思，它是真实的产品缺陷而不是测试 bug。** go-api 没有负缓存，缓存未命中加 `pgx.ErrNoRows` 会直接走到 AniList（`detail.go:423`）。**每一个未收录 id 的请求都会开一次实时的第三方调用**，而 AniList 限流时页面回的是 500 不是 404。本地加压复现过：`Error [ApiError]: AniList rate limited`，紧跟着 `GET /anime/999999999 500`。CI 那次重试几乎可以肯定就是这个。

八个测试各自在开这样一次调用。现在改用 id `0` —— `anime/[id]/page.tsx:1203` 在任何 fetch 之前就用**同一个** `notFound()` 拒掉它，所以它们仍然在检验这个套件存在的意义，而不必离开进程。留一个测试继续用大 id 覆盖查表那条路，答案是 500 时大声 skip 而不是失败。

**那个 500 没有被糊过去**：它连同「对爬虫来说它比 404 更糟」的理由写进了 TODOS.md，并附一条「不要为了让 CI 变绿把这些断言放宽成『404 或 500』」。

结果：连续 4 次干净，`--repeat-each=3` 下 57 条全过；此前是 3 次里挂 2 次。运行时间从约 25s 降到约 11s，因为套件不再等 AniList。

### 已知未闭合 — 剩下的简繁缺口，以及一个被自己推翻的方案

57 行有简体标题没有繁体，1,262 行反过来。这 1,319 行仍然只答应它被存成的那个字形。

TODOS.md 里原本的提议是把 t2s 结果回填进 `title_chinese`。查过那 1,262 行之后，这个建议在**两个方向上都是错的**。

先是原因很具体，不是长尾：`title_chinese` 只来自 Bangumi 富化，而这批行的 bgm_id 覆盖率是 **9.4%**，全目录是 **70.8%** —— 没有映射就没有 subject 可以读出简体标题。AniList 的 synonyms 反而带了一个繁体的，被 0022 的回填归进了 `title_hant`（1,262 行里 1,248 行来自 anilist，来自 opencc 的是 0 行 —— 后者按构造总有一个简体原文）。它们的 `bangumi_version` 一律是 3：**富化跑完了，什么也没找到。** 标题本身说明了原因 —— 几乎全是续作和篇章，正是标题匹配最弱的地方。而且它们并不冷门：315 部是 TV，包括进击的巨人、排球少年、BLEACH、药屋少女和海盗战记。

然后是回填方向没算进去的两笔成本：

- **没有 t2s 表。** 仓库只 vendor 了 `opencc-s2twp.txt`（简→繁，53,607 行），反方向需要自己的生成器和自己的 vendor 数据。
- **`title_chinese` 没有 SEO 闸门。** 0022 专门把 `title_hant_seo` 建成生成列，就是为了把 `source='opencc'` 的值挡在搜索结果之外。`title_chinese` 既没有 source 列也没有等价物 —— **它本身就是页面标题、卡片标题和 JSON-LD 读的那个值。** 往里写机器输出等于绕开 0022 立起来的纪律，而要把纪律恢复回来意味着一个 source 列、一个 `title_chinese_seo` 列，以及把每一个 SEO 读者都挪过去。

改成**转换查询**则什么都不存、什么都不显示，上面这些统统不适用。在推荐之前先量过：加载现有那张表一次性花 23ms、10.7MB（而且它今天是加载在 river worker 里，不在请求路径上），而读者真会打的那些简体形式，在这批行的真实样本上 **8/8 精确**转成了库里存着的繁体串。这覆盖 1,262 / 1,319 —— 而且是这个 22:1 的不对称里更大、更面向简体读者的那一半。

剩下 57 行仍然需要 t2s。五十七行显然不值得为它 vendor 第二张表；**记录下来，不做决定。**

---

## [3.18.1] - 2026-08-28

### 修复 — 五个路由家族对不存在的内容回 200，起因是一个文件放高了一层目录

`/anime/{id}`、`/seasonal/{season}/{year}`，和三个 `/u/{username}` 页。每一个都调了 `notFound()`，每一个都渲染了 not-found 的正文，**每一个都在那之前就把 200 提交到了线上。** Google 管这叫软 404，并把它当成一个真页面收录。

Next 自己写明了规则：

```
"Next.js will return a 200 HTTP status code for streamed responses,
 and 404 for non-streamed responses"
-- not-found.md（Next 随包文档）
```

`app/[lang]/loading.tsx` 在它下面的 24 个路由段上开了一道 Suspense 边界，于是每个响应都是流式的，于是在任何页面能设状态码之前，状态码已经发出去了。**那个文件注释的第一行写着「HomePage skeleton」—— 它就是首页自己的骨架屏，只是坐高了一层目录。** 现在它搬进 `(home)/`，只包住它当初为之而写的那一个页面。

另外两个 `loading.tsx` 是直接删掉而不是搬走：`anime/[id]`（167 行）和 `seasonal/[season]/[year]`（164 行）。两者都在 ISR 之下 —— 整页渲染完、一整块发出去，**这两个骨架屏从来没有被任何人看到过。**

### ★ 验证打的是生产镜像，不是 `next dev`

因为之前每一次对这件事的检查都是在 dev 里做的。生产镜像实测：**10 个 404 现在回 404，6 个 200 仍然回 200**，而变异（把 `loading.tsx` 放回去）让它们全部回到 200。

### ★ 两层守卫，因为它们抓的不是同一种东西

- `app/routeBoundaries.test.ts`：只要有人把一个 `loading.tsx` 放在任何能调 `notFound()` 的路由**之上**，它就失败。带一份有过期检查的白名单和反空转守卫，并且它自己也做过变异测试。
- `e2e/specs/sandbox/not-found-status.spec.ts`：数线上真实的那个数字，覆盖全部五个路由和三种语言形式，反方向的 200 也一起数。**这个套件在此之前断言过 404 状态码的次数正好是零。**

### 新增 — 别名 `/u/{handle}` 现在是真的 308

同一次删除顺带修好的：`permanentRedirect()` 之前同样设不了状态码，所以一个 contact 形状的 handle 回 200 并退化成一次客户端导航。现在是真 308，跳转目标是掩码后的 handle。

那条 `noindex` 暂时留着，作为佐证而不是唯一信号 —— 等 308 对着生产构建确认过再撤。**在这里乐观地提前删掉，等于重新暴露一个地址形状的 URL，那不是值得乐观的东西。**

### ★★ 一句被当成实测事实写进注释的话，在全仓传了大约十二个文件

`lib/lang-client.tsx` 的头注释写着：hydration 之后有个 effect 会用 `lang` **cookie** 把语言换掉，所以客户端叶子跟着存储的偏好走、周围服务端渲染的内容跟着地址走，两者「刻意不调和」。

**代码里既没有这个 effect，也没有这个 cookie。** 这个文件既没 import `useEffect` 也没 import `useState`，不读任何 cookie，`LanguageProvider` 没有任何调和步骤。

它之所以要紧，是因为它承了别人推理的重：同一句话在大约十二个文件里被重复，包括 `app/[lang]/seasonal/[season]/[year]/page.tsx` 里 SeasonalFilterChips 旁边那条 —— 那里拿它来论证一个设计决定；而且它还被引用来主张「404 页不能被信任按 URL 的语言渲染」。它能。三个语言现在各在浏览器里断言一次。

### 已知未闭合

五条查出来但不在这次修的，连同代价和「为什么它是独立的一次改动」写进了 TODOS.md：`proxy.ts` 的点后缀规则让邮箱形状的 handle 在裸 URL 上够不到、`/u/` 跳转丢掉语言前缀、404 带着和真页面一模一样的 `Cache-Control`、完全没匹配上的 URL 落到 Next 内置的 404，以及上面那一簇注释的清理。

---

## [3.18.0] - 2026-08-28

### 新增 — 后台「用户活跃度」：不是统计算错了，是这个事实从来没有被记录过

后台一直答得出「有多少账号」「每个账号订阅、评论、关注过什么」。**那些都不是活跃度。** `activity_events`（0018）只装 `watch_progress` / `comment` / `follow` 三种刻意的社交动作 —— 于是一个每天晚上都来、翻目录、什么都不发的读者，对这个仓库能写出来的每一条查询而言，和一个注册一次再没回来的人长得一模一样。

**DAU/WAU/MAU、最后一次出现、到访天数、D1/D7 留存，在旧 schema 下不是慢查询 —— 是无解。**

新表 `user_activity_daily`，一个用户一天一行（`first_seen_at` / `last_seen_at` / `request_count` / `login_count`），后台每一个可信数字都从它算出来。几个把话说死的地方：

- 主键是 `(activity_date, user_id)`，**顺序就是重点**：面板上每一个聚合读取都是一段有界日期范围的扫描，日期在前它才是单次索引范围扫描而不是全表过滤。想要反过来的那些逐用户读取有自己的 `(user_id, activity_date DESC)` 索引。
- `last_seen_at >= first_seen_at` 写成 CHECK 约束。upsert 用 `LEAST`/`GREATEST` 就是为了让它不可能被违反 —— 约束的作用是把这个声明变成**可检查的**，而不只是「本意如此」。
- 保留 400 天而不是 365，好让一个同比对比在被问到的那天两个端点都还在。定在 05:00 UTC，避开弹幕 TTL（04:00，0006）和 `activity_events` 的 prune（04:30，0021），三者不抢同一个 pg_cron worker 槽。
- 迁移里 `SET lock_timeout = '3s'`：那条外键会对 `users` 取 ShareRowExclusive 锁，一个无上限的 DDL 等待会把之后每一次登录和资料写入排在它后面。快速失败让部署重试，严格优于在这张表上排出一列锁队。

### ★ 日界是 +08 不是 UTC

所有 date 列按 Asia/Shanghai 分桶，因为读者在那儿。UTC 分桶会把一个晚上 00:00–08:00 那一段（正是这个目录的黄金时段）算到前一个自然日，**把一个人的一个晚上抹成两个「活跃日」**，同时把日趋势和到访天数一起抬高。这个偏移是固定且历史确定的：中国 1991 年之后没有夏令时，所以字面 `+08` 是精确值不是近似，也不依赖容器的 tzdata。写入端（`internal/activity`）用固定的 `time.FixedZone(+8h)` 算同一条边界 —— 两边必须一致。

### ★ 请求路径上一次数据库都不碰

中间件和登录钩子都以「互斥锁下写一次 map」结尾，Postgres 由后台 goroutine 每分钟写一次。实测每请求增量：

| 场景 | 增量 |
|---|---|
| 匿名 | +51ns，零分配 |
| 已认证，路由本来就要验 token | +1.3µs |
| 公开路由，别处没人验 token | +7.4µs |

+1.3µs 那一档靠的是一个 claims sink 把已验证的 claims 从 auth 中间件里带出来，而不是把 HMAC 再算一遍。见 `BenchmarkMiddleware`。

另外 `/api/avatars/` 不再算作 presence —— 它是个文件服务器，一个讨论页会记二十次「请求」。

### ★★ 方案的另一半（浏览器 beacon）在上线前被砍掉了 —— 它把 CDN 缓存命中变成了源站请求

`/anime/*` 走 Cloudflare 边缘缓存，命中时请求根本到不了源站。beacon 是 POST，而 CF 从不缓存 POST —— **于是每一次真人页面浏览都会产生一个本来不存在的源站请求。这是类别变化，不是百分比变化。**

还有第二笔：beacon 是写请求，会记进读者自己那份 per-IP 限流预算里 —— **遥测和这个人真正想做的事在抢同一份额度**，点得快的人可能被自己的遥测挡下来。成本落在读者头上。

砍掉它的代价是：页面浏览数、播放数、以及按站内区域的拆分，加上对未登录读者的**全部**可见性。除此之外不花任何东西 —— DAU/WAU/MAU、最后出现、到访天数、登录、留存全都来自服务端 recorder，而 `Navbar.tsx:276-310` 对持有 `auth_hint` 的人在每次 pathname 变化时都会重跑 `/api/auth/me`，所以已登录读者的软导航照样计得到。

`page_view_count` 和 `playback_count` 跟着一起删掉，而不是留成永远为 0 的列：**一个永远打印「播放：0」的看板，比一个压根不提播放的看板更糟。** 0025 还没部署过，所以是就地改而不是再发一个 0027 去撤。那张聚合用、没有 user 列的 `activity_surface_daily` 也一并去掉。

未登录读者的覆盖是一个真缺口，记在 TODOS.md 和 0025 的表头里：要补就从 nginx / CF 日志去拿，**不要在热路径上开一个写端点**。

### ★★ 删掉一个用户，能静默清掉所有人的计数

外部 Codex 走查发现的四个缺陷里最锐的一条。flush 是「所有用户一条语句」，出错就整批丢弃；而 access token 在账号被删之后还有效 15 分钟 —— 于是它发的每一个请求都会入队一个背后没有行的 uuid，撞外键失败，**那一分钟里所有人的计数一起消失**。修法是对 `users` 加 `WHERE EXISTS`，让它结构上不可能发生，而不是「被处理掉了」。

其余三条：

- **停机 drain 根本够不到。** `Close()` 被 defer 在一个 15s 的 `srv.Shutdown` 后面，而 Docker 的默认宽限期是 10s；并且错误分支直接退进程、defer 一个都不跑。现在两条路径都显式 Close，drain 有自己 3s 的预算，compose 给 go-api 加 `stop_grace_period: 30s` —— 那个早就存在的 15s shutdown 也是**第一次**真正够得到。
- **`Start()` 调两次会在第二次 `close(done)` 时 panic**，发生在停机时，在生产。改成 `sync.Once`；并且 `Close` 现在会在没人启动过时把循环起起来，免得它永远等一个不会有人去关的 channel。
- **溢出 flush 会给超出上限的每个请求各开一个 goroutine** —— goroutine 风暴恰好在数据库已经不舒服的时候到达。改成容量 1 的 kick channel 由现有循环消费，而且拒绝路径也要发信号（第一版漏了这半边）。

### ★ 0026 用别的表恰好见证到的东西重建历史 —— 并且必须把接缝画出来

这样面板一打开就有图而不是一片空白。但那些是**互动日**，是真实到访的一个严格且很小的子集；连着画、中间不加分隔的话，**切换点看起来就像产品起飞了，而且一定会被这么读。**

每一行重建出来的数据 `request_count` 都是 0，而实时 recorder 永远不会把它留在 0 —— 所以「埋点从哪天开始」是从数据里推出来的（`min(activity_date) FILTER (WHERE request_count > 0)`），不是一个会漂的手写常量。接口把它返回，图上把接缝画出来。

`episode_watches` 被**刻意**排除在数据源之外：0024 给每一条已存在的观看标记盖了同一个事务时间戳，拿它当源会凭空造出一个「全体用户在同一天全部活跃」的日子。

### 变更 — 留存

D1 / D7 逐日精确，每个 horizon 各有自己的资格闸门，所以今早才注册的账号不会被算成「明天没回来」。「曾经回来过」和它们并列展示 —— 因为一个 horizon 的 cohort 可能小到百分比说明不了任何事，只报比率会把噪声画成趋势。

### 测试

`lib/activityChart.ts` 有单测，但纯函数覆盖率抓不住一个**根本不调用这些函数**的组件。用 `renderToStaticMarkup`（react-dom/server，不引 jsdom，不加新依赖）钉住四件这个面板没人盯就会做错的事：四个可靠性档次仍然可区分、某个子查询失败要渲染成「不可用」而不是渲染成 0、埋点之前的柱子要带斜纹和那句说明而不是只有一个图例色块、全零序列产出 `height:0%` 而不是 `NaN%`。

新增：`login_count` 唯一写入方（含「登录失败不动它」）、ClaimsSink 的不变量（sink 写入不能伪造一个已认证上下文）、删号守卫打真 schema、留存的软失败、Start/Close 幂等、溢出合并。

### ★ effect 体里 setState 是硬 lint 错误，不是警告

D3 重写把 fetch 改成内联，`setLoading(true)` 直接写进了 effect 体，被 `react-hooks/set-state-in-effect` 拒绝。**lint 棘轮是闸门不是建议，而我本地只跑了 build + tsc。**

修法和仓库里 Navbar 那个鉴权探针同一个形状：每个 setState 都挪进 async helper，effect 期间不同步触发任何东西，也就不会引发级联渲染。同一轮里 `ActivitySection` 的 fetch effect 也不再把自己会写的 state 列进依赖 —— 那让一次失败发出了两个请求。

---

## [3.17.5] - 2026-08-28

### 修复 — 每个 404 页都带着两个自相矛盾的 robots 标签

```
<meta name="robots" content="index, follow">   <- 根 layout，全站
<meta name="robots" content="noindex">         <- notFound()
```

Google 解这个冲突的办法是取最严的那个值，所以不该被收录的东西确实没被收录。但**这个保证靠的是一条平局裁决规则，不是页面把话说清楚了** —— 而在这些路由上 `noindex` 是唯一的信号：`/anime/[id]` 和 `/seasonal/[season]/[year]` 有 `loading.tsx`，响应是流式的，按 Next 自己的文档，头一旦发出去状态码就再也改不了。一部不存在的番回的是 200。是那个 meta 标签在拦着它进索引，它不该先赢一场争论才行。

`index, follow` 本来就是爬虫的默认假设，全站声明它什么也没买到。删掉之后，正常页面没有 robots 标签（也就是可收录），404 页只剩一个毫不含糊的 `noindex`。

真正需要限制的页面本来就自己声明：`/login`、`/register`、`/profile`、`/settings`、无查询词的 `/search`，以及 `/u/[username]` 的别名形式；法律页走 `untranslatedRobots(locale)`。

### ★★ `/smoke` 是被这次删除翻出来的 —— 一刀切放行会让两种页面长得一模一样

`/smoke` 是个 debug 页，打印上游调用返回或失败的内容。它自己没声明任何 robots 指令，于是继承了根 layout 那条 `index, follow` —— **也就是说它一直在主动请求被收录。** 现在它自己声明 `index: false, follow: false`。

这就是一刀切放行造出来的风险形状：**它让一个想清楚了自己该可收录的页面，和一个从没想过这个问题的页面，变得无法区分。**

### 已知未闭合 — 这次没有交付真正的 404 状态码

两个候选修法都实测过，都被排除：

- **把 `notFound()` 挪进 `generateMetadata` 不会设状态码。** 听起来很有道理，一测就是假的。对照组成立：同一个 dev server 对完全没匹配上的路由照样正确返回 404。
- **只删掉那两个 SEO 路由的 `loading.tsx` 也不够。** 单删 `app/[lang]/anime/[id]/loading.tsx` 仍然回 200；逼出流式的那道边界是**根部的** `app/[lang]/loading.tsx`，删它能拿到真 404，代价是全站每一条路由都失去流式骨架。

所以剩下的决定是全站级的而不是逐路由的，得由权衡「冷加载时的骨架屏」和「软 404 报告」的人来做（3.18.1 做了这个决定）。Next 的文档写明收录风险已经被兜住："In the streaming case, this does not lead to indexation because the page is explicitly marked noindex in the HTML."

`global-not-found.js` 不适用 —— 它管的是完全没匹配上任何路由的 URL，而 `/anime/999999999` 是先匹配上了自己的路由，再调 `notFound()`。

### ★ 测试 — spec 的第一版自己是错的，而且只有 CI 抓得到

`robots-posture.spec.ts` 写成 e2e 而不是单测，因为缺陷不在任何一个页面的声明里 —— 每个页面单看都是对的。是一个**继承来的**标签撞上一个**声明的**标签，只有渲染出来的 HTML 才看得见。

第一版断言的是「最多只有一个 robots 指令」（一个渲染细节），而且读的是活 DOM。两个一样的 `noindex` 是冗余，不是矛盾；而**爬虫从不 hydrate** —— 这些路由是流式的，hydration 期间那个标签会短暂出现两次，本地跑不出来，CI 上炸。现在它直接查矛盾本身，并用 `page.request.get` 读服务端渲染的 HTML，也就是爬虫真正拿到手的那份文档。`--repeat-each=3` 实测 60/60，变异仍然咬得住。

---

## [3.17.4] - 2026-08-28

### 修复 — sitemap 只向 Google 报了目录里的 20 部番

`/sitemap.xml` 的番剧行来自 `/api/anime/yearly-top` —— 一个回答「某一年评分最高的那些」并且自己封顶 20 条的端点。sitemap 要的是 `limit=100`，`parseLimit` 静默夹到了 20。

生产实测：`/sitemap.xml` 出 75 条 URL，其中 60 条是 `/anime/` —— 20 部番 × 3 个语言。目录余下的部分，只有靠爬虫猜 id 才够得到。

**没有任何东西失败过。** 那个错的端点回答得完全正确，文档校验通过，没有一条测试或一行日志对它有意见 —— 这正是它活下来的原因。而且改 limit 也修不了它：`/yearly-top` 根本枚举不了一个目录。

### 新增 — `GET /api/anime/sitemap`，故意不加任何过滤

`anime_cache` 里每一行在 `/anime/{id}` 都渲染出 200，所以在这里写一个 `WHERE` 就是在替 Google 决定该藏起哪些真实存在的页面。从生产抽 12 个随机 id 验证，全部 200。

### ★ 分片按 `anilist_id % n`，不按 LIMIT/OFFSET

用 offset 的话，新增一行会把它之后的每一部番挤进另一个文件，重读整套的爬虫看到的是整条尾巴都在变动。取模让一个 id 终身待在同一个文件里，而且 id 空间够密，不调参也是均衡的 —— 生产实测 n=4 时四片的行数偏离均值不到 ±9%，四片之和与表的行数严格相等，无丢无重。

分 4 片而不是 1 片，是因为站点给每一部番的每一个语言各出一条 URL，整个目录的 URL 数已经越过 Google 每份 sitemap 50,000 条的上限，一个文档装不下。

### ★★ 分片放在 `/sitemaps/anime/`，因为 `app/anime/` 会吃掉整个 SEO 面

两条约束决定了文件的位置：

- **不能建 `app/anime/`。** 它会是 `app/[lang]/` 的**静态兄弟**，而静态段优先 —— 于是 `/anime/{id}`，站点全部的 SEO 面，会开始解析到一个没有页面能服务它的段上。
- **`/sitemap.xml` 必须保住自己的 URL。** 给根部 `app/sitemap.ts` 加 `generateSitemaps` 会把整个文档搬到 `/sitemap/0.xml`，等于退掉 Search Console 手上那个唯一的 sitemap 地址。

没有索引文档：Next 的 sitemap 约定只发得出 `<urlset>`，发不出 `<sitemapindex>`。robots.txt 接受任意多条 `Sitemap:` 指令而且 Google 全都读，所以在 robots.txt 里逐个点名就是分片被发现的方式。

那些 URL 由 `animeSitemapPath()` 一个函数算出来，robots.txt 和 sitemap 路由都从它取，两边不可能各说各话 —— **robots.txt 指着一个不存在的文件是这层间接存在的理由，而且它是无声的：sitemap 照常工作，只是 Google 永远找不到。**

分片数从常量来而不是从 count 查询来，也是同一类考虑：这段跑在构建时，而构建时的 fetch 是构建时的失效模式 —— 镜像构建期间 Go API 本来就不在（这是常态），一个 count 查询会返回 0 个分片，烤出一个完全没有番剧 sitemap 的站点。常量不会失败。

### 变更 — lastmod 改用行自己的 `updated_at`

上一版给每次 fetch 的每一行都盖 `new Date()`，等于告诉 Google 整个目录一秒前刚全部改过 —— 这是它一爬就能证伪的说法。而文档写明的、对一个不可信 lastmod 的反应是**整站不再相信 lastmod**，所以那笔账还连累着静态页的信号。

`app/sitemap.ts` 补上显式 `revalidate`。它原本每小时重建，只是刚被搬走的那个 fetch 的副作用；不补的话 `currentSeasonPath(new Date())` 会冻结在镜像构建那一刻，把这个函数当初就是为了修掉的那个「季度过期」bug 原样带回来。

### ★ 坏的分片坐标返回 400，不夹取

这和同一个包里 `parseLimit` 的做法正好相反，理由是两者夹取的后果不同：夹一个 limit 只是少返回几行，夹一个分片返回的是**别人的行**，而同一套 sitemap 里出现重复的 `<loc>` 是 Google 记在站点头上、不会报回来的缺陷。`shards=0` 单独覆盖，因为它否则会以 `id % 0` 打到 Postgres。

### 测试

沙箱 e2e 钉住分片 URL，并检查 robots.txt 宣告的每一个 sitemap 都真的解析得出来 —— 这个失效模式没有别的症状。

### 已知未闭合

合并后要去 Search Console 提交这四个分片 URL。Google 靠 robots.txt 一样发现得了，但 Sitemaps 报告只跟踪**提交过**的文档 —— 不提交的话，`/sitemap.xml` 看起来是从 75 条掉到 15 条，而整个目录的那些 URL 不出现在任何报告里。

---

## [3.17.3] - 2026-08-28

设计债计划 E0–E9 的十一项。

### 修复 — 分数徽章的文字和背景是分开选的

**每一部评分 75 以上的番，都在线上渲染成绿字配琥珀底** —— 而且是在全站流量最高的那个页面上。两半隔着一屏：一个阈值梯子决定文字颜色，徽章自己把背景硬编码成琥珀。单看哪一半都不算错，**缺陷只存在于接缝里**，而 `scoreBadge(color)` 这个签名就是把接缝打开的那个东西。6.0 和 5.7 恰好是对的，所以盯着页面看永远看不出来。

同一个阈值梯子一字不差地存在于三个文件里，其中第二个（`player/_components/EpisodeFileList.tsx`）也是错的；`components/anime/AnimeCard.tsx` 是对的，因为它的背景本来就是中性遮罩。

修法不是「把背景也做成一个函数」：而是**没有任何东西能再只要一半**。`scoreStyle.ts` 导出的样式函数每个都返回完整的一对，并且不导出任何单独取前景色的途径。压在封面图上的那个变体单独出一对（`scoreScrimStyle`）—— 12% 的淡色底在一张可能是任何颜色的封面上没有对比度保证，所以它用不透明遮罩，只让文字带分档。

### ★ token 词汇表：DESIGN.md 定义 31 个，`globals.css` 只声明了 10 个，其中 6 个已经漂移

一个没声明的 token 会让 `var()` 解析成空、整条声明被丢掉，所以失败模式是**一个看起来差不多对的组件** —— 于是每个需要圆角或间距档位的组件都改成硬编码字面量了。

改动前实测：整个 `src/` 里 `--accent` 有 9 处引用，`--bg` 和 `--text` 各 1 处，**调色板其余部分 0 处**。这也正是修正那 6 个漂移值属于安全操作而非视觉改动的理由：它们没有任何一个存在过一次 `var()` 引用。

### 变更 — 移动端首屏往上收，桌面端逐像素不变

390×844 实测，「修前」打的是生产：

| | 修前 | 修后 |
|---|---|---|
| 横幅 | 400px | 150px |
| h1 | 769px | 187px |
| 简介 | 799px | 217px |
| 主 CTA | 1190px | 811px |

桌面端两边完全一致（437 / 484 / 702），**这一条才是「这是一次位移不是一次重设计」的判据**。

### ★★ 周更表那排 tab 不可能有过焦点环

树里有四个不同的焦点环：规范里的蓝、teal、琥珀，以及每部番的封面取样色 —— 最后这个会随轮播换颜色，在某些封面上根本看不见。每一个在自己那个文件里都说得通，所以四个都活过了 review。

而周更表那排 tab 是另一种情况：它的内联样式以 `outline: none` 结尾，**内联声明压过任何样式表规则** —— 也就是说，任何写在别处的 focus CSS 都会被写出来、被 review、被合并，然后被静默忽略。现在它们是真正的 tablist，带 roving tabindex。

### 测试 — 每一道闸门都验过真的会咬

- `globals.test.ts` 解析 DESIGN.md 再和 `globals.css` 比对；
- `focusRing.test.ts` 扫描**每一个** `*.module.css` —— 只测 Button 会漏掉四个环里的三个；
- `scoreStyle.test.ts` 对 0..100 的每一个分数断言背景和前景命名同一档；
- `Button.test.ts` 抓一个没有 class 的 variant —— 那会渲染出 `class="undefined"`。

箭头键移动抽成了纯算术（`tabListNav.ts`）才测得了：仓库没有 DOM 测试库，活在事件处理器里的行为断言不了，索引算术可以。要钉的两件事都是「写错了也不会有人发现」的那种 —— 从 index 0 往回绕不能绕成负数（JS 的 `%` 跟被除数的符号），以及**不该管的键必须返回 null**，一个对所有键都 preventDefault 的 handler 会把 Tab 困在 tablist 里，那比它本来要修的「没有方向键」更糟。

另有 `e2e/specs/sandbox/anime-detail.spec.ts`：顶层的同名 spec 跑的是**已部署**的站点，只能在缺陷上线之后报告它；这一份在 sandbox project 里，是能让 PR 挂掉的那个。不带认证 —— 断言的是搜索访客看到的那个页面。

### 已知未闭合

- DESIGN.md 规定按钮 14px/500、padding `10px 20px`，抽出来的实际是 13px/600、`10px 18px`。这是视觉改动，要单独 review，**故意没做**。
- `page.tsx` 仍有约 1300 行，对着 800 行的上限；76 处内联样式里搬走了 19 处。

---

## [3.17.2] - 2026-08-26

### 变更 — 源站地址不该出现在公开仓库里

`scripts/rotate-mail-key.sh` 当初带着源站地址和 SSH 端口作为「省事」的默认值。这个仓库是公开的，而源站地址是一层纵深防御 —— 一个从当前代码树里 grep 不出来的地址，值得多留一会儿。`VPS_HOST` 和 `VPS_PORT` 现在都必须从环境变量给，没有兜底。

### ★ 这一步没有撤销掉什么

**从被跟踪的文件里删掉一个值，不等于把它从历史里删掉。** 这次提交没有、也不可能改变那一点。

所以诚实的立场是：源站地址应当被当作公开信息，网络层的访问控制得自己扛住全部重量，不靠隐蔽性帮忙 —— 它本来就是照这个前提配的。这次提交只是不再让它更容易，而这件事本身值得做。

---

## [3.17.1] - 2026-08-26

### 新增 — 轮换邮件 API key，而新 key 不离开你的终端

现在装在生产上的那把 key 曾被粘进一份聊天记录。它是 send-only 的，但域名验证之后，这已经足以以我们的域名发信 —— 而这正是一次钓鱼想要的东西，针对的还是一个当前信誉干净的域名。

`scripts/rotate-mail-key.sh` 用 `read -s` 读替换的 key：不回显、不进 shell history、不进任何记录。传给服务器走 **stdin 而不是 argv** —— argv 里的东西，在命令运行的整段时间里对机器上每一个进程的 `ps` 都可见。

### ★ 顺序反了会无声失败

先建新 key、装上、验过，**最后**才吊销旧的。反过来先吊销，会开出一个发信坏掉的窗口 —— 而发信坏掉从外面完全看不见，因为 forgot-password 无论信有没有发出都返回 200（它必须这样，否则响应就等于确认哪些地址注册过）。没有人会发现，直到有读者来抱怨。

所以脚本在把新 key 写到任何地方**之前**，先用它真发一封信，发不出去就拒绝碰生产。另外三道：只改**恰好一行** `SMTP_PASSWORD=`，找不到恰好一行就中止；改之前先备份；重启后打印进程实际选中的发信通道 —— 那里出现 `falling back to Gmail` 就说明新 key 没落地，同时点名可以回滚的那个备份文件。

重启而不是重新部署：变的只有环境变量，重建镜像要花几分钟，白白把那个窗口撑宽。

---

## [3.17.0] - 2026-08-26

### 新增 — 密码重置信发得出去，而这恰恰是没法知道它到没到的原因

密码重置信走 Gmail SMTP，以一个 gmail.com 地址发出。这能发；这也正是我们无法判断它对真正的读者有没有用的原因：**收信方绝大多数是国内邮箱服务商**，而它们对陌生域名的判定最严 —— 偏偏 From 头里那个域名和实际发信的那台机器之间，没有任何 SPF / DKIM / DMARC 关系。

只改 From 头会更糟：无论我们写什么，Gmail 都用 gmail.com 签名，于是这封信变成一个关于自有域名的、背后什么都没有的声明。唯一能让三项检查对齐的安排，是走一个**替我们的域名签名**的中继。所以这次加的是那条路径：`NewRelaySender(host, user, password, from)` 与既有的 `NewSMTPSender(user, appPassword)` 并存。

### ★★ 信封发件人不是 From 头

这两个是不同的字段，被不同的检查读：

- **信封**发件人（SMTP `MAIL FROM`）是 SPF 评估的对象；
- **`From:` 头**是读者看到的那个，也是 DMARC 要求 SPF 或 DKIM 与之**对齐**的那个。

在 Gmail 上两者都塌到账号地址上，所以这个区别根本看不见 —— 直到换成中继才分开。而中继的 AUTH 用户名经常压根不是一个地址：**Resend 的就是字面量 `"resend"`**。照 Gmail 那条路复用它当信封发件人，`MAIL FROM` 里就是垃圾，信在上面这一切生效之前就被拒了。现在它们是 `SMTPSender` 上两个独立字段，并有测试断言信封里带的是 `MAIL_FROM` 而不是 AUTH 用户名。

这个失败本来会是无声的：`SendPasswordReset` 的错误被 forgot-password handler **故意**吞掉（否则响应就等于确认哪些地址注册过），所以无论有没有真发出去，读者都看到 200。

### 变更 — 中继优先，Gmail 兜底

`SMTP_HOST` / `SMTP_USER` / `SMTP_PASSWORD` + `MAIL_FROM` 优先；`GMAIL_USER` + `GMAIL_APP_PASSWORD` 留作兜底。一台还没拿到中继凭据的机器会继续发信，而不是悄悄变哑 —— 正因为 handler 那个 200 会盖住这个差别，这个顺序才重要。启动日志打出选了哪条路，走兜底时明说这意味着信和自有域名对不齐。

`SMTP_HOST` 校验必须是 `host:port`（裸主机名是最可能的手误，否则要到 `PlainAuth` 里才炸，且错误信息既不提变量也不提值），`MAIL_FROM` 校验必须含 `@`（它原样落进 `MAIL FROM`）。中继密码**不做**空白字符剥离 —— 那个剥离是为 Gmail App Password 的四段显示形式存在的，对一个不透明的 API key 做同样的事会静默改坏它。

DNS 和服务商都还没配；`SMTP_*` 不设时，这次构建的行为和之前完全一样。

### ★ 一项检查没能跑，就不许判通过

新增 `scripts/check-mail-dns.sh`。这里每一种会中断「已认证投递」的失败都是无声的：**两条 SPF 记录是 permerror，而接收方把 permerror 当作完全没有 SPF** —— 所以为了「加」一个发信方而多加一条记录，等于把所有发信方的认证一起删了。DKIM selector 从来没发布成功、加发信记录时把 MX 行覆盖掉，也都不报任何错，信只是对那些会验的接收方停止到达，而那正是我们绝大多数读者。

**第一版脚本在自己身上把这个道理演示了一遍**：它用了 `mapfile`，macOS 的 bash 3.2 没有这个内建，于是 SPF 那一整节根本没跑 —— 脚本打印「没有错误」并 exit 0。`set -u` 对未设变量只往 stderr 报一句，不退出，所以没有任何东西拦住它。

重写成不用数组，并且结论改成：**一项检查没能被评估时拒绝判通过**，而不是把「没有」当成「通过」。它对当前状态的报告是：恰好一条 SPF 记录、Cloudflare Email Routing 的 include 还在、而且只有那一个 include —— 也就是说，现在还没有任何发信服务被授权以这个域名发信。

---

## [3.16.2] - 2026-08-25

### 修复 — 那个为了拿掉 AniList 依赖而加的 fixture，没拿掉 AniList 依赖

`locale-routing` 断言 `/anime/21`、`/en/anime/21`、`/zh-Hant/anime/21` 能渲染出来。为了让它不再依赖一次实时的 AniList 请求，此前给它加了 fixture —— 用的是 `ensureAnimeCached`，而那一行**没有 studios、没有 characters**，`isStale`（`go-api/internal/anime/detail.go`）对这两者中任意一个都会独立触发，和 `cached_at` 无关。一行 stale 的数据照样把 handler 送去上游。

于是合并闸门的关键路径上一直站着一个限流的第三方，而且它一直在说话：同一套件当天跑了 7 次，同样的 6 条 spec 一起挂了 4 次 —— main 上挂，topic 分支上也挂，两个并发跑在不同 runner 上还互相不一致。**跑没跑绿从来没跟着代码走。**

`ensureAnimeDetail` 才是做这件事的那个 helper，它自己的 docstring 早就这么写了。

### ★ 预热循环加的是 `/anime/0`，不是那个 fixture 用的 21

预热列表里没有任何一条路由够得到 `[lang]/anime/[id]` 段，而 Turbopack 是在一个段被首次请求时才编译它 —— 所以整个套件里第一个 `/anime/<id>` 要在**某条 spec 自己的超时里**付这笔编译，还是在三个 worker 的负载下付。

id 取 0 是故意的：`generateMetadata` 和页面都在 `anilistId <= 0` 上提前返回、不调 `loadDetail`，所以 0 编译了路由却一次 API 都不发。这里拿真 id 预热反而有害：这一步跑在 globalSetup 种任何数据**之前**，而 `loadDetail` 带着 `revalidate: 60` 取数 —— Next 会在那行数据已经存在之后，继续把这个 404 回放一分钟。

### ★ 200 行日志够不到失败

失败日志的 tail 从 200 行改到 4000 行。go-api 一直在打后台富化日志，等到收集证据那一步跑起来的时候，最后 200 行**全是套件结束之后产生的** —— 真正失败的那些请求早就滚出去了。诊断这件事的过程中，有两个错误结论就是从这个截断里得出来的。

### ★★ 让 `ensureAnimeDetail` 自己验证后置条件

它现在检查自己名字所承诺的那件事：种完之后行仍然读作 stale，就抛错并**点名是哪个 fixture**。

没有这道守卫的话，症状是「expected 200, received 404」出现在一条根本没提到任何 fixture 的 spec 里，每一个假设都要花一次 CI 往返去验。这不是假想 —— 这件事已经被用错的 helper「修」过一次，并且关掉了。

---

## [3.16.1] - 2026-08-25

### 性能 — 导入本地文件不再每个文件排 800 毫秒的队

`MatchCombined`（上游 `POST /api/v2/match`）是 dandanplay 客户端里**唯一没有缓存**的对外方法。导入时每个 cluster 调一次，`matchUnmappedFiles` 还会按未映射的集数再调一次。而每次调用都要从一个**进程级**令牌桶取票 —— `rate.NewLimiter(rate.Every(800ms), 1)`，burst 1，全站所有用户共用这一个桶。

现在给它加 24 小时结果缓存（未命中 10 分钟）。

### ★ 缓存放在客户端方法层，不是 handler 层

最顺手的做法是缓存整个 `/api/dandanplay/match` 响应，而那是错的。那个信封里带着 `siteAnime` 和 Phase 2 的 `anime` 块，两者每次调用都从 `anime_cache` 现读 —— 这正是管理员改一次绑定、下一个请求就生效的原因。缓存整个响应等于给一个今天完全没有滞后的面加上 TTL；更糟的是 **Phase 2 每次调用重新给候选排名**，缓存条目可能在数据库已经改对之后还一直返回**错的那部番**。

放在 `MatchCombined` 上，所有 Postgres 派生的字段和今天一样新鲜，被缓存的只有 dandanplay 自己对「这个哈希是哪一集」的回答。

### ★ 未命中也必须缓存

浏览器在每次库挂载、每次切回标签页、每次重扫都会重发一模一样的请求，而且不会因为上次没匹配上就停。不缓存未命中，这笔账就每次重付一遍，价格是每个文件 800 毫秒，且由全站用户共担。

但未命中和命中不是同一种事实：未命中通常意味着「dandanplay 还没索引这个哈希」—— Phase 1 的宽松匹配闸门就是为这个存在的。冻结 24 小时会把「还没索引」变成「今天都别想」。所以未命中 10 分钟、命中 24 小时，10 分钟是照客户端自己的重试节奏挑的（重扫节流 60 秒、周期兜底 120 秒、延迟重试 65 秒）。

**错误从不缓存。** 传输失败或重试链耗尽说明不了这个文件的任何事，下一个调用者必须能重问。

### ★ 首次导入也快一倍 —— 但只因为那句 Wait()

库导入不带 keyword，Phase 2 根本进不去，于是 Phase 1 和 Phase 3 在同一个请求里相隔几微秒问同一个问题。ristretto 的写是异步的：**不加 `Wait()` 实测 5 次里 5 次第二问都跑赢了后台落盘、照样走网络**（加了之后 8/8 只问一次）。落盘只发生在刚花掉一张 800 毫秒的票加一次往返之后，代价可以忽略，而它把一个匹配不上的文件的首次导入成本直接砍半。

### ★★ MaxCost 不是条数上限 —— 这个缓存差点只有名义容量的 1/57

`internal/cache` 的注释写着「cost=1 时 MaxCost 就是最大条数」。**这句话是错的**，而我照着它把新缓存配成了 `MaxCost: 2e5`。

ristretto 在把条目交给准入策略之前会加上自己的开销：`i.Cost += itemSize`，其中 `itemSize = unsafe.Sizeof(storeItem[any]{})` = 56 字节。所以 cost=1、MaxCost=N 的缓存实际只装 **N/57** 条 —— 这里是约 3,500 条，不是 200,000 条。

而它**静默失败**：`Set` 返回 true，`Wait()` 返回，`Get` 照样落空。没有报错、没有日志、没有指标，唯一症状就是本该命中的文件又去了一趟网络 —— 正是这个缓存要消灭的东西。

修法是给 `cache.Config` 加 `IgnoreInternalCost`（默认 false，对现有调用方是 no-op），只在这个缓存上打开，并把那段错误的包注释改对。陷阱本身钉进了 `cache_test.go`：同样 `MaxCost=1000` 写 900 个不同的键，默认配置只活下来 **17** 条，打开开关活下来 **900** 条。

### ★ 缓存键：`fileHash` 是攻击者可控的

`/match` 直接解 JSON body，没有任何东西保证 `fileHash` 真是它该有的 32 位十六进制。朴素拼接下，这两个请求会算出同一个键 `a:1:b:2:c`：

```
{fileHash:"a",     fileSize:1, fileName:"b:2:c"}
{fileHash:"a:1:b", fileSize:2, fileName:"c"}
```

于是先问的那个会替另一个回答。改成给哈希加长度前缀之后编码是单射的。

键还有一条：它由**将要发上线的请求体**算出，不是由 `MatchCombined` 的入参算出。请求体是归一化过的（hash 为空、size 非正时都会被丢掉），所以同一个文件从库导入（`rep.file?.size ?? 0`）和从播放器（`f._fileRef?.size ?? 0`）进来不会占两个格子。

### 测试

新增 8 条，全部通过变异验证 —— 8 种变异 8 杀：不缓存未命中、把错误也缓存、命中存成 `found=false`、键朴素拼接、键取入参而非请求体、去掉 `Wait()`、去掉 `IgnoreInternalCost`、把未命中当零值命中回放。

`TestMatch_RepeatedRequestAsksDandanplayNothing` 走真 handler 而不是直接调方法：要守的是「一个请求扇出到三个 phase 之后总共只问一次上游，重发则一次都不问」，不是「某个方法会记忆」。

第一轮变异里有一个存活，查下来是**等价变异** —— `cache.Get` 按值返回、`value()` 是值接收者，我那行显式拷贝是多余的。删掉冗余代码并把注释里记错功劳的那句改对，比留着一条不区分任何东西的断言强。

---

## [3.16.0] - 2026-08-25

### 修复 — 同名番剧不再各占一张卡

自动归类（`dedupeSeriesByAnimeId`）按 `Season.animeId` 分组。#105 删掉了往那个字段写值的兜底 —— 它存的是 bgm.tv subject id 塞在 dandanplay 形状的列里，`/api/dandanplay/match` 三个 phase 都不返回 dandanplay animeId，所以 `?? merged.bgmId` 不是兜底、是**唯一会触发的分支**。删是对的，但这一趟从此**没有键可分组**：自动导入的番根本没有 `Season.animeId`，两张同一部番的卡片再也碰不到一起。那个文件的头注释自己写了这件事，没人跟进。

现在按 `Series.anilistId` 分组 —— 而它可靠可用同样是 #105 给的（绑定扫描会给没人点过的番写上）。它是站点自己的主键，两个本地行共用一个就是「同一部番记了两遍」的定义。

### ★ 两个 id 空间不能共用一个 Map

`Season.animeId`（dandanplay 空间）仍有活人口（手动 rematch 走 dandanplay 搜索会写真值），所以有值时仍可用作键。但两个空间**会撞**：id `806` 在两边都是有效番剧、两个接口都返回 200。放进同一个数字 Map 会以满分信心合并两部无关的番 —— 正是 #105 的品牌类型在下面一层要防的那个碰撞。

所以键是**带命名空间的字符串**，不是数字：`anilist:806` 和 `dandan:806` 撞不了，因为它们不是同一个字符串。

### ★ 自动化的前提：手动拆分必须活下来

改成进库时自动跑，而不再只挂在工具栏按钮上 —— 看着两张一模一样的卡片的人，没理由猜到某个菜单项才是合并它们的东西。

自动化只因为两件事才安全：

- **`wasDeliberatelySplit` 守住拆分。** `splitSeries` 把血缘记在**新行**的 `splitFrom` 上，是单向的，所以要两边都查。没有这个守卫的话，自动动作会在每次访问撤销一次手动动作，而用户没有任何办法让它停 —— 那比不自动化更糟。
- **合并是软合并且留痕。** 不移动任何 Season / Episode / Progress 行，`performMerge` 追加一条 opsLog，而且弹的是和手动按钮**同一个撤销提示**。

它排在三个 sweep 的最后，好让绑定扫描当次写入的 anilistId 对它可见。这是靠声明顺序而非保证 —— 本次绑定、下次才合并只是显示上晚一拍，不是正确性问题，为此把两个 sweep 耦合起来不划算。

### 测试

`dedupeSeries.ts` 此前**零测试**。新增 13 条，四种变异全部验证：两个空间共用数字键（挂 5）、不守拆分（挂 2）、拆分只查单向（挂 1）、`Season.animeId` 优先于 anilistId（挂 1）。

`e2e/specs/sandbox/library-dedupe.spec.ts` 两例 —— 单测看不见「进库时到底跑不跑」（effect 不在 `renderToStaticMarkup` 里执行）。断言的是 `userOverride.mergedFrom` 这条软合并唯一的痕迹，而不是卡片数量，这样它不依赖网格怎么隐藏被合走的行。第二例**不用轮询**：等过合并本该发生的窗口再断言它没发生 —— 轮询「仍未合并」会瞬间通过、什么都证明不了。

配套 `dexie-seed.ts` 加 `splitFrom` 种子（**单向记录，照实现的样子**，两边都记会掩盖只查单向的守卫）和 `readSeriesRow` 的表名参数。

---

## [3.15.2] - 2026-08-25

### 修复 — 偏移量写进去了，但对账已经推完并挨了 400

3.15.1 让已绑定的番在进库时拿到偏移量。用户装上后 400 **仍然在**。

原因不在数据，在**顺序**：`reconcileLibrary` 被 `libraryReconciledRef` 按挂载锁死，而且它跑在偏移回填**完成之前**。所以最关键的那一次访问 —— 修复上线后的第一次 —— 推的仍是未翻译的集号、照样 400，然后被锁死、本次挂载再也不会带着刚写好的偏移重试。**用户在本该修好的那一次访问里，看到的正是他上报的那个错误。**

#105 给绑定扫描建过这个机制（扫完 `libraryReconciledRef.current = false; setReconcileEpoch(n => n + 1)`），偏移回填漏了同一步。现在补上，并带同样的一次性守卫 —— 回填自己的写入会改 `allSeries`，而那正是这个 effect 的依赖，不守就会变成推送风暴。

### 测试 — 这个顺序只能从外面数请求

新增 `e2e/specs/sandbox/episode-offset-sync.spec.ts`。断言的是**服务端的 `currentEpisode`**，不是界面上的任何东西：`= 10` 只可能发生在「数字在上线前被翻译过」的情况下，38 会被拒；而「界面显示 10、线上发 38」正是共享规则要防的那种无声分歧。

三件事要同时成立才算修好（前两件 3.15.0/3.15.1 已有，第三件是这次）：偏移可推导、已绑定的番拿得到、**对账在回填之后跑**。第三件活在 React effect 里，而这个仓库的组件测试用 `renderToStaticMarkup` 根本不跑 effect —— 和 #105 那个 re-arm 一样，只能从浏览器外面数 `PUT` 请求。

第二例钉住反向：第一季的偏移是**确信的 0**，0 必须什么都不移。这一条也是接口返回 `{known, offset}` 而非可空数字的理由 —— 若把「没有前作」当成「未知」它照样过，若把 0 当位移应用它就挂。

配套给 `e2e/fixtures/pg.ts` 加了 `seedPrequelRelation` 和 `format` 字段（**走查时的坑**：不种 `format` 的话链上一集都不计，会以满分信心报出偏移 0）。

---

## [3.15.1] - 2026-08-25

### 修复 — 3.15.0 那个修复够不到任何已经绑定的番

3.15.0 让集号偏移在**解析绑定**时取到。而 `resolveSeriesBinding` 对**已经绑定**的 series 会提前返回 —— 我在那行的注释里写了这个天花板，但没跟进它的后果：一个上报这个 bug 的人，他的番按定义早就绑好了，所以修复对他一点作用都没有。

和 #105 那次是同一个形状的错：逻辑是对的，触发点够不到存量数据。

新增 `episodeOffsetBackfill.ts` —— 进库时的一次性批量回填，和已有的 `episodeCountBackfill` 同一个触发点、同一批行、同一个 cap。**故意是两个 sweep 不是一个**：候选集不同（一个 series 可能有总集数没偏移，或者反过来），合成一遍会让已经答过的那一半被反复重问。

配套 `GET /api/anime/episode-offsets?ids=...`（批量）。单条接口服务不了这个 sweep：200 部的库会打 200 次请求，而 per-IP 是 1 req/s —— `/api/anime/episodes` 早就为集数解决过同一个问题，所以照抄它而不是另发明一套。

### ★ 判定条件里那个容易写错的地方

「已经有偏移了」是 `typeof x === "number"`，**不是 `x > 0`**。

`0` 是真实答案（「本季之前什么都没有」），而且是目录里大多数番的答案。用正数判定会让每一部独立作永远留在候选集里，sweep 每次重发同一批 id、永远不收敛 —— 服务端的 0015 和 0023 各踩过一次这个坑，这次是它从客户端这道门进来。

### 测试

新增 12 条（判定条件、`known:false` 锁存、**服务端没返回的 id 也要锁存**、请求失败**不能**锁存、一个 id 对应多个本地行、写失败不拖垮整个 sweep）。三种变异全部验证：用 `>0` 判定挂 3 条、请求失败也锁存挂 1 条、没返回的 id 不锁存挂 1 条。

集成测试补两例批量形态：**递归里多带的 `root` 列 + `DISTINCT ON` 会不会张冠李戴**（同一个系列的三季一次查，错配会看起来特别合理），以及未缓存的 id 是「不返回行」而不是「返回一行说未知」。

---

## [3.15.0] - 2026-08-25

### 修复 — 跨季连续编号的剧集绑错格子，而且进度从来没同步上去过

上报的现象：库里《葬送的芙莉莲 第二季》只有一集（第二季最后一集），却显示在第 1 格。

集号是本地文件名解析的（`recordFactory.js` 的 `number: item.episode ?? 0`），解析器读到的是 **38** —— 第一季 28 集 + 本季第 10 集，发布组按跨季连续编号命名。落库就是 38，没错。错在下游两处，而它们缺的是**同一个事实**：

- **显示** `episodeGridModel.normalizeEpisodeNumbers` 推导位移用的是 `shift = lowest - 1`，也就是假设「你手上最小的那一集就是本季第 1 集」。只有一集时 `lowest = 38`，位移 37，38 − 37 = 1。
- **同步** 追番推送发的是原始的 38，而 `subscriptions/validate.go` 按 `anime_cache.episodes`（10）判超界，**每次都 400**。这部番的进度从上架那天起就没同步成功过，而且没有任何提示。

实测这个推断只在「手上有本季第一集」时成立：

```
本地 [38]        总集数 10  →  修前 1      修后 10
本地 [36,37,38]  总集数 10  →  修前 1,2,3  修后 8,9,10
本地 [29..38]    总集数 10  →  两边都 1..10   ← 这才是它本来对的场景
```

### 新增 — `GET /api/anime/{anilistId}/episode-offset`

沿 `PREQUEL` 边递归求和，得出「本季之前有多少集」。

**没有新建表也没有新迁移。** `anime_relations` 从 0001 起就在存关系边，由 AniList 详情查询里已有的 `relations` 块填充 —— 中途曾写了一个存 `prequel_anilist_id` 的迁移 0025，发现重复后删掉了。递归 CTE 直接读现成数据。

生产实测：

```
154587 葬送的芙莉莲        known=t offset=0
182255 葬送的芙莉莲 第二季  known=t offset=28   ← 38−28=10
209939 （第三季）          known=t offset=38   两跳 28+10
```

**返回 `{known, offset}` 两个字段而不是一个可空数字。** 「前面没有任何一季」和「查不出来」是相反的答案，`offset ?? 0` 一行就能把后者变成前者 —— 而那正是「拿一个没人确认过的原点去重排用户文件」。拆成两个字段，调用方必须先读 `known` 才能拿到数字。

`known=false` 是 200，不是 404 也不是 500：约四分之一的目录没有关系行（17560 部中 13378 部有，76%），这是**常态答案**；用状态码区分「未知」和「请求失败」会让客户端对一个稳定答案无限重试。四种截断都归为未知：前作不在缓存（生产 87 条）、有两个 PREQUEL（生产 143 部，是歧义不是相加）、深度上限（防环）、TV 前作缺集数（`coalesce(...,0)` 会给出一个看起来合理的错数，比没有答案更糟）。

### 一条规则，两个消费者

显示层和同步层必须做**同一个判定**，否则会出现「界面显示第 10 集、推送发第 38 集」而两边都不报错。所以规则只写一份，放在 `lib/library/episodeOffset.ts`：

- `offsetApplies` —— 服务端知道番剧谱系，只有客户端知道盘上有什么，而 `format` 只是「是否参与连续编号」的粗略代理。所以偏移量**要先拿证据验证**：每一集减去它都必须落在 `[1, 总集数]` 内。
- `toSiteEpisodes` —— **全有或全无**。只翻译「恰好合适」的那几集，会把两个编号空间塞进同一个订阅，而且会让网格和推送对同一个文件产生分歧。

同步侧的三个承重细节：`PushPlanned` 同时带 `episodes`（本地号，写记忆）和 `siteEpisodes`（站点号，发线上）—— 让「哪个数组用在哪」由类型说了算，而不是靠调用点的纪律；`lastSyncedEpisodes` 语义不变（仍是「本设备推过的**本地**集号」），记成站点号会让 `local \ pushed` 永远减不干净、同一集反复重推；偏移量拿**整个已看集合**验证而不是拿增量 —— 平常增量只有一集，而单个 38 既符合偏移 28 也符合 37。

### 测试

新增 8 条显示层 + 6 条同步层。四种变异全部验证过：不翻译（挂 3）、拿增量验证（挂 1）、记忆记站点号（挂 1）、包含性断言比错空间（挂 1）。

★ **第一轮变异有两条没被抓到**，说明当时的注释在断言没有测试守着的性质 —— 补了一条「增量能过、整体过不了」的判别用例（`[1,2,38]`，偏移 28）和两条端到端推送用例才补齐。

另有 `test/integration/prequel_offset_test.go`（8 例：真实谱系两跳、锚点自身不计入、未缓存前作、双前作歧义、TV 前作空集数、链中电影只穿不计、成环、锚点不在缓存）。本地 Docker 未起，**没在本地跑过**，靠 CI 的 `-tags=integration` 闸门验证。

### 已知未闭合

没有关系行的那约四分之一目录仍走旧推断，因此「只有本季后几集」的场景在那部分番上仍会错位。修法需要那些番的详情被拉取过一次，属数据覆盖问题不是逻辑问题。

---

## [3.14.1] - 2026-08-25

### 修复 — 侧车 `.srt` 转出来了，却没送到播放器

3.13.2 让侧车字幕转成 WebVTT 之后，`.ass` 正常显示而 `.srt` 仍然一条都没有。线上插桩三次全同：文件读了、`convertSrtToVtt` 跑了、74 字节的 `text/vtt` blob 造出来了，而 `<track>` 的 `src` 是空的。**转换是对的，交接掉了。**

`VideoPlayer` 里接 VTT 的 effect 读 `artRef.current`，依赖却只有 `[subtitleUrl]`：

```ts
if (!art || !subtitleUrl) return;
}, [subtitleUrl]);
```

ref 从 null 变成实例**不会**重跑 effect，而 `subtitleUrl` 之后不再变 —— 所以侧车字幕只要早于 artplayer 建成一步，这次早退就是永久早退，整个 VTT 层静默消失。`.ass` 文件更大、转换更慢，落地够晚所以赢了这个竞态；`.srt` 更小更快，输了。旁边的 jassub effect 依赖里本来就带 `playerReady`，从来没这问题。现在这条也带上。

### 测试 — 补 e2e，并且写明它盖不住什么

新增 `e2e/specs/sandbox/subtitle-sidecar.spec.ts`（`.srt` / `.ass` / `.vtt` 三例）：断言 cue 数 > 0、track 文档以 `WEBVTT` 开头、`{\an8}` 没有活下来。这条守的是 3.13.2 那个洞（侧车原样交出去 → 浏览器整篇拒绝 → 零字幕），**不是**上面那个竞态。

不是没想到，是量过了：本地生产构建带修复与不带修复各跑一遍，**三例全绿，两次一样**。localhost 解析侧车太快，artplayer 永远已经就位，这个竞态在本地不复现。判据只在生产延迟下成立。spec 头注释里写着这件事，免得后来人把绿灯当成有覆盖。

配套加了 `e2e/fixtures/probe4s.webm`（真 VP8 片段）。原来的 `black1s.mp4` 是 144 字节的空壳，`loadedmetadata` 永不触发，浏览器因此**推迟加载 `<track>`** —— 拿它当底，任何字幕断言都恒读 0，跟代码对不对无关。

---

## [3.14.0] - 2026-08-25

### 修复 — 库里看过的番，首页「正在追」终于跟着动

追番进度同步这条管线本身是好的：103 个单元测试、三个触发点齐全、服务端读写路径都验证过。问题是**它的入口条件在真实库上基本不成立**。

对一个 26 部番的真实库逐条比对本地 Dexie、站点订阅和站点已看集合：

```
❌ 未绑定（Series.anilistId 为空）      19   73%   ← 从来没进过推送流程
🔴 集号超界（本地 13,14 vs 站点 12 集）   1    4%
—  已绑定但本地无已看集数                5   19%
✅ 已同步                                1    4%
```

**73% 的卡片没有 `anilistId`。** `reconcileLibrary` 对未绑定的 series 直接 skip，所以它们从未被推送过，也从未产生过任何错误信号。

**不是匹配算法的问题。** 把那 19 个标题喂给生产环境的匹配器（直接 import `seasonMatch.ts`，不做重新实现）对真实搜索端点跑一遍：18 个命中，几乎全是 score=100 的折叠标题完全相等。唯一失败的那个，标题被解析成了字幕组名。**解析从来没被调用过而已。**

原因写在 `resolveSeriesBinding.ts` 自己的注释里：绑定只在点卡片、进单番页、进播放器这三个用户动作上发生，因为「几百个未绑定 series 会变成几百个并发搜索」。这个论证是对的，结论选错了 —— 不该是「不做」，该是**「有界地做」**。

**改动**

- **有界扫描**（`bindUnboundSeries.ts`）：进库时对未绑定的 series 顺序解析，每次挂载封顶 40 个、间隔 350ms、失败锁存。形状照抄已经跑了很久的 `episodeCountBackfill.ts` —— 候选集从行本身推导，绑上的自动退出，所以它自己的写入触发的 liveQuery 不会变成死循环。
- **扫描完 re-arm 对账**：`reconcileLibrary` 由 `libraryReconciledRef` 锁死整个挂载期。不显式重开一次，这次新绑的番一部都不会同步 —— 用户开库看到的是「什么都没发生」。
- **失败第一时间可见**：上报阈值从「第 3 次确定性失败」改成第 1 次。原来的阈值挂在一个刷新即清零的模块级 Map 上，要在同一个不间断会话里连撞 3 次才弹提示；正常的观看节奏（看一集、关页、隔天再看）每次只累积 1–2 次，**永远够不到**。重试上限仍是 3，两个阈值现在是分开的：`REPORT_AT_ATTEMPT` 决定何时告诉用户，`MAX_PUSH_ATTEMPTS` 决定何时停止重试。
- **自动绑定留痕**：每条写一行 `opsLog`（`kind: 'rematch'`），详情页的「最近操作」可见。**故意不给撤销按钮** —— `opsLog` 的 kind 白名单里虽然有 `rematch`，但唯一实现的撤销是 `undoMerge`，它对其它 kind 直接抛异常。给一个按下去会抛的按钮比不给更糟。

### 修复 — `Season.animeId` 装的一直不是 dandanplay 的 id

`/api/dandanplay/match` **三个 phase 一个都不返回 dandanplay animeId**（phase1 只有 titleNative + coverImageUrl，phase2 只有 anilistId 和标题，phase3 是字面 `{}`），尽管 phase1 手里就攥着正确的值。所以客户端那条 `merged.dandanAnimeId ?? merged.animeId ?? merged.bgmId ?? 0` 的前两项永远不满足，`?? merged.bgmId` 不是罕见兜底而是唯一会触发的分支。

实测那 26 个值打 `/api/dandanplay/episodes/{id}` **全部 404** —— 它们是 bgm.tv 的 subject id。而两个 id 空间会撞，且两种问法都返回 200：`806` 当 dandanplay id 是《以柔克刚》，当 bgm id 是《铁臂阿童木》。数值范围也分不开（dandanplay 跨 806–36740，bgm 跨 1269–638497）。

**只删兜底，让新写入留空**，服务端暂不补发字段。因为空值（falsy）会重新激活 `importPipeline.js` 的结构性归位守卫，而「另一个空间的有效值」恰好会关掉它 —— 配上 `matchCache` 的 7 天 TTL，周更番第 8 天重扫就会长出第二张卡片。存量数据的翻译和服务端补发是另一件事。

**连带修好一个耦合**：`applyEnrichment` 里，Season 记录（身份）和标题/封面（元数据）本来共用同一个真值判断。id 变空之后元数据会跟着一起被跳过，新导入的番就留着解析出来的原始名。两者现在分开了：没有可用 id 就不写 Season 行（写成 0 更糟 —— `dedupeSeries` 按 `typeof animeId !== 'number'` 判，0 会当成真 id 把所有无 id 的番折叠成一张卡），但富化照常应用。

### 变更 — 两个 id 空间现在混不了，混了编译不过

同一个 bug 在这个仓库出现过两次（手动 rematch 一次、自动导入一次），两次分别修，**没有共用代码** —— 那正是会漂回去的形状。现在抽成 `animeIds.ts`：`DandanAnimeId` / `AnilistId` / `BgmSubjectId` 三个品牌类型，每个一个归一化函数。

把两个历史 bug 原样种回去，编译器直接拦：

```
dandanClient.ts: Type 'DandanAnimeId | 0 | BgmSubjectId' is not assignable to type '0 | DandanAnimeId'.
      Type '"bgm"' is not assignable to type '"dandanplay"'.
rematchPayload.ts: Type 'DandanAnimeId | AnilistId' is not assignable to type 'DandanAnimeId'.
      Type '"anilist"' is not assignable to type '"dandanplay"'.
```

两个细节是承重的：**`match()` 里那个 `as DandanMatchResult` 断言被删掉了** —— 品牌数字之间的**断言不报错、赋值才报错**，那个 cast 一直在拆掉这道闸门；以及 **`merged.bgmId` 故意留着**并加了类型 —— 它没有任何读取方，但它正是当初污染 `Season.animeId` 的那个数，留着当绊线。

残留的洞照实说：软品牌会被 `Number(...)` 抹掉，所以洗一道再赋值仍然编译得过。那一半是靠行为测试守的（喂真实信封进去，断言外来 id 永远不落地），不是靠类型。

### 变更 — 导入时就绑定，以及两处补上的审计痕迹

- **导入时绑定**：`siteAnime.anilistId` 在两个产出 siteAnime 的 phase 里都有，`mergeAnimeFields` 早就取了，`pickEnrichment` 把它丢了。现在它作为**第三个事实**独立传递（不折进 enrichment —— 折进去会让绑定取决于「刚好有没有封面」），经 `writeBinding` 落地。**覆盖面照实说**：只覆盖「首次导入 + 缓存冷 + 有 siteAnime + 没落到已有 series」这一种，`reuse`、缓存命中、结构性归位、ambiguous 全都绕过它。
- **`rematchSeries` 补写 opsLog**：`'rematch'` 这个 kind 从 v4 起就在白名单里，**从来没有任何东西写过**。payload 记两个 id 空间各自的 from/to，`from` 在**事务内、写入之前**捕获（晚一拍读，每行都会变成 `from === to`）；未触碰的空间记 `null` 而不是 `{from: null}`，因为后者等于断言「那一季没有 id」，而真相是「我们没看」。
- **接住服务端回传的写后集合**：`PUT .../episodes` 一直返回完整的写后集合，客户端把 body 丢了 —— 字节已经过线，白扔。现在断言**包含**（服务端是 UNION 不是替换，用相等会天天误报）。不匹配只 `console.warn`：这是 200，走 `recordFailure` 要么完全没痕迹、要么烧掉重试预算并在第一次就弹「这部番没在同步」冤枉一个健康的 series。

### 明确不做的

- **集号重映射**。实测样本里 1/26。`episode_watches` 只增，唯一删除路径是逐集取消或退订级联 —— 服务端改写一旦写错就不可逆、不可检测、无运维工具清理；而它比对的总集数是富化任务写的活动目标，现有注释接受竞态的理由正是「最多多一个 400，客户端会重试过去」。加了改写之后那变成永久错写。改成让它在第一次失败时就可见，配现有的手动重新匹配。
- **服务端 → 本地的反向进度同步**。站点上标记的已看不回流到本地库。另一个特性。

## [3.13.2] - 2026-08-25

### 修复 — 字幕里露出 `{\an8}`

`{\an8}` 是 ASS 的对齐覆盖标签，本该让那一行挪到画面顶部、标签本身消失。它被当成文本直接渲染了出来。

**同一条规则，四个站点，只有两个执行了：**

| 站点 | 剥标签 | |
|---|---|---|
| `convertAssToVtt` | ✅ | 手动选 `.ass` |
| `convertSrtToVtt` | ❌ | 手动选 `.srt` |
| MKV 的 ASS 轨 → VTT 兜底 | ✅ | 自己抄了一份正则 |
| **MKV 的 SRT 轨 → VTT** | ❌ | ← 上报的这个 |

SRT 那条是用户会撞上的。**一个字段同时关掉了两条纠正路径**：worker 那条分支返回 `type: 'vtt'`，而 `VideoPlayer` 的 jassub 门是 `subtitleType === "ass"` —— libass 不挂载；artplayer 5.4 按 `t.type || V(t.url)` 分发，标成 `"vtt"` 就跳过它**自带的、会剥标签的** SRT 读取器。下游没人接得住。

规则现在只有一份，四个站点都调它。写的时候顺出两件事：

- **正则从 `\{[^}]*\}` 改成 `\{[^}\r\n]*\}`。** 现在是整篇文档应用而非逐条，而 ASS 覆盖块从不跨行。用宽松版的话，某条字幕里一个没闭合的 `{` 加几条之后一个落单的 `}` 会匹配掉中间所有东西、吃掉时间轴 —— 把「看得见的标签」变成「解析器整篇拒绝」，那更糟。
- **两个 MKV 构建器从 worker 搬进了 `subtitleConvert`。** worker 留 EBML 解析（要真 Matroska 字节才能测），搬走的是纯字符串处理 —— 那部分以前**只能靠播片验证**，这正是 SRT 轨带着标签上线好几个月而旁边 ASS 轨没事的原因。worker 净减 49 行。

### 修复 — 视频旁边的 `.srt` / `.ass` 一条字幕都不显示

查上一条时发现的，比它更严重。

`resolveSubtitle` 把侧车字幕当作**原始文件的 blob URL** 直接交出去。而 `VideoPlayer` 三处都写死 `type: "vtt"`，artplayer 收到 `"vtt"` 就原样透传给浏览器 —— 原始 SRT 没有 `WEBVTT` 头，TextTrack 解析器**整篇拒绝**。不是样式难看，是一条都没有。

而侧车匹配的优先级是 `{ ass: 0, ssa: 1, srt: 2, vtt: 3 }` —— **最优先被选中的格式，恰好是最保证渲染不出来的那个**。

现在把不变式写下来并守住：**离开 `resolveSubtitle` 的 url 永远是 WebVTT**。三处 `type: "vtt"` 因此从「碰巧对」变成「构造上就对」。

**侧车 `.ass` 拿到了它从来没有过的东西**：`content` 现在带着原文，jassub 会挂载，文件第一次真正按它自己的排版渲染，而不是退化成明文。这连带挪了本地字体授权提示 —— 它原本以 `.mkv` 为条件，那在「只有 MKV 会产出 ASS 原文」的时代等价于「jassub 会不会挂」。不动它的话，侧车 `.ass` 会渲染成豆腐块而屏幕上没有任何线索。

`.vtt` 侧车保持同步 —— 它是唯一无事可做的情况，改成异步只会平白插进一帧没有字幕。

顺带两处改名，因为名字已经不成立了：`kind: "mkv"` → `"async"`、`MkvExtractionTask` → `SubtitleTask`（一个侧车 `.srt` 挂在叫 `mkv` 的字段下，正是会把下一个人带偏的那种名字），以及 `mkvBlobUrlRef` / `cleanupMkvBlob` 和 `usePlaybackSession` 头部那两条同名的不变式。

### 测试

新增 34 条，`subtitleConvert` 和 `resolveSubtitle` 此前**零测试**。全部做过变异验证：把剥标签去掉挂 2 条、正则放宽回 `[^}]` 挂 1 条、侧车退回原样透传挂 6 条、ASS 不回传原文挂 4 条、blob 里装原文而非转换结果挂 2 条。

worker 现在 import 一个 `.ts` 模块，这在本仓库没有先例，所以跑了真 `bun run build` 而不是假设。

**还有一条值得记：** 用 `fetch(blobUrl)` 读回内容的写法**单跑绿、全套红** —— bun:test 同进程共享，另一个 spec 装了会计数的全局 `fetch` mock，报错是 `fetch called more times than mocked` 却指向一个和字幕毫无关系的文件。改成在 `URL.createObjectURL` 处直接抓 Blob，不碰任何别人拥有的全局。

---

## [3.13.1] - 2026-08-23

### 修复 — 库和播放器的封面全部消失

3.13.0 把 `FadeImage` 迁到 `next/image` 之后，`/library` 渲染出 26 张空卡片，控制台 115 个 400。播放器的手动搜索、弹幕选集、剧集列表同理。

**原因**：`next.config.ts` 的 `remotePatterns` 只放行 `s4.anilist.co`。而库和播放器的封面来自 dandanplay 的匹配结果 —— `img.dandanplay.net`、bgm 镜像、以及任何未来的富化路径写进 IndexedDB 的东西。这些主机走优化器一律 400，而 `FadeImage` 没有 `onError`，于是什么都不显示。

**修法**：这六处标 `unoptimized`，直接输出原始 `src`，回到改动前的行为。这也是更安全的默认 —— `SeriesCard` 自己的守卫注释就写着这些 IDB 值「may be attacker-influenced」，不该变成由我们服务器发出的请求。

**为什么三道闸门全绿**：`unoptimized` 是可选属性，`remotePatterns` 只在运行时求值。构建、类型检查、lint 都不可能知道某个 `src` 在运行时指向哪个主机。

**为什么上线前没测出来**：几何回归跑了 `/`、`/welcome`、`/anime/[id]`、`/seasonal/*`、`/search`，**唯独没有 `/library` 和 `/player`** —— 那两个页面的内容在浏览器本地，测试用的服务器上是空的。把「测不了」当成了「不用测」。而且那套检查比的是同一张图的几何，对照组里根本没有「图不出现」这种情况。

---

## [3.13.0] - 2026-08-23

### 变更 — 图片走优化器，首页图片体积降八成

AniList 对所有尺寸只给一张图：一张 460×650 的封面，常常是 PNG，常常 370 KB，被塞进 230px 宽的卡片里。全站 46 个图片渲染点里，此前有 45 个是裸 `<img>`，直连 `s4.anilist.co`。

现在它们走 Next 的优化器，按真实渲染宽度输出 AVIF。实测（同一台机器、同一份 Playwright 脚本、只等 load 不滚动）：

```
                    改前        改后
/          1440   11,295 KB   1,665 KB   −85%
/           375    2,829 KB     557 KB   −80%
/anime/*   1440    1,464 KB     195 KB   −87%
/anime/*    375    1,464 KB     119 KB   −92%
```

详情页那两行尤其值得说：**改之前手机和桌面传输的字节完全一样**（都是 1,464 KB），手机一直在下桌面尺寸的图。

#### 三件反直觉的事

**其一，`quality` 不是 AVIF 的 quality。** Next 传给编码器的是 `quality - 20`，所以默认的 75 落到 AVIF q55 —— 在扁平赛璐璐上色的素材上过了拐点，天空和皮肤开始起色带。要 q65 就得写 85。这个失败模式只表现为「图看着差了点」，永远不报错，所以靠每个调用点记住是不可能的；默认值放进了 `FadeImage` 本身。

**其二，选 AVIF 不是因为它压得小，是因为色度。** 等体积下 AVIF 的色带是 WebP 的 1/3.8、JPEG 的 1/9.4。根因是 sharp 的 AVIF 默认 4:4:4，而 WebP 和 JPEG 默认 4:2:0，把色度分辨率砍半 —— 正好糊掉动画赖以成立的彩色线条边缘。WebP 在朴素对比里的体积优势，是拿色度换来的。

**其三，`sizes` 会把 HTML 撑爆。** 给了它，Next 就改成输出完整的候选清单（15 档、每张约 2 KB 的 srcSet）。首页有 207 张封面，那是约 400 KB 的额外 HTML，在一个目标 180 KB 的页面上。所以网格卡片一律不给 `sizes`，只给 `width`，拿 1x/2x 两档；只有首页轮播和详情页横幅给了 —— 那里总共五张图，而渲染宽度真的从 1440 摆到 217。

实际代价：首页 HTML 未压缩 326 → 354 KB，传输 70 → 84 KB。这是这次唯一变差的指标，换来的是 9.5 MB 的图片。方案里 §4 那条「HTML ≤ 180 KB」的目标属于另一个阶段（精简 DTO / 减少首屏条目），本次没做，而且离得更远了。

#### 一个只在浏览器里才看得见的回归

`width`/`height` 这两个 HTML 属性**不只是元数据，它们是 presentational hint**，也就是真实的 CSS 声明。原先这些封面是没有尺寸属性的裸 `<img>`，高度由 CSS 的 `aspect-ratio` 算出；`next/image` 一加上 `height="240"`，这个 hint 就压过了 `aspect-ratio`，卡片从 155×206 长成 155×240，把底下的时间和评分行挤了出去。

构建过、类型检查过、也没有任何警告。修法是给那七个「只声明了 `width` 和 `aspect-ratio`」的样式对象补上 `height: "auto"`。

#### 类型上的一道闸门

`next/image` 自己的 `ImageProps` 把 `width`/`height` 声明成可选（因为还有 `fill` 模式），于是漏掉两者的调用点**能通过编译、到运行时才抛** —— 而且是在客户端组件里，表现为一整块空白。这个仓库跑的是 `strict: false`，别处也指望不上。`FadeImage` 的 props 因此收成了判别联合：要么给 `width`+`height`，要么给 `fill`+`sizes`。那 16 个漏掉尺寸的调用点是编译器列出来的，不是我翻出来的。

#### 头像不走优化器，这是刻意的

`FallbackImg` 的调用点全是头像，而头像由 go-api 从自己的卷里服务在 `/api/avatars/*`。Next 解析同源路径走的是 `fetchInternalImage` —— 一个在 next-app 进程内部处理的 mock request，而生产环境的 next-app 根本没有 `/api` 路由（`rewrites()` 在 prod 返回 `[]`，那是 nginx 的活）。优化头像会 404 进 `onError`，静默显示成默认卡片。所以它留在裸 `<img>` 上。

#### 本地静态资源

优化器管不到的那批，逐个量过再动：

```
og-default.png            203.3 KB →   62.1 KB   调色板，保留 alpha
card_default/card.jpg     123.3 KB →   41.7 KB   1287² → 704²
apple-touch-icon.png       50.2 KB →   21.0 KB   调色板 + 去 alpha
favicon-192.png            44.7 KB →   18.5 KB   调色板 + 去 alpha
card_default/backdrop.jpg 188.7 KB →  142.3 KB   仅重新编码，尺寸不变
card_default/avatar.jpg        新增 →    2.8 KB   96×96
```

**`og-default.png` 不能拍平成 JPEG。** 实测它 **15.9% 的像素是半透明的**（0% 全透明），拍平等于把这些像素合成到一个我猜的底色上。所以只做调色板量化，alpha 保留；改动后与原图逐像素比较，均值差 1.91/255，肉眼不可辨。两个图标则相反：实测 **100% 不透明**，alpha 通道纯属浪费，直接去掉。

**新增的 `avatar.jpg` 是为了一个优化器解决不了的问题。** 评论楼、在看的人、通知项里那些圆头像**实测全部渲染在 24×24**（详情页上八个），而它们一直在用会员通行证卡面那张图 —— 那张图的尺寸是按卡面 252×352 在 DPR2 下的需求定的。等于用 41.7 KB 去画 576 个像素，在 Google 抓取的那个页面上。它没法走 `next/image`，理由同上（头像的真实 src 是 `/api/avatars/*`）。96×96 覆盖 24×24 到 DPR4，2.8 KB。

#### ⚠️ 上线前置

`/_next/image` 的响应带 `Vary: Accept`（Next 无条件发，改不掉），而 Cloudflare 对带 Vary 的响应默认**不缓存、报 BYPASS**。所以边缘缓存不是加一条路径规则就行，需要 Cache Rules 的 `vary` 设置把 `accept` 按 avif/webp 归一化。详见 `docs/` §12.6。没有这条，每一个尺寸变体的每一次请求都回源。

---

## [3.12.1] - 2026-08-23

### 修复 — 站点一直在给自己的 LCP 降优先级

三处改动，全部只碰加载优先级，不碰任何一个像素。

**详情页的横幅不是图片，是一条 CSS 背景。** `background: url(...) center/cover` 的地址只存在于样式声明里，而浏览器的预加载扫描器只解析原始 HTML 字节流里的标签属性 —— 它在 CSSOM 建好之前根本发现不了这个地址。于是全站最重要的那个页面（`/anime/*` 是整个 SEO 面）的 LCP 元素，198 KB，没有任何形式的预加载；而该页**确实**发出去的那条图片 preload，指向的是它下面那张 210×300 的封面。

换成真 `<img loading="eager" fetchPriority="high">` 之后两半一起修好：扫描器第一遍就看见它，而且 React 19 会自动为它抬出一条 `<link rel="preload" as="image">`。顺带查清了一件之前没人写下来的事 —— 那些 preload 全站零处显式调用，**它们本来就是 React 从 `fetchPriority` / 非 lazy 的 `<img>` 反推出来的**。

**`FadeImage` 给每一张非 priority 的图显式设了 `fetchPriority="low"`。** `low` 不是「排在优先的后面」，它是一道硬降级，图片进了视口也照样生效。而在网格页上（`/seasonal`、`/library`）LCP 元素**正是第一张卡片**，那里没有任何一张图标了 priority —— 等于站点在主动给自己的 LCP 降优先级。想要的那部分（把屏外封面让开）`loading="lazy"` 已经做到了，所以这里改成不设，让浏览器自己去提拔它在视口里发现的东西。详情页实测 33 张图带 `low`，现在 0 张。

**`FallbackImg` 没有默认的 `loading`，于是全是 eager。** 它的调用点清一色是 24–48px 的头像（评论楼、在看的人、通知项），大多在首屏以下。而 eager 的代价不止是早拉：React 19 会把非 lazy 的 `<img>` 变成一条 hoisted preload —— 所以 `/anime/[id]` 一直在预加载 `/card_default/card.jpg`，一个 1287×1288 / 126 KB、渲染尺寸 24×24 的文件，挂在 Google 抓取的那个页面的关键路径上。改成默认 lazy，那条 preload 消失了。已经在视口里的头像不受影响，lazy 对它们不是延后。

**关于「不碰像素」的验证**：横幅那处在 768 和 1440 下与改动前**逐字节相同**（两张 PNG 的 sha256 一致）。奇数视口宽下有一处均值 4/255、肉眼不可见的差异，成因已定位并证伪过其它解释：源图 1900 宽是偶数，居中裁切起点 `(1900−W)/2` 在奇数宽下落在半像素上，背景绘制路径和 `<img>` 绘制路径对这半个像素的舍入不同。实测 375 差、376 相同、377 差、378 相同，与预测完全吻合。

---

## [3.12.0] - 2026-08-22

### 新增 — 本地播放器不再需要账号

`/player` 一直挂在登录墙后面。而它恰恰是全站唯一**什么都不需要我们提供**的功能：文件在你自己的硬盘上，匹配和弹幕在你自己的浏览器里跑，服务端一个字节都不经手。用注册来换一个不消耗我们任何资源的功能，这笔交易讲不通。

裸 `/player` 现在公开。带 `?seriesId=` 的库交接仍然要登录 —— 在 `proxy.ts` 里挡一层，`PlayerShell` 里另有八处守卫把每一次库读取都锁在同一个参数后面。带语言前缀的形式（`/zh-Hant/player?seriesId=`）也一起挡了，这是这个仓库过去差点漏掉过的那一类。

顺带的结构变化：`/player` 从动态渲染变成了 SSG 预渲染（三个语言各一份）。这是取消登录墙的自然结果，不是额外做的优化。

### 变更 — 首页英雄区从「两个区块」变成「一个构图」

首页顶部新增本季精选轮播。但更值得记的是评审阶段发现的一件事：**割裂感的成因是亮度，不是接缝。**

页面近黑，横幅是制作方画的 —— BLEACH 那张是一片白色爆炸。而 `.art` 上挂着 `brightness(1.03)`，等于把一块白砖打在黑纸上。第一次修补是给图底加渐隐遮罩，结果更糟：白色淡向透明、背后是黑，中间必然经过灰，一块白砖变成一团灰雾。

真正的修法是两条：把图压进页面自身的亮度区间，再用 scrim 刷到 `#07090c` —— 图的底部和文案的顶部于是是**同一个像素值**，没有可看见的交界；以及让文案压在图的下三分之一上，而不是从图结束的地方开始。`≥1600` 那段原来的规则字面就是 `top: var(--desktop-media-height)`，用代码保证了两个区块永不重叠。

同时把 `align-content: center` 换成 `start`。居中意味着文案在剩余黑区里浮动，重叠量随视口漂移 —— 1840 下重叠得很好，768 下标题离图还有 35px。锚到 `--desktop-copy-top` 之后，重叠量在每个桌面宽度都恒等于 `--hero-copy-lift`。

**轮播里的文字现在和页面其它文字对齐。** `.copy` / `.rail` 用的是 `clamp(24px, 5vw, 76px)`，而英雄区以下的一切都活在 `.container`（max-width 1400、24px padding、居中）里 —— 1920 下标题比页面上任何一个标题都靠左 208px。两者现在通过 `--hero-gutter` 复现同一套算术。背景图按设计保持满幅。

**十二个争抢注意力的元素减到九个，且零信息损失。** `03 / 05` 计数器和竖排侧标各自重复了导轨和 eyebrow 在几个像素外、且沿阅读方向已经说过的话；评分和格式码回答的是「决定要不要在意这部番」这一刻没人问的问题，详情页一点就有。

**主按钮不再取海报色。** `--hero-accent` 每次轮播都变，等于让访客见到的第一个按钮每几秒改写一次全站唯一被学会的信号 —— 蓝色是可以按的 —— 而且没有对比度保证：海报色是画面里恰好占主导的那个颜色，其中一些根本撑不起可读的文字。海报色仍然驱动 eyebrow 的线、进度条和 hover 底色。

**680px 以下隐藏游客 CTA。** `/player` 需要 File System Access API，没有任何移动端浏览器提供。手机访客顺着那个按钮点进去，看到的是一句「请用电脑」，没有原因也没有退路 —— 比从没见过那个按钮更糟，因为他们花了一次点击才知道网站对自己是坏的。

### 变更 — 社区区块的库引导从卡片变成通栏

它是 1352×336 横跨两列，压在一张 140px 的讨论卡上面 —— 于是「正在热议」这个区块里最大的物件是一篇下载教程。现在是 1352×195 的通栏：这一页上唯一既不是卡片也不是海报的形状，所以不读字就知道「这是说明」。

内部**四层嵌套方框归零，五个色系收成一个**。三个步骤原本是三个带边框的盒子，说的是「三件事」；现在是一条带连接线的序列，说的是「然后，然后」。Chrome 那条要求原本有独立面板、绿色强调、44px logo 和一行全大写标签来传达一句话的注意事项 —— 修正时又矫枉过正成 10.5px 灰字配一个在那个尺寸下只是色斑的四色标记。现在是白字的浏览器名，放在它所约束的那个按钮旁边。

一条 `LOCAL // DANMAKU` 水印画在卡片上、又被插画面板盖住，于是从中间被切断。连同面板一起删除。

**文案主语从目录机制换成「我的库 + 播放」**，三个语言同步。评审时撤回了一处措辞：授权步骤原本写「授权一次」，但 `useFileHandles.reauthorize()` 会调 `handle.requestPermission()` —— 这条恢复路径存在，恰恰因为 Chrome 会在浏览器重启后掉授权。代码兑现不了的承诺不写。

### 修复 — 三个「状态存在但看不见」

**磁力搜索的标题切换按钮根本没有选中态。** 每一个都渲染同样的描边，看起来像选中的那抹蓝其实只是 hover。于是这一行按钮无法回答它存在的唯一目的：当前这批结果是按哪个标题搜出来的。现在按 `manualQ` 判定（不是搜索框文本 —— 手动输入应当让所有按钮取消选中；打开时为 null，正确地表示「服务端正在同时展开全部变体」），用下方剧集按钮已经在用的同一套实心蓝，并带 `aria-pressed`。

它上面那条提示原本是目录建议加一个跳去库的链接。但打开这个弹窗的人正在找资源，而最可能挡在他和资源之间的，是发布组对这部番的写法和我们不一样。那排按钮本来就解决这件事，只是从没说过。提示现在指向它们，并和按钮共用 `titleOptions.length > 1` 这个条件 —— 只有一个标题时，「点下面的标题」比沉默更糟。

**`/player` 空闲态的两个盒子宽度不一致。** 试用横幅 598px、拖放区 459px，各自缩到自己的内容宽度 —— 因为 **flex 项目在交叉轴上带 auto margin 就不会 stretch**，会退回 fit-content。两个居中但不等宽的盒子，读起来像一个什么都没决定的版面。现在共用一个 720px 列，两条边齐平；从库进入的那个回退状态也用同一个居中舞台，不再把拖放区顶在窗口最上面。

### 此前已合并、未单独发版的

- **过期标签页提示**（#93）：`next.config.ts` 把构建 id 在**构建时**内联进客户端和服务端两份产物，`/version.json` 由当前运行的实例回答。两者不同即说明这个标签页比部署旧。Sentry 上 `/anime/:id` 那个 `Cannot read properties of undefined (reading 'call')` 就是这种情况的表现 —— 客户端导航去要一个当前部署已经不存在的 chunk。
- **繁体列漂移的可见化**（#96）：river 富化 worker 持续填简体列，繁体列只有人工跑 `hantbackfill --apply` 才动。于是繁体树会一格一格地退回简体，且永不自愈；症状是隐形的 —— `/zh-Hant/anime/:id` 的读者拿到的是**简体**简介，因为渲染阶梯是回退而不是留空。管理面板现在把这个漂移量显示出来并设了下限。
- **集数显示**（#97）：`episodes` 为 NULL 时详情页整块网格消失，而 NULL 的意思是「我们还不知道」，不是「这部番没有集数」。现在有数就显示数，没有就明说，并补了一个走 Bangumi 的回填 worker（带身份闸门）。同时删掉了死掉的 `EpisodesSection`。

---

## [3.11.0] - 2026-08-16

### 新增 — 本地库和追番进度之间，此前没有传动

库里的进度 UI 一直是全套写好的：卡片进度条、完成度筛选、继续观看行、逐集勾选、观看节奏条。五处读侧，渲染出来全是空的。原因不在任何一处 UI，而在它们共同读的那张表**没有生产者**——`progressRepo.put` 有完整定义和零个调用点，全仓库搜 `completed: true` 一个命中都没有，唯一写过 `progress` 的是一次性的历史数据迁移，而且硬编码 `completed: false`。

服务端那一侧同样是半条链：`subscriptions.current_episode` 模型完整、动态事件的写入路径带行锁和去重，但整个前端只有详情页那两个 ± 按钮在调它。播放器和库从不触碰订阅接口。

两端都建好了，中间是断的。这一版补的是中间那段。

**播放器成为进度的生产者，且按来源分流。** 从库里进入播放的剧集手上有 `episodeId`，写 Dexie，参与同步；直接拖文件进播放器的没有 `episodeId`，只写 localStorage 记住播放位置，不产生完成状态、不同步。这条线不是实现细节妥协，它就是产品定义——**没导入库就不追踪**。

位置写入节流定在 30 秒而不是沿用 localStorage 那条路径的 5 秒，`pause` / `visibilitychange` / `beforeunload` 三个点强制落盘。理由是双标签页：`progress` 表上挂着两个 `liveQuery` 全表订阅者，一个 `toArray()`、一个 `orderBy().reverse().toArray()` 外加两次 `bulkGet`。日常单标签播放时库页面是卸载的，这两个订阅者不存在；但一旦一个标签开着库、另一个在播，播放器每写一次位置，库那边就全表扫两遍。三个强制落盘点让断点精度不降反升——人离开时必定落盘，比固定 5 秒还准。

这两个 `liveQuery` 也是为什么播放器写完之后库页面会自己更新：进度条、筛选、继续观看行全部自动生效，没有一行刷新接线。

**不回退守卫放在服务端，不放客户端。** 客户端能比较的只有自己那份缓存副本，多设备下它本身就可能是旧的。PATCH 新增 `monotonic` 参数：为真时 `current_episode` 走 `GREATEST`，只能前进。这不是理论风险——Mihon 有过完全对应的事故（A 设备读到第 21 话，B 设备打开第 16 话，追踪器被压回 16）。详情页那两个 ± 按钮传假值，**人工必须能调低**。

守卫要连时间戳一起守，否则只挡住一半。陈旧标签页推一个更小的集号时 `GREATEST` 让集数不动，但如果 `last_watched_at` 和 `updated_at` 照常 `now()`，这次无效推送仍然会把这部番顶到首页「继续观看」的第一位。所以单调模式下集号没有真正前进（且没同时携带状态或评分）时，两个时间戳都保持原值——一次空推送是**彻底的**空操作。这里有个容易搞混的耦合值得写下来：列表排序读的是 `updated_at`，不是 `last_watched_at`。

**集数上界由服务端校验，超界拒绝而不截断。** 权威的总集数在 `anime_cache` 手里，客户端那个字段是可选的、经常不准。超出总集数的推送是「客户端绑错番了」的证据，截断成最大值等于把这个错误存成看起来合理的进度。连载中（总集数未知）的不校验。

**只有正篇参与同步。** 集号解析器不管文件是什么类型都会从文件名里抠出一个数字，所以 `[NCOP01]` 会变成「第 1 集」。不拦的话，看一遍无字幕 OP 就会把高水位推成 1；再配上服务端的 `GREATEST`，这个错误**再也退不回去**。特殊篇更狠，一个解析成 12 的 SP 能把只看到第 5 集的番推到 12。本地照常打勾，只是不进同步。

**对账靠差值，没有队列。** 每部番记一个「已经告诉过服务端的集数」，本地高水位超过它才推，推成功才回写。状态即真相，所以重放任意次都安全，不需要队列、不需要重试计数器、不需要担心补推顺序。断网时本地照常打勾，联网后任意一个触发点都会把差值补上。

**自动标记的阈值移植自 Animeko**：`min(时长 × 0.9, 时长 − 100 秒)`。长片按百分比，短片按「离结尾 100 秒」——短片的片尾曲占比高得多，24 分钟的番在 90% 处触发，3 分钟的短片在离结尾 100 秒处触发。四道守卫和用户开关一并移植，开关默认开。

有一个取舍是**睁着眼睛接受**的：判定只看播放位置，所以把进度条拖到片尾就会被标记成看过。本地库的使用姿势本来就是拖着确认文件对不对，所以这**会**真实发生。测试里专门用「这是有意为之」的措辞固定住了这个预期，免得半年后有人当 bug 去修。

**本地剧集和 AniList 之间，此前没有持久化的关联键。** 本地记录里唯一的外部 id 是 bangumi 和 dandanplay 的，跟 AniList 不是一个 id 空间。映射是每次会话拿标题现搜出来的，只缓存在内存里，关掉页面就没了。这一版把它落到本地库里并建索引，索引白送一个反向查询：从 AniList id 找回本地剧集。

自动匹配和人工重匹配收进同一个写入口，**人工绑定优先，自动匹配物理上不可能覆盖它**。这一条是 Taiga 十年前就验证过的形状:用户的选择要排在官方标题前面。

解析发生在两个「用户表达追踪意图」的时刻——点击卡片、进入播放器。刻意**不**在库挂载时批量解析：一个成熟的库可能有几百部未绑定，那会变成一次几百个搜索请求的突发。

**点击即追踪是幂等创建，状态只能人工改。** 原有的订阅创建走的是「冲突时更新状态」，用在点击即追踪上会把用户手动标成「已弃番」「已看完」的番静默复活成「在看」。新增一条冲突时什么都不做的路径，既有订阅原样返回。

**自动标记开关**落在设置页新的「播放」区。写入失败（无痕模式、存储写满）会明确告诉用户没保存成功，并且**把开关留在原位**——写失败意味着所有读者仍然看到旧值，显示用户请求的状态是撒谎。

**动态事件表加了 90 天保留策略。** 此前它没有任何保留策略，这在只有手动社交动作写入时活得下去；一口气刷完一季就是 24 条事件，写入速率会提高几个数量级。修剪分批执行，撞上单夜上限时写日志而不是悄悄停手。同时给指向该表的那个外键补了索引——它带「删除时置空」语义，没有索引的话每删一条父行就要全表扫一次通知表，分批根本救不了。

### 修复 — 手动重匹配一直在把 AniList id 写进 dandanplay 的 id 空间

搜索接口返回两种行：来自本站缓存的带 AniList id，来自 dandanplay 的带 dandanplay id。这两种形状**完全不相交**——各自都没有对方那个字段。而重匹配对话框写的是 `dandanAnimeId ?? anilistId`，所以**每一次从缓存那一区（更丰富、排在前面的那一区）选择**，都会落到第二项，把一个 AniList id 写进只该放 dandanplay id 的位置。

后果不是显示错误，是身份错乱：导入流程靠 dandanplay id 复用季，被污染的那一行**再也匹配不上**，下次导入会多出一张重复卡片；弹幕则指向恰好共用这个数字的另一部番。

对话框的载荷类型现在把两个 id 空间拆成两个字段，各自独立解析，两个都没有才拒绝。已经写进本地库的错误数据无法可靠地自动识别（受影响的记录当初根本没存过 AniList id，没有可比对的基准），用户重新做一次手动重匹配即可自愈。

### 修复 — 多标签页会让本地库升级卡死成「一直转圈」

本地库的连接在模块加载时就建立，所以任何一个库页面或播放器页面一打开就持有它。新版本的标签发起数据库升级时，旧版本标签持有的连接会让升级进入阻塞状态，而阻塞**不是错误**——它就是无限等待。用户看到的是库一直转圈，像是数据丢了。

两条一起上：旧标签收到版本变更通知时主动让出连接（升级完成后自己重新打开），新标签在阻塞超过宽限期后把等待变成一条明确的提示。只做前者不够（那已经是底层库的默认行为），只做后者也不够——底层库的自动打开路径会吞掉拒绝，调用方照样挂住，所以提示必须走独立的订阅通道，不能指望被拒绝的打开操作。

---

## [3.10.0] - 2026-08-15

### 新增 — 让观看长成内容，让内容把人带回来

站点此前已经有追番、评论、关注、动态和公开主页。缺的不是功能，是这些功能之间的传动：用户做了行为，行为没有长成内容；产生了内容，内容没有触发关系；形成了关系，却没有通知把人带回来。这一版补的是传动本身。

**活动事件流。** 订阅表只保存最新进度，状态覆盖了历史，撑不起观看日记和内容级 Feed。新增 append-only 的 `activity_events`，把「看到第几集」「发了评论」「关注了谁」记成事件而非状态。事件写入和业务写入在同一条语句里完成：更新进度时先 `FOR UPDATE` 锁住旧值，只有集数**确实变了**才追加事件——重试和纯状态更新因此不会造出重复的 Feed 条目。

**通知。** 回复、获赞和新关注终于有了稳定的回访入口。去重靠 `(user_id, dedupe_key)` 唯一键，重复投递返回既有那条而**不重置已读状态**：取消点赞再点回来，不会把一条已读通知变回未读。

**分集讨论前置。** 讨论此前要先进详情页、再点开某一集才看得见，有内容也难以被感知。现在卡片上有预览，分集网格里可直接展开，评论支持快捷反应。

**安全治理。** 剧透标记、拉黑、举报与后台审核队列一并落地。拉黑是一次原子操作——建立记录的同时双向切断关注边、通知与反应。策略是对称的：任一方的拉黑都关闭双向互动，即使只有发起方持有那行记录。举报保留证据快照，所以被举报内容删除后审核仍然成立；同一目标重复举报返回既有的待处理记录，不堆积。

**首页发现。** 新增「正在热议」。排序以参与人数为主、评论量次之、反应更次，再除以一个平滑的时间衰减因子，让一场新鲜的小讨论能盖过一条陈旧的长贴——不设硬性日期边界，排序不会在某个时刻跳变。**标记为剧透的内容在服务端就被置空**：不是前端打码，载荷里根本没有那段文字。

**埋点只落聚合计数**：不记 IP、不记 UA、不记匿名标识、不记评论正文，不留任何单用户事件行。

### 修复 — 社区安全控制曾经是「可选的」

上述功能的每一个新查询，最初都是通过一个可选接口加类型断言拿到的。五处断言失败会报错，另外七处没有 else 分支，于是断言失败时**静默跳过**：被拉黑的人可以关注、可以看到完整主页、可以回复、可以点赞，而三类活动事件会悄悄不写入。

生产从未出错——`*dbgen.Queries` 实现了全部十二个方法，每次断言都成功。但没有任何东西**保证**这一点，而且这个形状还藏着第二个问题：social 的测试替身没有实现那两个安全方法，所以该包自己的测试跑的正是「跳过」分支。在 social、comments、notifications 三个包的测试文件里搜 `block`，一个结果都没有。safety 包把「建立拉黑」测得很足；没有任何东西测「遵守拉黑」。

把这些方法收进 `SocialDB` / `CommentsDB` / `SubscriptionsDB` 之后，一个无法执行拉黑的依赖会变成编译错误，而不是一次静默的策略变更。

另外两处：`UpdateSubscription` 曾用 `any` 承载两个分支的不同行类型——两者今天字段与 JSON tag 完全一致，所以响应从未真的不同，但只要有人给 CTE 的 `RETURNING` 加一列，API 形状就会随分支而变，且没有任何东西会拦下来。分页曾写作 `int32((page-1)*limit)`，该表达式会截断：`?page=42949674&limit=50` 落到 `OFFSET -2147483646`，Postgres 直接拒绝。新的 `pageOffset` 用除法判界，那个会溢出的乘积从不被计算。

十三个新测试覆盖「遵守拉黑」——主页遮蔽、关注、回复、反应，以及通知收件箱，包括**未读角标必须与点开后的列表一致**：角标显示 3、点开却是空的，才是用户真正会来反馈的那个版本。每个测试都做过变异验证：把守卫关掉，它们会红。

### 性能

`community-welcome.jpg` 按渲染尺寸重新导出：1081×1600 → 252×373，386 KB → 31 KB。它落在首页的第一个内容块里，而 `FadeImage` 是原生 `<img>`、不经 Next 图片优化，源文件是原样发给浏览器的——显示尺寸只有 84×118。

---

## [3.9.1] - 2026-08-09

### 修复 — 合并后的卡片只显示了一半集数

`SeriesDetailSheet` 用 `.where("seriesId").equals(series.id)` 取集数。而 `performMerge` 是**软合并**——它从不移动 episode 行，只把 source id 追加到 target 的 `mergedFrom`。于是合并出来的卡片读的是目标自己的行，被合并进来的那一半**从未被查询**。合并一次就能触发。

`progress` 那一行是同样的形状，问题更隐蔽：就算集数出现了，观看进度也拿不回来——看起来像观看记录被删了，而不是查询写错了。

隔壁的 `useSeriesDetail` 一直正确处理了 `mergedFrom`，但没有任何东西通过它渲染。两套并行实现，屏幕上跑的恰好是忘了这件事的那套。

两条路径现在统一走 `resolveMergedSeriesIds`，且是**传递闭包**而非一跳：A 合进 B、再把 B 合进 C 之后，`C.mergedFrom = [B]` 而 `B.mergedFrom = [A]`，单层读取会丢掉 A 名下的一切——而 `useLibrary` 把出现在任意 `mergedFrom` 里的 id 都从网格过滤掉。**文件在磁盘上、已索引、界面上哪里都进不去。** 带环保护：`userOverride` 是用户可写、跨版本存活的状态，这里死循环会让详情面板挂起且控制台什么都没有。

**关于测试**：第一版 15 个测试全在 `resolveMergedSeriesIds` 这个纯函数上，而它从来没坏过——测试护着一个没病的东西，真正的病灶零覆盖。查询逻辑因此被抽到 `loadMergedSeriesRows`，其假 Dexie **记录调用方式**且 `equals` 直接抛异常：只返回正确数据的桩挡不住这个回归，因为坏版本用 `.equals` 照样能从桩里拿到东西。24 个测试覆盖两个模块，含 progress 那一半、链式合并、从 source 反向上溯、以及没有 `userOverride` 表的老库。

---

## [3.9.0] - 2026-08-09

### 新增 — 把「订阅」从终点改回起点

生产数据把问题指得很死：802 个注册用户里 **659 人（82%）再没回来过**，7 日活跃 31 人。而 255 个订阅者里 **234 人只订阅了 1 部**，且 **245 人的全部订阅发生在注册后 10 分钟内**——之后还再订过的不到 20 人。

这不是偏好分布，是被结构夹死的分布。六路调查收敛到从 Google 落地到形成习惯之间的三处物理断点：

**断点 ①：`from` 在注册那一跳被丢弃。** 全站只有两个 `/register` 入口（`LoginForm`、`Navbar`），都写死 `href="/register"`，而 `sanitizeFromParam(undefined)` 兜底 `"/"`——**100% 的注册用户落在首页**，不在他刚才想追的那部番上。新增 `authHrefWithFrom` 统一生成，顺带修好三处同类断链，其中 `settings/page.tsx` 用的是 `?next=`，而 `/login` 只读 `from`，该参数一直被完全忽略。

**断点 ②：全站只有一个地方能订阅。** `SubscriptionButton` 的唯一渲染点是详情页的 `DetailActions`；`AnimeCard`（首页热追 / 季度 / 搜索共用）零行订阅代码。加第二部番要走五步。现在卡片右下角有快速追番，页面级**一次** `GET /api/subscriptions` 供整个网格使用（不是每卡一次——go-api 有 per-IP 限流）。

**断点 ③：注册让首页变得更空。** `ContinueWatching` 在零订阅时 `return null`，整块连标题一起消失——用户交出邮箱密码，换来一个比匿名时信息更少的页面。现在有第三态，且订阅成功后会就地换掉（走 `subscriptionBus`，不跑 `router.refresh()`）。

**卡片角标的一条铁律：永远不删数据。** `+` 写入，`✓` 只导航到详情页的完整面板。审查发现原设计里 `✓` 绑 DELETE，而集合来自无 status 过滤的接口——一部 2025 年看完打了 10 分的番会在 2026 年的搜索结果里显示 ✓，手机误触就是整行删除（评分、进度一起没）。`POST` 是 `ON CONFLICT DO UPDATE SET status`（非破坏），`DELETE` 才是抹行的那个，所以网格拿安全动词，详情页拿另一个。误点的补救是成功 toast 里的「撤销」——那一刻刚创建，是唯一安全的删除时机。

其余：导航栏加「我的追番」（「我的库」降级进头像菜单，链接数不变）；订阅成功 toast 从一个 `✓` 字符换成有内容的文案 + 首次带列表入口；登出时广播信号，网格角标与首页追番网格一起清空（共享设备上登出后仍列着上一位用户的追番清单）。

### 新增 — 注册要求二次确认密码，并可切换明文

这个站的密码找回路径大概率是坏的：**79.6% 的注册邮箱是 @qq.com**，而发信从 gmail.com 直发，送达率过不了一半且裸 SMTP 看不到退信。注册时打错密码 ≈ 账号永久锁死，所以二次确认在这里不是仪式，是唯一兜底。校验先长度后匹配（两个都错时先报"太短"，否则用户改完确认框再提交才发现密码根本不够长），不 trim 空格（粘贴带来的尾随空格是真字符），确认框是正确前缀时保持沉默。明文切换按钮**故意可聚焦**——`tabIndex={-1}` 会让键盘用户彻底用不了它，违反 WCAG 2.1.1。

### 修复 — 隐私政策不再公开会话机制

第二节原文列出 `session` / `refreshToken` / `auth_hint` 三个确切 cookie 名并标注哪个是 httpOnly、哪个前端可读。没有任何隐私法要求这种粒度，等于公开一份会话机制说明书。改为只讲用途、承诺不做跨站追踪、告知清除方式，并补上标题承诺却从未提及的本地存储。第四节改用「接收方类别」写法（GDPR 第 13(1)(e) 条明确允许），去掉品牌名与技术描述。三个法律页的联系邮箱统一为 `animegoanime@animegoclub.com`。

### 修复 — 导航栏在匿名访客眼里有个洞

「我的追番」为消除布局抖动在匿名态也占位（`visibility: hidden`），结果是「搜索」与「关于」之间一个常驻空洞。方向错了：匿名访客是绝大多数流量，他们 100% 时间看到缺陷，换来的只是少数登录用户少一次挂载位移。现在只在 `probing` 窗口占位——而 probing 仅在 `auth_hint` 存在时进入，也就是真链接已在路上。

### 变更 — 卡片与头像的交互细节

卡片 `+` 改为指针设备上 hover 淡入（`@media (hover: none), (pointer: coarse)` 与 `:focus-within` 双重兜底，触摸和键盘都不失效）；`✓` 静止缩至 `scale(0.72)`、hover 展开——`+` 是行动召唤该完整呈现，`✓` 是状态信息，二十个满屏蓝点没有意义。全部走 transform/opacity，且**不引入 `backdrop-filter`**（A/B 实测 p95 帧时间 41.6ms vs 19.9ms）。导航栏头像 36→44px，hover 放大 + 蓝环，探测期骨架同步尺寸以免头像出现时右侧整组跳动。

---

## [3.8.2] - 2026-08-08

### 修复 — 机翻在替我们思考：92.6% 的输出 token 花在了思维链上

3.8.0 上线后队列里持续出现 `deepseek: response carried no content`，累计 **2,911 次**、约 **596 万输出 token 白烧**（≈$1.73），并有几十个 job 长期挂在 retryable。三条独立调查线（统计对照 / 协议实证 / 代码推演）收敛到同一个根因，且**推翻了两个看起来很合理的假设**。

**根因**：`deepseek-v4-flash` 和 `-pro` 都是**推理模型**，且都默认开启思维链，而 `max_tokens` 是**推理 + 正文的总预算**，思维链先生成、先计费。原来的 2048 是按"最长简介约 600 token、中文比英文短"算的——这个推算对正文完全正确，**对思维链一无所知**。模型在思维链里把预算烧光，`finish_reason: "length"`、`content: ""`、HTTP 200。客户端只解析 `content`，于是把"预算耗尽"误报成"上游畸形响应"。

**被证伪的两个假设**：① **不是内容审核**——露骨内容实测被完整翻译，`content_filter` 一次未出现，`refusal` 字段根本不存在；控制原文长度后 Hentai 分类的失败率反而低于非 Hentai。② **不是"任务卡死"**——曾失败的 1,033 行里 924 行（89.4%）已靠重试自愈，永久失败为 **0**；看到的"卡住"是一张移动中的退避快照。真正的驱动因素只有**输入歧义度**（冷僻专名越难定译名，模型越纠结），表现为失败率随原文长度从 3.4% 单调爬到 95.7%。

**真正的修复是关掉思维链，不是抬预算。** 抬到 8192 只是把墙往后挪：同一条输入的思维链实测在 3,061 和 8,192 之间无界抖动，`reasoning_effort: "low"` 照样吃满 8192 返回空。官方参数 `thinking: {type: "disabled"}` 才是解法，同一条输入实测 **3,220 → 155 输出 token（降 20 倍）、25.4s → 2.8s（快 9 倍）**，此前反复失败的行第一次就翻过。全量成本从 ≈$4 降到 **≈$0.33**；`max_tokens = 8192` 保留为安全网而非修复，注释里写死了这个区别。

**同时修掉误报本身**：`client.go` 里 `len(choices)==0`（真·畸形上游，该重试）和 `content == ""`（已决结局，该盖章）原本共用一条错误分支——这正是"两眼一抹黑"的来源。现在拆开：空正文返回 `("", nil)` 落进 worker 已有的 `validateTranslation` 闸门 → 盖章 → 面板上从"永远待处理"迁到"已判定失败"。**worker 生产代码一行未改**，这是取消一个特例而不是新增特例。同时把实测存在的字段全部解析进日志：`finish_reason`、`reasoning_tokens`、完整 usage、前缀缓存命中数、`X-Ds-Trace-Id`（找 DeepSeek 支持的唯一凭据）——有这些字段，这次事故 5 分钟就能定位，而不是花掉三个调查 agent。

**来源标记**：关掉思维链后模型不再"顺手"删掉目录元数据，而输入侧的剥离正则锚定在 `$`，漏掉了 240 行 —— AniList 在署名后又追加了 `Note:` 段落，把标记挤出了锚点范围。正则改为不锚定，同时**要求 `source` 后必须有冒号**，否则 `(source of his power)` 这类正常句子会被静默删除（比原 bug 更隐蔽）。

**顺带修掉一个静默 20 天的隐患**：`401/402/403`（key 失效、余额不足、权限撤销）是确定性错误，却和 429 走同一条 25 次 `attempt⁴` 退避阶梯 ≈ 20 天——欠费会让整个 sweep **安静地停摆而不是响亮地失败**。新增 `ErrUpstream.Permanent()`，worker 侧**只抬日志级别到 ERROR，行为一字不动**：不盖章（账户的事实不是行的事实，盖章等于给上万行从没试过的行记上"试过了没结果"）、不 return nil（那会拆掉退避，变成每小时 600 次对着死 key 的计费失败）。匹配用本地接口 + `errors.As`，queue 包仍然不 import deepseek。真需要止血时正确动作是暂停 `description_llm` 队列——scan job 就挂在同一队列上，暂停即同时切断投喂与消费，这个设计当初已经预留。

顺手修正了若干注释腐烂：成本估算（旧值低估约 4.7 倍且没写明不确定性）、引用了一个**根本不存在的常量** `descriptionLlmMaxWorkers`（真实并发在 `main.go`）、以及推理模型让 60s 客户端超时从"保险"变成"活约束"的新张力。`validateTranslation` 的空值检查标注为 load-bearing——将来有人看到 8192 觉得空正文不可能发生而删掉它，事故就会原样复现。

测试：deepseek 包新增思维链参数上线断言（漏发不会编译报错、不会运行时报错、别处也测不出来，只会静默恢复 20 倍花销）、空 content / null content / finish_reason+usage 解析 / `Permanent()` 六个状态码；queue 包新增空正文盖章回归（**实测把旧行为塞回去会失败，确认不是摆设闸门**）、permanent 与 non-permanent 两个方向相反的守卫、来源标记四种形态（含"正常括号必须活下来"）。`go test -race` deepseek + queue 全绿。

---

## [3.8.1] - 2026-08-08

### 新增 — /admin 能看见机翻兜底层的状态

3.8.0 让 LLM 兜底跑起来了，但面板上完全看不见它：那块「中文简介」的四个数字全是 Bangumi 通道口径（建在 `description_cn_eligible` 视图上），机翻翻了多少、卡在哪、DeepSeek 是不是欠费了，一概不知——只能 SSH 上去看日志。这一版把它接进现有的富化面板。

**新增一个并列区块，不是往老区块里塞行**。理由是分母：Bangumi 层的分母是「绑定可信的行」，LLM 层的分母是**「Bangumi 已放弃 + 永远够不着」的那批行**——两组行几乎不相交，并排放会诱导人去加它们。所以是两条独立进度条（紫 vs 青，颜色也刻意拉开）、各自的四个数字、各自的两个心跳。新口径叫 `remit`（职责内）而非 `eligible`，就是为了在命名上就杜绝误读。

**四个数字**：职责内（分母）/ 已译（`source='llm'`）/ 已拒（译文没过汉字密度或长度校验，或原文剥完是空——**传输失败不在此列，那些会重试**）/ 待处理（含 30 天冷却到期的重试）。已拒与待处理**故意重叠**，与 Bangumi 块同款语义，永远不能相加。「已译」这个数**可以合法地往下走**——机翻行会回到 Bangumi 的 30 天重查，被真人简介替换掉是契约在生效，不是数据丢失，注释里写死了这一点。

**队列与心跳独立**：新 `descriptionLlm` 队列快照（排队/重试中/已放弃三态）+ 两个心跳（扫描存活 / 写入活动，各读自己的时间戳列）。**重试中这个数在这一层比 Bangumi 层更要紧**——DeepSeek 限流、key 过期、余额耗尽全部落在 retryable 上，这是它们在彻底停摆前唯一的暴露点。融进一个计数器的话，「余额没了」和「bgm.tv 慢」就分不出来了。

**软失败保持三路独立**：`GetDescriptionCnLlmStats` 是第三次独立往返（不是往 GetAdminStats 上加列），任何一块挂掉都不影响另外两块——测试从两个方向都钉死了（Bangumi 挂时 LLM 仍是真实读数，反之亦然）。前端同样有 `NO_DESCRIPTION_CN_LLM` 零值兜底，滚动部署期间老 go-api 的响应不会把 /admin 打崩。

过程中被两个既有闸门当场抓住，都是真问题：**词典漂移闸门**抓到我把键写成了 `admin.backfill.llm.*` 而实际挂在 `admin.llm.*`（不修就会在页面上渲染出键名——正是 3.4.3 建这个闸门的原因）；**tsc** 抓到第二个测试夹具缺 `descriptionLlm` 字段。测试：admin 新增 remit 四值口径用例（夹具覆盖 remit 谓词每一条臂：已译 / 无绑定 / Bangumi 试过后被 LLM 拒 / 仍属 Bangumi / 已有真人简介 / 无英文原文）+ 双向软失败用例；两个队列 JSON 字节级断言纳入新字段。go 27/27 包全绿，bun 487/487，tsc 回到基线。

---

## [3.8.0] - 2026-08-08

### 新增 — 中文简介的 LLM 兜底层：Bangumi 捡不走的，DeepSeek 当轮捡

Bangumi 简介通道第一轮扫完后的现实：全库 29% 有中文简介，**当季新番 1/100**。剩下的 11,600 条有英文原文的行分两类——4,108 条试过但 bgm 条目没有可用中文，约 7,500 条因绑定过不了交叉验证门槛（或压根没有 bgm_id）永远进不了那个通道。这一版给它们建唯一的通道：`description_llm_backfill` 常驻清道夫，把英文简介翻成中文落库。全量回填的账算过：约 100 万输入 token + 250 万输出，**deepseek-v4-flash 约 1 美元**，翻一次存一辈子，线上永远是纯数据库读。

**接管规则在 SQL 里钉死，永不与主通道抢**：候选查询只选「Bangumi 已盖章试过」或「不在 eligible 视图里」的行；写入守卫是 `description_cn IS NULL`——机翻永远只填空白，覆盖不了 bangumi/manual/更早的自己。**反向覆盖是特意打通的**：`ListDescriptionCnCandidates` 加了 `source='llm'` 臂，机翻行回到 Bangumi 扫描的 30 天重查周期，哪天社区写了真人简介就换掉机翻（manual > bangumi > llm 单向语义，管理面板的 pending 镜像同步拓宽——change one, change both）。

**管线复用 0015 的全部骨架**：migration 0017 加独立的 `description_cn_llm_attempted_at` 盖章列（与 Bangumi 通道的章分开，两个扫描器互不挤兑对方的队列）+ 同款 NULLS FIRST 部分索引，收尾保证的算术原样成立。新 river 队列 `description_llm` 独立可暂停，MaxWorkers=4——这是全站唯一真能并行的队列（预算是 DeepSeek 往返秒数不是共享令牌桶），600 行/小时批量约 20 小时走完存量。job 载荷只带 anilist_id，工作时重读源文并复查 `description_cn`——**Bangumi 在 scan 与 work 之间抢先落地时，worker 让位不花 token**（有单测钉死）。

**质量与红线**：发送前 Go 侧剥 HTML/实体/尾部 "(Source: ...)"（省 token 也防模型回吐标签）；回包过验证闸——汉字 ≥10 且占比 ≥25%（英文拒答与未翻译回声全灭）、长度 ≤4×源文+200（防跑飞）——不过闸就盖章不落库，30 天后带着更好的模型重试。**SERP 边界**：metadata/JSON-LD 继续只读英文原文；详情页正文渲染机翻时出「简介由 AI 翻译自英文原文」标注行并进 nosnippet（与 Bangumi 转写文本同一豁免通道），Google 摘要永远见不到机翻。系统提示词是常量——DeepSeek 前缀缓存价是 miss 的 1/50，一万一千次请求里省的就是大头。

**部署开关**：`DEEPSEEK_API_KEY` 环境变量。没有 key 时 worker 照常注册（残留 job 永不会遇到未知 kind）、扫描器以 disabled 姿态每小时打一行 info——**可以先部署后配 key**，key 落进 `.env.production` 重启即自动开扫。DeepSeek 官方已预告近期涨价，趁现价把存量走完。

测试：deepseek client 3 例（httptest：鉴权/载荷/非 2xx ErrUpstream/空 choices）；worker 13 例（扫描映射/禁用不读库/Bangumi 抢先不花 token/瞬态不盖章 river 重试/拒答不落库/剥离与验证闸全分支）；12 个既有 Enqueuer 测试替身补齐新接口方法。**本地全量 27 包 go test 首次全绿**（Docker 起着，testcontainer 四包一并实跑），bun 487/487，tsc/eslint 零新增。

---

## [3.7.3] - 2026-08-08

### 修复 — 库自动扫描「时灵时不灵」：五个互不相同的搁浅缝一次关掉

用户报告新片加入模块有时候会失效。逐行排查 rescanController / rescanService / useAutoRescan / folderWatcher 后证实五个缝，每个都产生"有时候没反应"，且触发条件互不相同——叠在一起就是"时灵时不灵"的体感。经 /plan-eng-review 完整对峙（D1-D7 七个决策，含一次对 0804-D7 的正式修订），修法如下：

**周期兜底替代补丁**（D2）：最大的洞是"人坐在页面上等下载完，永远等不到"——能在页面静止时发现新文件的只有 Chrome/Edge 133+ 才有的 FileSystemObserver，其余浏览器静默降级成"只靠切标签页"。原计划给被节流吃掉的 visibility 触发做补偿定时器，评审时推翻：**observer 缺席且页面可见时每 120 秒轻量 diff 扫一次**（`PERIODIC_FALLBACK_MS`），走 maybeScan 现成守卫链，反而砍掉了专用补偿器——无聊轮询换聪明补丁，覆盖更全代码更少。

**重试链弹回续期**（D5）：deferred-retry 闹钟开火撞上守卫（正在导入/正在播放/忙/刚扫过）时被消耗且无人续期，链条静默死亡；observer 环境里文件写完那刻的 watch 触发被弹回同理——写完了不会再有事件，搁浅到下次切标签页。现在 watch/deferred-retry 被四种守卫弹回都重新武装同一个 65 秒闹钟，直到真正扫成一次。

**黑名单限次**（D3，修订 0804-D7）：一次瞬态导入失败（IDB 抖动、哈希线程崩）曾把该文件整个会话永久拉黑，UI 零痕迹。改为每键最多 3 次尝试：瞬态错误有两次自愈窗口，永久坏文件的浪费钉死在每会话 2 次额外的 16MB 哈希+dandanplay 匹配——防重试风暴属性与原设计等价。`failedCount` 语义统一为"到限被放弃的键数"。

**静默期容差带**（D4）：`now - mtime < 60s` 推迟的算式对未来时间戳永远为真——NAS 时区飘移或坏时间戳的文件每次扫描都被推迟，永久进不了库。改为三段带：超前 2 分钟以内当"正在写"继续保护（SMB 服务器时钟快一两分钟的活写场景），超前更多视为坏时间戳成品立即导入；病理场景有 size-变化 superseded 重导自愈兜底。

**观察面补全 + 降级可见**（D1/⑤）：「+ 添加文件夹」后新根目录从未被实时监听（watch effect 只在挂载时武装）——新增 `rearmWatch()`，加文件夹成功后重新观察全部根。observer 缺席时文件夹区域出现一行提示"此浏览器不支持文件夹实时监听，页面打开期间每 2 分钟自动检查一次新文件"——本次排查的起因就是静默降级没有任何提示，用户把设计行为误判为故障。

实现为三车道并行（两个 subagent 扛控制器与扫描服务，主线接 hook/UI/词典）。测试：控制器 +7 例新增 +1 例 IRON 回归改写（28/28），扫描服务 +3 例（12/12），全量 bun test 487/487（main 基线 477），词典四份齐上 spaDictCoverage 闸门绿，tsc/eslint 零新增。**OPFS 沙箱 e2e 本地实跑 3/3 绿**（全栈：dev postgres 迁移到 0016 + go-api 容器 + next dev + Playwright，autorescan 正向链路含真 FileSystemDirectoryHandle 枚举→diff→哈希→卡片浮现）。控制器头部陈旧的 ASCII 触发流程图（缺 watch/deferred-retry 两个既有触发源）随本次更新为六源完整矩阵。

设计文档与评审记录：`~/.gstack/projects/lawrenceli0228-animego/lawrence_li-main-design-20260808-autorescan-fixes.md`（含 HTML 版）。剩余人工项：真 Chrome 133+ 环境验 rearmWatch 与弹回续期（e2e 的 Chromium 无 FileSystemObserver，自动化到不了）。

---

## [3.7.2] - 2026-08-07

### 修复 — AniList 熔断器从未生效：限流风暴里每个请求白烧满 5 秒

从 Sentry 两条报错查下去，挖出的真问题比报错本身大。生产 24 小时里 go-api 记了 865 次 `AniList upstream error`，cause 全是 `anilist: retry sleep: context deadline exceeded`，每条 `duration_ms=5000`；同一窗口 nginx 上 `/anime/{id}` 的 5xx 有 1043 次。打过来的几乎全是爬虫：meta-externalagent（Meta 的 AI 训练爬虫）267 次，剩下 751 次来自一个住宅代理池——**每个 IP 恰好只出现一次**、桌面 Chrome UA 轮着换。

根因在 `anilist/client.go` 的 429 分支：只有「重试次数耗尽」那条路会 `tripBreaker()`，而生产实际永远走的是另一条——AniList 叫我们退避 60 秒，请求预算只剩不到 5 秒，`sleep(ctx)` 把剩余预算睡光后报错返回，**不触发熔断**。于是精心设计的 30 秒熔断从上线起一次都没开过：风暴期间每个冷详情页请求都排队烧满 5 秒再 502，而不是微秒级快速失败。

修三处：退避时长塞不进请求剩余预算时**不睡了**，立即熔断并返回 `ErrRateLimited`；睡眠中途被打断同样熔断（429 是真发生了的）；限流器排队醒来后复查熔断器，风暴中排队的请求不再逐个去捅一个正叫我们减速的上游。

顺手把三个 handler（detail 冷路径 / search / schedule）的限流错误从 502 统一改成 503——它是熔断窗口内的瞬态条件，和真上游故障在 nginx/CF 日志里应当一眼可分。

同一次调查的另两笔：`/seasonal` 是全站唯一没有 error.tsx 的核心路由，RSC 一炸就整页白屏（Sentry NEXTJS-2 的 15 次崩溃全在 `/seasonal/summer/2026`，用户连刷两三次后放弃；08-06 部署后零复发但旧日志已随容器重建丢失，无法验尸定罪）——补上边界，错误只换页面段、导航留着、`reset()` 可重试，边界内显式 `Sentry.captureException`（error.tsx 会把错误在全局 handler 之前吞掉）。robots.txt 全站禁掉 meta-externalagent：它是链接预览爬虫 facebookexternalhit 的邻居但不是它，只做 AI 训练采集，带不来任何搜索或引流价值，而且它文档承诺遵守 robots.txt——对住宅代理池那种无名氏这招没用，对它有用。

测试：anilist client 新增 2 例（退避超预算→1 次 HTTP、零睡眠、熔断开；睡眠被打断→哨兵与 ctx 错误同链、熔断开），detail 新增冷未命中限流→503 一例（顺带钉死 `errors.Is` 穿透包装链），search 的限流用例随映射改为 503。`go test -race` anilist/anime 两包通过。

---

## [3.7.1] - 2026-08-06

### 新增 — /admin 能看见中文简介回填的进度与状态

P3 让 sweep 跑起来了，但它在后台跑得无声无息：要知道回填到哪了、还活着没有，只能 SSH 上去数 SQL。这一版把它接进现有的富化面板。

**先说一句地基**：这个需求的八成已经建好了——`GetAdminStats` 是单行多子查询聚合（加统计=加子查询）、`EnrichmentBar` 已经是完整的进度+状态+控制面板且轮询都调好了、`QueueStatusFn` 已经在读 river 队列深度。所以没有新页面、新端点、新轮询器、新组件体系。

- **进度用覆盖率而不是进度条**。sweep 是永续 job，候选数会随回填推进而变小、也会因新绑定和冷却到期而变大。套 `processed/total` 的批次模型会显示一个假的完成度——最糟的情况是 sweep 已经卡住，但因为候选数也在变小，进度条反而显示"快好了"。
- **两个心跳，不是一个**。「上次扫描」查 river_job 里 scan 任务的最新完成时间（**它还在跑吗**），「上次写入」查 `max(description_cn_attempted_at)`（**它最近干活了吗**）。单一指标分不出「没活干」和「死了」——回填跑完后 sweep 每小时空转，写入时间会冻在几天前，看起来和 job 挂掉一模一样。
- **队列按 state 拆开**：排队 / 重试中 / 已放弃。`retryable` 的意思是"这条失败了等着重试"，混在"有活"里会让 bgm.tv 挂掉时看起来完全健康。
- **信任门槛抽成 `description_cn_eligible` view（migration 0016）**。这个判断此前散在候选查询和写入语句两处，加上仪表盘就是三处。三份手抄的条件会漂移，而漂移的后果是仪表盘报的数和 sweep 的实际行为分家——**一个会撒谎的仪表盘比没有仪表盘更坏**。
- **轮询给 backfill 单独的 30 秒档**。首次回填要跑 31 小时且队列全程非空，套现有的 5 秒档等于开着 admin 页就连续打 22,320 次请求。而 scan 每小时才动一次，5 秒刷新只是在看同一个数字重复 720 遍。

### 修复 — 两处「故障时显示健康」（审查发现）

这次审查抓到的两个问题都在同一条线上：仪表盘在坏掉的时候不能看起来是好的。

- **`rows.Err()` 没检查**。pgx 的 `Scan` 出错会让后续 `Next()` 返回 false，**和干净读完完全无法区分**。一次中途断流会留下被截断的聚合，而截断只会让数字变小——`retrying` 少报、`queued` 少报，正好读成健康。现在读完显式检查，有错就整体走零值 + warn，不 publish 半截数据。
- **四个新计数挂在 `GetAdminStats` 里会拖垮整页**。它们依赖 0016 建的 view，而 `GetAdminStats` 此前只读基表。view 缺失时整条语句挂 → /admin 的用户数、番剧数、富化进度、队列**全部一起消失**。现已拆成独立查询并 soft-fail，`GetAdminStats` 的 SQL 与改动前逐字节相同。

另外纠正了 `rejected` 的语义注释：它涵盖四种「已决」结局，其中 bgm 404（绑定失效）和写入端报错**都不产生 retryable job**，所以队列区块会一片绿，`rejected` 攀升是唯一信号。原注释写的"大 rejected 是正常的"会教运维忽略掉真正的故障。

`go build` / `go vet` / `go test ./...` 全过，`bun test` 477/477（新增 81 条）。migration 0016 在真实 postgres 上跑通，接口实测返回 `eligible=13 done=9 rejected=4 pending=0`，与手工 SQL 逐字一致。

---

## [3.7.0] - 2026-08-06

### 新增 — 中文简介存量回填

P2 把管子接上了，但**存量 17k 行没有任何入口能重新入队**——admin 的 re-enrich 只覆盖 `bangumi_version` 0/1/2，而存量绝大多数已经是 version=3。这一版补上入口。

- **做成 river 周期 job 而不是一次性 CLI**。这让"首次回填"和"日常补漏"变成同一件事：job 每小时扫一次候选，首跑时有约 9,100 行待处理、一天多消化完；之后每次扫只剩每周 `bgm_id_map` 刷新带来的零星几条。跑完它就安静地变成每小时一次索引 SELECT 的 no-op，而哪天有一批行被重新绑定，它不需要谁记得它存在就会捡起来。
- **必须跑在 server 进程内**——bangumi 的限速器是 `*bangumi.Client` 实例上的进程内令牌桶，独立 CLI 会开出第二个桶，对 bgm.tv 的实际速率直接翻倍，而 `AnimGo/1.0` 那个 UA 的白名单是最不该赌的东西。跑在进程内还白捡 river 的断点续跑和重试。
- **专用队列 + `MaxWorkers: 1`**。独立队列买到的是隔离和可单独暂停，买不到并行——三个队列共用同一个令牌桶，多开 worker 只会挤占在线富化的派发槽。
- **不复用 V3**：V3 写 `title_chinese` 是无条件覆盖语义，有些行的中文标题来自 dandanplay heal（比 bgm 的 `name_cn` 更准）。回填 worker 的依赖接口只声明了它需要的两个方法，**类型层面就写不到 `title_chinese` 和 `bangumi_version`**。
- **扫描 SQL 里就带信任门槛**，不只在写入时挡：约 3,200 行是我们持有但没有独立来源确认的绑定，注定写不进去，先滤掉能省下同样数量的无用上游请求——否则每一轮都会重抓一次，而它们永远不会变。

### 修复 — 回填会在 5% 处永久停摆（migration 0015）

初版候选查询用 `description_cn IS NULL` 找活、按 `anilist_id` 排序，注释里写着"处理完的行会自动退出结果集"。**这只对成功的行成立。** 实测约 37% 的 bgm 简介是未翻译的日文原文，被语言闸门拒绝后 `description_cn` 仍是 NULL，于是永远留在候选集里；而按 id 排序让它们的位置固定，会一直堵在每一批的最前面。

这不是"变慢"，是硬天花板，且有闭式解——设批大小 B、单行成功率 p，第 k 轮产出 `B·p^k` 行，全生命周期写入总量收敛到：

```
p·B/(1−p)   行，永远
```

B=300、p≈0.6 时约 **450 行**，而待处理约 9,100 行——覆盖率封顶 5%。这个式子同时否掉了"调大批量"这个直觉解法：要覆盖全部需要 `B ≥ N(1−p)/p ≈ 6,100`，即单轮拉走三分之二个番库、吃掉 80 分钟以上的令牌桶，批量化本身就没意义了。

- **加 `description_cn_attempted_at`**，每次**已决**结果都打戳（文本存下、闸门拒绝、bgm 上查无此条），候选查询改按它排序并排除冷却期内的行。被拒的行去队尾而不是堵队头，sweep 就能推进到底。
- **网络失败不打戳**——超时或 5xx 说明不了这一行的任何事情，打戳等于为一个网络问题把它冷藏 30 天。那种交给 river 重试。
- 冷却期 30 天顺带白拿了一个**重试**能力：bgm 的中文简介是社区逐步补的，今天只有日文原文的条目，下个季度可能就有中文了。
- 索引换成 `(description_cn_attempted_at NULLS FIRST, anilist_id)` 的部分索引以支持新排序，`EXPLAIN` 已确认命中。

`go build` / `go vet` / `go test ./...` 全过（含 testcontainer 包）。migration 0014+0015 在真实 postgres 上跑通，并实测验证打戳的行确实退出候选集。

---

## [3.6.0] - 2026-08-06

### 新增 — 中文简介通道（上线时零可见变化）

站点一直对中文用户渲染 AniList 的英文简介，因为从来没有中文简介可渲染。而 Bangumi 的 `Subject.Summary` 是社区写的中文 synopsis，**enrichment worker 每次请求都收到它、然后原样丢弃**——`bangumi/client.go` 早就解析了这个字段，`internal/queue/` 全目录零引用。本次把这条通道打通。

**本次上线看不到任何变化**：迁移跑完两列全是 NULL，页面逐字节不变。存量回填是下一阶段的事（见文末）。

- **schema（migration 0014）** — `anime_cache` 加 `description_cn` 与 `description_cn_source`（CHECK 限定 `bangumi`/`llm`/`manual`）。两列而非一列，是为了让日后的机翻能垫在真人文案下面而不丢失区分能力，也让任一来源可以整批撤回。优先级 `manual > bangumi > llm`。刻意**不加** hash 列——那是机翻判断原文是否变化用的，等写机翻的那个阶段再落。
- **语言闸门 `bangumi.CleanSummary`** — 这是本次最需要实测支撑的一块。拿 150 个通过信任门槛的 prod 条目实拉 bgm.tv 统计：**可用中文只有 57.3%，日文原文占 36.7%**，空 3.3%、过短 2.7%、带 `[简介原文]` 分隔符 1.3%。没有这道闸，超过三分之一的富化行会把用户读不懂的英文换成同样读不懂的日文。闸门做三件事：剥掉 `[简介原文]` 及其后的日文原文、规范换行与剥尾注、然后过「空 / 短于 40 字 / 假名占比 > 0.35」三关。
  - 阈值 0.35 有依据：实测分布是干净的双峰，86 条落在 0–0.1（中文散文，假名只来自引用的日文专名），50 条落在 0.5–0.8（日文散文，假名必然多于汉字），中间几乎是空的。**早期试的 0.12 是错的**——它会误杀引用日文原名的中文简介，实测样本 bgm 37459 的中文 synopsis 因为引用「バトルスキッパー」达到 14.7% 假名占比却是纯中文。测试里放了这条真实回归用例专门防止有人把阈值调低。
- **信任门槛写在 SQL 的 WHERE 里** — `UpdateDescriptionCn` 的门槛不是 Go 里先查后写，而是与 UPDATE 同一条语句，因此不存在"判断完到写入"的时间窗。门槛问的也不是 `bgm_match_source = 'id_map'`（那列只记录绑定是怎么建的，0011 之前的老行即使正确也是 NULL），而是**独立权威源同不同意这个绑定**——去 `bgm_id_map` 查有没有同样的 pair。2026-06 审计里有权威答案的 7,183 行一致率 100%，已知错绑全在无映射的尾巴上，正好被挡掉。赌注不对等：标题绑错是一个词错，**简介绑错是整页在讲另一部番**。
  - 语句同时按 `bgm_id` 定位，把 job 执行期间发生改绑的窗口也关掉了——只按 `anilist_id` 写的话，改绑会让旧 subject 的简介落到新绑定上。
  - `'manual'` 永不被覆盖：那是人工修正，自动 sweep 不该撤销它。
- **零额外上游请求** — worker 只是把已经拿在手里的那个 `/v0/subjects/{id}` 响应体里的 Summary 顺手落库。测试里用 `subjectCalls == 1` 把这条钉成回归断言。写入失败只记 warn 不让 job 失败（简介是增益，V2/V3 的主职责是评分和中文标题）；闸门拒绝走 debug 级——实测约四成条目走这条路径，用 warn 会刷屏。
- **前端** — `pickDescription(detail, lang)` 沿用 `pickTitle` 的模式，zh 优先中文、en 恒取英文。命中中文时正文下方渲染「简介来自 Bangumi」署名并回链条目，整段包 `data-nosnippet` 让这段转载文本不参与搜索摘要竞争。
- **SEO 面一行未动** — `generateMetadata`、JSON-LD、og/twitter/canonical/hreflang、缓存配置全部保持原样，仍用英文 description。让中文简介进 SERP 需要分桶灰度加 GSC 观察，是独立阶段的事，代码里加了注释标明这条边界。

- **「展开更多」的截断阈值改按视觉宽度算** — 原来的 `300` 是字符数，而这个单位对两种语言含义完全不同：300 个英文字符是两三句话，300 个汉字是好几段，且汉字全角、视觉宽度是拉丁字符的两倍。实测这批简介，中文全部落在 55–289 字之间而对应英文是 235–1100 字符，于是**整个中文语料都在阈值以下、按钮只对英文读者出现过**。现在按「拉丁计 1、CJK 计 2」的视觉宽度判断，阈值仍是 300：英文行为逐字节不变，中文在 150 字处触发。
  刻意没有用「按 `lang` 分两个阈值」这个更省事的写法——中文读者遇到没有中文简介的番会回落英文原文，那段文本必须仍按英文规则判断，所以判据只能看文本本身，不能看界面语言。

`go build` / `go vet` / `go test` 通过，`bun test` 396/396（新增 24 条），`tsc` 非测试文件零错误。migration 已在 prod 真实 schema 上用事务包裹 dry-run 验证语法后回滚，零残留。

**已知边界（下一阶段）**：admin 的 re-enrich 入口只覆盖 `bangumi_version` 0/1/2，而存量绝大多数行已经是 version=3，**目前没有任何代码路径能把它们重新入队**。所以上线后 `description_cn` 只随新番和新绑定缓慢增长，17k 存量不动。0014 已经建好配套的部分索引 `idx_anime_cache_description_cn_missing`，回填 sweep 与周期 top-up job 留给下一阶段。

---

## [3.5.1] - 2026-08-06

### 修复 — /seasonal 的分类筛选点了等于清空列表（静默失效两个月）

在 `/seasonal` 点任何一个分类 chip 都是「0 部 / 暂无番剧」，而不筛选时有 98 部。prod 与本地行为一致，`?genre=Action` 稳定返回 0 张卡片。

根因不在前端：**Go 的 `/seasonal` 端点从来没返回过 `genres` 字段**。前端 `applyFilters` 里 `a.genres?.includes(genre)` 拿到的永远是 `undefined`，可选链一路 falsy，于是筛选把整个列表滤空。

这正是 `lib/types.ts:79-85` 两个月前写下的预言：

> Go API 的 `/seasonal` 只返回 16 列主表，不 join genres 子表。老的 Express 端点会从 Mongo 富化缓存里带出 genres，seasonal 页的筛选依赖它。**Go 切换完成后，要么 seasonal handler 加个 genre join，要么这个字段保持可选、筛选静默失效。**

老栈已于 2026-06-01 退役（`8ba923b`），落到的正是后一种。可选链让它没有报错、没有 500、没有任何日志——只是安静地永远返回空列表。

- `GetSeasonalAnime` 加一列 `genres` 聚合子查询。**没有额外查询代价**：同一条 SQL 的 Hentai 排除条件本来就在查 `anime_genres`。
- wire 由 sqlc 自动生成 `Genres []string \`json:"genres"\``，前端 `SeasonalAnime.genres` 已声明为可选、filter 逻辑一行没改，字段一到位就自愈。
- 顺带更正三处已经过时的「16 列」注释，并补上一条冷启动语义：从未富化过的季度（AniList 现拉）子表还是空的，`genres` 会是空数组、筛选匹配不到；站点实际会列出的都是已富化季度，首次请求就带 genres。

已在 prod 数据上直接验证该 SQL 返回正确分类数组。`go build` 与 `go test` 通过（两个 testcontainer 用例需 Docker daemon，本地未启动，由 CI 覆盖）。
||||||| 8849f62
## [3.5.0] - 2026-08-06

### 新增 — 内容枚举汉化（词典层）

中文用户此前在站内看到的 genre、format、staff 职位全是 AniList 的英文枚举；而关系条目与推荐位明明拿到了中文标题，渲染时却被 `x.title || x.titleChinese` 一句写死成罗马字。本次把这些"标签类"内容一次性汉化。

**为什么不进 `locales/`** — 这些是数据枚举映射，不是 UI 文案。四套词典（`zh.ts`/`en.ts`/`zh-spa.js`/`en-spa.js`）有漂移前科（3.4.3 刚为此加了 `spaDictCoverage.test.ts` 闸门）。新建的 `lib/contentLabels.ts` 是**服务端与客户端共用的单一普通模块**，结构上不可能漂移，也不进那道闸门的管辖范围。所有 helper 对 `en` 恒等返回原文，让"英文不退化"由类型和实现机械保证，而不是靠人工评审。

- **genre 19 词全覆盖** — 接入详情页标签行、卡片 hover、首页轮播、完结佳作、年度榜、`/search` 与 `/seasonal` 两处筛选器。附完备性测试：AniList 日后新增 genre 会直接让 CI 失败，而不是静默渲染英文。顺带修掉 `/search` 页 "Action 类型的动画" 这种中英夹杂标题。
- **两份硬编码 GENRES 合一** — `SearchFilters.tsx` 与 `SeasonalFilterChips.tsx` 各存了一份 18 项数组（前者注释自陈是从 legacy SPA 的 `constants.js` 逐字节抄来）。现在共用 `FILTER_GENRES`。它刻意是 `GENRE_LABEL`(19 项) 的子集——`Hentai` 是目录里的真实值、详情页必须能渲染，但不作为浏览筛选项。
- **format 补上缺失的 `MUSIC`** — 原映射表只有 6 个值，prod 有 7 个，导致 1,207 部（占 7%）的徽章 fallback 成裸枚举。另外 `MOVIE`/`SPECIAL` 等在 en 下从 `SCREAMING_SNAKE` 变成 `Movie`/`Special`，与 `/seasonal` 筛选器一贯的标签对齐——改动前同屏会出现筛选 chip 写 "Movie"、正下方卡片徽章写 "MOVIE" 的不一致。
- **staff 职位可译了** — 这项原本判断是"自由文本、没救"：prod 有 4,794 个不同 role 值。但剥掉 `(eps 3, 7)` `(ED2)` `(12 episodes)` 这类限定后缀后只剩 1,405 个，**55 词表即覆盖全部 104,412 条 staff 记录的 91.8%**。译文保留原限定后缀（`Episode Director (eps 3, 7)` → `演出 (eps 3, 7)`），未命中的原样返回。有意义的括号如 `ADR Director (English)` 不剥离，否则会把不同职位并成一个。
- **解除关系/推荐的中文压制** — 三处 `x.title || x.titleChinese` 换成 `pickRelatedTitle`。实测 prod：推荐位 50,990 条里 **40,354 条（79.1%）** 的目标番有中文标题，关系条目 30,849 条里 15,020 条（48.7%）有——一直白白压着不用。顺带修掉 `title` 为纯空白时是 truthy、会渲染出空 chip 的老毛病。
- **`WatchingSection` 反向修** — 它此前不分语言恒取 `titleChinese` 优先，对英文用户是强喂中文。改为按语言走 `pickTitle`。这条是"英文体验不退化"的双向兑现。

**客户端叶子的取舍** — `getLang()` 服务端恒返 `zh`（ISR islanding 决策），所以 RSC 里就地汉化会让英文用户永久看到中文。详情页最显眼、文字最密集的两处（genre 行、format 徽章）下沉到 `LocalizedChips.tsx` 客户端叶子走 `useLang()`；`SeasonRankings` 是纯 RSC，同理抽了 `RankingMeta.tsx`。**但刻意没有把这个待遇逐条扩展到 staff 职位和关系标题**——那是每页几十个节点，而紧挨着它们的 `pickTitle`/`RELATION_LABEL` 无论如何都是中文，重绘只会买到一个半吊子翻译，却付出真实的 hydration 代价。要真正解决得解开 `getLang` 的钉，那是拿边缘缓存换，属另一个决策。此取舍已写进 `page.tsx` 的文件级注释，防止后来人逐项扩散。

**顺带修掉一个没预料到的布局跳变** — 两个 `loading.tsx` 的筛选器骨架屏硬编码了按英文标签量出来的像素宽度（`"Mahou Shoujo"` → 96px）。中文标签整行窄约 33%（18 个 genre：1462px → 966px），足以在约 1200px 容器上少掉一行——骨架预留两行、真实内容一行，切换时跳一下。新增 `lib/chipWidth.ts` 从标签本身推导宽度，日后改文案不会再悄悄把这个位移带回来。

`bun test` 372/372（新增 46 条）。`tsc --noEmit` 与 `eslint` 在改动文件上均干净。

---

## [3.4.3] - 2026-08-05

### 修复 — 库溢出菜单等 5 处直接显示 i18n 键名

用户截图:溢出菜单里那一项渲染成了 `library.overflow.rescan` 而不是中文。

根因是**两套词典无声漂移**:客户端组件走 `useLang()`,它背后是 `locales/*-spa.js`;而 `zh.ts`/`en.ts` 只有服务端组件读。两者之间没有任何关联,只往 `zh.ts` 加键在 SSR 侧看着好好的,到浏览器就露键名。3.4.0 的 watch-folder 那批工作往 `zh.ts`/`en.ts` 加了 5 个键、`-spa` 一个都没加。`t()` 命中失败时返回键名本身(见 `lib/i18n.ts`),既不报错也不打 warning,所以一路混过了评审和 CI。

- **补齐 5 个键**(zh + en 两份 `-spa`):`library.overflow.rescan` / `rescanBusy`、`library.scanToast.kicker` / `title`、`library.persistHint`。文案沿用 `zh.ts` 里已经定好的(「重新扫描文件夹」「扫描中…」「自动扫描」「发现 {{count}} 个新剧集」)。
- **加防漂移闸门** `locales/spaDictCoverage.test.ts` — 扫描所有用了 `useLang()` 的文件里的 `t("…")` 调用,断言每个键在两份 `-spa` 词典里都能解析,失败时直接打印 `文件:行号 键名`。带 `defaultValue` 的调用豁免(那是显式的"可以缺"契约,如 `player.dropRelease`);另有一条「扫到的调用点 > 50」的自检,防止正则或路径改动让断言变成空跑。实测把 `rescan` 删掉能精确报出 `LibraryShell.tsx:1124`。
- 顺带查清 `zh.ts` 相对 `-spa` 共缺 87 个键,但其余 82 个都只被服务端组件使用(meta/seasonPage/landing/admin 等),不受影响 —— 闸门按**实际调用点**而非键集合求交,不会误报这些。

`bun test` 326/326。

---

## [3.4.2] - 2026-08-04

### 修复 — 播放页/库卡片挂错季度的评分与「查看详情」链接

用户实测:拖入《无职转生Ⅲ》的文件,标题显示正确(Ⅲ),但下面整排徽章全是**第二季**的——★8.1 / BGM 7.3(15,368)/ 13 集 / 夏季 2023 / 已完结,「查看详情」也链到 `/anime/146065`(第二季)。真正的第三季是 178789(2026 夏 / 14 集 / BGM 7.9)。

根因是**匹配到了正确的番剧,却把身份丢掉重猜了一遍**:Phase 1 靠文件哈希命中 dandanplay,拿到的是精确条目(所以标题对);但 `siteAnime`(评分/年份/集数/anilistId)不沿用这个结果,而是拿标题回头去 `anime_cache` 做 ILIKE 模糊搜索,再 `rows[0]` 盲取第一行。而 ① SQL `LIMIT 10` 没有 `ORDER BY`,返回顺序是堆序;② 取行时零打分、零阈值。实测 prod 搜「无职转生」返回顺序恰好是 `[146065(S2), 166873, 178789(S3), …]`,于是每次都稳定取到第二季。前端 `useSiteAnimeForSeries` 有一份同源的孪生 bug(`hits[0]` + 双向 `includes` 一律 50 分,季号不参与打分)。

- **精确身份优先(治本)** — dandanplay `/api/v2/bangumi/{animeId}` 的响应里本来就带 `bangumiUrl`(bgm.tv subject)和 `onlineDatabases` 的 AniList 链接,只是客户端从没解析。现在解析进 `EpisodeData.AniListID/BgmID`,`/match` 按 **anilistId(anime_cache 主键)→ bgmId → 模糊搜索** 的顺序解析 siteAnime。前两条是精确命中且**不产生任何额外上游请求**——这个 payload 本来就为了取剧集列表拉过了。注意 `bangumiId` 字段是个陷阱:它重复的是 dandanplay 自己的 animeId,不是 bgm.tv subject。
- **模糊兜底不再可能挑错季** — 新增 `seasonmatch.go`:从标题抽「季号 / 部分号」二元组(第二季 / 第2期 / Ⅱ / II / Season 2 / 2nd Season,以及独立的 第2部分 / 第2クール / Part 2 / Cour 2),季号不一致直接**淘汰**而非降权。相似度沿用已有的 `bangumi.TitleSimilarity`(它会剥季号标记,所以只回答「同一系列吗」),季号守卫回答「同一部吗」——两个信号各司其职,缺一不可(S2 和 S3 的标题相似度约 0.85,光靠相似度分不开)。挑不出就返回 null:**卡片不显示徽章,远好过显示错的**。
- **Phase 2 一并修** — 它按顺序走候选、取第一个能解析出剧集的,而一个系列的**每一季**都能解析出第 1-n 集,所以先返回的那行就赢了。现在先按季号过滤 + 相似度排序再走。
- **SQL 补 `ORDER BY anilist_id`** — 纯为确定性:没有它,`LIMIT` 截断的是任意堆序切片,同一个查询能随 vacuum 漂移。相关性排序放在 Go 里做(SQL 表达不了季号守卫)。
- **前端同修** — 新增 `lib/seasonMatch.ts`(go-api 那份的浏览器孪生),`useSiteAnimeForSeries` 去掉 `hits[0]` 盲兜底。这里的文本比较**故意剥掉季号标记**:dandanplay 写「无职转生Ⅲ」而站内缓存写「无职转生 第三季」,带着序号比字符串会让正确的一对看起来毫不相干;季号身份交给抽出来的序号单独判定。
- **测试** — go 侧 21 个新单测(季号抽取全notation 表驱动 / 五季五部精确区分 / 宁可返回 nil 也不返错季 / 三条腿优先级 / 关键词放宽搜索但标题定裁决 / 跨链接解析与缺失容错),前端 19 个。顺带修正 4 个既有 Phase 2 fixture——它们的行没有任何标题,等于把「随便哪行都行」写进了断言,而那正是这个 bug 本身。go 全量 + `bun test` 323/323 通过。
- **已知残留**:修复前导入本地库的卡片,`titleZh/posterUrl` 已按错的季度落库,需要用溢出菜单「重新匹配」刷新;新导入不受影响。

---

## [3.4.1] - 2026-08-04

### 修复 — watch-folder 新集并入已有番剧卡(跨批次归属)

3.4.0 上线当晚用户实测发现:自动扫描进来的新集**不并入已有的番剧卡**,而是另开一张新卡,溢出菜单的「合并重复」也无效。根因:跨批次合并此前只有一条路——dandanplay 给新文件匹配出 animeId 且旧系列也带同 animeId 的 season;**刚放出的新集 dandan 哈希库往往还没收录**(匹配失败→本地标题路径→铸新卡),clusterizer 预留的跨批次标题提示(`_titleHint`)从未接线(DB season 行没有该字段,索引恒空),本地标题系列更是连 season/animeId 都没有;「合并重复」按 animeId 去重,对无 animeId 的新卡自然无感。

- **三层"归属守卫"补齐跨批次合并**(强度降序,任何歧义都放弃自动合并、留给手动):①内容 hash(3.4.0 已有)②**同文件夹**——新文件所在目录的既有文件全部属于同一系列即并入(watch-folder 主场景:qB 按番剧分文件夹)③**同标题**——规范化标题词元唯一命中一个既有系列(平铺下载目录场景),带季号守卫(S2 文件不并入仅 S1 的卡)与碰撞墓碑(两系列同名→不猜)。
- **dandan 身份优先**:dandanplay 明确识别为其他番剧的文件绝不被文件夹/标题启发式拐走。
- 索引构建 best-effort(fake db 缺 toArray 自动降级,不破测试);派生归属无 animeId 不写 matchCache(避免裸缓存条目)。
- **测试**:6 个新单测(文件夹并入/混合目录跳过/标题唯一并入/同名碰撞跳过/季号冲突跳过/dandan 优先);OPFS e2e 扩展成完整回归——第二集写入同文件夹后卡片数必须仍为 1、episodes 到 2;顺带修掉 e2e fixture 文件名带 "[E2E]" 被解析器读成"第 2 集"的自坑(全部文件解析成同一集号,第二个文件被当成同集备用源)。全套 304/304 + sandbox e2e 3/3。
- 存量重复卡清理路径:升级后把重复的新卡「移出库」,再点菜单「重新扫描文件夹」,文件会重新导入并并入正确的卡。

---

## [3.4.0] - 2026-08-04

### 功能 — 播放库 watch-folder 自动更新(一期):下载丢进文件夹,回到网站就能看

此前 `/library` 没有任何重扫路径:新下载的剧集必须重新点「+ 添加文件夹」、重挑同一个目录、全库每个文件重读 16MB 做哈希才进得了库(还每次多铸一条 fileHandles 记录)。本次给库补上「对账式增量扫描」—— 打开 /library 或切回标签页时,用已持久化的目录 handle 静默重枚举、与已导入文件 diff、只把新文件喂进现有导入管线,成本 O(新文件)。方案经 6-agent 调研 + 反驳验证 + /plan-eng-review 全流程评审(8 项决策,详见 workflow 设计文档)。

- **增量 diff 扫描(`rescanService`)** — 全库 `(relPath,size)` 基线(历史多 libraryId 脏数据天然免疫,重复 root 第二次枚举 0 新增);**60s 静默期守卫在哈希阶段之前**挡还在写盘的种子文件(BT 乱序写,前 16MB 不完整会产生脏 hash);同 relPath 但 size 变化 → 按新文件导入 + 旧 fileRef 行打 `supersededAt` 标记(不删行,Episode.primaryFileId 引用保持有效,播放按 relPath 解析天然拿到新内容)。
- **纯控制器 + 薄 hook(`rescanController` / `useAutoRescan`)** — 全部决策逻辑(守卫链、60s 节流、roots 按 lastSeenAt 降序 + isSameEntry 内联去重、per-root 故障隔离、会话级失败跳过集防「持续失败簇每轮重哈希 + 重打 dandanplay」的重试风暴、手动导入 cancel + 5s 有界等待抢占)在纯函数模块里,bun test 全覆盖;hook 只做 mount / visibilitychange 两个触发点的接线。权限契约:自动路径只读、**绝不** `requestPermission`;granted 静默跑,prompt 落回现有 reauthorize 手势;扫描失败的 root 走 `refreshHandles()` 进现有可用性 UI,不进 console(e2e 零报错断言不破)。
- **关键修复:reuse 导入现在会刷新 `series.updatedAt`** — `persistFileRefsOnly` 只写 episodes/fileRefs,而 useLibrary 的 liveQuery 只监听 series 表 → 老番出新集从来不会浮进 NewAdditionsRow(注释写了设计意图但代码路径缺失)。补 `seriesRepo.touchSeries()` 在 reuse 分支回写,新集现在会自动浮到「新加入」行。
- **UI** — 静默扫描以 mini pill 呈现(不弹全屏导入抽屉);完成后 toast「发现 N 个新剧集」;溢出菜单新增「重新扫描文件夹」(手势路径,denied root 先走授权,并清空失败跳过集强制重试);UnavailableSeriesSection 下新增持久授权引导(授权弹窗选「每次访问时都允许」或安装 PWA,重启浏览器后免点击)。
- **测试** — 新增 29 个单测:rescanService 分支全覆盖(9)、控制器守卫链/排序/隔离/抢占(16)、importPipeline reuse 分支 IRON 回归 + touchSeries(4,经真实 runImport + fake Dexie 表面);e2e seeded 用例加「自动扫描不得弹全屏 scrim」断言,现有两条 spec 一行未改。`bun test` 280/280;生产构建编译 + TS 检查通过。
- **(PR-2)管线加固** — 修掉一个存量崩溃:matchCache 里缓存的裸 `{kind:'new'}`(无 animeId,即"本地标题导入 + dandan 未命中"的产物)被复用时没有 seriesRecord 可持久化,`upsertCluster` 校验直接抛错、整簇计失败——每次重导入本地标题系列都触发;现按缓存未命中处理、重新匹配。新增**重导入守卫**:簇内每个文件都已有归属(同内容哈希 → 同 fileRef id → 各自挂在同一系列的剧集上)时不再铸新系列、翻转为 reuse——堵住"缓存过期 + dandan 不可达 → 重复卡片"和移动/改名场景;文件跨两个系列、或命中 ambiguous 行时守卫保守跳过。派生 reuse 无 animeId 不写缓存(避免再造裸条目)。5 个新单测。
- **(PR-2)player 页 tab-return 扫描** — `useAutoRescan` 增加 `enabled` / `triggers` / `shouldDefer` 选项(shouldDefer 进控制器守卫链,可单测);PlayerShell 在 library 模式挂 hook:仅 visibilitychange 触发(挂载扫描是 /library 的职责)、`playbackPhase === "playing"` 时让位(哈希 worker 不与解码抢 CPU)、扫到新集后调 `seriesDetail.refresh()`(它是一次性加载非 liveQuery)让 EpisodeNav 立即出现新集——"看完第 6 集,下完第 7 集切回播放页点下一集"的续看场景就此闭环。
- **(PR-3)FileSystemObserver 实时监听** — `folderWatcher.js` 薄适配(规格未入 whatwg/fs,所有触点防御式包裹、调用方只依赖模块自有接口):特性检测(Chrome/Edge 133+ 桌面默认启用,Firefox/Safari/Android 与 Playwright 自带 Chromium 均无 → 自动降级 no-op)、`observe({recursive:true})`、事件一律当"提示"——落地动作永远是同一个 diff 扫描;appeared/modified/moved/disappeared → 10s debounce;`unknown`(官方语义"丢过事件,请轮询")→ 立即扫;`errored` → 转入可用性重探。控制器新增 `watch` 触发档位(10s 细节流)与**静默期推迟重试**:扫描发现"还在写盘被推迟"的文件时定一次 65s 后的单发重试,页面开着时刚下完的文件也能在守卫窗口过后自动进库(此前会一直等到下次切换标签页)。hook 按 handlesStatus 对每个 ready root 起观察、卸载时 disconnect + dispose。12 个新单测(watcher 8 + 控制器 4)。
- 已知边界:FSA 为 Chromium-only(与现状一致);重启后能否静默扫描取决于用户授权选项(引导文案即为此);实时监听仅在页面存活期生效,挂载对账扫描始终是兜底真相。

---

## [3.3.0] - 2026-06-19

### 开源 — 项目改用 AGPL-3.0 许可 + 社区贡献治理骨架

AnimeGo 从「公开可见但专有（All rights reserved）」正式转为 **GNU AGPL-3.0 开源**并开放社区贡献。本次只动许可证与治理文档，**无任何运行时 / 代码行为改动**。

- **许可证：专有 → AGPL-3.0** — `LICENSE` 由原「仅供透明 / 参考、未授予任何使用权」的专有声明替换为官方 AGPL-3.0 全文。选 AGPL 而非 MIT/Apache 的理由：强 copyleft + **网络服务条款**（别人若把修改版当线上服务跑，必须向其用户公开修改后的源码），既防被闭源 fork 走、又与同类动漫弹幕项目（Animeko 等）的许可一致。`README.md` / `README.zh.md` 的 License 与 Contributing 两段同步翻新（EN+ZH），旧的「proprietary / 暂不接 PR」措辞全部替换。
- **可单方面 relicense 的前提已核实** — 全部提交的人类作者均为本人的不同 git 身份；历史里仅有的 `Co-authored-by` 是 Claude 各型号（Anthropic），AI 产出不构成独立版权人 → 本人为唯一人类版权人，有权单方面把许可从专有改为 AGPL。
- **DCO 而非 CLA** — 贡献走 [Developer Certificate of Origin 1.1](DCO)：贡献者保留各自版权、仅签核（`git commit -s`）声明有权提交，符合 AGPL 社区惯例、对贡献者更友好。代价（已知并接受）：DCO 下无法日后单方面把整库闭源或卖闭源 fork（需全体贡献者同意），要留这条路才需改用 CLA。
- **磁力子系统不接外部贡献（三层围栏）** — 种子 / 磁力元数据搜索子系统（`go-api/internal/torrents/`、`TorrentModal*`）因法务与维护原因由 owner 独维，**不接受外部代码 PR**（欢迎提 issue）：`.github/CODEOWNERS` 锁目录 + `CONTRIBUTING.md` 列明 out-of-scope + `PULL_REQUEST_TEMPLATE.md` 加「本 PR 不碰磁力子系统」勾选项。注：真正 block 合并要在 GitHub 后台给 `main` 开「Require review from Code Owners」，该开关属仓库设置、不在本仓文件内。
- **新增治理文件** — `CONTRIBUTING.md`（DCO + 贡献范围 + Conventional Commits + 本地测试命令）、`CODE_OF_CONDUCT.md`（Contributor Covenant 2.1）、`SECURITY.md`（私密漏洞披露，不走公开 issue）、`DCO`、`.github/` 下 PR 模板 + bug/feature issue 表单 + config。

---

## [3.2.2] - 2026-06-05

### 性能 — 首页走 CF 边缘缓存(冷启动 1.6s → 0.12s)

详情页 `/anime/*` 早先已上 CF 边缘缓存(实测 HIT ~0.2s),首页 `/` 却一直 `cf-cache-status: DYNAMIC`、冷启动 TTFB 1.6s —— 每次访问(含爬虫 + 匿名 SSR 读)都回香港源站全量渲染,正是当初引发详情页 500 爬虫事故的那批流量。本次把首页也搬到边缘。

- **根因:3 个服务端 `no-store` fetch** — `safeSchedule` + `ContinueWatching` + `ActivityFeed`,任何一个都让整页 dynamic;其中两个还**读 cookie 渲个性化数据**(你的在追 / 关注动态),HTML 因人而异,**不能直接缓存**(否则一个人的追番列表会被发给所有匿名访客 = 缓存投毒)。`page.tsx` 那句"prod 有 CF HTML cache 兜底"的旧注释实测是错的(一直 DYNAMIC,从没缓存到)。
- **修法:把两个按用户区块 island 化** — 新增 `useAuthGatedList` hook(`auth_hint` cookie gate → `authFetch` → loading 骨架 / 匿名 stub / 数据;SSR 初始恒渲 loading 骨架,人人一样、可安全缓存),`ContinueWatching` / `ActivityFeed` 从服务端组件改成客户端岛(挂载后自取,匿名零 auth 请求)。沿用 Navbar / 详情页同款 `auth_hint` 探测模式。
- **`force-dynamic` 故意保留** — 首页是无参静态路由,**不能**像 `/anime/[id]` 那样靠 `generateStaticParams→[]` 躲成 ISR-on-demand;而构建时 go-api 不可达(`GO_API_INTERNAL_URL` 是 runtime env 非 build arg),首页若转 ISR 会把空数据烤进构建、每次部署后首页空 60s。故保留 `force-dynamic`(每次源站渲染拿真数据),缓存放边缘。
- **CF Cache Rule on `/`** — Edge TTL 选「Ignore cache-control header and use this TTL = 1 分钟」(覆盖源站发的 no-store)+ serve-stale-while-revalidating;带 `session`/`refreshToken` cookie(登录用户)或 `rsc` header(客户端导航预取)的请求 bypass。
- **实测**:匿名首次 MISS → 之后 HIT、TTFB **1.6s → 0.12s**;登录用户 / RSC 请求全 `DYNAMIC`(拿个性化、不吃缓存);匿名 HTML 只有骨架、无登录 CTA、无用户数据。tsc / eslint / 生产构建(`/` 仍 `ƒ Dynamic`)/ `useAuthGatedList` 单测(7)全过。部署 prod + CF 规则上线 + 四项 live 验证通过。(PR #45,`5484478`)

---

## [3.2.1] - 2026-06-03

### 修复 — 公开页用户名 %20 · 主页背景显示错番

3.2.0 上线后用户报的两个 profile 页 bug，均已修复部署。

- **公开页用户名显示成 `the%20creator`（含空格用户名）+ 粉丝/关注页 404** — 带空格的用户名（如 "the creator"）在路由里是 `the%20creator`，**Next 16 不解码动态路由参数**，所以公开页 `/u/[username]` 直接把编码后的串当名字渲染（大标题 + `<title>` 都中招）。更隐蔽的是 followers/following 页对**本就已编码**的 slug 又 `encodeURIComponent` 一次 → `the%2520creator` → 后端解一层得到 `the%20creator`、查无此人 → **404**，整个粉丝/关注页打不开。**修法**：加 `lib/username.decodeUsername`，在每个页面边界把参数解码一次用于**显示**，所有拼 URL/接口的地方统一 `encodeURIComponent`（主页 + 粉丝页 + 关注页 + hero 链接 + 关注按钮 + 关注列表行 + 分享）。原则：代码里用解码后的真实用户名，只在 URL 边界编码。（`2f0aeea`）
- **`/profile` 主页大背景显示的不是所选番剧（退回了第一部番）** — 用户在设置里选了某番当背景，导航右上角 mini-card 显示正确（服务端按真实 anilist id 解析），但主页大背景却是另一部。**根因**：`ProfileClient` 把候选背景列表 `slice(0, 60)` 砍到 60 部，订阅 ≥ 61 部的用户若所选背景排在 60 名之后就 `find` 不到，`ProfileHero` 又错误地退回 `backdropOptions[0]`（用户的**第一部**番）→ 显示错番。导航走的是服务端解析（不砍），于是两边不一致。**修法**：把服务端已解析好的 `backdropBannerUrl`/`backdropCoverUrl`（与导航同源）传进 `ProfileHero` 优先用，彻底绕开 60 上限；去掉"匹配不到就用第一部"这个错误兜底（只在用户**没选**背景时才用第一部当自动默认）。同样的"选了但不在列表里 → 回退默认而非乱显示"守卫也加到了公开页 hero。纯前端改动，live 域名验证（标题/h1 解码、粉丝关注页 200）。

---

## [3.2.0] - 2026-06-03

### 会员通行证 · 个人设置页 · 头像文件存储

会员主页升级成「会员通行证」，新增标准设置页，头像改文件存储 + 全站统一。一次大特性（PR #29），含上线前一轮 `/qa` 多代理审查的加固。

- **会员通行证（`/profile` + 公开 `/u/[username]`）** — 个人页做成电影感的「会员通行证」卡（把 Pokémon TCG 全息倾斜光泽引擎移植成 React hook），带专属编号 `#AGC-xxxxxx`、入会年份、状态徽章。卡面 + 背景都从 DB 读（`avatar_url` / `backdrop_anilist_id`），所以**别人（含匿名访客）也能看到你选的照片和背景番剧** —— 公开页本就是分享 / SEO 入口。背景用所选番剧的宽幅 banner（非拉伸封面）。
- **`/settings` 设置页（中英双语）** — 改用户名、上传 + 裁剪照片（canvas 前端先缩到 1280px 再 `toDataURL` JPEG q0.9，约 50–120KB）、选背景番剧。**自助改密码移除**（会话被劫时不安全）→ 引导到登录页「忘记密码」走邮箱验证流。右上角实时 mini-card 预览。
- **头像文件存储（go-api `/api/avatars/*`，migration 0012）** — 原方案把头像当 data URL 存进 DB，每条 `/me`/在追/关注/评论行都驮着约 100KB base64、撑爆响应又不可缓存。改为：解码 → 校验 → 重编码 JPEG（剥 EXIF）→ 写命名卷 `{uuid}.jpg` → 存短 URL `/api/avatars/{uuid}.jpg?v={unix}`（`?v=` 变更即刷 CF）；go-api 直接服文件，不动 nginx + CF 源站防火墙。`users` 加 `avatar_url` + `backdrop_anilist_id` 两列。
- **导航方块头像 + 横幅 mini-card** — 导航换 Steam 风方块头像，下拉头部是所选番剧的横幅 mini-card；管理后台入口按 role 门控。
- **全站头像一致** — 在追「X 人在追」、关注列表、评论的头像统一回退链：照片 → 所选番剧封面 → 默认卡图（列表查询 `LEFT JOIN anime_cache` 取背景封面）。
- **订阅状态探测 404 → 200** — 详情页每次都探「是否已订阅」，未订阅返 404 在控制台刷红、像 bug；改为 `200 {"data":null}`（前端读 null 即「可订阅」）。
- **`FallbackImg`** — AniList 封面会轮换 hash 导致 404，加一次性 `onError` 兜底切默认卡图（在追 / 关注 / 评论 / 导航 / 设置都接）。

**上线前 `/qa` 加固**（3 个子代理审 + 浏览器全面 QA，无 CRITICAL）：

- **头像上传抗解压炸弹** — `image.Decode` 在尺寸检查前就分配整张 W×H×4 RGBA，一张小压缩图能撑到几十 MB OOM 容器；改用 `image.DecodeConfig` 先读头部尺寸再拒，不分配像素缓冲。
- **`avatarUrl` 锁死自家 URL** — 默认分支原样存任意 URL → 可被塞外链当全站追踪像素；限定只收 `/api/avatars/` 前缀。
- 容忍换行 base64 + JPEG/PNG 白名单 + 精确错误提示；清头像先清库再删文件（防写失败留悬空指向）。补 `avatars` 包首份测试（含解压炸弹 / 路径穿越）。
- 评论发表 / 删除失败原来静默吞 → 加双语 toast；背景图 `url()` 过 `cssUrl()` 守卫（纵深防御）；头像 `alt` 补用户名（无障碍）。

部署 prod：迁移 0012 已应用、`avatars` 命名卷创建、live 域名验证（`/u`·`/settings`·`/api/avatars`·首页全 2xx）。（#29）

---

## [3.1.5] - 2026-06-03

### 修复 — 详情页 AniList 上游抖动时 500(降级失效)

- **`/anime/{id}` 间歇 Internal Server Error** — go-api 详情接口在缓存过期时会向 AniList 重拉刷新。根因双重:① `refetchFromAniList` 注释声称用 15s 独立超时"不占用 5s 请求预算",但 `context.WithTimeout(parentCtx, 15s)` 会被父 ctx 的 5s `queryTimeout` 截断,重拉实际把 5s 预算吃光;② AniList 上游变慢/失败时重拉耗尽 5s 后失败,本应"返回旧缓存"降级,但紧接着的 `enrichRelations(ctx)` 用的还是那个**已到期的 ctx** → `context deadline exceeded` → 500 `query failed`。于是 AniList 一抖,所有缓存过期的详情页(SEO 主战场)就 500。**修法**:重拉失败时直接用**已读到的旧行**组装返回(关系富化走无 DB 的 `convertRelationsToDetailRelations` 兜底,不再在到期 ctx 上查库),并把旧结果写入缓存——避免上游抖动期一堆过期页各自重试 5s 拖垮 worker 池。go build / vet / `go test ./internal/anime/...` 全过。

---

## [3.1.4] - 2026-06-03

### 修复 — 登录态"假掉线" + 登录后导航不刷新

- **反复"掉登录"被迫重登 / 登录成功后导航栏不变(需手动刷新)** — 两个症状同一处根因:`Navbar.tsx` 的登录态探测。排查实据:go-api 与 next-app 的 `JWT_SECRET` 一致、容器稳定运行 23h 且 0 重启、`proxy.ts`(Next 16 新版 middleware)的服务端 session 刷新在跑 —— **排除密钥失效 / 部署冲 token / middleware 没跑**。真因:① Navbar 用**裸 `fetch("/api/auth/me")`**,15 分钟的 `session`(access token)一过即 401 → 显示未登录,但 7 天的 `refreshToken` 仍有效、裸 fetch 不会刷新 → **假掉线**,用户误以为 JWT 失效而重登;② 探测 effect 依赖为 `[]`,只在挂载跑一次,客户端登录后 `router.refresh()` 不重挂该客户端组件 → 导航卡在未登录、须整页刷新。**修法**(只动 `Navbar.tsx`):`/api/auth/me` 改走 **`authFetch(..., { skipRedirectOnFailure: true })`**(401 自动用 refresh token 刷新重试,session 过期自愈;真过期则渲染匿名、不强跳 /login);effect 依赖改 `[pathname]`,登录跳转后即刻重探、导航实时更新。本地 typecheck + eslint 通过。

---

## [3.1.3] - 2026-06-02

### 详情页 AggregateRating 富媒体修复 — 补 ratingCount

- **GSC 报「应指定 ratingCount 或 reviewCount」(38 个 `/anime/*` 页无效)** — 详情页注入的 `TVSeries` JSON-LD 里的 `AggregateRating` 用的是 AniList `averageScore`,而 Schema.org 要求 `AggregateRating` 必须带评分人数(`ratingCount`/`reviewCount`)。查后端,AniList 这条数据**只存分数、无投票计数**(`go-api` schema 仅 `average_score`),凑不出 count → Google 全部判无效 → 这些页拿不到搜索结果里的⭐星级富媒体(从 5/27 起随新页收录涨到 38)。**修法**:改用 Bangumi 的 `score + votes`(`bangumi_votes` ← `Subject.Rating.Count`,有真实计数),且与详情页本就可见的「★ x.x (n)」徽章一致(满足 Google「结构化数据须与可见内容一致」)。门槛 `bangumiVotes > 0` 才输出 rating;无 Bangumi 票数的冷门页不输出评分(AniList-only 本就无效,输出也是白报错)。被收录的多为高分热门番、Bangumi 票数齐全,覆盖面几乎不受影响。部署后在 GSC 点「验证修正情况」清掉 38。

---

## [3.1.2] - 2026-06-02

### 全屏字幕闪修复 · 品牌名统一 AnimeGoClub · 详情页按钮解 gate · CI 清理

3.1.1 之后陆续合并的几项,本批一起记录。

- **播放器全屏字幕闪(控制条 / 弹幕热图)** — 全屏下每当字幕换行,底部控件 + 弹幕热图跟着闪一下(每句对白一次、暂停即停、关字幕也闪因为轨 still `hidden`、窗口模式太小看不出)。**真根因(live 调试定位)**:Chrome 原生全屏把 `<video>` 提到**硬件 overlay 平面**(位于 DOM 之下、独立扫描输出),而字幕/控件/热图是 DOM 层合在上面;每次 `cuechange` 重写 `.art-subtitle` innerHTML → overlay 平面 ↔ DOM 层重新合成不同步 → 那块闪。**先试的「提 DOM 合成层」(给 `.art-subtitle`/`.art-control-heatmap` `translateZ(0)`)只减轻没根除** —— 不同步在「overlay vs DOM」之间、不在 DOM 内部。**根治(#24)**:全屏时给 `<video>` 加 `filter: opacity(0.999)`,让 Chrome 无法把它放上 overlay 平面 → 视频 + 字幕 + 控件走同一条 GPU 合成路径、保持同步;作用域限定全屏(`.art-fullscreen` / `:fullscreen`),窗口播放保留高效 overlay、不付额外功耗。排错:DevTools `cuechange` 日志定位单条 hidden 字幕轨 + 逐档 live 注入 CSS 验证(提层全失败 → 视频脱离 overlay 成功)。cf. Stremio #644。(#22, #24)
- **品牌名全站统一为 AnimeGoClub** — 之前分裂:首页 title/h1/JSON-LD = AnimeGoClub,其余页 title 后缀 / `og:site_name` / 导航 logo / 页脚 = AnimeGo。AnimeGo 是红海通用词(撞俄罗斯盗版站 AnimeGO.org),animegoclub 与域名一致、独特、能稳排第一;分裂信号拖慢 Google 实体消歧。统一搜索信号 + 视觉字标为 AnimeGoClub(title 模板 / 各页 og:site_name / titleDefault / 详情页·`/u`·`/search`·`/welcome`·`/calendar`·`/faq` 标题 / navbar / footer / 登录注册页 / spa locales);仅保留 JSON-LD `alternateName` + keywords 里的 "AnimeGo" 当别名。顺带修了 `/calendar`+`/faq` 预存的双品牌标题 bug(plain title 内嵌品牌又被模板追加)。(#21)
- **详情页磁力 / 播放按钮不再 gate 在 AniList 集数** — 之前 `episodes` 为 null(连载中番剧 AniList 不给总集数)就把磁力 + 播放按钮藏了;但种子搜索靠标题、播放器自己选集,与集数无关。改为始终显示。(#19)
- **删废 CI workflow** — `e2e-sandbox.yml` 启的是已退役的 Mongo/Express 老栈(`docker-compose.yml` 早无 mongo),每个 PR ~10 秒就挂、刷失败邮件、给 PR 挂红叉、零信号。删除;保留其余 workflow 的真失败邮件。残留 sandbox 套件(specs / config / globalSetup 的 mongo seed)记 TODO 单独清。(#20)

**已知 / 延后**:`HeatmapTuner` + `VideoPlayer` 设的热图自定义样式 CSS 变量(`--heatmap-h` / `--heatmap-scale-y` / `--heatmap-fill` / `--heatmap-opacity` / `[data-heatmap-always]`)在迁移时整块丢失,仓库里无消费这些变量的规则 —— 热图当前走 artplayer 默认样式(`applyHeatmapPath` 仍直接改 SVG path,形状正常)。与本次闪修复无关,单独一轮重建。

---

## [3.1.1] - 2026-06-01

### 修复 — 播放器字幕缓存 · 详情页 SEO 信号 · 品牌图标 · 标题重复

ISR 上线当天修掉的几个用户可见问题(均已部署上线)。

- **播放器字幕偶尔不出(jassub WASM 缓存)** — 2MB 的 jassub worker WASM(`/jassub/wasm/jassub-worker.wasm`)用 Next `public/` 默认 `max-age=0` 服务,且 Cloudflare 默认不按扩展名缓存 `.wasm`(`Cf-Cache-Status: DYNAMIC`),每次开播放器都从香港源站重下 2MB;慢加载时超过 jassub 10s init 预算 → 字幕暂时不出(控制台 `[jassub] renderer.ready still pending after 10s`)。加 `next.config` `headers()`:`/jassub/:path*` → `max-age=2592000`(30 天,不加 `immutable` 因路径非 content-hash、升级可自愈)。CJK 回退字体走本地系统字体(Font Access API),非网络瓶颈。(#15)
- **假 hreflang** — `/anime/[id]` 的 `generateMetadata` 对外宣称 `?lang=en` 是 en-US 版,但 `getLang()` 恒 `zh`、服务端忽略该 query,实测吐相同中文 → 无效 hreflang + 重复缓存键。删 `alternates.languages`,留 zh canonical,等真有本地化 URL 再加。(#15)
- **详情页匿名守卫** — `/anime/*` 被 CF 边缘缓存,`page.tsx` 顶部加注释守卫:服务端渲染须保持匿名(勿读 `cookies()`/`headers()`/per-user fetch),防未来回归把某用户的个性化页缓存给所有人。(#15)
- **浏览器标签图标** — `app/favicon.ico` 一直是 `create-next-app` 自带的黑底白三角占位图(P3.0 skeleton 起从没换过),品牌角色图只放进了 `public/`(`apple-touch-icon.png` / `favicon-192.png`)。从角色图重新生成 `favicon.ico`(48×48,9.8KB)。(#16)
- **标签标题 AnimeGo 重复** — player/library/admin layout 设 `title: dict.X.pageTitle`,过根模板 `"%s . AnimeGo"`;而 pageTitle 串已含 "— AnimeGo" → 渲成 `我的库 — AnimeGo . AnimeGo`。去掉 pageTitle 里的 "— AnimeGo"(zh + en 共 6 处)。(#17)

**延后(未做)**:① 详情页 soft-404 — `/anime/{坏id}` 因 streaming `loading.tsx` 返 HTTP 200 非 404;Next 自动注入 `noindex` 已护住索引 → LOW,记 TODO(`global-not-found.js` 实验特性对匹配路由内 streaming notFound 大概率无效)。② CF 边缘缓存规则(`/anime/*` HTML + `/jassub/*` WASM)—— 浏览器 / 源站层已修,全球首次加载的边缘层待建,**不急**,完整方案见 `docs/migration/CF-EDGE-CACHE-PLAN.md`。

---

## [3.1.0] - 2026-06-01

### SEO 详情页 ISR — `/anime/[id]` 从动态渲染变静态缓存(前端 islanding,不动 URL)

`/qa` 巡检 + `/seo` 调查发现:标榜 ISR 的 SEO 页其实全是 per-request 动态 SSR(`Cf-Cache-Status: DYNAMIC`,详情页 TTFB ~844ms),根因是 root layout 渲染时读 cookie/header(语言)+ 一个 no-store 的 `/api/auth/me` 取用户,且 `api.ts` 每个服务端 fetch 都转发 cookie —— 全把页面打成动态。经 eng review + 独立 outside-voice 评审,放弃原计划的 `/[lang]/` URL 分段大迁移(省掉几周 + 全站重索引),改用前端 islanding 的便宜路。

`next build` 现在把 `/anime/[id]` 标为 `●`(SSG/ISR),不再是 `ƒ`(Dynamic)。

- **layout / Navbar**:root layout 不再服务端拉 `/api/auth/me`(no-store forcer)—— Navbar(`"use client"`)挂载时自己取用户,gate 在新的 `auth_hint` cookie 上,**登出访客零 auth 请求**;去掉 `x-pathname` header 读取。
- **i18n**:`getLang()` 不再读 cookie/header —— 服务端渲规范默认 `zh`,`lang-client` 客户端读 `lang` cookie 并在 hydration + 切换时 swap(中文优先,zh 为 SEO 索引目标)。
- **api.ts**:`buildHeaders` 加 `auth` 开关(默认 `true` 不变);公开 ISR fetch(`loadDetail`、watchers)走 `auth:false` 跳过 cookie/header。
- **page.tsx**:去掉服务端 `cookies()` 的 loggedIn 读取(ISSUE-001)—— 客户端组件改读 `auth_hint`;加 `generateStaticParams`(Next 16 里 `[param]` 路由需要它才能 ISR)。
- **go-api**:登录/注册/刷新设非 httpOnly `auth_hint=1` 伴随 cookie、登出清除 —— 客户端可读的"会话大概存在"信号,不读服务端 cookie 也能保住 ISSUE-001 的"登出不探测"。

**验证**:`next build` → `/anime/[id]` = `●`;`bun test` 全绿;go-api auth 测试绿;活体实测 —— 登出详情页 auth 请求 0(旧 3 个)+ 控制台 0 错误,语言切换客户端 swap + 跨页持久。EN 详情内容现为 zh(服务端规范默认),chrome + 客户端岛切 EN。`welcome`/`home`/`sitemap` 仍动态(与 main 一致,可同样 `auth:false` follow-up)。

---

## [3.0.0] - 2026-06-01

### 全栈 Go 架构 + 老栈退役 — Express / Mongo / Vite SPA 全部退场

历时 2026-05-10 → 06-01,把 AnimeGo 从 Vite SPA + Express + MongoDB 整体重写为 **Next.js 16 (RSC) + Go + PostgreSQL** 多语言栈,并在 06-01 物理退役全部 legacy 代码。线上 [animegoclub.com](https://animegoclub.com) 已跑新栈(05-31 切换)。

**架构(现 5 个服务)**

- **go-api**(Go 1.26 · chi · pgx v5 · sqlc · river queue · ristretto):48 个 HTTP endpoint 全量重写替代 Express;类型安全 SQL(sqlc);富化走 river 后台队列。
- **next-app**(Next.js 16 App Router / RSC · Bun 构建 · standalone Node):SSR 接管所有用户路由替代 Vite SPA;SEO 关键页(`/anime/:id`、`/seasonal`、`/search`)服务端渲染。
- **ws-server**(独立 Node + socket.io):弹幕实时层从 Express 抽出成独立微服务。
- **postgres**(PostgreSQL 16 · pg_cron · pg_trgm):单一主存储替代 MongoDB;normalize schema + 生成列全文检索 + 弹幕 TTL pg_cron。
- **nginx**:反代 + SSL,源站锁 Cloudflare 段。

**富化匹配重构**

- 老 `selectHit` 抓搜索 `list[0]`、无相似度校验 → ~10% bgm 错绑(错中文名 + 错评分)。重写为**置信度分级匹配**:权威 AniList↔Bangumi id-map(10,972 条)命中直接绑;否则评分模糊匹配(标题相似度 + 年份 + 集数),高置信绑、**低置信宁可不绑、转人工复核**(「宁可显示罗马音也不显示错的中文名」)。
- 富化版本 v0→v1(绑定)→v2(评分 / 角色中文名 / 集数)→v3(中文补救)分阶段链式。dandanplay 作为**离线独立交叉验证 oracle**(不在请求热路径)。
- 一次性 backfill 修正存量 **340 个 id-map 错绑**(One Piece 等被绑到剧场版的全部修回主条目),零 dandanplay 配额。

**老栈退役(2026-06-01)**

- 删除 `app`(Express)+ `mongodb` 服务、`client/`(Vite SPA)+ `server/`(Express)目录、根 Dockerfile + mongo 迁移工具(共 46,002 行删除)。
- mongo 数据 `mongodump` 归档后删容器 + 卷;prod 内存占用降约 900MB。

**其它**

- dandanplay client 加 429 / 5xx 退避(认 Retry-After + full-jitter 指数退避、ctx 可取消)。
- admin 富化表:表头 / 数据列错位修复 + 匹配来源列(id_map / fuzzy_high / fuzzy_low 徽章)+ BGM ID 跳 bgm.tv 链接。
- Auth:JWT access 15m / refresh 14d,httpOnly cookie,SSR 感知刷新。
- 部署:单 `docker compose` 5 服务 + nginx SSL,VPS 在 Cloudflare 后。
- **分支模型**:`main` = 稳定 / 生产(只进测试过的代码,从此部署);`feat/go-backend` = 活跃开发。

---

## [2.0.1] - 2026-05-11

### libass-wasm 字幕渲染 — MKV 内嵌 ASS 不再乱码

v2.0 上线后看 SweetSub 等组的 MKV,底部字幕区出现 `x□34626□ l□(Q□Up□□1...` 二进制乱流。诊断结论:Chrome 把 MKV 容器内嵌的 ASS 字幕轨自动暴露成 `video.textTracks`,自身没有 ASS 渲染能力,把 SimpleBlock 的原始字节当 plain text 喂到 cue 里。本版本接入 [jassub](https://github.com/ThaUnknown/jassub)(libass 的 WebAssembly 端口)做完整 ASS 渲染。

**新增**

- **jassub libass-wasm 集成**: 当 `subtitleType === 'ass'` 时,挂载 jassub canvas overlay 在 `.art-video-player` 容器内,z-index 在 video 之上 / 控制条之下。完整保留 ASS 的样式、定位、Sign 排版、动画。
- **MKV ASS 完整解码**: `mkvSubtitle.worker.js` 现在解 Matroska ContentEncoding zlib 压缩(SweetSub 等组常用),改用 pako 同步 inflate(替代 DecompressionStream 的 per-block async 调用)。2000+ 事件解压从 ~4s 降到 ~500ms。
- **CJK 字体回退**: 通过 `queryLocalFonts()` 加载系统 PingFang SC / Hiragino Sans / Microsoft YaHei / Noto CJK 作为 libass defaultFont。ASS 引用的混淆字体名(`7GYB4TKY`、`GI6JUCQT`、`PJSM6X93` 等 fansub 用的 subsetted Dream Han SC 别名)全部 fallback 到 CJK 系统字体。Chrome 首次需要授权 "本地字体" 权限。
- **VTT plaintext 兜底**: jassub 加载失败(权限拒、WASM init 失败、SAB 不可用)时自动落回 artplayer 原生 VTT plaintext 字幕,用户不至于"完全没字幕"。
- **Native textTrack disable**: 拦截 Chrome 自动暴露的内嵌字幕轨,disable 时排除 artplayer 自己 inject 的 `<track>` 元素(否则它的 `.cues` 会变 null,`Array.from(null)` 崩)。`loadedmetadata` 和 `addtrack` 双事件覆盖异步出现的轨道。

**Performance**

- 第一集冷启动:点播放 → 字幕首帧约 1.5s(其中 MKV 读 + EBML 扫 + pako 解压 ~1s,jassub WASM init ~500ms)。
- 后续切集:~500ms(jassub module + CJK font 已缓存)。
- jassub WASM 资产 ~2MB,player route 才懒加载。

**Cross-origin isolation**

jassub 内部用 emscripten pthread workers,要求 SharedArrayBuffer,只在 cross-origin isolated context 下可用。`client/vite.config.js` server.headers 加:

```
Cross-Origin-Opener-Policy: same-origin
Cross-Origin-Embedder-Policy: credentialless
Cross-Origin-Resource-Policy: same-origin
```

`credentialless` 而非 `require-corp` 因为 AniList / Bangumi 海报 CDN 不发 CORP — `credentialless` 用 no-cors 兜底放行跨域图片。CORP 仍要发(spec:credentialless 不放宽 worker/iframe 子资源)。生产部署 nginx 需要同样的 3 个 header,已加进 `docs/migration/MIGRATION_PLAN.md` § Phase 6。

**资产路径**

- `client/public/jassub/` 由 postinstall 脚本 (`copy:jassub`) 从 `node_modules/jassub/dist/` 拷贝 wasm/worker/default.woff2。原因:Vite 直接 serve `/node_modules/jassub/...` 时给 Content-Type: text/html(SPA fallback),worker 加载失败。
- `mkvSubtitle.worker.js` 通过 `?raw` import + `URL.createObjectURL(blob)` 加载,bypass Vite worker pipeline,避免 COEP 下的 transform URL 拦截。pako 内联到同一个 Blob(~24KB)。
- jassub 主 worker 走 Vite `?worker&url` (`dist/worker/worker.js`),Vite bundle 解析 abslink / lfa-ponyfill 等 bare imports。

**Tests**

1342/1342 client tests / 0 failures。`VideoPlayer.test.jsx` 加 9 个新测试(native textTrack 排除 artplayer track / jassub mount lifecycle / VTT fallback / hide-on-success / destroy-on-unmount)。`resolveSubtitle.test.jsx` 和 `usePlaybackSession.test.jsx` 的 createObjectURL 计数 baseline 更新(blob worker URL + VTT URL 两次创建)。

**Tradeoffs**

- CJK 字体回退要 Chrome `local-fonts` 权限。拒绝则降级到 LiberationSans(纯拉丁),CJK 字符渲染为 □ 但 Sign typesetting 和 Latin 字符正常。
- Firefox 不支持 FSA 持久化 + `queryLocalFonts`,本地媒体库这条线 Firefox 仍是 v2.0 之前的状态(本版本不增强)。
- libass 在 console.debug 级别输出 `JASSUB: fontselect: Using default font family...`,每个 ASS 事件一行。用户侧解法:DevTools Console filter 关掉 Verbose 级别。代码侧尝试过 wrapper worker + 预注册 Style fonts,前者破坏 jassub 初始化时序,后者让 libass 在 wasm 里崩 `null function`,放弃。

**Refs**

- [jassub](https://github.com/ThaUnknown/jassub)
- [WICG Credentiallessness](https://wicg.github.io/credentiallessness/)
- [Chrome dev blog: COEP credentialless](https://developer.chrome.com/blog/coep-credentialless-origin-trial)
- [Matroska ContentEncoding spec](https://www.matroska.org/technical/notes.html)

---

## [2.0.0] - 2026-05-10

### 本地影音库 (P1-P5) — 浏览器里的本地番剧管理 + 播放

把 AnimeGo 从"在线追番清单 + 详情页"扩成"本地番剧库 + 弹幕播放器"。所有解析在浏览器内完成,文件不上传,数据存在 IndexedDB。

**新增**

- **Library 模块**: 本地视频库浏览页 (`/library`),支持多文件夹导入、按系列归类、续看、最近添加。
- **File System Access API 持久化**: 支持跨会话保留文件夹访问授权 (Chrome / Edge / Brave)。Safari 走 drag-and-drop fallback (内存模式)。
- **Import pipeline**: 文件夹枚举 → 文件名解析 → 哈希指纹 (md5) → dandanplay 双通道匹配 (文件名 + 哈希) → 自动归类成 Series / Season / Episode。
- **手动覆盖**: 合并、拆分、重新匹配、改集号四种操作。一次选定,后续导入默认这条规则 (`userOverride` 持久层)。
- **24h 操作日志**: 详情页抽屉式 ops log,展示该系列最近的写库操作,支持撤销。
- **Watch progress**: 续看进度 (`progressRepo`) 跨设备本地存储,从 localStorage 自动迁移。
- **Cinematic library UI**: HUD header (// 02 / LOCAL · MEDIA · LIBRARY //) + 横向滚动行 (// 继续看 // / // 最近添加 //) + 主网格 + 详情 sheet + by-folder 文件树 + 控制室 (filter chips · search · watch rhythm strip)。
- **DropZone 极简空状态**: 单 SVG 六边形 + 类型韵律,替换原"灰底无个性"空态。
- **跨字幕组合并**: 自动检测同 animeId 的重复系列并合并,支持撤销。
- **可用性检测**: 硬盘断连时 OFFLINE / PARTIAL 角标,不影响其他系列。
- **未归类区**: 解析失败的文件单独陈列,支持忽略 / 创建本地系列 / 搜索归番。
- **批量操作**: 长按进入选择模式,支持批量合并 / 删除。
- **重置库**: 一键清空 IDB 全部表 (磁盘文件不动)。

**集成**

- **animes.garden 替换 dmhy RSS**: 磁力资源后端从直连 share.dmhy.org RSS 切到 [api.animes.garden](https://api.animes.garden) 的 JSON API。Garden 已聚合 dmhy + bangumi.moe + 其他源,coverage 严格优于直连。结构化 fansub 名称、KB→MB/GB 自动格式化、provider 透传 (在 source pill 显示为 `via dmhy` / `via moe` 工具提示)。
- **Zero-result tripwire**: garden 返回 200 OK 但 resources 为空时打 `console.warn`,oncall greppable,防止上游 schema 变更静默退化。
- **Dandanplay 富化**: 命中后写 AnimeCache,后台拉 AniList 补齐 titleZh / titleEn / posterUrl。

**Player 增强**

- **手动字幕拾取 + ASS/SRT → VTT 转换**: 浏览器内字幕格式转换。
- **解说轨道检测**: 自动识别 commentary 音轨,路由到 supplementary 通道。
- **弹幕模糊修复**: 关掉 `artplayer-plugin-danmuku` 注入的 `perspective: 500px`,跨 Chrome / Safari 字体清晰。
- **§5.10 视觉 token 集中化**: 整套 oklch 色板 + JetBrains Mono / Sora 字体 + LOCAL 六边形标识从散落到统一 `shared/hud-tokens.js`。

**i18n**

- **库页面中英文切换补全**: 60+ 新 key 覆盖 DropZone / FilterChips / SearchBar / BulkActionToolbar / WatchRhythmStrip / 行标题 / SeriesCard 菜单 / SeriesActionsMenu / HudOverflowMenu。
- `useLang()` 加 `FALLBACK_LANG`,组件级单测无需 wrap LanguageProvider。

**Schema 升级**

- IDB schema v5: 新增 `progress` / `userOverride` / `migrationFailures` / `seasons` / `opsLog` 表。
- 一次性迁移: localStorage 旧进度 → IDB,幂等。

**Tests**

- 1327 client tests / 317 server tests / 0 failures (排除过期 worktree 噪声)。
- 测试覆盖含: episode parser、series matcher、import pipeline、merge/split/rematch ops、buildLibraryMatchResult、SeriesCard 渲染、SeriesGrid 长按选择、TorrentModal 源分布、anime.garden 解析 (含 KB→MB 格式化、provider 透传、malformed JSON graceful fallback)。

**修复**

- `callDandan` null-guard + `useLibrary` 在 React 19 strict-mode 下双调用安全。
- `SeriesCard` 标题裁剪到 2 行避免网格卡片高度抖动。
- 弹弹分隔符容错。

**已知 deferred** (写进 TODO.md)

- 持久化磁力 cache (Redis / 跨进程 cache,缓解 cold-start 风暴)
- enqueueEnrichment 支持 bgmId 键入队
- Library 详情页 / 模态对话框 / Drawer 类的 i18n Phase 2

---

## [1.0.18] - 2026-05-03

### 弹幕滚动模糊修复 — 关掉 plugin 的 perspective:500px

**问题:**

- 弹幕在横向滚动时字体严重模糊,看不清,跨 Chrome / Safari 都有

**根因:**

- `artplayer-plugin-danmuku@5.3.0` 给每条弹幕 `<div>` 设置了 inline `perspective: 500px`,触发 3D 渲染上下文
- 配合 `transform: translateX(...)` 的横向位移,浏览器按 3D 透视矩阵 rasterize 字形,在非整数像素位置产生亚像素模糊 + 抗锯齿失效
- `will-change: transform` 已经把元素提到独立 GPU layer,perspective 是好心办坏事

**修复(`client/src/styles/artplayer-overrides.css`):**

- 覆盖 `.art-video-player .art-danmuku > div`:`perspective: none !important` + `transform-style: flat`
- 加 `-webkit-font-smoothing: antialiased` + `text-rendering: geometricPrecision` 做防御
- 弹幕回到纯 2D 平移(仍是 GPU 加速),滚动中字体保持锐利

---

## [1.0.17] - 2026-04-28

### 弹幕热力图视觉重调 — 缝隙消除 + Tuner/Production 同源

**问题:**

- v1.0.16 上线后,进度条上方的弹幕热力图与进度条之间有明显缝隙,缩放越大缝隙越显眼
- HeatmapTuner 调参面板显示效果与刷新后 production 渲染不一致(峰值高度差异),无法所见即所得

**根因:**

- 插件 `artplayer-plugin-danmuku@5.3.0` 把 `yMax` 硬编码为 128,而单集真实 max bucket count 通常 ~10,导致 peaks 只占 viewBox 顶部 ~8%,scaleY 放大后那 92% 的空白就是看到的"缝隙"
- Tuner 走 `buildHeatmapPath` 默认 yMax=128,Production 之前用动态 yMax + 1.18 headroom,两边算法不同,所见非所得

**修复(`client/src/lib/heatmapPath.js`):**

- 新增 `applyHeatmapPath(art, opts)` 共享 helper:动态 `yMax = peak`(无 headroom),peaks 直达带顶,缝隙归零
- VideoPlayer.jsx 与 HeatmapTuner.jsx 同时调用此 helper — 单一真相来源,Tuner 实时预览即 Production 最终效果

**默认值(design review 收敛):**

- 几何: `sampling=7 / smoothing=0.35 / flattening=0.05 / scale=0.011 / minHeight=4`
- 视觉: `bandHeight=2px / scaleY=17.35 / fillColor=#ffffff / fillOpacity=0.4`
- 行为: `alwaysVisible=true`(`data-heatmap-always="1"`),不再随 chrome 淡入淡出

**清理:**

- HeatmapTuner 调参面板从 PlayerPage 卸下挂载,组件本体保留以便后续微调
- 删除生产代码中过期的 control-fade handler 与 1.18 headroom 常量

**测试:**

- VideoPlayer 单测断言更新到新默认值,删除过期的 fade-with-chrome 测试
- 663/663 通过

---

## [1.0.16] - 2026-04-27

### 切换弹幕时旧弹幕残留 — artplayer-plugin-danmuku 5.3.0 API 误用修复

**问题:**

- 用户进入播放页 → 点 "切换弹幕" 选另一集匹配 → 上方弹幕轨道仍是旧集的内容,新弹幕不显示
- 复现 100%:点击播放后任何 `loadComments` 触发的列表更新都不生效

**根因:**

- `artplayer-plugin-danmuku@5.3.0` 的 `load()` 内部只在**不带参**时才走 reset 分支(清 queue / states / `$danmuku.textContent`),源码 `dist/artplayer-plugin-danmuku.mjs:361`:
  ```js
  if (danmuku === void 0) { this.reset(); this.queue = []; ... }
  ```
- 旧代码 `danmuku.load(danmakuList)` 显式传了参数,等于"追加",老队列从来没清掉
- 另一个隐性 bug:`if (danmuku && danmakuList?.length)` 短路掉了空列表 — 切到 0 弹幕的集数时旧弹幕也保留

**修复(`client/src/components/player/VideoPlayer.jsx:209`):**

```jsx
useEffect(() => {
  const danmuku = artRef.current?.plugins?.artplayerPluginDanmuku;
  if (!danmuku) return;
  danmuku.config({ danmuku: danmakuList || [] });
  danmuku.load();  // 不带参才触发内部 reset
}, [danmakuList]);
```

**测试:**

- VideoPlayer 单测 +3:list→list 切换 / list→空 切换 / null 归一化
- 完整 632/632 通过(顺手把 SEO 改造遗留的 5 个 AnimeCard 测试更新到 `getByRole('link')`)

---

## [1.0.15] - 2026-04-25

### 详情页 halo 客户端兜底采样 — AniList 无 k-means 色的番剧也能拿到真实色身份

**问题:**
- v1.0.13 commit 49b06f8 把 `posterAccent === '#8B5CF6'` 的 halo 渲染显式压住了(品牌紫不当真色用),解决了"假色冒名"问题
- 但 AniList 对一部分番剧不返回 `coverImage.color`(例如 anilistId 179828 カッコウの許嫁 S2),server 回落到品牌紫,detail 页就完全没 halo,海报旁边一片黑
- v1.0.15 给这条路加客户端 canvas 兜底:server 没拿到色时,前端从封面采

**实现:**

- `client/src/utils/oklchAccent.js`:`server/utils/normalizeAccent.js` 的 ESM 镜像,Björn Ottosson OKLab 矩阵 + L 0.56–0.70 / C ≥ 0.11 同款 clamp 带,parity test 走 `createRequire` 直接加载服务端模块,任一侧漂移 CI 就红
- `client/src/utils/sampleCoverAccent.js`:off-DOM `new Image()` + 32×48 canvas downscale + 24-bucket hue 直方图,a/b 笛卡尔累加避开 0/2π 拼接,最大桶 < 12% 视为没主色返回 null。失败路径(CORS/404/decode/灰度/abort)统一返 null,Hero 等价 "no halo"
- `AnimeDetailHero` 状态机重排:`serverAccentHex`(server 真色) → 若无则 `sampledAccent`(客户端采),`effectiveAccent = serverAccent ?? sampledAccent`,`fastHalo` 仅 server 真色路径享有
- `accentCache` 加 `source: 'server' | 'client'` 字段,采样结果写入有 7 天 TTL,重访短路秒回

**调试踩坑(写出来给未来自己):**

- 第一版上线后浏览器 console 报 `CORS policy: No Access-Control-Allow-Origin header`。AniList CDN(s4.anilist.co)实际是 origin-reflective 的:带 `Origin` 头才回 CORS header,不带就不回
- 页面 `<img src>` 先于 sampler 触发,浏览器以非 CORS 模式拿了图并缓存了无 CORS header 的响应。sampler 的 `crossOrigin='anonymous'` 请求复用了同一缓存条目,直接报 CORS 错(根本没到 CDN)
- 修复:sampler 拼 `?accent=1` 走独立缓存条目,触发新请求时浏览器才会送 `Origin`,AniList 才会回 `Access-Control-Allow-Origin`,canvas 不污染。AniList 忽略未知 query 参数,字节相同

**测试:**

- `oklchAccent.test.js` 16 case 客户端/服务端 parity(含真实 AniList 色 `#c19902`)
- `sampleCoverAccent.test.js` 10 case 覆盖 solid / split / 灰度 / 黑 / 白 / SecurityError / load fail / abort 全错误路径
- 完整套件 630/630 通过

**Subagent 参与:**

- planner 给整体规划(三方案选 client canvas)
- code-reviewer 一轮深审给出 3 HIGH + 5 MEDIUM + 4 LOW 清单,HIGH 全修(SecurityError 局部 try/catch / 状态机简化 / `#8B5CF6` 防御性 guard),MEDIUM 选修(parity test loader / cache-bust 注释)

---

## [1.0.14] - 2026-04-25

### `/about` 九章节 HUD 化 · OKLCH 章节色身份 2.0 · 从单色到多色数据流

**背景:**
- v1.0.13 把 /about 立起来,但九个 section 的视觉语汇还不统一:§04 bento 已经 HUD 风,其余八个是杂志排版。"HUD 感"只存在于一张 bento,散到整页像补丁
- 另外一个问题:每个 section 都是"一色到底"。§02 Stats 四个数字全蓝,§03 DataSources 48 个 chip 全琥珀,数据密度再高也一眼过,抓不住注意力
- v1.0.14 做两件事:① HUD 家族扩到 §01–§09 全章节共享基建;② 给颜色单调的密度区"换血",每个单元拿到自己的 hue

**Sprint 1 — 共享基建 + 其余八章节 HUD 升级:**

- `shared/hud.jsx` + `shared/hud-tokens.js` + `shared/motion.css`:SectionNum(右上 §0X 标号)、SectionHeader、ChapterBar(3×52px OKLCH scaleY 入场)、CornerBrackets、`mono`/`label` 令牌、`useCountUp` 滚动计数、共享 keyframes(`hudBlink` 等)
- 八章节逐个重写(每章都走 engineer + design review gate):
  - `§01 Hero` 去 iOS 系统色、HUD CTA 取代蓝色胶囊、海报 bezel + CornerBrackets
  - `§02 Stats` 2-1-1-1 editorial 网格 + 滚动计数
  - `§03 DataSources` SystemNode (live-dot + idx + 名字 + 延迟) + 遥测 footer (计数 48 + 进度条)
  - `§05 PosterIdentity` HUD 化 + OKLCH 5 STOPS 调色板
  - `§06 Differentiator` EP lock console (LOCKED 读出 + 进度条,取代裸 12 格选集)
  - `§07 Danmaku` 3 车道滚动 + 3 条 pinned + LIVE 角标 + 底部密度条
  - `§08 FAQ` [OPEN]/[CLOSE] mono marker 取代旋转 "+"
  - `§09 FinalCta` mask-reveal 标题 + HUD CTA 取代白色大胶囊

**Sprint 2 — OKLCH 调色基建 + gamut 修 + 和声次色:**

三个 subagent 深度分析(editorial 理论 / OKLCH 技术 / 电影造型)给出三条路径,选稳妥路径:

- `hud-tokens.js` 新增 `HUE` / `L` / `C` 令牌矩阵 + `oklchToken(layer, hue, alpha)` 组装器,六级 L 分层 (`trench` / `rail` / `primary` / `readout` / `hot` / `flash`)
- **sRGB gamut 修**:§02 蓝 `C 0.19→0.17`、§07 青 `L 62→68 C 0.19→0.13`、§08 黄绿 `L 62→82 C 0.19→0.16`。之前在 sRGB 空间夹角色,显示器实际渲染不准
- **六章节和声次色**:每个 section 除了主色 (P1) 还有 1-2 个和声伙伴色 (P2/P3),比例 70/25/5:
  - §01 Cold Verdigris 175 (ghost CTA) + Paper Cream 80 (meta 分隔)
  - §02 Deep Indigo 255 (hero 分隔) + Brass Signal 75 (关键数字 dot)
  - §03 Terracotta 25 (chip hover) + Straw 75 (footer 计数)
  - §06 Reticle Lime 100 ([LOCKED] dot) + Gunmetal Whisper 245 (分割线)
  - §07 Chatter Rose 330 (一条 pinned 弹幕) + Mint Spark 95 (密度条右端)
  - §08 Lilac Fog 300 (分隔线 listener)

**Sprint 3 — §02 四色数据流 + §03 48 chips 5 色循环:**

用户测试反馈:"§02/§03 颜色太单调不能吸引注意"。单一主色 + 小面积次色还不够,需要让每个数据单元拿到自己的色身份:

- `§02 Stats` 每个 stat 独立取色,顺便回应下游章节:
  - `LIBRARY` Blue 210 · 数据根基
  - `DANMAKU` Cyan 195 · 回 §07
  - `SOURCES` Amber 40 · 回 §03
  - `DAILY` Chartreuse 85 · 回 §08
  - 一行 Stats 现在变成整页的"目录预告"
- `§03 DataSources` 48 chips 的 dot + latency 循环 5 色 (Amber / Cyan / Magenta / Lime / Violet),每条信源自带色身份,marquee 横滚形成多色节奏,琥珀仍占首位

**验收:**
- 共享基建 + 八章节升级 + 调色 + 多色数据流 = 21 个 commit,每个都过 lint + build green
- 所有 OKLCH 色值 gamut-safe,WCAG AA 对比度保持
- reduced-motion 全尊重(所有 hudBlink / 滚动 marquee / spring 入场都有降级分支)

---

## [1.0.13] - 2026-04-24

### 介绍首页 `/about` — dark editorial × OKLCH 章节色身份,7 张有密度的 bento

**背景：**
- v1.0.12 把"海报色身份"做到了详情页,但站点对外没有一张页面讲"我们到底在做什么"。从 Navbar 点 logo 直接进 HomePage 列表页,新用户没有任何着陆体验
- 要做一张接得住第一眼印象的页面:告诉访客**为什么我们值得收藏**,而不是"又一个 AniList 列表皮"
- 参考锚点锁死在 **Apple TV+ / A24 / Linear / Spotify Year Wrapped** —— dark editorial + 杂志目录感 + 章节化色身份。不做 Discord-泡沫风,也不做 SaaS-card-grid 模板

**Sprint 1 — `/about` 落地(commit `aaca423`):**

- 9 个 section 单文件,纯 inline style 跟现有 pages 约定一致:`HeroSection` / `StatsRow` / `DataSourcesTribute` / `FeaturesBento` / `PosterIdentityShowcase` / `DifferentiatorSection` / `DanmakuInsert` / `FaqSection` / `FinalCta`
- 排版锚:Sora 大字 manifesto + JetBrains Mono 做数据 / 代号 / `§01`-`§08` 杂志目录编号。字体对比度足以让"主张"和"证据"分属两套视觉系统
- **章节色身份系统**:四个 hue(330 粉 / 210 蓝 / 155 绿 / 40 橙)在 StatsRow 竖条、Bento 章节卡、FinalCta 渐变条、FAQ 打开态 bar 四处串联,形成色身份索引。不是装饰渐变,是目录式的编号色
- Hero showcase hue 锁 295,跟 Bento poster 卡 hue 330 解耦,避免同色语义重复
- `Navbar` 加 `/about` 入口,删 `lastY` 死代码;`Footer` 站点列加 "关于" 外链(commit `b7c9ac4`)
- 两轮 engineer review + design review,HIGH 问题全部收掉

**Sprint 2 — §04 bento 重做:从 4 张空旷卡做到 7 张有密度的功能卡(commit `9c5bb7f`):**

v1 的 bento 只有 4 张大卡,用户反馈"太空旷了,看不到到底做了什么"。这个 section 是"我们到底在做什么"的落点,空旷等于沉默。

- 扩到 7 张卡,12 列 asymmetric 网格:hero 带(7+5 + row-span 2)+ 中排(4+4+4)+ 下排(6+6)。每张卡一个 hue,按章节编号递进
- **7 个 feature 一一对应真实产品能力**,不是榜单式罗列:
  1. `§01 海报色身份` —— OKLCH 归一化演示(k-means + Lab → OKLCH),ΔE 对比度条 + AA 安全区标注
  2. `§02 弹幕热力` —— 60 格密度时间线 + 3 个峰值(04:10 / mid / 18:42)+ playhead at 52% + top-3 高频弹幕
  3. `§03 字幕组聚合` —— 3 个种子源(动漫花园 / Nyaa / ACG.RIP)并行抓取 + 4 行字幕组 release(SubsPlease / LoliHouse / 桜都 / VCB-Studio)+ 过滤 tail。**这一栏 v1 曾写成"多源视频流失效切换(AGE动漫/樱花动漫/Bilibili/Bangumi)"—— 本站根本没有这个能力,是 copy 捏造。v2 改成真正写在 `server/controllers/anime.controller.js` 里的 `Promise.allSettled([fetchDmhy, fetchAcgRip, fetchNyaa])`**
  4. `§04 手动选集` —— 3 张 FlowCard(bad → pick → locked)+ handoff 箭头动画 + 周统计条 + kbd 提示
  5. `§05 续播队列` —— next-drop ticker + 3 行带海报色小块 + SVG ProgressRing(r=12, stroke-dasharray, rotate -90)
  6. `§06 周表` —— 7 列每天 + 当日高亮环 + 每日点阵 stack + next-airing 行
  7. `§07 拖拽解析` —— drop zone marching dashes + MKV SVG icon + 解析 input/output 预览 + 接受格式 pill(.mkv .mp4 .webm .avi .srt .ass)

- **动效原则**:compositor-only(`transform` / `opacity`),所有 `@keyframes`(`posterBreathe` / `arrowDotTravel` / `dropMarch` / `featPulse` / `featMarch`)在 `prefers-reduced-motion: reduce` 下全部降级到 none
- 7 张卡 staggered entrance,`viewport={{ once: true }}` 只触发一次,`ease: [0.16, 1, 0.3, 1]` 出场曲线
- Hover:转成 ::before radial gradient spotlight(跟鼠标),章节 bar 拉高 scale

**f1 海报色身份 —— 三张海报真封面 + 吉祥物(也在 `9c5bb7f`):**

v1 的 f1 卡里三张海报是纯渐变 tile + 烫金标题,没有"这是一张真封面"的说服力。第一稿修成从 trending(10)match 标题取封面,但 `药屋` / `败犬` 不稳定在 top-10 里,刷新一次少一张。

- trending 列从 10 拉到 30 增加 hit rate,但仍非保证
- 最终方案:**直抓 AniList ID**,三张海报锁死:`154587`(芙莉莲)/ `161645`(药屋少女的呢喃)/ `171457`(败犬女主太多了)。三个 ID 全部走 `curl` 打 graphql.anilist.co 校验过 romaji 匹配
- `useAnimeDetail(id)` 优先,trending match 保留兜底,两层 fallback 不闪
- `img` 叠在渐变 tile 上 + 底部黑色 gradient 压字,hue 徽章保留(20°/330°/340°)
- **右侧补吉祥物**:`public/mascot-wink.png`(蓝发,比耶,眨眼),`position: absolute; right: 0; bottom: 0`,`height: 480px` 延伸到海报 tile 底线,与下方 INPUT/OUTPUT 规格块顶边紧贴。`< 1180px` 隐藏避免挤占
- 三轮迭代才定位置:top-right 第一版悬空有 gap → 挪进 PosterVisual 的 tile 容器 + `bottom: 0` 锚定才算紧贴

**验证:**
- 客户端 `vite build` — 全绿(989 kB / 293 kB gzip,相比 v1.0.12 略增是 bento 变密 + 吉祥物 PNG)
- 真机肉眼在 1920 / 1440 / 1180 / 720 四档断点验证 bento 布局切换:12 col → 6 col → 1 col
- `prefers-reduced-motion: reduce` 下所有 loop / hover / entrance 降级确认
- 三张海报 AniList ID 用 graphql 单测校对过 romaji,不会拉错番

**教训:**
- **空旷 ≠ 极简**。当 section 的职责是"证明我们做了什么",空旷就是沉默。极简留白要放在那些"观众已经同意你的主张"的地方,不是"你正在招募观众"的地方
- **捏造的 feature 再漂亮也会暴露**。v1 写的"多源视频流失效切换"视觉上很酷,但用户第一眼就指出"我们好像没有这个功能吧你看一下"。写 landing copy 的唯一来源是 `server/controllers/` 里真跑的代码,不是脑补
- trending 数据做视觉锚点要**假设 miss**。一旦依赖"top-N 里一定有 X",就会有刷新闪烁。要么不依赖,要么给 fallback。锁 ID 是最稳方案
- 装饰图的放置没有"一次就对"——第一版 bottom-right、第二版 top-right、第三版嵌进 tile 容器 + bottom 锚定才算贴合。图片不是丢进 `<div>` 就结束,是"相对谁贴紧"的布局题

**配套:**
- `CHANGELOG.md` 本条
- `public/mascot-wink.png`(2.7 MB PNG,透明底)
- landing 全站组件同步迭代:HeroSection / StatsRow / DataSourcesTribute / PosterIdentityShowcase / DifferentiatorSection / DanmakuInsert / FaqSection / FinalCta 均有小改(见 `git diff` stat)

---

## [1.0.12] - 2026-04-22

### 前端设计系统现代化 — Tailwind v4 底座 + shadcn 接入 + OKLCH 海报色身份

**背景：**
- v1.0.11 收尾后一度标了维护模式，但"每个详情页长得一样"这件事越看越不对劲 —— 20 个番放在一起没任何视觉身份，iOS 系统蓝铺满所有 CTA，品牌色 `#8B5CF6` 纸上谈兵从没真的出现
- Arc / Linear / Apple TV+ 的共同做法是**把调色权交给内容**：每个 tab、workspace、影片都有自己的一抹色，交互表面跟着变，装饰底再跟着呼应。AniList GraphQL 本身就返回 `coverImage.color`（每张封面的 k-means 主色），之前在 schema 里却没透传
- 同时前端栈也到了需要刷新的节点：自制 style object 散落在各组件里，没有 design token 中心，也没有 primitive 可复用。Sprint 1/2 一并重铺

**Sprint 1 — Tailwind v4 + token 双向同源（commits `d4c54fa` `cd34e92` `bfe6986`）：**

- 接入 `@tailwindcss/vite` v4，配 `@import "tailwindcss"` 一行起步
- 建立 **token 双向同源**：`client/src/index.css` 里一份 `:root` CSS 变量（color / radius / font / spacing），Tailwind v4 通过 `@theme` 块同步消费，手写 style object 走 `var(--...)`。改一处、两边都跟上
- 清掉 Vite 脚手架残留的 `App.css`
- 写 `CONTRIBUTING.md` 锁死 **JSX-only 政策** —— 本项目不引 TypeScript，新组件一律 `.jsx`；给未来的 agent / 贡献者一个明文约束，免得又回到混合栈的老路
- `tailwindSmoke.test.jsx` 做 CI 级烟雾测试，保证 Tailwind 工具类真的产出样式

**Sprint 2 Step 1 — shadcn JSX 底座（commits `45dd50f` `8220633`）：**

- 由于我们坚持 JSX-only，shadcn CLI 直接不可用。手工搬 `cn` util（`clsx` + `tailwind-merge`）+ `class-variance-authority`，做一版纯 JSX 的 `Button` primitive，variants 抽到独立的 `button-variants.js`
- token 扩充：新增 `--radius-card` / `--radius-chip` / `--space-section`，圆角全局 -2px（偏硬朗一点，离 Discord-泡沫风格远一步）
- `button.test.jsx` 做 primitive 行为锁定

**Sprint 2 Step 2 — 海报色身份链（commits `fe84e21` `93acae4` `6ecc386` `1d3e01d` `20e52b2`）：**

这条链是五个递进的 commit，最后一步才让它真的"可信"。按顺序：

*Step 2a — 服务端透传原始 coverImage.color（`fe84e21`）：*
- `animeDetail.graphql.js` / `seasonalAnime.graphql.js` / `searchAnime.graphql.js` / `weeklySchedule.graphql.js` 四个 query 全部加 `coverImage { color }` 字段
- `anilist.service.js` normalize 层透传到 JSON API，schema 里加 `coverImageColor: String`
- 这一步只给"原料"，不做任何归一化

*Step 2b — 客户端 DetailHero 接入（`93acae4`）：*
- `AnimeDetailHero.jsx` 外层 div 挂 `--poster-accent` / `--poster-accent-rgb` CSS 变量
- banner 渐变顶部染 14%、cover 图 80px `-20px` 扩散光晕
- 第一版铺完发现：**肉眼看不见变化**。AniList 返回的色里～30% 是接近中性的淡奶油色（鬼灭的 `#f1c9ae` 之类），在 14% 不透明度下无限趋近于零

*Step 2c — 强度上调，先让肉眼先看见（`6ecc386`）：*
- banner 14% → 35%、halo 加 border + 60px @ 45%
- 只是 debug 用途：确认管道通了，用户才看见

*Step 2d — Skeleton 预挂色 + 导航 state 传递（`1d3e01d`）：*
- 点卡片进详情页时会"闪一下" —— skeleton 是纯灰，mount 完才变色
- `AnimeCard` / `HeroCarousel` / `RelationSection` / `RecommendationSection` 四个入口全改走 `navigate(path, { state: { posterAccent, posterAccentRgb } })`
- `DetailSkeleton` 从 `useLocation().state` 读同一批值，让 skeleton 就跟详情页同色 —— 点进去视觉上是连续渐变，不是二段跳
- 同步把 Step 2c 的强度回退到保守档

*Step 2e — 服务端 OKLCH 归一化，根治"washed pastel"（`20e52b2`）：*

这是整条链的决定性一步。前面四步都在客户端绕，直到一次三 agent review（engineer / design / art-direction 仲裁）判出：**别在消费端改，去源头归一化**。

- 新增 `server/utils/normalizeAccent.js` ~120 行，零依赖手搓 Björn Ottosson 的 sRGB↔linear↔OKLab↔OKLCH 矩阵。选 OKLCH 是因为它的色度 / 亮度跟人眼感知线性对应，clamp 起来不会让红色突然变粉
- 规则：
  - 色度 chroma 下限 `0.11` —— 低于此的淡奶油色按品牌紫 `#8B5CF6` 兜底
  - 亮度 lightness 夹在 `[0.56, 0.70]` —— 暗的抬、亮的压，保证落在"在黑底上看得清、又不刺眼"的带宽
  - 灰度 / null / malformed / 太短的 hex —— 一律兜底到品牌紫
- 产物字段：`posterAccent`（归一化后的 hex）、`posterAccentRgb`（"R, G, B" 字符串，前端直接塞 `rgba()`）、`posterAccentContrastOnBlack`（WCAG 对比度预计算，给未来 per-title CTA 染色当 gate 用）
- 四个 ingest 点接入：main media / relations / recommendations / weekly schedule
- `AnimeCache.js` 三个 sub-doc（主 + relations + recommendations）都加这三个字段。不做迁移脚本，旧缓存 TTL 24h 自然刷新，空值前端走品牌紫兜底
- 客户端 `hexToRgbCss` util 删干净 —— RGB 预计算已经下沉到服务端

*Step 2f — Banner 保持干净,身份收敛到 cover 框架（`2cbc120`）：*

归一化接通后做视觉复审，发现亮色系 banner（晴空、浅色调封面）上叠海报色顶部渐变会出现两种失败态：normal-mode 高 alpha 会把亮像素压成接近白的 desat 色块；soft-light 混合在 0.85 alpha 又会把整幅云层染成 accent 色（试过两轮都被打回）。

- 参考 Apple TV+ / A24 / Spotify 的做法：**hero 照片保持干净，accent 身份放在交互框架里**
- `AnimeDetailHero.jsx` `bannerOverlay` 恢复纯黑底渐变（顶 40% transparent，底 30%→95% rgba(0,0,0)）
- accent 身份改由三处承载：cover 图 `rgba(accent, 0.55)` 1px 描边 + 双层 boxShadow 扩散光（64px/24px）、Read More 按钮文字走 `var(--poster-accent)`
- `Skeleton.jsx` `DetailSkeleton` 的 bannerBg / coverShadow 同步镜像修改，保证 skeleton → hero 挂载时不跳色
- 在鬼灭、你的谎言在四月、Spy × Family 三种差异化 banner 上肉眼验证通过

**验证：**
- 服务端 `jest` — **307/307** 全绿（含 16 个新 `normalizeAccent` 单测覆盖 null / grayscale / 淡奶油升色 / 饱和红保持 / 暗色抬亮 / 亮色压暗 / idempotent / 大写 hex / 无 `#` 前缀 / 在黑底对比度 > 3）
- 客户端 `vitest` — **604/604** 全绿
- 手工在 AoT、Demon Slayer、One Piece、MyGO 等饱和度差异大的标题上肉眼确认：每个详情页都带上了真正属于自己的那抹色，skeleton ↔ hero 之间不再闪
- 三 agent review 结论：SHIP。两个 MEDIUM issue（sRGB gamut mapping 缺失 + OOG 测试未覆盖）列入后续

**教训：**
- "给前端原料让前端处理" 是本能的直觉，也是这次走弯的起点。AniList 给的色是"每张封面的统计答案"，不是"能直接上墙的设计 token"；前者到后者之间有 ~30% 失败率（淡色、近灰、极暗极亮），只在消费端调不透明度永远修不好长尾
- **归一化应该发生在最上游的那次写入里**：这一层做对了，后面详情页、skeleton、未来的 CTA 染色、hover glow 扩展全部免费受益，都只是读同一个已经保证过下限的字段
- OKLCH > HSL > Lab：HSL 的"亮度"跟人眼感知差得远（纯黄色 HSL 50% 实际看起来亮得刺眼），OKLCH 是目前最能"按设计意图 clamp"的色空间
- CJS 项目要引 OKLCH 不必上 `culori`（ESM-only）—— Ottosson 的矩阵就那几个数字，手搓 60 行更透明、易审、零依赖
- 无 TypeScript 项目引 shadcn 要手工搬：CLI 只认 `.tsx`，但把 `cn` + `cva` 这两个关键 util 手抄过来就够了，剩下的 primitive 自己写比 CLI 产物干净

**配套基建：**
- `CONTRIBUTING.md` 明文锁 JSX-only —— 后续 agent / 贡献者不能偷偷引 `.tsx`
- `jsconfig.json` 改了 path alias，`@/` 指向 `client/src`，跟 shadcn 对齐
- `eslint.config.js` 加 JSX token 相关规则

---

## [1.0.11] - 2026-04-20

### 白屏修复 — shared util 拆成 server/utils + client/utils 两份同步副本

**背景：**
- 上个版本 v1.0.10 为了让服务端 + 客户端共用三级 fallback 逻辑，把 `buildEpisodeMap` 抽到了 `shared/episodeMap.cjs`（CJS 格式）
- `npm run dev` 起来后整站白屏，控制台报 `ReferenceError: module is not defined`
- 根因：Vite dev server 把 `.cjs` 文件按静态资源**原样 serve 给浏览器**，末尾的 `module.exports = { buildEpisodeMap }` 在 ESM 环境里直接炸，整个 bundle 加载失败
- 附带发现：`Dockerfile` 只 `COPY server/`，根本没拷 `shared/` 目录，生产构建启动会 `Cannot find module '../../shared/episodeMap.cjs'`——这条潜在生产 bug 一起修掉

**改动（commit `9badb61`）：**

*新增两份原生格式的副本：*
- `server/utils/episodeMap.js` —— CJS 原生，服务端 `require` 用
- `client/src/utils/episodeMap.js` —— ESM 原生，Vite `import` 用
- 两份文件逻辑字节级一致，开头都有 `⚠️ KEEP IN SYNC with ...` 注释

*更新 4 处 import：*
- `server/services/dandanplay.service.js`：`require('../../shared/episodeMap.cjs')` → `require('../utils/episodeMap')`
- `server/__tests__/episodeMap.test.js`：同上
- `client/src/hooks/useDandanMatch.js`：`import ... from '../../../shared/episodeMap.cjs'` → `import ... from '../utils/episodeMap'`
- `scripts/test-real-files.js`：同步（本地脚本，未纳入提交）

*删除 `shared/episodeMap.cjs` 和 `shared/` 目录。*

**为什么不用其他方案：**
- ❌ 升级 Docker 到 Node 22+（支持 `require(ESM)`）—— 影响面太大，生产 Node 20 稳定
- ❌ 动态 `import()` 包装 —— 给所有调用点加 `async`，侵入性强
- ❌ UMD wrapper —— 复杂度换取 40 行重复的可读性不划算
- ✅ 双份副本 + sync 注释 —— 40 行代码、零运行时开销、未来改逻辑两处一起改（本来就不常动）

**验证：**
- 服务端 `jest` — **286/286** 全绿
- 客户端 `vitest` — **594/594** 全绿
- `curl http://localhost:5173/src/utils/episodeMap.js` 正确返回 ESM，末尾无 `module.exports`
- 浏览器热加载不再白屏

**教训：**
- Vite 不做 `.cjs` → ESM 的互操作转换，跨 runtime 共享必须选原生格式
- Docker 多阶段构建 `COPY` 清单要审，否则抽公共目录会在生产静默失败
- "共享代码"在 monorepo 里最安全的做法是 workspace 子包（有自己的 `package.json` + 构建步骤），次选就是这种同步副本。中间的"放一个目录两边都引用"在 CJS/ESM 混合栈里永远有坑

---

## [1.0.10] - 2026-04-20

### 续作季节弹幕映射修复 — loose-title gate + 三级 fallback 提取 shared

**背景：**
- 用户报告 `[LoliHouse] Oshi no Ko S3 - 11.mkv` 在 dandanplay 里**没有弹幕，手动选择也没有**
- 实测发现是两个耦合的静默失败：
  1. **服务端 Phase 1 闸门过严** — 字幕组新番 raw 未入 dandanplay hash 索引时，API 返回 `isMatched:false` 但候选 `animeTitle: "【我推的孩子】 第三季"` 其实宽松匹配用户 keyword。旧闸门 `if (combined?.isMatched)` 直接丢弃，`/match` 响应 `matched:false`，客户端跳转"手动选择"
  2. **客户端 `selectManual` 只做纯 number 匹配** — 续作季 dandanplay 把 raw number 编成 `25..35` + `C1/C2/C3` OP/ED specials。用户文件名解析出 `epNum=11`，level-1 的 `episodes.find(e => e.number === 11)` 永远返回 undefined，手动选完 episodeMap 空 → 无弹幕
- 这个 bug 对所有 S2/S3 续作（芙莉蓮 第二季 raw 29..38、DanDaDan 第二季 raw 13..24 等）都潜在，只是 Oshi no Ko S3 因为 C1/C2/C3 specials 打乱数组下标让 Naive index fallback 也失效，才暴露得最彻底

**改动（commit `197a332`）：**

*新增 `shared/episodeMap.cjs` — 三级 fallback util，客户端 + 服务端共用：*
```js
// level 1: number === epNum
// level 2: rawEpisodeNumber 匹配 /^[OS](\d+)$/  (OVA/Special)
// level 3: pool[epNum - 1]，pool = 只含 /^\d+$/ 的 raw 过滤后数组
```
- level-3 关键细节：过滤到纯数字 `rawEpisodeNumber` 的池子里回退，C1/C2/C3 specials 不进池，避免 E12 落到 C1 Opening
- `.cjs` 扩展名同时兼容服务端 CJS `require` 与 Vite 客户端 ESM `import`

*`server/services/dandanplay.service.js`：*
- 原 46 行内联 `buildEpisodeMap` 删除，改从 `../../shared/episodeMap.cjs` 引入
- `module.exports` 继续 re-export，服务端下游代码零改动

*`server/controllers/dandanplay.controller.js`：*
- 新增 `normalizeTitle(s)` — 小写化 + 剥掉 `[] 【】 ()《》「」『』 空格 标点`
- 新增 `titleLooselyMatchesKeyword(animeTitle, keyword)` — 归一化后双向 `includes`
- Phase 1 闸门放宽：
  ```js
  const accept = combined && (
    combined.isMatched ||
    (combined.animeId && titleLooselyMatchesKeyword(combined.animeTitle, keyword))
  );
  if (accept) { ... }
  ```
- 对新番 fansub（hash 没入索引但候选标题对）的场景首次可用

*`client/src/hooks/useDandanMatch.js`：*
- `selectManual` 里内联的 `epData.episodes.forEach(ep => { if (requested.includes(ep.number)) map[...] })` 替换为 `buildEpisodeMap(epData.episodes, episodes)`
- 手动选择路径首次获得 OVA 前缀 + index fallback 能力

**覆盖：**

*`server/__tests__/episodeMap.test.js`（新）：*
- level 1 纯 number / level 2 OVA 前缀 / level 3 index fallback 单元测试
- **Oshi no Ko S3 fixture**（raw `25..35` + `C1/C2/C3`）：
  - E11 → `9035` 第35话 ✓
  - E12 → 不能落到 `8001/8002/8003`（C-specials）✓
  - 批量 `[1..11]` 全部映射 ✓
- 混合 level-1 + level-3 partial hit 测试

*`server/__tests__/dandanplay.controller.test.js`（扩展）：*
- Phase 1 闸门 4 个 regression：isMatched:false + 标题匹配 → 接受；不匹配 → 拒绝；bracket/space 归一化；isMatched:true 依然生效

*`client/src/__tests__/useDandanMatch.test.jsx`（扩展）：*
- `selectManual` Oshi no Ko S3 E11 → `9035`（不是 `8001` C1 Opening）

**E2E 真实文件验证（`scripts/test-real-files.js`）：**
- 8 个真实 fansub 文件（Enen S3 / Kakkou S2 / Yofukashi S2 / Frieren 第二季 [29-38] / DanDaDan S2 / Sono Bisque S2 / Kaoru Hana / Oshi no Ko S3）计算首 16MB MD5，打 `/api/v2/match`，应用闸门 + `buildEpisodeMap` → 8/8 管道全过
- Oshi no Ko S3 manual pick（animeId=18901，raw `25..C3`）：
  ```
  ep  1 → 189010001  第25话 投入
  ep  2 → 189010002  第26话 盘算
  ...
  ep 11 → 189010011  第35话 那就是一切的开端   ← 原 bug 正确映射
  ```

**验证：**
- 服务端 `jest` — 32 suites, **286/286**
- 客户端 `vitest` — 71 files, **594/594**
- Oshi no Ko S3 E11 手动选择端到端可用

**关键点 / 附带发现：**
- dandanplay 对 Oshi no Ko S3 E11 的 hash 索引**错挂到第二季**（id=18086）—— 自动匹配返回 isMatched:true 但 animeTitle 是"我推的孩子 第二季"。这是 dandanplay 数据问题，不是我们的 bug；用户看到的"有弹幕但是上一季第22话"就是这个原因。手动重选 S3（id=18901）走修好的 `selectManual` 路径现在能映射正确
- 三级 fallback 是保守设计：只在纯 number raw 池里做 index 回退，绝不会把特别篇/OP/ED 当正片。宁可映射失败也不乱映射
- 抽 shared util 不只是 DRY — 旧客户端 `selectManual` 里的是 **degraded 版** 逻辑（只做 level 1），服务端是完整三级。统一到一处后两边行为对齐，以后只有一处要改

---

## [1.0.9] - 2026-04-19

### SEO 品牌统一 — AnimeGo → AnimeGoClub + Organization schema

**背景：**
- Google 搜 `animegoclub`、`animego`、`animegoclub 番剧` 主页都排不到前几，搜索结果还出现 "Did you mean: animego club" 的拆词提示
- 根因：站内从 `<title>` / `<h1>` / `og:site_name` 到 JSON-LD 一律自称 `AnimeGo`，域名 `animegoclub.com` 的品牌信号从未在正文落地
- `AnimeGoClub` 只在 `WebSite` schema 的 `alternateName` 出现过一次，权重不足以让 Google 把它固化成独占 token
- 搜 `animego` 时前两位是 animego.com.au（Adelaide 线下节）和 animego.net（老牌流媒体），这条战场短期内抢不下来；唯一可独占的品牌词只有 `animegoclub`

**改动（commit `738b91e`）：**

*`server/middleware/ogTags.js`：*
- `SITE_NAME`: `'AnimeGo'` → `'AnimeGoClub'`
- 新增模块级 `ORG_JSON_LD` 常量：`Organization` schema，logo 指向 `/favicon-192.png`
- 主页 title：`AnimeGoClub · 番剧追番 · 动漫发现 · 新番评分`
- 主页 H1：`AnimeGoClub · 番剧追番与动漫发现平台`
- 主页新增 `<meta name="keywords">`：`AnimeGoClub,animegoclub,番剧,追番,新番,动漫,二次元,弹幕,动画评分`
- 主页正文扩写一段业务描述，让品牌词在可见内容中反复出现（最终 HTML 里 `AnimeGoClub` 字符串出现 14 次）
- 主页 `WebSite` JSON-LD 的 `alternateName` 从单值改为数组 `["AnimeGo", "animegoclub"]`，再独立挂一份 Organization JSON-LD
- 详情页 fallback 描述文案里的 `AnimeGo` 也统一到 `AnimeGoClub`
- 搜索关键词里的"动画" → "番剧"（和 Google 中文用户搜索习惯对齐）

*`client/index.html`（SPA shell 同步）：*
- title / description / keywords 跟服务端保持一致
- JSON-LD `WebSite.name` → `AnimeGoClub`，`alternateName` 改数组
- 额外挂 Organization schema

*`server/__tests__/ogTags.test.js`：*
- 详情页 fallback 断言字符串 `"<title>动画 #99999 - AnimeGo</title>"` → `"...- AnimeGoClub</title>"`

**验证：**
- `npx jest ogTags.test.js sitemap.test.js` — 17 cases 全绿
- 线上部署后 `curl -A "Googlebot" https://animegoclub.com/` 返回新 HTML：title / og:site_name / WebSite+Organization schema 均为 AnimeGoClub
- Google Search Console 已重新提交 sitemap.xml（5003 URLs 已发现）+ 主页请求编入索引

**关键点：**
- 这不是为了抢 `animego` 关键词（对手太强），而是让 Google 把 `animegoclub` 这个**域名专属 token** 完整识别成一个词、并绑定到本站主页
- `AnimeGo` 作为 `alternateName` 保留，不是弃用，而是告诉 Google "两个名字指同一实体"
- Sitelinks（Bilibili 那种"下载"/"GIF 教程"子链接）是 Google 自动生成的，不能通过代码强制触发，只能靠品牌搜索量 + 清晰导航慢慢积累

**预期时间线：**
- 几小时~1 天：sitemap 重新抓取，"上次读取时间"更新
- 2-3 天：主页索引状态刷新
- 1-2 周：搜 `animegoclub` 主页进前几位
- 2-4 周：搜 `animegoclub` 主页稳定第一，`animegoclub 番剧` 等长尾品牌词也指向主页

---

## [1.0.8] - 2026-04-19

### PlayerPage 状态机收敛 — 拆出 usePlaybackSession + resolveSubtitle，补全 DanmakuPicker 测试

**背景：**
- `PlayerPage.jsx` 419 行,7 块职责耦合在一个组件里:文件投递、dandanplay 三段匹配、弹幕加载、**播放会话(playingFile/Ep/videoUrl/subtitleUrl 等 6 个 useState)**、**字幕来源仲裁(外挂 vs MKV embedded worker)**、**MKV blob 生命周期**、DanmakuPicker 模态触发
- 4/5/6 块是同一个会话生命周期的三个面向,散在 6 个 useState + 1 个 ref + 3 个 effect 里。每次切集都要手动同步,漏一个就泄漏 blob 或残留旧字幕
- 隐藏 race:切集时旧 mkv.worker 只在自己回包/onerror 时才 terminate,30s timeout 也只 terminate 不 cancel onmessage —— 用户连续切集,前一集的 worker 5s 后回包污染当前集字幕

**重构(commit `f0e04db`):**

*Step 1 — 抽 `client/src/hooks/usePlaybackSession.js` (118 行):*
- 内部收敛 6 个 useState (playingFile/Ep/videoUrl/subtitleUrl/subtitleType/subtitleContent) + mkvBlobUrlRef + subtitleTaskRef
- 暴露 `play(fileItem, episodeMap)` / `back()`,对 `useDandanMatch` 只读 (读 episodeMap,不回调改 MM.phase)
- **stale-task guard**:`subtitleTaskRef !== sub.task` 时直接 revoke late blob,不污染当前集状态
- unmount cleanup 收敛到一处 useEffect

*Step 2 — 抽 `client/src/utils/resolveSubtitle.js` (81 行):*
- 纯函数返回 `{ kind: 'sync' | 'none' | 'mkv', state? | task? }`
- mkv 路径返回 `{ promise, cancel }` task 对象,30s timeout 由 utility 自管
- **defensive nullify**:`finish()` 内部先 `worker.onmessage = null; worker.onerror = null` 再 terminate,关掉 worker.terminate 自身的 race window
- 字幕仲裁逻辑从 PlayerPage 内联 worker 启动剥离,可独立单元测试

*PlayerPage 收尾:*
- 419 → 354 行,删 6 个 useState + 1 个 ref + 1 个 cleanup effect
- `handlePlay` 缩成 `startPlayback(fileItem, matchResult?.episodeMap)`
- `handleBackToList` 缩成 `stopPlayback()`
- `uiPhase = playbackPhase === 'playing' ? 'playing' : phase` 派生公式不变,UI 渲染零回归

**测试(7 + 8 = 15 cases,全绿):**
- `usePlaybackSession.test.jsx` — phase 初始态、play/back blob 回收、切集 terminate 旧 worker + 忽略 late 响应、unmount cleanup、external subtitle 短路、null episodeMap 边界
- `resolveSubtitle.test.jsx` — sync/none/mkv 三路分支、worker error → null、no result → null、cancel terminate + nullify、30s timeout via `vi.useFakeTimers`
- 调测过程中发现并修了一处真实 bug:test 的 `__respond` 在 cancel 之后还能触发原 onmessage,因为 MockWorker.terminate 不会 nullify handler。defensive nullify 同时解决了真实代码的同名 race

**DanmakuPicker 测试补全(commit `8cb2033`):**

`DanmakuPicker.jsx` (359 行) 此前 0 测试,补 `DanmakuPicker.test.jsx` 32 cases / 7 组:
- closed state — `isOpen=false` 不挂载、不发请求
- header & dismiss — ✕ / overlay / ESC / 关闭后 listener 不残留(防内存泄漏)
- tabs — current 与 search 切换,无 currentAnime 时 current tab 隐藏
- auto-load — 打开时自动 loadEpisodes、defaultKeyword → titleNative → titleRomaji → titleChinese 回退链、重开时 selected/pickedAnime 全部 reset
- current anime episode list — EP 补零、rawEpisodeNumber 回退、currentMatch 标记、loading/empty/error 三态
- search 流 — 按钮 + Enter 触发、whitespace-only 不发请求、结果渲染、无结果空态、API 抛错回退空数组
- pick → load picked anime episodes → ← 返回搜索
- onConfirm — current 路径回 `null` anime,search 路径回 picked anime 对象

**测试模式备忘:**
- Worker 测试用 `class MockWorker` 收所有 instance,`__respond({data})` 手动驱动 onmessage,`await act(async () => ...)` 刷 Promise.then 微任务
- Cancellable Promise pattern (`{ promise, cancel }`) 比直接传 `AbortSignal` 更适合 worker 场景:cancel 是同步的,可以原地 settle
- defensive handler nullify 是 worker 重构的标配 —— 不能信任 `terminate()` 单独完成清理

**结果:**
- 客户端:`npx vitest run` 全绿 — **71 files / 593 tests** (相比 v1.0.7 +3 文件 / +47 cases)
- 服务端:`npx jest` 全绿 — **31 files / 273 tests** (无变更)
- 合计:**102 文件 / 866 tests**
- PlayerPage 状态机 churn 来源消除 —— 切集 race + blob 泄漏 + 字幕残留三个面向收敛到 `usePlaybackSession` 一个所有者

**为什么值得记一笔:**
- 真实 race bug 修了:之前用户连切两集,前集 mkv.worker 5s 后回包能污染当前集字幕,这是 production 行为不是 refactor 引入的,借抽 hook 顺手治
- 字幕仲裁纯函数化是 `mkvSubtitle.worker.js` (263 行 0 测试) 后续补覆盖率的入口
- DanmakuPicker 357 行 0 测试 → 32 测试,UI 文案/交互改动现在会被立刻 fail 捕到
- PlayerPage churn 在 v1.0.6 retro 里被点名:本周 7 次改 EpisodeFileList + 10 次改 PlayerPage 大半都是手动同步 6 个 useState。这次重构后,后续切集相关 bug 应该收敛到 `usePlaybackSession` 单点

### 提交记录

- `f0e04db` refactor(player): extract usePlaybackSession + resolveSubtitle from PlayerPage
- `8cb2033` test(player): cover DanmakuPicker — 32 tests across 7 behavioral groups
- 设计文档:`docs/designs/playerPage-state-machine.md` (含 mermaid as-is/to-be 状态机图、不变量清单、YAGNI 边界)

---

## [1.0.7] - 2026-04-18

### 测试覆盖扩张 — 从关键路径走向全栈覆盖

**背景：**
- 此前测试集中在核心 hooks、AuthContext、关键 controller/service/model，大量 UI 组件、pages、schema-only 模型、socket 入口未触达
- 客户端缺少对 home/detail 页次级 section 的渲染断言，dandanplay/播放链路上的小组件（`EpisodeNav`/`MatchProgress`）无 spec
- 服务端 socket.io 的引导路径（cors、auth、每包 token 过期中间件）完全未测

**新增测试文件（20 个）：**

*客户端 `client/src/__tests__/`（19 个，逐个验证通过）：*
- `ContinueWatching.test.jsx` — 登录门、loading/空态、`/anime/:id` 链接、进度条裁剪
- `HeroCarousel.test.jsx` — 空列表、indicator、自动轮播 + 暂停、prev/next 按钮
- `WeeklySchedule.test.jsx` — 日 tab 切换、per-day 计数、score/airing 格式化
- `CharacterSection.test.jsx` / `StaffSection.test.jsx` — 中日英命名策略、CV 区块开关、role 映射
- `RelationSection.test.jsx` / `RecommendationSection.test.jsx` — `ORDER` 排序、titleChinese 偏好、点击/Enter 导航
- `CompletedGems.test.jsx` / `SeasonRankings.test.jsx` — refresh invalidate、episode badge、rank 分段染色
- `AnimeStats.test.jsx` — 计算热门 genre/season、donut 中心总数、空态三路 divider
- `EpisodeNav.test.jsx` / `MatchProgress.test.jsx` — EP 补零、active 禁点、step status 图标映射
- `ForgotPasswordPage.test.jsx` / `ResetPasswordPage.test.jsx` — mismatch 校验、success navigate、服务端 error message 透传
- `SearchPage.test.jsx` / `SeasonPage.test.jsx` — URL param 同步、Show More 增量、genre/format/status 过滤
- `FollowListPage.test.jsx` / `ProfilePage.test.jsx` — 返回导航、tab 切换触发 hook 重调、列表搜索 + 无匹配态

*服务端 `server/__tests__/`（1 个）：*
- `socket.index.test.js` — `Server` 构造 cors 默认/env 注入、`io.use(socketAuth)`、connection handler 注册、per-packet 过期中间件分路径断言（过期 → `auth:expired` + disconnect；有效 → `next()`）

**顺手修复的既有 flaky 测试：**
- `ScrollToTop.test.jsx` 第二集：`setTimeout(navigate)` 未被 `act(...)` 包裹导致 scrollTo 只统计到 1 次，改成 `useEffect` 触发 + `await act(async () => render(...))`，稳定到 2 次
- `useAdmin.test.jsx` 的 `useEnrichmentList` 断言：hook 签名已扩展到 5 个参数，`toHaveBeenCalledWith(1, 'missing', 'naruto')` 不再匹配，补齐 `undefined, undefined`

**测试模式备忘：**
- `ContinueWatching`/`HeroCarousel` 等只做渲染/交互断言的组件，全部走 `vi.mock('../context/LanguageContext', ...)` + `vi.mock('../hooks/*')` 隔离外部依赖，不再拉起 `QueryClientProvider` + `LanguageProvider`
- 导航断言统一走 `MemoryRouter` + `<Route path="*" element={<LocationProbe />}>` pattern，比 `mockNavigate` spy 更贴近真实行为
- schema 校验直接用 `Model.schema.indexes()` / `Model.schema.paths`，不连 MongoDB
- socket 测试 mock 掉 `socket.io` 的 `Server`，抓 `on('connection', ...)` 的 handler 手动触发，断言 per-packet 中间件逻辑分支

**结果：**
- 客户端：`npx vitest run` 全绿 — **68 files / 546 tests**
- 服务端：`npx jest` 全绿 — **31 files / 273 tests**
- 合计：**99 文件 / 819 tests**，本 session 期间新增 20 文件、修 2 个 pre-existing flaky

**为什么值得记一笔：**
- 覆盖了用户肉眼能看到的每个主要 UI section（home/detail/profile/search/season/password flow/follow list），对未来视觉或文案改动会立刻被 fail 捕到
- socket 入口有 auth/过期两条分支的 regression net，以后换 JWT 库或调 cors 不会静默出错
- 顺手消除的 flaky 测试避免 CI 间歇性红灯（act warning 下只通过一半）

### 提交记录

- 本次改动限于 `client/src/__tests__/*.test.jsx` 与 `server/__tests__/socket.index.test.js`，无生产代码变更

---

## [1.0.6] - 2026-04-18

### CSP 放行 blob: 让本地视频在生产环境恢复播放

**症状：**
- 生产域名 `animegoclub.com` 上用户拖入本地 mp4/mkv，ArtPlayer 只显示黑幕加播放按钮，点击无响应
- 开发者工具 console 连 4+ 条 CSP 违规：
  - `Connecting to 'blob:https://animegoclub.com/...' violates "connect-src 'self' wss://animegoclub.com"`
  - `Fetch API cannot load blob:... Refused to connect because it violates the document's Content Security Policy`
  - `Creating a worker from 'blob:...' violates "script-src 'self'"`（worker-src 回退到 script-src）
  - 最终 `NotSupportedError: The element has no supported sources`
- 本地 `npm run dev` 正常，只有 nginx 反代后的生产环境触发

**根因：**
- `client/src/hooks/useVideoFiles.js:52` 用 `URL.createObjectURL(file)` 生成 `blob:https://animegoclub.com/...` URL 交给 ArtPlayer 的 `<video>` 元素
- `nginx/default.conf:73` 的 CSP 三处同时拦 blob：
  - `media-src` 未显式声明 → 回退到 `default-src 'self'`，`<video>` 无法加载 blob URL
  - `worker-src` 未显式声明 → 回退到 `script-src 'self'`，ArtPlayer 内部创建的 blob worker 被阻止
  - `connect-src 'self' wss://$host` 无 `blob:`，浏览器内部 Fetch 读 blob 字节流被拒
- 开发环境走 Vite dev server，没挂 nginx 头，所以观察不到

**修复：**
- `nginx/default.conf` CSP 补三条最小授权：
  - 新增 `worker-src 'self' blob:` — 单独声明避免 fallback 到 `script-src`，blob 只允许跑 worker，不打开可执行脚本口子
  - 新增 `media-src 'self' blob:` — 让 `<video>` 加载本地 blob URL
  - `connect-src` 列表插入 `blob:` — 让 Fetch 能读 blob 字节流
- 不改 `script-src 'self'`，保持内联脚本注入面不变
- 部署方式：只改 nginx 配置文件，VPS 上 `git pull && docker compose restart nginx`，无 app 容器重启，无停机

**附带：放行 Cloudflare Web Analytics beacon**
- CF 对走 proxy 的域名自动注入 `https://static.cloudflareinsights.com/beacon.min.js`，被 `script-src 'self'` 拦，Web Analytics 收不到 RUM 数据
- `script-src` 加 `https://static.cloudflareinsights.com`，`connect-src` 加 `https://cloudflareinsights.com`（beacon 上报端点）
- 换来 Core Web Vitals（LCP/INP/CLS）、按国家/设备的访问量、JS 错误率；免 cookie，不影响隐私合规

**安全权衡：**
- `blob:` URL 永远是同源浏览器内存对象，不引入跨域或外部脚本执行风险
- `worker-src 'self' blob:` 精确到 worker 场景，没顺手加进 `script-src`
- Cloudflare 两个域是官方边缘资源，信任边界明确

**验证：**
- 本机 `curl -sI https://animegoclub.com/ | grep -i content-security-policy` 确认线上头已包含 `worker-src 'self' blob:` / `media-src 'self' blob:` / `connect-src 'self' blob: wss://animegoclub.com https://cloudflareinsights.com` / `script-src 'self' https://static.cloudflareinsights.com`
- 实机在生产环境播放 `C1 Opening 1.mkv`，视频正常解码、字幕正常渲染、无 console 错误

### 提交记录

- `3d29f63` fix(nginx): allow blob: in CSP for media/worker/connect
- `4885a7d` fix(nginx): allow Cloudflare Web Analytics beacon in CSP

---

## [1.0.5] - 2026-04-18

### dandanplay 匹配成功后番剧信息富化降级

**症状：**
- 拖入本地文件夹（如 `/Volumes/T7 Shield/.../Akiba Maid Sensou`），dandanplay 成功匹配 7 集弹幕，页面却只显示标题 `秋叶原女仆战争` + 封面
- 评分、题材、工作室、制式、`查看详情` 按钮全部不渲染
- 同样的番剧走「关键字搜索」Phase 2 路径时富化正常，问题只在 Phase 1

**根因：**
- `POST /api/dandanplay/match` 的 Phase 1 combined /match 路径里，富化只用 `dandanplay.animeTitle`（中文）去 `searchAnimeCache` 命中 AnimeCache
- 用户文件夹名通常是英文/罗马音（`Akiba Maid Sensou`），AnimeCache 主键命中字段是 `titleRomaji` / `titleEnglish`，dandanplay 返回的中文 `秋叶原女仆战争` 未必出现在缓存文档里
- 单路径 miss 就直接返回 `siteAnime: null`，前端 `EpisodeFileList.jsx` 的 `{sa && (...)}` 优雅降级成「裸标题+封面」

**修复：**

1. **`findSiteAnime(title, userKeyword)` 3 级降级** — `server/controllers/dandanplay.controller.js` 新增 helper：
   - ① `searchAnimeCache(dandanplay title)` 命中即返回
   - ② 未命中 → `searchAnimeCache(user keyword)` 命中即返回（REGRESSION 修复点）
   - ③ 仍未命中 → `bangumi.fetchBangumiData(title, userKeyword)` 拿 `bgmId` → `AnimeCache.findOne({ bgmId }).lean()`
   - 全部 miss 返回 `null`，`matched:true` 不受影响

2. **bgm.tv 超时 + 异常吞掉** — 第 3 步包在 `Promise.race` 里，`BGM_FALLBACK_TIMEOUT_MS = 2000`，`.catch(() => null)` 兜住任何 `fetchBangumiData` 抛错。bgm.tv 挂 / 5xx / 网络慢 都不会卡 `/match`，最多延迟 2s 返回 `siteAnime: null`

3. **`bangumi.service.js` 导出 `fetchBangumiData`** — 之前是模块私有函数，controller 拿不到，测试也 mock 不了

**测试：**
- 新增 `server/__tests__/dandanplay.controller.test.js`（6 用例）：title 命中 / keyword 降级命中（REGRESSION）/ bgmId 降级命中 / 全 miss 返回 null / `fetchBangumiData` 抛错吞掉 / 永挂 2s 超时
- 实机验证：`Akiba Maid Sensou` 文件夹现在渲染 `★ 7.4 · BGM ★ 6.6 · TV · 已完结 · 12 集 · 秋季 2022 · Action/Comedy/Drama · 查看详情 →`，与 Phase 2 路径一致

**设计文档：** `docs/designs/implementDandanplay.md` 新增「Phase 1 番剧信息富化降级（siteAnime）」小节，记录 3 级降级顺序和超时保护

**已知限制（写入 TODO.md）：** `enqueueEnrichment` 以 `anilistId` 做队列键，bgmId-only 的番剧暂时进不了后台富化。Phase 1 的实时降级已覆盖主要用户路径，后台富化队列的扩展留作 P2 改进

### 提交记录

- `8f12d9c` fix(player): enrich siteAnime when dandanplay title misses AnimeCache

---

## [1.0.4] - 2026-04-17

### 修复 token 过期时 401 级联导致的 React 崩溃

**症状：**
- access token 过期后进入弹幕播放页，控制台连续报 `/api/auth/refresh` 401、两条 `/api/dandanplay/comments/xxx` 401，紧接着 React minified error #300 和 #520，整棵树白屏
- 未登录用户打开播放页也触发同一链路（弹弹 play 数据是公开的，不该绑登录）

**根因链：**
1. access token 过期 → `comments` 请求 401
2. axios 响应拦截器调用 `/auth/refresh` → refresh cookie 也已过期，再 401
3. 同步栈内 `setAccessToken(null)` + `dispatchEvent('auth:expired')` + `Promise.reject`
4. `AuthContext` 监听器同步 `setUser(null)`
5. `ProtectedRoute` 看到 `!user` 立即 `<Navigate to="/login">`，`PlayerPage` 开始卸载
6. 此时 `useDandanComments` 的 catch 分支还在跑 `setState`，对一个正在卸载的组件写状态 → React #300（render 期间 update 另一个组件）
7. 另一条 pending 请求同步走完 catch，触发 #520 包裹错误

**修复（方案 D 全量落地）：**

1. **公开端点去掉 auth gate** — `server/routes/dandanplay.routes.js` 四个路由（`match` / `search` / `comments/:episodeId` / `episodes/:animeId`）是弹弹 play 的透传代理，本身无用户态，不需要登录。移除 `authenticateToken`，游客也能看弹幕。IP 级 `apiLimiter`（300 req/min）已经兜底抗刷。

2. **`auth:expired` 派发延后到 microtask** — `client/src/api/axiosClient.js` 的 refresh 失败分支，把 `window.dispatchEvent(new CustomEvent('auth:expired'))` 包进 `queueMicrotask(() => ...)`。`setUser(null)` 不再发生在 axios reject 的同步栈里，卸载路由的时机也就不会撞到仍 pending 的 setState。

3. **`useIsMounted` + 异步 setState 护栏** — 新增 `client/src/hooks/useIsMounted.js`（useRef + useEffect 清理）。`useDandanComments` 和 `useDandanMatch` 所有 await 点后、catch/finally 里的 `setState` 调用前，先 `if (!mounted.current) return;`。组件卸载后 async 回调静默退出，不再触发警告。

4. **401 静默处理** — 两个 hook 的 catch 里，遇到 `err.response?.status === 401` 时重置 state 并 `return`，不把 401 当错误往 UI 抛。401 的登出流程由全局 `auth:expired` 事件统一处理，hook 自己不该重复报。

**测试：**
- 新增 `client/src/__tests__/useDandanComments.test.jsx`（6 用例）：happy path、静默 401、非 401 错误仍冒泡、卸载后 pending resolve 不 setState、`clearComments` 重置、falsy episodeId 短路
- 新增 `client/src/__tests__/axiosClient.test.js`（3 用例）：用 `vi.hoisted` 捕获拦截器 handler，验证 microtask 延迟派发、非 401 直通、refresh 端点自身 401 不再递归

### 提交记录

- `c58e09e` fix(player): stop 401 cascade from crashing react tree on token expiry

---

## [1.0.3] - 2026-04-17

### 播放器字幕大小与位置滑块

**动机：**
- MKV 内嵌字幕默认 20px，在 1080p 以上分辨率上偏小，用户反馈看不清
- 字幕默认紧贴进度条（`bottom: ~10px`），被控制条和进度条遮挡

**实现：**
- Artplayer 齿轮菜单新增两项原生 `range` 滑块（`settings[]` 内置 API，零新依赖）：
  - **字幕大小**：14-48px，步长 2，默认 20
  - **字幕位置**：距底部 10-200px，步长 5，默认 60（把字幕抬离进度条）
- tooltip 实时跟随滑块当前值（`"28px"` / `"120px"`）
- `localStorage` 持久化（key：`animego:subtitleFontSize` / `animego:subtitleOffset`），跨集、跨会话保持
- 读取时 `clamp(value, min, max)`，历史越界数据自动拉回合法区间，不崩
- 换集时 `art.subtitle.switch(url, { style: ... })` 重新应用当前字号和位置，避免 Artplayer 切字幕轨重置 style

**重构：**
- 提取 `readNumberPref` / `writeNumberPref` 通用 localStorage 访问函数，字号和位置共用
- `VideoPlayer.jsx` 新增 `subtitleOffsetRef`，与 `subtitleSizeRef` 并列

**测试：**
- 新增 `client/src/__tests__/VideoPlayer.test.jsx`（6 个用例）
- 覆盖：默认值、localStorage 读写、越界 clamp、slider onChange、换集保持
- mock Artplayer 构造函数捕获 config，断言 `subtitle.style` 和 `settings[].range`

### 提交记录

待提交。相关文件：
- `client/src/components/player/VideoPlayer.jsx`
- `client/src/__tests__/VideoPlayer.test.jsx`

---

## [1.0.2] - 2026-04-17

### 番剧详情页新增弹幕播放入口

**「▶ 弹幕播放」按钮：**
- 番剧详情页在「磁力资源」旁新增播放入口，点击在新标签页打开本地弹幕播放器
- 文案从通用「播放」改为「▶ 弹幕播放」，明确与 VLC 等通用播放器的差异（弹幕是核心卖点）
- `window.open('/player', '_blank', 'noopener,noreferrer')`：新标签页隔离，防止 `window.opener` 反向访问详情页
- `aria-label` 包含「在新标签页打开」提示，满足 WCAG 3.2.5（屏幕阅读器点击前可预知行为）

**按钮行视觉与交互统一：**
- 抽出 `OutlineButton` 组件复用给 Share 和 Torrent,新增完整 hover/focus 状态机（边框 65%→90%、文字 60%→92%、微弱背景、`translateY(-1px)` 抬起、灰色 focus ring）
- 所有按钮高度统一到 40px(`padding: '10px 18px'`、`minHeight: 40`),满足 iOS HIG 触控目标
- 主 CTA(蓝色 Play)靠颜色而非尺寸突出,行内节奏均衡,过渡曲线 150ms 一致
- 父容器改用 `gap: 12` 统一水平间距,移除子级 `marginLeft` 避免重复计算导致的节奏不均

**空番剧防御：**
- 未开播番剧(`episodes === 0`)时 Play 和 Torrent 共用条件一起隐藏,避免"播放不了"的孤儿按钮出现在 Share 旁边

**i18n：**
- 新增 `detail.openPlayer` / `detail.openPlayerAria` 中英文键值

### 提交记录

待提交。相关文件：
- `client/src/pages/AnimeDetailPage.jsx`
- `client/src/locales/zh.js`
- `client/src/locales/en.js`

---

## [1.0.1] - 2026-04-17

### 播放器稳定性修复 + DanmakuPicker 统一 + 生产上线

**Hook-order 崩溃修复：**
- 移动端早期 return 移到所有 hooks 之后；窗口缩放跨越 600px 不再抛 "Rendered fewer hooks than expected"
- 通过 state + resize listener 驱动，响应式行为保留

**MKV 字幕 blob 内存泄漏修复：**
- 用 ref 追踪 blob URL，在替换、换集、卸载时统一 `URL.revokeObjectURL`
- 长会话切换多部 MKV 不再持续吃内存

**播放体验增强：**
- 自动连播：`VideoPlayer.onEnded` 自动跳到映射的下一集
- 弹幕加载指示器：顶栏 badge 显示 1-3s fetch 进度，避免用户误以为无弹幕
- 进度记忆：按 animeId + 集数 localStorage 存取 `currentTime`（30 天 TTL，5s 节流，末 10s 不记录以免跳过片尾）

**DanmakuPicker 架构统一：**
- 之前 `EpisodeFileList` 和 `PlayerPage` 各自挂一个 picker，跨列表 ↔ 播放视图时搜索词和 tab 状态丢失
- 现在提升到 `PlayerPage` 单一挂载，由 `pickerEp` state 驱动；`EpisodeFileList` 只发 `onSetDanmaku(epNum)` 事件
- 移除死代码 `useDandanMatch.goManual`（自 `3f2c854` 以来未被使用）

**文档更新：**
- README 重写为 v1.0.0 版本，介绍 dandanplay 播放器能力与致谢
- TODO 待办十五（启动时扫描未富化番剧）标记为 v0.8.0 已完成
- TODO 待办十八（部署后 GSC 操作）标记完成：sitemap 确认、首页 + 2 个详情页请求编入索引、`/anime` 301 双 UA 验证

### 生产部署

- VPS `<OLD_IP>` 更新到 `3415298`
- 三容器（app/nginx/mongodb）健康，season 缓存预热 SPRING 2026 共 89 部
- 生产域名 `https://animegoclub.com/` 返回 200

### 提交记录

| Hash | 描述 |
|------|------|
| `3415298` | refactor(player): unify DanmakuPicker and drop dead goManual export |
| `c1f042e` | docs: mark TODO 15 (sweepUnenriched) as completed |
| `8fc6de5` | fix(player): resolve hook-order crash, blob leak, and add UX polish |
| `1dd5c4e` | docs: rewrite README for v1.0.0 with dandanplay player and credits |

---

## [1.0.0] - 2026-04-17

### 弹幕手动设置 + SEO 修复

**弹幕源手动选择器（DanmakuPicker）：**
- 每一集新增「设置弹幕」入口（💬 按钮），支持在匹配列表和播放状态下随时更换弹幕源
- 双标签模式：「当前番剧」直接切换集数，「搜索其他番剧」可搜索任意动画后选集
- 搜索标签打开时自动用文件解析关键词预搜索，不会出现空白状态
- 选择新番剧后自动传播：后续所有集的弹幕选择器默认使用新番剧的集数列表
- Modal 标题显示当前番剧名，方便辨识
- 键盘支持：ESC 关闭、Tab 导航、Enter 选择

**SEO 修复：**
- 消除重复内容信号：季度页 canonical 加 `?year=&season=` 参数，详情页移除多余 `keywords` meta
- TV 动画启用 `contentRating` 富文本片段（PG-13、R-17+）

**导航栏优化：**
- 播放页滚动 50px 后导航栏自动隐藏，回到顶部才重新出现

### 提交记录

| Hash | 描述 |
|------|------|
| `3fac2b4` | style(player): show anime title in DanmakuPicker modal header |
| `3f2c854` | style(player): improve DanmakuPicker a11y and touch targets |
| `7f635b9` | feat(player): add per-episode danmaku source picker |
| `6c170ef` | fix(seo): eliminate duplicate-content signals and enable TV rating rich snippets |
| `481d573` | style: adjust navbar hide threshold to 50px |
| `c4b590f` | style: navbar only reappears when scrolled to top in player page |

---

## [0.9.6] - 2026-04-15

### 播放器设计审查 + 性能优化

**设计审查修复（/design-review）：**
- 封面图比例从 5:7 修正为 DESIGN.md 规定的 `3:4`，圆角从 10px 修正为 12px（`--radius`）
- 集数列表播放图标响应行悬停，hover 时变蓝（之前写死不变色）
- 所有播放器阶段（拖放/匹配/手动搜索/文件列表/播放中）加 fadeUp 入场动画（300ms）
- DropZone 重设计：emoji 📂 替换为 SVG 播放图标，背景改为 `#1c1c1e` 卡片底色，加边框脉冲空闲动画，宽度从 600px 扩至 720px
- 播放状态顶栏升级：毛玻璃背景（`backdrop-filter: blur`）+ Ghost 返回按钮 + 集标题显示 + 弹幕数 teal pill badge
- 移除播放器下方孤立的弹幕数文字行，整合到顶栏右侧

**导航栏播放适配：**
- 播放页向下滚动超过 50px 时导航栏自动隐藏（`translateY(-100%)`），滚回顶部才重新出现
- 导航栏与播放顶栏之间新增 16px 间距

**换集延迟消除：**
- MKV 内嵌字幕提取从阻塞 `await` 改为后台异步：点击换集后视频立刻播放，字幕提取完成后通过 `art.subtitle.switch()` 动态挂载
- VideoPlayer 的 useEffect 依赖从 `[videoUrl, subtitleUrl, subtitleType, subtitleContent]` 精简为 `[videoUrl]`，字幕变化不再触发播放器销毁重建
- 弹幕 API 请求与视频加载并行，不阻塞播放

### 提交记录

| Hash | 描述 |
|------|------|
| `68b52e7` | perf: eliminate episode switch delay by making MKV subtitle extraction non-blocking |
| `c42646a` | style: navbar auto-hide on scroll in player page, add spacing above play header |
| `1ca0e7d` | style(design): glass header with ghost back btn, episode title, danmaku badge |
| `df811b5` | style(design): redesign DropZone with SVG icon, card bg, pulse animation |
| `8f7b32f` | style(design): add fadeUp entrance animation to all player phases |
| `c1cd15a` | style(design): play icon highlights blue on row hover |
| `03f07e7` | style(design): fix cover aspect ratio to 3/4 and border radius to 12px |

---

## [0.9.5] - 2026-04-15

### 播放器匹配增强 + 站内番剧关联

**站内番剧信息展示：**
- dandanplay 匹配成功后，用番剧名称反查 AnimeCache，在播放页展示站内番剧信息（评分、BGM 评分、类型、状态、季度、制作公司、来源、Genre 标签）
- Phase 1（dandanplay 直接匹配）和 Phase 2（AnimeCache 关键词匹配）均支持
- 手动选择时同样携带站内番剧信息
- 新增「查看详情 →」按钮，新标签页跳转至站内番剧详情页

**续集集数匹配修复：**
- 文件列表始终发送至服务端（含 fileName + episode），不再依赖 hash 计算成功
- 逐文件匹配（`matchUnmappedFiles`）不再要求 fileHash，仅凭文件名即可调用 dandanplay match API
- 解决跨季连续编号（如 ep 13-23）无法匹配 dandanplay 按季编号（1-11）的问题：每个文件独立发送完整文件名，由 dandanplay 服务端识别具体集数

**播放列表改进：**
- 所有视频文件统一显示为可播放行，未匹配弹幕的文件同样可点击播放
- 移除底部「未匹配」文件堆砌区域
- 移除「重新匹配」按钮

---

## [0.9.4] - 2026-04-14

### 本地播放器：弹弹Play 弹幕匹配 + MKV 内嵌字幕

**弹弹Play 匹配流程重构：**
- 匹配改为 dandanplay-vi 的方式：前端预计算所有文件 MD5 hash（前 16MB），一次请求发送 hash + 文件名，不指定 matchMode 让 API 自动决策
- 三级降级匹配：Phase 1 hash+文件名 → Phase 2 AnimeCache 关键词 → Phase 3 逐文件 hash 回退
- 逐文件 hash 回退（`matchUnmappedByHash`）：对 `buildEpisodeMap` 无法按集号映射的集数，逐个用文件 hash 匹配弹弹Play，解决 OVA/特别篇等非标准集号的弹幕匹配
- 新增 `matchCombined()` 服务函数，整合 hash + 文件名发送到 `/api/v2/match`
- hash 回退添加去重检查，避免多集映射到同一个 dandanEpisodeId

**OVA/特别篇集号映射修复：**
- `buildEpisodeMap` 新增 `O{n}`/`S{n}` 匹配：对弹弹Play 集号为 "O1"、"O2"、"S1" 等 OVA/SP 集数，按数字部分映射到用户文件集号
- 保留原始 `rawEpisodeNumber` 字段用于 OVA 匹配
- 修复辉夜大小姐 OVA 两集显示相同标题的问题

**MKV 内嵌字幕提取：**
- 新增 `mkvSubtitle.worker.js`：Web Worker 解析 EBML/Matroska 结构，提取 ASS/SSA/SRT 字幕轨
- 支持 CodecPrivate（ASS 头部 + 样式）+ Cluster 块（Dialogue 行）的完整重建
- ASS 自动转换为 VTT 纯文本版本（`buildVttFromEvents`），去除 ASS 样式标签，通过 Artplayer 原生字幕显示
- 30 秒超时保护，大文件（600MB+）在浏览器中正常工作

**播放器 UI 改进：**
- 集列表每行下方显示弹弹Play 匹配的集标题（加粗）
- 「手动匹配」按钮始终可见，用户随时可重新选择番剧
- 容器宽度、封面尺寸、字体大小整体调大

---

## [0.9.3] - 2026-04-13

### SEO 索引修复

**解决 Google 96% 页面未索引问题（4,832/5,014 页）：**

**丰富爬虫 HTML 内容（核心修复）：**
- `/anime/:id` 页面从 2 行文字扩展为 8 个内容板块：作品信息（类型/集数/状态/首播/时长/原作/制作公司/别名）、标签、双评分（AniList + Bangumi）、角色与声优（最多 12 个）、制作人员（最多 8 个）、关联作品链接、相似推荐链接、剧集列表（最多 50 集）
- 新增中文翻译映射：FORMAT_CN、STATUS_CN、SEASON_CN、SOURCE_CN、RELATION_CN、ROLE_CN
- 目标：修复 1,149 个"已抓取 - 尚未编入索引"页面（Google 因内容太薄拒绝收录）

**Canonical URL 统一：**
- 所有爬虫页面改用固定 `SITE_URL` 而非请求头动态生成的 `base`，避免 http/https 或域名不一致
- 首页 canonical 加尾部斜杠，与 `index.html` 保持一致

**品牌搜索优化：**
- 首页正文加入 "AnimeGoClub (animegoclub.com)" 品牌文案
- 结构化数据添加 `alternateName: "AnimeGoClub"`（首页爬虫 HTML + index.html）
- 解决搜索 "animegoclub" 时首页不在第一位的问题

**robots.txt 完善：**
- 新增 Disallow：`/login`、`/register`、`/forgot-password`、`/reset-password`、`/profile`

**Sitemap 优化：**
- 排序从按时间改为按评分降序，高分动画优先被 Google 爬取
- 动态优先级：评分 80+ → 0.9、60+ → 0.8、40+ → 0.7、其余 → 0.6

**错误处理改进：**
- DB 查询失败时返回有意义的 fallback HTML，而非空 SPA shell（修复 4 个 5xx 错误页面）

---

## [0.9.2] - 2026-04-12

### SEO 增强

**Google 搜索 Favicon 修复：**
- 生成 `favicon.ico`（16/32/48 多尺寸 ICO 格式），解决 Google 搜索结果不显示图标问题
- 新增 `site.webmanifest` 声明图标信息（Google 推荐的图标发现方式）
- index.html 和 ogTags 中间件均添加 favicon.ico 和 manifest 链接

**结构化数据升级：**
- 动画页从通用 `WebSite` 升级为 `TVSeries`/`Movie` 等具体类型
- 添加 `aggregateRating`（评分/满分/评分人数），可触发 Google 星级富摘要
- 添加 `numberOfEpisodes`、`datePublished`、`genre`、`alternateName` 等字段
- 所有页面添加 `BreadcrumbList` 面包屑导航结构化数据

**标题格式优化：**
- 从 `AnimeGo - 动画名` 改为 `动画名 - AnimeGo`，内容优先提高点击率

**安全加固：**
- JSON-LD 输出中 `<` 转义为 `\u003c`，防止 XSS 注入

---

## [0.9.1] - 2026-04-11

### SEO 优化

**修复 Google 搜索结果无图片问题：**
- favicon.png 从 JPEG 转为真 PNG 格式，尺寸从 32x32 升级到 48x48（Google 最低要求）
- 新增 favicon-192.png（192x192，Google 推荐尺寸）
- 创建 og-default.png（1200×630 品牌 OG 图片），修复之前引用不存在文件的问题
- og:image / twitter:image 从相对路径改为绝对 URL
- ogTags 中间件首页/季度/搜索页补上默认 OG 图片（之前传空字符串）

**提升首页 SEO 权重：**
- 首页爬虫 HTML 从 2 行文字扩展为完整页面：导航链接、30 部热门动画（含评分）、15 个类型标签
- 添加 JSON-LD WebSite structured data（含 SearchAction）
- Google 现在能看到丰富的内部链接结构，而非空壳页面

**标题更新：**
- 网站标题改为「AnimeGo - 动漫 · 二次元 · 发现」

---

## [0.9.0] - 2026-04-11

### 追番体验优化

**在追卡片重设计：**
- 标题从卡片底部移入封面图内，使用渐变遮罩（`linear-gradient`）叠加在封面底部
- 进度条移至标题下方，背景改为半透明白色轨道 + 蓝色填充
- 移除卡片背景色和边框，视觉更干净

**自动完成/恢复：**
- 当集数 ≥ 总集数时，自动将状态切换为「已看完」
- 在集数控制上方显示内联完成提示（绿色 `已看完 ✓`），2.5 秒后淡出
- 当「已看完」状态减集数低于总集数时，静默恢复为「在看」

**剧集列表颜色逻辑：**
- 已看完状态：全部集数显示绿色
- 在看状态：已看的集显示绿色，当前集显示蓝色
- 修复最后一集看完后仍显示蓝色的问题

### 缓存一致性修复

**问题：** 管理后台修改中文标题后，本周更新仍显示旧标题。

**根因：** 双层缓存未清除。
- 服务端 `scheduleCache`（内存 Map，30 分钟 TTL）
- 客户端 React Query（`staleTime: 30min`）

**修复：**
- `admin.controller.js`：更新 titleChinese 时调用 `clearScheduleCache()` 清除服务端缓存
- `useAdmin.js`：`useUpdateEnrichment` 的 `onSuccess` 中 `invalidateQueries(['weeklySchedule'])` 清除客户端缓存

### 本周更新设计探索

制作了 7 个独立 HTML demo 评估不同布局方案：
- **A 时间轴流**：按播出时间垂直排列，时间线 + 圆点 + 已播/待播状态
- **B 横向滚动**：按天分列水平滚动，今天列高亮放大
- **C 杂志分栏**：Feature 大图 + Side 卡片 + 紧凑行列表
- **D 泳道视图**：每天一条泳道，卡片横向滚动
- **D2 日历 Bento**：7 列网格，每列一天
- **E 瀑布流卡片墙**：Masonry 布局，混合 tall/wide/square 比例
- **E2 当前设计微调**：保留现有网格，增加已播/待播状态和相对时间

所有 demo 使用数据库真实当季番剧数据（芙莉莲 S2、Re:Zero S4、JOJO 飙马野郎等）。

---

## [0.8.0] - 2026-04-10

### AniList 429 限流修复与历史回填

**问题：** `warmAllSeasons` 全量回填 2014–2026 共 50 个季度时，AniList API 返回 429 Too Many Requests，导致整季数据为空。

**429 自动重试：**
- `queryAniList` 读取 `Retry-After` 响应头，等待指定秒数后自动重试（最多 3 次）
- 每季之间插入 10 秒冷却，避免突发请求触发限流

**并发保护：**
- `warmAllRunning` 互斥锁，防止管理后台触发和定时任务同时运行 `warmAllSeasons`
- 重复调用直接跳过并打印警告

**数据验证：**
- 回填完成后与 AniList API 逐季对比，50 个季度共 5,547 部番剧，100% 匹配

### 可持续的季度更新策略

**问题：** 每年都有新番上架，新季度动态出现，如何保证数据时效性？

**启动 warm：**
- 服务启动时只 warm 当前季度（快速启动，~2 页数据）

**24h 定时刷新：**
- 每 24 小时自动 warm 当前年份所有季度 + 下一季度
- 覆盖场景：季度转换期新番、当前年历史数据更新、提前预热下季预告
- 例：2026 年 10 月（FALL），24h warm 覆盖 WINTER/SPRING/SUMMER/FALL 2026 + WINTER 2027

**cachedAt 过滤移除：**
- 季番列表查询路径 ① 移除 `cachedAt` 新鲜度过滤，避免旧数据被排除导致列表不完整
- 后台 warm 会刷新数据，前端始终展示最新可用数据

### 孤儿 v0 条目恢复

**问题：** 服务器重启后内存中的富化队列清空，导致已入队但未处理的 v0 条目永远卡在 v0。部署后发现 331 个孤儿条目。

**修复：**
- 启动时扫描 `bangumiVersion: 0` 的所有条目，自动重新入队到 Phase 1 管线
- 启动日志输出 `🔧 Re-enqueued N orphaned v0 entries for enrichment`

### 管理后台：Re-enrich 按钮

**功能：** Enrichment Status 进度条的 v0/v1/v2 标签旁新增 Re-enrich 按钮，与已有的 v3 Heal All 功能对齐。

**后端路由：**
- `POST /api/admin/enrichment/re-enrich?version=0|1|2`
- v0 → 重新入队 Phase 1（搜索 Bangumi）
- v1 → 重新入队 Phase 4（评分 + 角色 + 集数）
- v2 → 有 bgmId 的入队 V3 Heal；无 bgmId 的直接升版本到 v3（Bangumi 无此番剧，无法 heal）

**v2 无 bgmId 问题：**
- 1,455/1,456 个 v2 条目的 bgmId 为 null（Phase 1 在 Bangumi 搜不到匹配）
- V3 Heal 需要 bgmId 查 name_cn，这些条目无法入队，导致看起来"卡住"
- 修复：Re-enrich v2 时，无 bgmId 条目 `updateMany` 直接升到 v3

### 管理后台：富化表格排序

**功能：** 富化管理列表支持点击表头排序。

**可排序列：**
- AniList ID、标题（titleRomaji）、中文标题（titleChinese）、版本（bangumiVersion）、评分（bangumiScore）

**实现：**
- 后端 `listEnrichment` 接收 `sort` / `order` 参数，白名单校验排序字段
- 前端 `sortField` / `sortOrder` 状态，表头点击切换 asc ↔ desc，带排序方向指示箭头

### 数据架构说明

**全部走 MongoDB 永久层，无独立缓存：**
- `AnimeCache` collection 同时承担缓存和持久存储角色
- AniList 数据通过 `findOneAndUpdate(upsert)` 写入
- Bangumi 富化通过 `updateOne` 在同一条文档上叠加字段和升版本
- 内存中只有调度队列（`enrichMap` / `enrichPhase4Map` / `enrichV3Map`），不存实际数据
- 详情页访问时 TTL 7 天过期刷新，触发缺失字段补富化

### 提交记录

| Hash | 描述 |
|------|------|
| `2ba3b93` | fix: promote v2 entries without bgmId directly to v3 in re-enrich |
| `b7306e5` | feat: add sortable columns to admin enrichment table |
| `b289cfc` | feat: add re-enrich buttons for v0/v1/v2 in admin dashboard |
| `22c9bdb` | fix: re-enqueue orphaned v0 entries on startup after restart |
| `5eac2a2` | fix: 24h warm covers current year + next season for upcoming anime |
| `6a8b935` | fix: remove cachedAt filter from warmed path to prevent stale data exclusion |
| `c5ddff0` | fix: 24h scheduled warm covers all seasons of current year |
| `c9f94ce` | fix: replace startup backfill with 24h scheduled re-warm |
| `256bd8a` | fix: auto-retry on AniList 429 with Retry-After header, add mutex |
| `92c1dde` | fix: add retry with backoff to warmAllSeasons for AniList 429 |
| `d4b1419` | feat: backfill all historical seasons on startup |
| `1eaeda9` | feat: add warm-all-seasons admin endpoint for historical backfill |
| `e7e984b` | fix: resolve season page title flash and perPage cap |

---

## [0.7.2] - 2026-04-09

### 反镜像站防护

**问题发现：**
- Google 搜索 AnimeGo 出现 `bokeba.com` 而非 `animegoclub.com`
- 该站抓取/反代了 AnimeGo 的内容，因域名年龄更久排名更高

**Host 检查中间件：**
- 新增 Host header 白名单校验，拒绝非授权域名的请求（返回 403）
- 白名单默认 `animegoclub.com,localhost`，可通过 `ALLOWED_HOSTS` 环境变量扩展
- 支持子域名匹配（`*.animegoclub.com` 自动通过）

**robots.txt 增强：**
- 新增 `Host: https://animegoclub.com` 指令，声明权威域名

**AnimePGT AI 助手规划：**
- `FUTURE.md` 新增 AI agent chat 完整规划（架构、工具、分阶段实现、模型候选）
- 包含 Codex (GPT-5.4) 可行性审查结果和范围裁剪建议

### 提交记录

| Hash | 描述 |
|------|------|
| `1878940` | fix: block unauthorized host domains and add robots.txt Host directive |
| `83e73f7` | docs: add AnimePGT AI agent chat feature plan |

---

## [0.7.1] - 2026-04-09

### 域名与 SEO 修复

**域名错误修复：**
- `sitemap.js` 默认域名从 `animego.site` 改为 `animegoclub.com`，之前 Google 抓到的全是错误域名
- `index.html` 的 `<link rel="canonical">` 同步修正
- 修复后 Google Search Console 成功读取 sitemap（1,148 个页面）

**Google Search Console 接入：**
- 添加 `/googlec1c1aceafd3279a2.html` 验证路由（Express 路由返回，非静态文件）
- 所有权验证通过，sitemap 提交成功

### 安全加固

**敏感文件清理：**
- 从 GitHub 移除 `CHANGELOG.md`、`CLAUDE.md`、`.claude/`、`scripts/setup.sh`（含 SSH 端口等部署信息）
- `.gitignore` 新增以上路径 + `*.archive`（数据库备份）
- 文件仅从 git tracking 删除，本地保留

### 数据库备份

- 通过 `mongodump --archive` 导出 VPS MongoDB 到本地（2.3MB）
- 当前数据量：1,145 条 animecaches（2.3MB），其余集合合计 < 3KB

### 文档

- README 重写，反映当前全部功能（弹幕、角色、V3 自愈、Docker 部署、SEO）
- 新增 `README.zh.md` 中文版，GitHub 顶部语言切换链接

### 提交记录

| Hash | 描述 |
|------|------|
| `617a810` | chore: ignore database backup archives |
| `d8b035e` | docs: add Chinese README with language toggle |
| `df43631` | docs: rewrite README with current features |
| `404e8dd` | feat: add Google Search Console verification route |
| `a4ff5cc` | fix: update domain from animego.site to animegoclub.com |
| `5039dbe` | chore: remove sensitive files from tracking |

---

## [0.7.0] - 2026-04-09

### V3 中文标题自愈系统

Phase 1-3 搜索 Bangumi 时需要日文名精确匹配才取 `name_cn`，导致大量有 `bgmId` 但无中文标题的番剧。V3 阶段直接用 `bgmId` 调 `/v0/subjects/{bgmId}` 获取 `name_cn`，自动补全。

**富化管线扩展：**
- `fetchBangumiTitleCn(bgmId)` — 直接通过 bgmId 查 Bangumi subject 获取 `name_cn`
- V3 队列系统 — 与 Phase 1-3 / Phase 4 相同的优先队列架构（`enrichV3Map` + `enrichV3Priority`）
- Phase 4 完成后自动入队 — `processPhase4Queue` 检测 `titleChinese == null && bgmId` 后自动推入 V3
- `getAnimeDetail` 缓存命中自愈 — `bangumiVersion === 2 && bgmId && !titleChinese` 时以 priority 入队 V3
- 已有中文标题的条目 — V3 处理时直接升版本号，不发 API 请求

**管理后台进度监控：**
- Enrichment Status 进度条新增 v3 (蓝色 `#5ac8fa`) 分段
- `getQueueStatus()` 返回三个队列实时深度 + V3 批量进度 (`total` / `processed` / `healed` / `paused`)
- **Heal All 按钮** — `POST /api/admin/enrichment/heal-cn` 批量查找 `bgmId != null && bangumiVersion ∈ [2,3) && titleChinese == null` 并入队
- **进度条动态显示** — 条纹流动动画（`linear-gradient 135deg` + `background-position` 循环），V3 运行中 2 秒轮询，百分比实时更新
- **暂停/继续** — `POST heal-cn/pause` / `heal-cn/resume`，暂停时进度条变橙色 + PAUSED 标签
- `VersionBadge` 颜色区分 — v3+ 蓝色、v2 绿色、v1 橙色、v0 红色
- **缺中文标题筛选** — `no-cn` filter，管理员可快速定位缺标题的条目

**TanStack Query v5 兼容修复：**
- `refetchInterval` 回调签名从 `(data)` 修正为 `(query)`，数据通过 `query.state.data` 访问
- 影响 `useAdminStats`、`useAnimeDetail`、`useSeasonalAnime` 三个 hook

### 季度页 Show More

替换分页为 "显示更多" 按钮，一次请求整季全部数据（`perPage=200`），客户端控制可见数量。

- 初始显示 20 部（4 行 × 5 列），每次点击追加 20 部
- 全部展示完后按钮自动消失
- 切换季度/筛选条件时重置为初始数量
- `useSeasonalAnime` 支持 `perPage` 参数

### SEO 基础设施

SPA 应用对搜索引擎不友好（空 `<div id="root">`），补全 SEO 基础：

**`index.html` 增强：**
- `<title>` / `<meta description>` / `theme-color` / `canonical`
- Open Graph + Twitter Card 默认值（首页分享卡片）

**动态 sitemap (`/sitemap.xml`)：**
- 自动包含所有 AnimeCache 番剧详情页 URL（最多 5000 条）
- 带 `lastmod`（来自 `cachedAt`）、`changefreq`、`priority`
- 1 小时内存缓存

**`/robots.txt`：**
- 允许全站爬取，禁止 `/admin` 和 `/api/`
- 指向 sitemap

**ogTags 中间件扩展：**
- 新增匹配 Googlebot / Bingbot / Baiduspider / YandexBot / DuckDuckBot
- 支持 `/`（首页）、`/season`（季度页）、`/search`（搜索页），不再仅限 `/anime/:id`
- 统一 `sendOgHtml` 函数，包含 `<meta description>` + `<link rel="canonical">`

**客户端动态 `document.title`：**
- 详情页：`葬送的芙莉莲 第二季 — AnimeGo`
- 季度页：`2026年春季新番 — AnimeGo`
- 离开页面时恢复默认标题

### 提交记录

| Hash | 描述 |
|------|------|
| *pending* | feat: V3 中文标题自愈 + 管理后台进度监控 + 季度页 Show More + SEO |

---

## [0.6.0] - 2026-04-08

### 香港 VPS 部署上线

**基础设施：**
- Docker 多阶段构建 — Stage 1 编译客户端（Vite build），Stage 2 仅运行 server + 生产依赖
- `docker-compose.yml` 三服务架构：app（Node.js）、mongodb（mongo:7，wiredTiger 缓存限 0.4GB）、nginx（反向代理）
- Nginx 反向代理配置 — WebSocket upgrade（Socket.IO）、Cloudflare 真实 IP 透传（`set_real_ip_from`）
- 自签名 SSL 证书 + Cloudflare Full 模式（443 端口 HTTPS）
- UFW 防火墙（<OLD_SSH_PORT> SSH / 80 HTTP / 443 HTTPS）
- `scripts/setup.sh` 一键初始化 Debian 12（Docker、git、UFW）

**域名：** animegoclub.com — Cloudflare Registrar + DNS（A 记录 proxied）

### 生产环境 Bug 修复

- **Resend API 崩溃** — `new Resend(undefined)` 启动即抛异常，改为条件初始化 + 未配置时静默跳过
- **弹幕发送无响应** — JWT payload 字段为 `userId`，danmaku handler 用了 `socket.user.id`，修正为 `socket.user.userId`
- **弹幕框发完收缩** — `DanmakuOverlay` 在 `items.length === 0` 时 return null 导致容器坍塌，移除提前返回
- **角色/配音中文名未显示** — `warmCurrentSeason` 不抓角色数据，`getAnimeDetail` 缓存命中时空角色被视为新鲜缓存跳过；Phase 4 对已 v2 记录不重试。修复：stale 检查加入 `!characters?.length`，Phase 4 skip 条件加入 `needsCharCn` 检查，缓存命中分支增加角色缺失时重新入队逻辑

### 管理后台增强

- **富化内容手动编辑** — `PATCH /api/admin/enrichment/:anilistId` 支持修改 `titleChinese`、`bgmId`、`bangumiScore`，自动标记 `manually-corrected`
- 管理后台富化表格新增行内编辑 UI（编辑/保存/取消按钮，与用户管理同样交互模式）

### 其他

- 站点标语更新为「Rundle Streetが暮れる。東京が灯る。」
- Footer GitHub 链接指向真实仓库地址

### 提交记录

| Hash | 描述 |
|------|------|
| `b5098dd` | feat: add Docker deployment config for Hong Kong VPS |
| `4f4e78f` | fix: gracefully handle missing Resend API key in production |
| `900ef0b` | fix: add HTTPS (443) to Nginx for Cloudflare Full SSL mode |
| `2466895` | fix: use correct JWT field name (userId) in danmaku handler |
| `5e88820` | fix: keep danmaku overlay area visible when no items are flying |
| `d508656` | fix: re-fetch from AniList when characters missing, re-run Phase 4 for CN names |
| `6e6c65c` | feat: add inline enrichment editing in admin dashboard |
| `bf0823b` | chore: update site tagline and footer GitHub link |

---

## [0.5.0] - 2026-04-07

### 首页内容丰富化

**新增区块：**
- **经典好番推荐 (Completed Gems)** — Bilibili「猜你喜欢」风格，5 列网格，封面全铺 + 底部渐变遮罩叠加标题/类型/评分，"换一批"按钮随机刷新
  - `GET /api/anime/completed-gems?limit=6` — MongoDB `$sample` 随机采样高分完结番（≥75 分）
- **年度评分排行榜 (Yearly Rankings)** — Editorial 紧凑列表，Top 10 按评分降序，Top 3 金色排名数字
  - `GET /api/anime/yearly-top?year=2026&limit=10` — 查 AnimeCache 全年 TV/MOVIE/ONA 按评分排序

**视觉统一：**
- `AnimeGrid` 从 `auto-fill minmax(160px)` 改为固定 5 列网格（900px→3 列，600px→2 列），季度页/搜索页同步生效

**UX 改进：**
- 路由切换自动滚顶 — `ScrollToTop` 组件监听 `pathname` 变化调用 `window.scrollTo(0, 0)`

**首页排列顺序调整：**
Hero → 热追 → 我的在追 → 本周更新 → 经典好番 → 关注动态 → 年度榜

**已创建后删除的功能（代码已清理）：**
- 类型精选 (Genre Spotlight) — 硬编码类型列表不够灵活，待改为动态热门类型后重做
- 制作公司精选 (Studio Spotlight) — 封面比例问题 + 偏高级功能，延后

---

## [0.4.0] - 2026-04-07

### Bug 修复

- **Shimmer 骨架屏动画不可见** — `@keyframes shimmer` 动画 `background-position` 对纯色背景无效，6 处骨架屏全部改为渐变背景 + `backgroundSize: '200% 100%'`（`AnimeDetailHero`, `EpisodeList`, `TrendingSection`, `ActivityFeed`）

### 代码质量

- **Follow controller DRY 重构** — `getFollowers`/`getFollowing` 提取公共函数 `paginateFollows`，消除重复逻辑
- **AnimeDetailHero 样式重构** — 30+ 内联样式提取到模块级 `S` 常量对象，提升可读性
- **新增骨架屏单元测试** — `AnimeDetailHero.test.jsx` 6 个测试覆盖中英文模式、富化中/完成/未富化场景

### 新功能：管理后台

**RBAC 基础设施：**
- `User.role` 字段（enum: `'admin'` / `null`）
- JWT 三路径（register/login/refresh）携带 role 到 accessToken
- `adminAuth` 中间件，非管理员返回 403

**仪表盘概览（`GET /api/admin/stats`）：**
- 8 路并行 `countDocuments` 汇总：用户数、番剧数、追番记录、关注关系、待审查数
- 富化进度条可视化（v0/v1/v2 占比）

**富化管理：**
- `GET /api/admin/enrichment` — 分页列表 + 4 种筛选（全部/需审查/已修正/未富化）+ 搜索（数字匹配 anilistId，文本匹配三语标题）
- `POST /api/admin/enrichment/:id/reset` — 重置 bangumiVersion=0，清除富化字段，优先重新入队
- `POST /api/admin/enrichment/:id/flag` — 设置/清除 adminFlag（needs-review / manually-corrected / null）
- `AnimeCache.adminFlag` 字段 + 索引

**用户管理（CRUD）：**
- `POST /api/admin/users` — 创建用户，用户名/邮箱重复检测返回 409
- `PATCH /api/admin/users/:id` — 编辑用户名/邮箱，重复检测
- `DELETE /api/admin/users/:id` — 删除用户 + 级联清理（Subscription + Follow），禁止自删
- `GET /api/admin/users` — 分页列表 + 搜索 + MongoDB aggregate 批量统计追番数/粉丝数

**前端：**
- `AdminDashboard.jsx` — 三板块 UI：仪表盘概览 / 富化管理（搜索+筛选+分页表格） / 用户管理（创建表单+行内编辑+两步确认删除）
- Navbar 管理员入口（仅 `role === 'admin'` 可见）
- 中英文本地化 ~70 个 key

**测试：**
- `admin.controller.test.js` — 22 个测试覆盖：adminAuth 403、stats、enrichment list/filter/search/reset/flag、user list/create/update/delete/self-delete-prevention/duplicate-detection
- 服务端 124 tests、客户端 95 tests 全部通过

### 提交记录

| Hash | 描述 |
|------|------|
| `f88dcb7` | fix: repair shimmer animations and refactor follow controller + detail hero |
| `af4b41c` | feat: add admin dashboard with enrichment management and user CRUD |

---

## [0.3.0.0] - 2026-04-05

### 方向决策（CEO Review + Office Hours）

- **产品方向待验证** — Phase 1-4 功能完整但零真实用户。通过 CEO Review 确认：在没有用户数据前不做新功能开发，先验证需求
- **核心假设重新定义** — 创始人自我审视后认为产品核心不是"社交平台"，而是"观影伴侣"：看完一集后轻量参与的情绪价值（类似 B 站弹幕体验）
- **dandanplay 集成方向** — dandanplay 有播放+弹幕但无社区，AnimeGo 有社区但无播放。组合可能是差异化方向，决定做独立 PoC 验证

### 计划：10 人验证 + dandanplay PoC

**验证期（2 周）：**
1. 部署现有 Phase 1-4 到 Railway 生产环境
2. 招募 10 个追番朋友实际使用，观察留存和行为
3. dandanplay API 申请 + 独立 HTML PoC（本地视频播放 + 弹幕匹配）
4. 收集反馈数据，决定下一步方向（追番社区 vs 看番平台）

**Pre-deploy checklist：**
- `server/.env` 生产配置（独立 JWT_SECRET、MongoDB Atlas URI）
- `app.set('trust proxy', 1)`（Railway 反向代理 TLS 终止必需，否则 secure cookie 不生效）
- httpOnly cookie secure 标志 + HTTPS
- Express morgan 生产日志开启
- 不开启 Railway autoscaling（单实例 Socket.IO，10 人规模）

**成功标准：** ≥30% 留存（第二周仍在使用）、≥5 条功能反馈、dandanplay API 可行性确认

**失败预案：** 留存不达标 → 转向 dandanplay 集成方向（需独立验证）

### 设计文档

- Office Hours 设计文档：`~/.gstack/projects/.../lawrence_li-feat-community-phase4-design-20260405-121020.md`
- CEO Plan：`~/.gstack/projects/.../ceo-plans/2026-04-05-validation-direction.md`
- 审查评分：设计文档 8/10（2 轮对抗性审查）、Outside Voice 发现 6 个问题（trust proxy 已修复）

---

## [0.2.2.0] - 2026-04-03

 ### Changed
  - **角色 & 配音演员名固定显示日文** — `CharacterSection` 移除语言适配逻辑，`nameJa`/`voiceActorJa` 始终优先，不再因语言设置切换为中文名
  - **Bangumi 标题匹配精确化** — `fetchBangumiData` 从 `list[0]` 改为在最多 5 条结果中寻找 `name === titleNative` 的精确匹配；仅精确命中时才写入
  `titleChinese`，非精确匹配仍保留 `bgmId` 但不写中文标题，避免续集 OVA 等变体污染正片翻译


### Fixed
- **bgmId 为 null 时客户端无限轮询** — Phase 1-3 搜索不到番剧时（新番未被 Bangumi 收录），原来写 `bangumiVersion: 1`，Phase 4 因 `!bgmId` 跳过，`bangumiVersion` 永远停在 1，客户端每 4 秒轮询死锁。现在直接写 `bangumiVersion: 2, episodeTitles: []` 标记完成
- **历史卡记录自愈** — `getAnimeDetail` 缓存命中时检测 `bangumiVersion === 1 && !bgmId` 的残留记录，自动推进到 version 2，无需重启或手动干预

### Performance
- **详情页秒开（placeholderData）** — `useAnimeDetail` 新增 `placeholderData`，优先从 seasonal/trending/search 的 React Query 缓存中查找该番数据；有 placeholder 时 `isLoading = false`，从列表点进详情页立刻渲染，无 loading spinner
- **富化优先队列** — `bangumi.service.js` 为 Phase 1-3 和 Phase 4 各增加 `enrichPriority` 数组；`enqueueEnrichment(items, priority)` 和 `enqueuePhase4Enrichment(items, priority)` 支持 `priority` 参数；`getAnimeDetail` 调用时传 `priority = true`，用户主动点击的番剧从队尾插到队首，等待时间从 60s+ 降至 ≤800ms；Phase 1-3 的 priority 项完成后，Phase 4 继承 priority

---

## [0.2.1.0] - 2026-04-02

### Fixed
- **历史季度中文标题缺失** — `getSeasonalAnime()` 路径③（部分缓存）和路径④（冷启动）新增 `enqueueEnrichment()` 调用，历史季度番剧现在在首次访问后会触发 Bangumi 富化
- **第一页中文标题不更新** — `useSeasonalAnime` 新增条件轮询（`refetchInterval: 20s`），当页面中存在 `bangumiEnriched: false/undefined` 的条目时自动轮询，富化完成后无需手动刷新即可看到中文标题，全部富化完成后自动停止轮询

### Changed
- `useSeasonalAnime` `staleTime` 从 5 分钟缩短为 1 分钟，确保富化完成后下次访问能及时拿到中文数据
- `SeasonPage` `AnimeGrid` 添加 `key={dataUpdatedAt}`，季度切换时新数据到达触发 `fadeUp` 动画，所有卡片同时出现而非逐个替换

---

## [0.2.0.0] - 2026-04-02

### Added
- **社区 Phase 1：趋势发现** — `GET /api/anime/trending` 热追排行榜（订阅数聚合，1h 内存缓存）；`GET /api/anime/:anilistId/watchers` 在看用户头像列表
- **社区 Phase 2：社交图谱** — 单向关注系统（`POST/DELETE /api/users/:username/follow`）；公开个人主页 `/u/:username`；关注者/关注列表；好友动态 Feed（`GET /api/feed`）
- **社区 Phase 3：实时弹幕** — WebSocket（socket.io）弹幕系统，按 `anilistId:episode` 隔离房间；`EpisodeWindow` 集合原子化首播窗口（`$setOnInsert` 消除竞态）；1 条/5s 用户级内存限流；JWT Handshake + 每事件重验（`TokenExpiredError` 精准识别）
- **iOS Blue 设计系统** — `#0a84ff` 替换全站紫色系；Apple True Black 三层背景（`#000000 → #1c1c1e → #2c2c2e`）；DESIGN.md 设计规范文档
- **WCAG 触控区域修复** — 弹幕颜色、语言切换按钮、轮播圆点、日期筛选标签均 ≥44px
- **磁力搜索重设计** — 入口迁至番剧详情页，三源并发（动漫花园 + acg.rip + Nyaa），集数筛选器，字幕组识别
- **测试基础设施** — Vitest（client）+ Jest+Supertest（server）；新增 17 个测试文件，共 81 条用例（ActivityFeed、FollowButton、danmaku.controller、follow.controller、profile.controller 等）
- `TrendingSection` 组件（首页横向卡片）；`WatchersAvatarList` 组件（番剧详情页）；`FollowButton`；`ActivityFeed`；`DanmakuOverlay`；`DanmakuInput`；`DanmakuSection`；`UserProfilePage`；`FollowListPage`
- `Danmaku` 模型（含 1 年 TTL 自动清理索引）；`EpisodeWindow` 模型；`Follow` 模型
- i18n：新增 `social.*`、`danmaku.*` 键组（中英双语）
- `useDanmaku`、`useSocial` hooks；`social.api.js`、`danmaku.api.js`
- CLAUDE.md 架构文档；docs/designs/community-platform-v2.md 设计决策记录

### Fixed
- `danmaku:join` 移入 `socket.on('connect', …)` 回调，修复重连后房间订阅丢失
- `follow.controller.js` 自关注检测改用 `ObjectId.equals()`，修复字符串与 ObjectId 比较误判
- `comment.controller` 内容长度校验改为 `content.trim().length`，修复含尾随空格内容被误拒（400）
- 弹幕 `lastSent` Map 添加 10k 上限防内存无限增长；`danmaku:join` 加参数校验 + 10 房间上限
- 弹幕历史接口加 500 条上限并倒序取最近（`.sort(-1).limit(500).then(reverse)`）
- `torrentCache` LRU 500 条上限；查询长度上限 200 字符防缓存投毒；RSS `magnet:` 协议校验
- `useFeed` 添加 `enabled: !!user` 防止未登录时发起请求
- `useSubscription` 静默处理 404（未订阅用户不抛错）
- `AuthContext` 使用 `useRef` 防止 React 18 StrictMode 下双重初始化
- `FollowButton` 关注/取消关注后显示 toast 反馈；`minWidth: 88px` 防按钮宽度抖动
- `ActivityFeed` 未登录时隐藏；空动态时显示友好提示文案
- `profile.controller.getProfile` 订阅列表上限 200 条；`getFeed` 关注列表上限 500 条

### Changed
- `server/index.js` 改用 `http.createServer` + `server.listen`，Socket.IO 绑定同端口
- 全站 success 色从 `#34d399` 统一为 Apple `#30d158`；次要文字从 `#475569` 统一为 `rgba(235,235,245,0.30)`
- `DanmakuSection` 标签色改用 teal `#5ac8fa`（符合 DESIGN.md 弹幕分区规范）
- `SeasonSelector` / `ProfilePage` tab 背景从 `rgba(26,34,53,0.8)` 统一为 `#1c1c1e`

---

## [0.1.5.0] - 2026-03-28

### Added
- **测试覆盖扩充** — 新增 5 个测试套件（48 → 48 个用例）：`EpisodeList` 高亮边界条件（6）、`LanguageContext` 回退行为（4）、`TrendingSection` 状态（4）、`WatchersAvatarList` 显示逻辑（5）、`comment.controller` 权限与内容校验（7）
- `UserProfilePage` 番剧列表"显示更多"分页（默认 12 部）
- `test/setup.js` 添加 `localStorage` mock，解决 jsdom 环境下 `LanguageContext` 语言初始化问题

### Fixed
- `UserProfilePage` `ShareButton`：`navigator.clipboard.writeText` 现在有 try/catch，剪贴板权限被拒时弹出错误 toast 而非静默失败
- `DanmakuInput` `handleSend()` 添加 `!connected` 防卫，修复通过 form 提交绕过 disabled 按钮的边界情况
- `comment.controller` 内容长度校验改为 `content.trim().length`，修复含尾随空格的合法内容被误拒（400）问题
- `UserProfilePage` `expanded` 状态在切换用户时（路由参数变化）正确重置
- `LanguageContext.test` 每个用例前清空 `localStorage`，防止测试间语言状态泄漏
- `test/setup.js` localStorage mock 添加 `configurable: true`，避免多线程测试池下重定义报错
- `UserProfilePage` 补充缺失的 `useState`/`useEffect` React 导入
- `zh.js`/`en.js` 新增 `detail.linkCopyFailed` 翻译 key

---

## [0.1.4.0] - 2026-03-28

### Added
- **磁力搜索重设计** — 入口从全局搜索迁移至番剧详情页，支持集数筛选器（全部/01/02…），字幕组识别从标题方括号提取
- `GET /api/anime/torrents` 新增第三方源：动漫花园（dmhy）+ Nyaa.si，三源并发 `Promise.allSettled`，任一失败不影响其他结果
- **iOS Blue 设计系统** — 全站 `#0a84ff` 替换紫色系，Apple True Black 三层背景（`#000000 → #1c1c1e → #2c2c2e`）
- WCAG 触控区域修复：弹幕颜色、语言切换按钮、轮播圆点、日期筛选标签均≥44px

### Fixed
- `danmaku:join` 事件移入 `socket.on('connect', …)` 回调，修复重连后房间订阅丢失问题
- `follow.controller.js` 自关注检测改用 `ObjectId.equals()`，修复字符串与 ObjectId 比较误判
- 弹幕 `lastSent` Map 添加 10k 上限防内存无限增长；新增 `username` 非空校验
- `torrentCache` 增加 500 条上限 LRU 淘汰；查询长度上限 200 字符防缓存投毒
- RSS `magnet:` 协议校验（acg.rip + Nyaa），阻断 `javascript:` / `data:` URI 注入
- followers/following 路由注册补全

### Changed
- **弹幕 `liveEndsAt` 竞态修复** — 引入 `EpisodeWindow` 集合（唯一索引 `{anilistId, episode}`），用 `findOneAndUpdate + $setOnInsert` 原子化首播窗口创建，消除并发首条弹幕竞态
- **Socket JWT 过期断连** — `socket.use()` 每事件重验 JWT，过期时 emit `auth:expired` 并断开连接；客户端监听后触发统一登出流程
- `danmaku.controller.js` `liveEndsAt` 改从 `EpisodeWindow` 读取，历史弹幕不再携带冗余字段

---

## [0.1.3.0] - 2026-03-25

### Added
- **社区 Phase 3：实时弹幕** — WebSocket（socket.io）弹幕系统，按番剧+剧集隔离房间
- `Danmaku` model：`{ anilistId, episode, userId, username, content, liveEndsAt }`，含 `(anilistId, episode, createdAt)` 复合索引
- `socketAuth` 中间件：JWT Handshake 验证，连接时附加 `socket.user`
- `danmaku.handler.js`：`danmaku:join/leave/send` 事件处理，1条/5秒用户级内存限流，2小时首播窗口（`liveEndsAt`）
- `GET /api/danmaku/:anilistId/:episode`：HTTP 历史弹幕接口，返回弹幕列表 + `liveEndsAt`
- `DanmakuOverlay` 组件：4 条泳道飞行弹幕，CSS `right→left` 动画，自动清理已播完元素
- `DanmakuInput` 组件：50字上限，字符计数器，WebSocket 连接状态指示灯（绿/灰）
- `DanmakuSection` 组件：HTTP 历史 + 实时消息合并去重，LIVE 绿色徽章，首播窗口关闭后显示只读提示
- `useDanmaku` hook：`useDanmakuHistory`（TanStack Query）+ `useDanmakuSocket`（socket.io-client）
- i18n：新增 `danmaku.*` 键组（中英文）
- `server/index.js`：改用 `http.createServer` + socket.io 绑定同端口
- `EpisodeList`：剧集展开面板顶部插入 `DanmakuSection`，评论区保留在下方

### Changed
- `server/index.js`：`app.listen` → `http.createServer(app)` + `server.listen`

---

## [0.1.2.0] - 2026-03-25

### Added
- **社区 Phase 2：关注系统** — `POST/DELETE /api/users/:username/follow`，单向关注模型，`Follow` 集合含 `(followerId, followeeId)` 唯一索引
- **公开个人主页** — `GET /api/users/:username`，含追番列表（按状态分组）、粉丝/关注数、`isFollowing` 字段（需 JWT，可选）
- **关注者/关注列表** — `GET /api/users/:username/followers` 与 `/following`
- **好友动态 Feed** — `GET /api/feed`，返回已关注用户近 40 条追番动态，按更新时间倒序
- `FollowButton` 组件：关注/取消关注切换，未登录重定向到 `/login`，自身主页隐藏
- `ActivityFeed` 组件：首页好友动态列表，含时间相对显示（timeAgo）
- `UserProfilePage` — `/u/:username` 公开主页，含渐变头像、粉丝/关注跳转、分享按钮、追番列表分 Tab 展示
- 分享按钮：`AnimeDetailPage` 和 `UserProfilePage` 均集成 `navigator.share()` + 剪贴板回退
- `optionalAuth` 中间件：有效 JWT 附加 `req.user`，无 token 静默跳过
- i18n：新增 `social.*` 键组（中英文），含 `follow`、`followers`、`feedLabel`、`action_*` 等
- `client/src/hooks/useSocial.js`：`useUserProfile`、`useFollow`（TanStack Query mutation + cache invalidation）、`useFeed`
- `client/src/api/social.api.js`：对应 REST 封装

### Changed
- 首页布局：在 `ContinueWatching` 与 `WeeklySchedule` 之间插入 `ActivityFeed`
- `App.jsx`：注册 `/u/:username` 路由指向 `UserProfilePage`
- `server/index.js`：注册 `/api/users` 路由与 `/api/feed` 端点

---

## [0.1.1.0] - 2026-03-25

### Added
- **社区 Phase 1：热门排行榜** — `GET /api/anime/trending`，按订阅数聚合排名，1 小时内存缓存（TTL），最多返回 20 条
- **社区 Phase 1：在看用户** — `GET /api/anime/:anilistId/watchers`，返回正在观看的用户头像列表及总人数
- `WatchersAvatarList` 组件：彩色首字母圆形头像 + "+N 人" 溢出文本，展示在番剧详情页订阅按钮下方
- `TrendingSection` 组件：首页水平滚动热门卡片，含 shimmer 骨架屏加载态
- `AnimeCard` 支持 `rank` 和 `watcherCount` props：显示 `#N` 排名徽章和 👥 观看人数徽章
- i18n：新增 `home.trendingLabel`、`home.trendingTitle`、`anime.watchers`、`anime.watchersMore`（中英文）
- `Subscription` 模型新增 `anilistId` 索引（优化 trending 聚合查询）
- 服务端测试：新增 `getTrending` 和 `getWatchers` 的 6 个 Jest+Supertest 单元测试（覆盖 happy path、参数校验、边界情况）

### Changed
- 首页布局：在 `ContinueWatching` 上方插入 `TrendingSection`
- 番剧详情页：在 `SubscriptionButton` 下方插入 `WatchersAvatarList`

---

## [0.1.0.0] - 2026-03-25

### Added
- 弹幕/评论删除：改用内联确认 UI，替换浏览器原生 `window.confirm()` 弹窗
- 评论发布失败时显示内联错误提示（`postError`）
- 种子搜索 5 分钟内存缓存（Map + TTL），减少对 acg.rip 的重复请求
- 测试基础设施：client 使用 Vitest + @testing-library/react（9 个测试），server 使用 Jest + Supertest（6 个测试）
- `docs/designs/community-platform-v2.md`：社区平台三阶段设计文档（含 CEO / Eng / Design Review 评审结论）
- 版本追踪：创建 `VERSION` 文件（`0.1.0.0`）

### Changed
- 种子数据源：从 Anime Garden 切换至 acg.rip RSS（`anime.controller.js`）

### Removed
- `client/src/utils/constants.js` 中的 `SEASON_LABELS` 对象（无消费者）
- `client/src/components/subscription/StatusBadge.jsx`（未被使用）

---

## 2026-03-07

### 18:26 · 初始化项目

- 创建 GitHub 仓库 `lawrenceli0228/animego`，配置 SSH 认证
- 写入 `.gitignore`（保护 `.env`、`node_modules`）
- 写入项目架构说明 `README.md`
- 配置 `.claude/launch.json` 开发服务器启动项

---

### 18:58 · 完成全栈代码开发（64 文件 / 7775 行）

**后端 · Express + MongoDB Atlas**

- `server/index.js` — Express 入口，CORS + Cookie 配置
- `server/config/db.js` — Mongoose 连接 MongoDB Atlas
- `server/models/` — User（bcrypt 加密）、AnimeCache、Subscription
- `server/services/anilist.service.js` — AniList GraphQL 代理 + MongoDB 缓存
- `server/controllers/` — 认证（JWT 双 Token）、番剧、订阅
- `server/routes/` — `/api/auth`、`/api/anime`、`/api/subscriptions`
- `server/middleware/` — JWT 验证、限流、错误处理

**前端 · React 18 + Vite**

- 暗色电影风格 UI（`#0a0e1a` 深海军蓝 + `#7c3aed` 紫 + `#06b6d4` 青）
- 页面：首页、季度、番剧详情、搜索、登录、注册、个人追番
- 组件：AnimeCard / AnimeGrid / 订阅按钮 / 季度选择器 / 搜索栏 / 分页
- TanStack Query 管理服务端状态，axios 自动刷新 Token

---

### 19:08 · 修复 AniList API 429 限流

**问题：** 每次请求都直接打 AniList，触发 90次/分钟限制

**修复（`anilist.service.js`）：**

- `getSeasonalAnime` — 优先读 MongoDB 缓存，命中则跳过 AniList 请求
- `searchAnime` — 新增内存缓存，相同搜索 10 分钟内不重复请求
- `queryAniList` — 加出站限速，两次请求间隔 ≥ 700ms（≈85次/分钟）

**涉及文件：**
- `server/services/anilist.service.js`

---

### 19:17 · 修复页面无限刷新死循环

**问题根因：**

```
无 cookie → /auth/refresh 返回 401
→ axios 拦截器捕获 → 再调一次 refresh → 失败
→ window.location.href = '/login'  ← 触发整页重载
→ AuthContext 重新挂载 → 再调 refresh → 无限循环
```

**修复：**

- 拦截器跳过 `/auth/refresh` 自身的 401（防套娃）
- 移除 `window.location.href` 硬跳转，改为派发 `auth:expired` 自定义事件
- `AuthContext` 用原生 `axios` 调 refresh，不触发拦截器
- `AuthContext` 监听 `auth:expired` 事件，优雅清除登录状态

**涉及文件：**
- `client/src/api/axiosClient.js`
- `client/src/context/AuthContext.jsx`

---

### 19:22 · 改为访客友好模式，无需强制登录

**需求：** 用户打开网站直接显示番剧内容，不强制跳转登录页

**修复：**

- `AuthContext` 拆分 `initializing`（启动 session 检查）和 `loading`（登录/注册操作中）两个独立状态
- `ProtectedRoute` 改用 `initializing` 判断，session 检查完成前不提前重定向
- `LoginPage` / `RegisterPage` 新增：已登录用户访问时自动跳回首页

**访客浏览流程：**

```
打开网站 → 首页番剧立即渲染
         → 后台静默检查 session（~200ms）
         ├── 有 session → Navbar 显示用户名和追番入口
         └── 无 session → 访客模式，Navbar 显示登录/注册按钮
仅 /profile 需要登录，其余页面全部公开访问
```

**涉及文件：**
- `client/src/context/AuthContext.jsx`
- `client/src/components/common/ProtectedRoute.jsx`
- `client/src/pages/LoginPage.jsx`
- `client/src/pages/RegisterPage.jsx`

---

---

## 2026-03-08

### · 根本修复缓存翻页与 429 限流（`fix: cache pagination and rate limit with full season pre-fetch`）

**问题：** 翻到第 2 页及以后返回空数组；每次翻页仍打 AniList 存在 429 风险

**修复：**

- 新增 `warmSeasonCache(season, year)` — 服务器启动后后台预热整个季度（每页 50 条全量写入 MongoDB）
- `getSeasonalAnime` 改为 4 级降级策略：已预热 MongoDB → 未预热时实时拉取 → 内存缓存兜底
- `server/index.js` 服务器就绪后异步触发 `warmCurrentSeason()`，不阻塞启动

**涉及文件：**
- `server/services/anilist.service.js`
- `server/index.js`

---

### · 首页新增每周放送日历（`feat: add weekly airing schedule to homepage`）

- 新增 `/api/anime/schedule` 接口，从 AniList 拉取本周 7 天播出计划，按本地日期分组，30 分钟内存缓存
- 新增 `WeeklySchedule` 组件：日期标签页（今天高亮）+ 当日番剧竖向列表（封面、集数、播出时间、评分）
- 前端 `localToday()` 用客户端本地时区计算今日日期，避免 UTC 时差错位

**涉及文件：**
- `server/queries/weeklySchedule.graphql.js`（新增）
- `server/controllers/anime.controller.js`
- `server/routes/anime.routes.js`
- `client/src/components/anime/WeeklySchedule.jsx`（新增）
- `client/src/hooks/useAnime.js`
- `client/src/api/anime.api.js`

---

### · 首页改版：Hero 轮播图 + 每周卡片网格（`feat: replace homepage grid with top-5 hero carousel`）

- 移除首页番剧方格，改为当季评分前 5 轮播图（`HeroCarousel`）：全宽 banner、Ken Burns 缩放、5 秒自动切换、悬停暂停、方向箭头、点状指示器
- 每周更新从竖向列表改为横向卡片网格（`auto-fill, minmax(140px, 1fr)`），每张卡片含封面（3:4）、标题、集数标签、时间、评分，悬停上浮效果

**涉及文件：**
- `client/src/components/anime/HeroCarousel.jsx`（新增）
- `client/src/components/anime/WeeklySchedule.jsx`（重构为卡片网格）
- `client/src/pages/HomePage.jsx`

---

### · 首页新增「继续追番」板块（`feat: add continue watching section to homepage`）

- 登录用户首页轮播图下方、每周更新上方新增横向滚动卡片条
- 展示当前状态为 `watching` 的全部追番：封面图 + 观看进度条 + 集数角标（已看/总集数）+ 标题
- 未登录或无在追番剧时自动隐藏，不占空间

**涉及文件：**
- `client/src/components/anime/ContinueWatching.jsx`（新增）
- `client/src/pages/HomePage.jsx`

---

### · 中英文切换 / 集数列表 / 磁力搜索 / 每集评论（`feat: add i18n, episode list, torrent search and episode comments`）

**中英文切换（i18n）**

- 新增 `LanguageContext.jsx` — 轻量级双语 Context，暴露 `t(key)` 和 `lang`，语言偏好持久化到 `localStorage`，默认中文
- 新增 `client/src/locales/zh.js` / `en.js` — 全站 UI 文案翻译字典（Navbar、首页、详情页、订阅、搜索、季度、登录/注册、集数评论等）
- Navbar 右上角新增 `中 / EN` 切换按钮，全站文案实时响应无需刷新

**集数列表**

- 新增 `EpisodeList.jsx` — 番剧详情页底部显示全部集数（自适应卡片网格）
- 已登录用户：已看集数绿色高亮，当前进度集数紫色高亮（数据来自 `useSubscription`）
- 每集卡片含集数编号 + 已看标记 + 磁链按钮
- `episodes` 为空时显示「集数信息待更新」友好提示

**磁力链接搜索**

- 新增 `TorrentModal.jsx` — 点击每集磁链按钮弹出全屏遮罩 Modal
- 后端 `GET /api/anime/torrents?q=` 请求 Nyaa.si RSS，`fast-xml-parser` 解析 XML，返回标题、磁链、大小、做种数
- 默认搜索词：`{titleEnglish || titleRomaji} + {episode}`；支持用户手动修改后重新搜索

**每集评论**

- 新增 `EpisodeComment` 数据模型（`anilistId + episode + userId + content`，双字段复合索引）
- 新增 REST 接口：`GET/POST /api/comments/:anilistId/:episode`（公开读 / 登录写）、`DELETE /api/comments/:id`（仅本人）
- 新增 `EpisodeComments.jsx` — 点击集数卡片手风琴展开评论区，同时只展开一集
- 已登录：文本框 + 发布按钮；访客：「请登录后参与评论」提示 + 登录跳转链接

**涉及文件：**
- `server/models/EpisodeComment.js`（新增）
- `server/controllers/comment.controller.js`（新增）
- `server/routes/comment.routes.js`（新增）
- `server/index.js`（注册 `/api/comments` 路由）
- `client/src/context/LanguageContext.jsx`（新增）
- `client/src/locales/zh.js` / `en.js`（新增）
- `client/src/components/anime/EpisodeList.jsx`（新增）
- `client/src/components/anime/TorrentModal.jsx`（新增）
- `client/src/components/anime/EpisodeComments.jsx`（新增）
- 全站各组件/页面接入 `t()` 双语支持

---

### · Bangumi 中文标题集成 + bgmId 外链（`feat: integrate Bangumi API for Chinese titles with bgmId linking`）

**中文标题后台富化**

- 新增 `server/services/bangumi.service.js` — 封装 Bangumi API（速率限制 800ms/请求），全局异步富化队列，不阻塞主请求
- `AnimeCache` 模型新增 `titleChinese`（String）、`bgmId`（Number）、`bangumiEnriched`（Boolean）三个字段
- `fetchBangumiData()` 搜索 `bgm.tv` 返回 `{ titleChinese, bgmId }`，优先用日文原名匹配
- `anilist.service.js` 三处调用 `enqueueEnrichment()`：季度预热、搜索结果、详情页懒加载；放送表额外批查 `titleChinese` 并合并

**前端中文标题显示**

- 新增 `pickTitle(obj, lang)` — 中文模式优先级：`titleChinese → titleNative → titleRomaji → titleEnglish`
- 5 个组件（AnimeCard / AnimeDetailHero / HeroCarousel / WeeklySchedule / ContinueWatching）全部切换使用 `pickTitle`

**bgm.tv 外链**

- 番剧详情页 `AnimeDetailHero` 徽章栏新增「在 Bangumi 查看 / View on Bangumi」外链按钮（红色主题，`target="_blank"`）
- 仅在 `bgmId` 非空时渲染，链接至 `https://bgm.tv/subject/{bgmId}`

**涉及文件：**
- `server/models/AnimeCache.js`（新增 3 字段）
- `server/services/bangumi.service.js`（新增）
- `server/services/anilist.service.js`（富化调用 + 放送表 titleChinese 拼接）
- `client/src/utils/formatters.js`（新增 `pickTitle`）
- `client/src/components/anime/AnimeDetailHero.jsx`（bgmId 外链）
- 5 个组件接入 `pickTitle`

---

### · 磁链搜索重构：接入 Anime Garden API + 三栏 UI（`feat: redesign torrent search with Anime Garden API`）

**数据源切换**

- 废弃 Nyaa.si RSS + XML 解析，改用 [Anime Garden API](https://api.animes.garden)（聚合 Nyaa.si + ACG.rip）
- API 直接返回预解析的 `fansub.name` 字段，覆盖 ANi、LoliHouse、SubsPlease 等中文字幕组
- 默认搜索词改为 `{titleRomaji} - {零补位集数}`（如 `Sousou no Frieren - 05`），匹配 Nyaa 命名惯例
- `formatBytes()` 处理 Anime Garden 以 KB 为单位的文件大小，正确转换为 MB/GB

**三栏弹窗重设计**

- 左侧栏：番剧名 + 字幕组过滤列表（自动聚合 + 按数量排序 + 数量 badge + 「全部/All」按钮）
- 中间列表：每条结果显示完整标题（单行省略）、分辨率 badge（1080P/720P）、编码 badge（HEVC/AVC/WEB-DL）、文件大小、日期
- 右侧：番剧封面图
- 复制磁链 ⎘ 按钮（点击变绿 ✓）+ 直接打开磁链 ↗ 按钮（唤起本地 BT 客户端）
- 切换字幕组时列表立即过滤；新搜索完成后自动重置为「全部」

**涉及文件：**
- `server/controllers/anime.controller.js`（重写 `getTorrents`，移除 XML 相关代码）
- `client/src/components/anime/TorrentModal.jsx`（完整重写）
- `client/src/locales/zh.js` / `en.js`（新增 `groupAll`、`openMagnet`、`date` 键）

---

## Git 提交记录

| Hash | 时间 | 说明 |
|------|------|------|
| `feb3ca6` | 2026-03-07 18:26 | Initial project setup: AnimeGo anime website |
| `5e8aa59` | 2026-03-07 18:58 | feat: implement full-stack AnimeGo anime website |
| `800f0ef` | 2026-03-07 19:08 | fix: resolve AniList 429 rate limit with cache-first strategy |
| `083d5ec` | 2026-03-07 19:17 | fix: resolve infinite page refresh loop on startup |
| `f11f453` | 2026-03-07 19:22 | fix: guests see content immediately, no forced login |
| `c459c6a` | 2026-03-08 | fix: cache pagination and rate limit with full season pre-fetch |
| `1585800` | 2026-03-08 | feat: add weekly airing schedule to homepage |
| `8df7df3` | 2026-03-08 | feat: replace homepage grid with top-5 hero carousel |
| `95eaeba` | 2026-03-08 | feat: add continue watching section and weekly grid layout |
| *(pending)* | 2026-03-08 | feat: add i18n, episode list, torrent search and episode comments |
| *(pending)* | 2026-03-08 | feat: integrate Bangumi API for Chinese titles with bgmId linking |
| *(pending)* | 2026-03-08 | feat: redesign torrent search with Anime Garden API |

仓库地址：`https://github.com/lawrenceli0228/animego`
